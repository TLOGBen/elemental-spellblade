Goal and acceptance:
Round 19b for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade (git repo; round 19 is uncommitted in the working tree on top of commit c981d4b). Round 19 moved the on-hit element proc from perk entry point 51 into a native SKSE plugin (C++ in native/, ElementsSpellblade.dll). An independent review found it crash-safe but NOT acceptable. This round fixes the blocking findings and cleans the foundation so the next slices (N2: the DLL computes proc magnitude at hit time; N3: marks and target state in the DLL) can be built on it without a rewrite. Report in Traditional Chinese, code and paths verbatim.

Read first: `.codex/fix-round19-briefing.md` (round 19's contract — still in force), `design-latency-2026-09-20.md` section 4.11 and section 6 (the native architecture and slices), `build/native-verification.md`, `build/fix19-probes.md`, and the review findings summarised below. Context on the previous implementer (the user's experience): it tends to leave things half-done, write hard-to-read code, introduce odd bugs, and force checks to pass. Do not repeat that: every check must test what the spec requires, and anything you cannot finish must be reported, not papered over.

Review findings to act on (verified by the reviewer against the code, the binary and a rebuild):
  F1 (blocking) No on-screen notifications. native/src/Plugin.cpp has no notification at all; 6 of 7 probe-card steps require reading the log outside the game. The round-19 contract required on-screen notification for every step the player waits on.
  F2 (blocking) build/native-verification.md claims the DLL's CastSpellImmediate path is equivalent to SKSE DoCombatSpellApply. It is not: the DoCombat wrapper (0x14054CBE0) first calls 0x140552E80 and skips casting if the target already has an active effect from the same spell; CSI has no such gate. Every proc spell carries the 30-second ESSB_EngagedEffect, so the paths differ. CSI re-applying every hit is the intended behaviour (it matches entry 51, which fired every hit), but the document is wrong and build/fix19-binary-evidence.txt lacks the disassembly for 0x1406282E0, 0x14054CBE0, 0x14054C5F0, 0x14054CD10, so the evidence cannot be reproduced.
  F3 src/ESSBController.psc:1828 calls ESSBNative.IsNativeHitActive() on every hit; it is registered as a non-latent-safe default native, so each call waits for the main thread (about a frame) — against the latency goal — and when the DLL is absent (a state the user accepted) Papyrus logs an unbound-native error on every hit.
  F4 Test padding: of the 39,888 selection cases about 34,320 (build/fix19_native.py:78) exercise veto and health-boundary combinations the production path never passes to select(); the wiring from engine conditions to the selection Input is not tested offline at all. Selection.h's veto and enabled/active fields are dead in production.
  F5 build/fix8_verify.py:243 skips both design-document hash checks with `continue` (dead check). `HITPROC ok: DLL base spells` (build/fix18_verify.py:128,202) re-reads the old snapshot rather than the DLL. native/build.py:26 writes hard-coded `compiler`/`tests` strings into the receipt. The entry-51 check is duplicated (build/fix19_native.py:124 and :130) and inspects DATA[0] without the PRKE type, so an ability entry whose spell FormID low byte is 0x33 would false-fail.
  F6 Residue and fragile coupling: build/fix19_edit.py (applied one-shot, not idempotent); native/deps/ holds 23 cmake.part.* chunks, 3 CMake zips and two *-evidence clones; native/out/selection_test.exe/.obj from a manual compile; the DLL freshness hash covers the whole build_v03.py (any MCM string change forces a DLL rebuild); generate_header hard-codes the MO2 absolute path.
  F7 Parity with the 74 entry-51 segments: the DLL adds filters the segments did not have — kHitBlocked, unarmed-with-a-spell-in-the-other-hand, staves. Either match the old segments exactly or keep a filter only with a stated reason; in both cases make Papyrus's difference patch consistent (today those edge cases may get the script bonus without the base).
  F8 Code quality: Plugin.cpp packs several statements per line (e.g. :176-178, :228, :231 where `if(hash) …;BCryptClose…` reads as if both were guarded), scattered mutable globals, and Handle does filtering, engine-state reading, selection, casting and logging in one function. Low-severity: logMutex could self-deadlock if an access violation happens inside fputs while held (Plugin.cpp:21-23, 37-41); /MD with VS 17.14's constexpr std::mutex requires VC++ runtime >= 14.40 (fine on this machine — System32 has 14.51 — note it).

Required outcome:
  1. Notifications (F1): the player gets an on-screen notification (Debug.Notification equivalent from the DLL, rate-limited so combat is not spammed) when: the plugin is disabled or refuses to start (with the reason: version, Address Library, fault, MCM off); a fault occurs; and, at debug level >= 2 only, each proc applied (element and spell name, one line). Keep the log lines too. Verify the notification API you use in the CommonLib source you pinned and record it in native-verification.md.
  2. Rewrite build/fix19-probes.md so no step needs the log: each step states exact console commands where needed (e.g. setting the enemy's health instead of "the usual way"; the player cannot paste into the console, keep commands short), the expected on-screen notification or visible effect, pass/fail, and for coexistence what the other mod's effect looks like and how to see it. Add the step "same element, two hits within 30 seconds, both deal proc damage" (F2). Keep it about five minutes. Where a step still genuinely needs the log, say why.
  3. F2: correct native-verification.md (CSI vs DoCombat: the same-spell gate, why CSI is the intended behaviour), and add the missing disassembly to the evidence so it is reproducible.
  4. F3: Papyrus reads the `ESSB_NativeHit` GLOB instead of calling the native function per hit; no per-hit native calls remain; no unbound-native errors when the DLL is absent (keep the MCM-facing native functions, called only from the MCM).
  5. Restructure the DLL (F8) into clear stages — filter, read state, pure selection, apply, log — with the mutable state in one place, one statement per line, and the selection's inputs built by a function that can be unit-tested with a mock of the engine reads. Make the tests honest (F4): test the production path (the inputs the handler can actually produce) against the round-18 truth table, plus the wiring from engine-state readings to inputs; drop cases that cannot occur and dead fields; report the new case counts with what each group proves. Keep the lightning band behaviour and its distribution test.
  6. F5, F6, F7: fix or remove the dead/duplicate checks with a one-line reason each; the entry-51 check reads the PRKE type; the receipt records what was actually used; the freshness hash covers only what the DLL is generated from; no hard-coded MO2 path (derive it or make it a setting); delete the residue, keep native/deps reproducible (pinned) but not bloated; resolve the parity filters as stated in F7.
  7. Nothing the player can feel changes except the notifications and the parity decisions in F7 (list each). The status layer and the Papyrus difference patch otherwise stay as they are.

Passing = `python build_v03.py` exits 0 with every check line ok and every check meaningful; the native tests pass; the release ESP has zero entry-51 entries of ours; a short changelog in 實作紀錄.md (append) and the progress ledger `.codex/impl-fix-round19b.html` list each finding F1-F8 with what you did or why not.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\** and D:\Game\Other\SKSE\MO2\mods\**; you may run python, the build, MSVC/CMake from the installed Visual Studio 2022, and read-only disassembly tools.
  - Write set: native/** (ours; dependencies stay pinned), src/*.psc, build_v03.py, settings.json, state-schema.lock.json, build/*.py, build/** generated outputs, build/native-verification.md, build/fix19-probes.md, package/Elements Spellblade/** (generated), 實作紀錄.md (append), .codex/pre-fix19b-snapshot/** (take it FIRST: src, build_v03.py, settings.json, build/v03-formids.json, native/src, native/CMakeLists.txt), .codex/impl-fix-round19b.html.
  - Network: not needed — the dependencies are already fetched under native/deps and pinned. Do not use the network unless a pinned dependency is genuinely missing; if so, fetch only that pinned version.
  - MUST NOT: write under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE; edit 元素魔戰士規劃-v0.3.md, 元素魔戰士規劃-v0.3_alter.md, design-latency-2026-09-20.md, design-compromises-2026-09-22.md, README.md, art-book/**, .strategic-advance/**; use git to commit, reset, checkout or stash (the commander commits); hook or patch engine code (event sinks and library APIs only); add ESP masters beyond Skyrim.esm.
  - MUST: keep file encodings and line endings as they are (build_v03.py is CRLF).

When done, report: VERDICT, what you changed per finding, test counts and what they prove, anything left undone and why, and the exact files the commander should review first.

Supervision (this is a supervised long task):
  - Progress ledger: D:\Game\Other\SKSE\.codex\elemental-spellblade\.codex\impl-fix-round19b.html (already created by the commander as a skeleton; it is in your write set). Update it after each meaningful milestone: after reading the contract and code (a short plan), after each finding F1-F8 is done, after each build/test run (with exit code), and when you hit a blocker. Record decisions and tradeoffs as auditable summaries, not private reasoning. Keep it current: the commander patrols every 5 minutes and treats a stale ledger during active edits as a warning sign.
  - The snapshot .codex/pre-fix19b-snapshot/ was already taken (2026-09-23 19:47, before any product edit) by an earlier attempt that was stopped before editing. Reuse it; do not overwrite it.
  - Work in milestone order so an interruption leaves a usable checkpoint: (1) F3 and F2 (small, independent); (2) F5, F6, F7; (3) F8 restructure together with F1 notifications and F4 honest tests; (4) the probe card; (5) full build and final report. If you are resumed after an interruption, read the ledger first and continue from the last completed milestone.

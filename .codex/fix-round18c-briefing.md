Goal and acceptance:
Tiny fix to the round-18 probe plugin of the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade. Report in Traditional Chinese.

In-game evidence (Papyrus.0.log, 2026-09-21 21:07-21:22 session): the player ran the probe; ESSBProbeMeter started and finished at 21:21:36 but wrote ZERO `[ESSB-PROBE]` lines. Cause: `OnEffectStart` in build/fix18-probe-package/Source/Scripts/ESSBProbeMeter.psc (source generated by build_v03.py) does `If Mode == 0 || Victim == Player -> Dispel(); Return` with no trace. It also logs `Error: Unable to call UnregisterForAllModEvents - no native object bound` from OnEffectFinish line 223 after the early dispel.

Required outcome:
  1. Every silent exit in the probe scripts (ESSBProbeMeter and ESSBProbeSegment) logs one `[ESSB-PROBE]` line saying exactly why and what to do, in plain words the player can act on: for a wrong perk combination, list which of the three probe perks (show their load-order FormIDs, e.g. via GetFormID in hex) the player currently has and which combination each probe needs; for the target being the player, say the console target was the player and to click the bandit first. Also log a `[ESSB-PROBE] START` line as the very first thing OnEffectStart does, so "the meter never started" and "the meter started and refused" are distinguishable.
  2. The OnEffectFinish native-object error must not occur (guard or restructure so it is not called on an effect whose native object is gone).
  3. Nothing else changes: verdict logic, controls, thresholds and the release package stay byte-identical (release package hash check as in round 18b). Extend the offline harness to cover the new refusal messages.
  4. Update build/fix18-probes.md only where needed to mention the new START/refusal lines.

Passing = `python build_v03.py` exit 0 with all check lines ok, release package hashes unchanged, harness covers refusal paths.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\Data\*.esm; executing PapyrusCompiler.exe allowed.
  - Write set (exact): build_v03.py (probe-package section only), src/ESSBProbe*.psc if they exist there, build/*.py helpers, build/fix18-probes.md, build/fix18-probe-package/**, 實作紀錄.md (append), .codex/impl-fix-round18c.html.
  - MUST NOT: change anything in package/Elements Spellblade/**; write under D:\Game\Other\SKSE\MO2 or SkyrimSE; edit design/plan documents or .strategic-advance/**; access the network. Keep encodings/line endings (build_v03.py is CRLF).

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

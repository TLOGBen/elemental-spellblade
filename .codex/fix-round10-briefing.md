Goal and acceptance:
Round 10 for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade: four defects observed in the user's third in-game session (the round-9 build ran correctly — 1423 [ESSB] events, no Papyrus errors from our stacks). Evidence: .codex/smoke3-essb-events.log (every [ESSB] line of that session plus error context). Report in Traditional Chinese (code, identifiers, paths verbatim).

Defects:
  A. 化灰 fires on non-divine kills. The user played 神聖 form around 22:12, then switched to 黑暗; from then on EVERY darkness kill logs "[ESSB][ASH] ... turned to ash" (85 ash lines in the session) even though the preceding damage line is `element=10`. Design 5.9/8: ash only when the killing damage was divine (element 7). Read the log to confirm the pattern, then find the real root cause — do not guess. Commander's partial analysis, verify or refute it: `ESSBController.LastDamageElement` is one sticky global with no owner and no reset; `ESSBElem2.OnKillTree/ShouldAsh` reads it via LastDamageWasElement(7); `ESSBElem2` line ~711 (divine judge) and ~854 call `NoteDamageElement(7)`; `ApplyDamageRaw` sets it on every reaction damage but the per-hit proc path may not, so a stale 7 can survive form switches. Fix so that ash is decided by what actually killed THIS victim: prefer recording the element per target (the registry/status already tracks 8 targets; a "last damage element" per slot plus a fallback for unslotted victims) over a global. State clearly which paths write it. This also matters for 5.12: ash marks the corpse MagicNoReanimate, so a wrong ash permanently blocks 亡者歸來 on that corpse.
  B. Every kill is processed twice: the log shows two identical "[ESSB][kill] <id> element=N" lines per victim (one log site only, at the end of ESSBController.OnKillEvent, after the SettledDead 8-entry dedup ring that claims the victim before dispatching). So either the dedup does not hold, or OnKillEvent runs on two paths, or the ring is cleared between the two calls. Find the real cause and fix it so every on-kill effect (飲血, 血承, 化灰, 亡者歸來, 收割, 連殺…) fires exactly once per victim. Add an offline regression for double delivery.
  C. A form sound keeps looping forever, including after the form is closed ("耳邊一直有特效聲音，哪怕關形態了"). `ESSBController.PlayFormSound` calls `chosen.Play(player)` and discards the returned instance id, so a looping SOUN (the FormActive family copied from Phenderix, ESSBFX_ZZSound_FormActive_*) can never be stopped. Fix: keep the instance id, stop it on form close, on form switch (before starting the next one), on load, and when the state breaks; make it idempotent (stopping an already-invalid instance must not log "Attempting to stop an invalid sound instance"). Check every other Play() call in src/ for the same leak (Release / DrawSheathe / Charge / OnHit families and any domain/aura sound) and say which are one-shot and therefore safe. If a copied SOUN is marked looping when the design wanted a one-shot, fixing the record in build_v03.py is also acceptable — say which you chose and why.
  D. "[ESSB][node][L2] noform backlash magicka" appears repeatedly while a form is ACTIVE (interleaved with element=10 hits). 反噬 is a 無形態樹/破魔 branch. Determine from 元素魔戰士規劃-v0.3.md (5.1/5.2) whether no-form-tree node effects are supposed to apply while a form is active. If they are not, gate them; if they are, say so and leave it. Do not change the balance either way beyond the gating question.

Passing = `python build_v03.py` exits 0 with every existing check line ok (READBACK masters ['Skyrim.esm'], records == manifest, CSF 13/13, DELIVERY ok, LAYOUT ok, DOT ok, MCM ok, SCHEMA ok, FIX7/FIX8/FIX9 ok, PLAN COVERAGE 0 unmapped, all scripts 0 errors); versus .codex/pre-fix10-snapshot/v03-formids.json no existing FormID changes (ESSB_DebugLevel 0x000811) unless a member-variable change forces a state_schema_version bump — if it does, bump it per the round-9 rule and list the re-allocated quests; each defect gets an offline regression that fails on the pre-fix sources and passes after. You run the build yourself.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\Data\*.esm and D:\Game\Other\SKSE\MO2\mods\** as the build needs; executing the PapyrusCompiler.exe that build_v03.py invokes is allowed.
  - Write set (exact): src/*.psc, build_v03.py, settings.json, state-schema.lock.json, build/*.py verification helpers, 實作紀錄.md (append only), build/** and package/Elements Spellblade/** (generated), .codex/impl-fix-round10.html (progress ledger; create first, update per item).
  - MUST NOT: write anything under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE (the game is running); edit 元素魔戰士規劃-v0.3.md (record deviations instead), review-*.md, fx_extract.py, .strategic-advance/** (the commander's campaign ledger is append-only and changes between rounds — never assert on its hash); add masters; introduce Utility.Wait loops or non-single RegisterForUpdate; access the network.
  - MUST: keep file encodings and line endings as they are (build_v03.py is CRLF).

Background (MAY):
  - Rounds 1-9 are accepted and installed; round 9 fixed the array/save-upgrade disaster. Do not undo any of them.
  - The user's trees this session: darkness 18, common 19, divine 2, noform ~3 — so almost no branch nodes are owned; effects that need a branch should NOT be firing.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

Goal and acceptance:
Second fix round for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade, driven by the user's first in-game session. Report in Traditional Chinese (code, identifiers, paths verbatim).

Work items:
  1. Papyrus runtime errors seen in game — the real log is .codex/smoke1-Papyrus.0.log (grep "essb", case-insensitive; the mod's own lines are tagged [ESSB]). Eliminate every error class whose stack touches an ESSB script, without changing behaviour:
     - "Cannot cast from None to Int[] / Float[]" at ESSBController.CancelSwap (assigning None to array variables) — 12 occurrences.
     - Calls on a finished active effect ("no native object bound"): ESSBStatus.OnEffectFinish -> UnregisterForUpdate; ESSBStatus.OnUpdate -> RegisterForSingleUpdate; ESSBMark.OnEffectFinish -> controller EndMark/FinishMark/OnMarkFinish -> Dispel on the already-finished mark. Guard these (finished flag / skip self-dispel when the caller is the finishing effect) rather than relying on the engine to swallow them.
     - Any other ESSB-stack error you find in that log. List each class with count, cause, and fix.
  2. Skill-tree layout (CSF JSON generated by build_v03.py). The user reports the trees are hard to click. Today nodes span y 0..8.5 (tiers 2.0 apart, branches at +0.5) so the tree is far taller than a vanilla tree and needs camera travel. Re-lay every tree so it fits vanilla-like bounds: y within 0..4.5, x within -4.5..4.5, three routes side by side, tier order bottom-to-top preserved, no two nodes closer than 0.9 on both axes at once (no overlaps), branch nodes adjacent to their mainline node, and `links` still forming mainline chain + branches hanging off their tier's mainline node. Add a build-time assertion for bounds and minimum spacing, printed as a "LAYOUT ok" line.

Passing = `python build_v03.py` exits 0 with all existing check lines still ok (READBACK masters ['Skyrim.esm'], records == manifest, CSF 13/13, DELIVERY ok, PLAN COVERAGE 0 unmapped, 17 scripts compile) plus the new LAYOUT line; no FormID of an existing record changes (compare build/v03-formids.json with .codex/pre-fix2-snapshot/v03-formids.json; ESSB_DebugLevel stays 0x000811); a FIXED / NOT FIXED table appended to 實作紀錄.md as a new dated entry "fix round 2（首次實機回饋）" and returned in your final report.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade.
  - Write set (exact): src/*.psc, build_v03.py, 實作紀錄.md (append only), build/** and package/Elements Spellblade/** (generated), .codex/impl-fix-round2.html (progress ledger; create it before the first product edit, update after each item).
  - MUST NOT: write outside the campaign root; touch D:\Game\Other\SKSE\MO2\** or SkyrimSE\** (the game is running right now); edit 元素魔戰士規劃-v0.3.md, review-*.md, fx_extract.py, .strategic-advance/**; add masters; renumber records; introduce Utility.Wait loops, non-single RegisterForUpdate, Spell.Cast for damage, or DamageActorValue on enemy health; access the network.
  - MUST: keep .psc files UTF-8 with their existing line endings (reason: last round flipped three files LF->CRLF, which bloats diffs).

Background (MAY):
  - The commander already shortened the settings MessageBox button labels in build_v03.py (they overflowed the screen); leave that edit in place.
  - Everything else in the log (FloraHarvestScript etc.) belongs to other mods — ignore.
  - In the same session the engine path worked: hits, marks, Open/End, Freeze, Burst, Kill, tree level-ups all logged normally.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

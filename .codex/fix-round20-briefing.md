Goal and acceptance:
Round 20 = slice N2 of the native plan for the Skyrim SE 1.5.97 mod 元素魔戰士 at D:\Game\Other\SKSE\.codex\elemental-spellblade (git repo, clean at commit 5cc7df6). N1 (round 19b) is live: the SKSE plugin in native/ (ElementsSpellblade.dll) picks the on-hit element proc spell and casts it with CastSpellImmediate, effectiveness 1.0. N2 moves the proc MAGNITUDE into the DLL: computed at hit time and applied as a magnitude override, replacing Papyrus pre-writing magnitudes and its dozen-plus rewrite trigger points. Report in Traditional Chinese (台灣); code, identifiers and paths verbatim.

Read first: `元素魔戰士規劃-v0.4.md` (design truth; every row tagged "DLL N2" is in scope — grep them; the N2 row of the slice table near line 1613 is the summary), `design-latency-2026-09-20.md` section 4.11 and section 6 (native architecture, slice rules, the "分期斷裂總檢" rule at the end of section 6), `build/native-verification.md`, `build/native-verification-2.md` (what the engine APIs can and cannot do — the NO items are binding), `.codex/fix-round19b-briefing.md` (the previous contract; its code-quality and honest-test rules still apply), `.strategic-advance/essb-standalone-build/CHECKPOINT.md` section "N2 開工要點".

Context on quality (the user's experience with the previous implementer): it left work half-done, wrote hard-to-read code, and forced checks to pass. Do not repeat that. Every check tests what the spec requires; anything you cannot finish is reported, not papered over.

Required outcome:
  0. Two cleanups first: (a) the B-group native tests hardcode 1 (right hand) and 24 (health) instead of comparing against the production constants — make them derive from the code under test; (b) retire the stale 2026-09-22 evidence files and point `build/native-verification.md:85` at `build/fix19b-build.log`.
  1. Hit-time magnitude in the DLL, per v0.4: random base B per hit (interval roll), lightning best-of-N with N fixed at 1 for now (N from charges is N4), blood-position linear curve (hit multiplier and lifesteal ratio interpolated), power attack ×1.5 and sneak-shot-counts-as-power-attack from the hit flags, lightning crit (5% + 2% per charge — charges read as 0 until N4 unless already represented in an engine effect; say which), environment bonuses (rain/snow/standing in water → treat targets as soaked for slow+amplify only; night +20% darkness; day +20% divine), undead/daedra ×1.5 for divine, base lifesteal on bleeding targets, formless baseline true damage 5 × G (power ×1.5), and every "+X% per point" node row tagged DLL N2 (read the player's perk ranks). The lightning mana drain (50%) is a separate single-effect spell cast after the damage spell with its own override (the override applies to every effect of a spell — verified NO item; split spells wherever effects need different magnitudes).
  2. 無元素 (element-less) hit mechanics tagged DLL N2: mana siphon on hit, small dispel (normal attacks burn target mana 5 × G after the siphon, true damage = burned × dispel multiplier, only when the player's mana > 0), heavy-attack dispel (spend X, burn Y, true damage, dispel mark, silence on burning to 0), and their node rows tagged DLL N2 (奪魔, 枯竭, 靜寂, 噬命, 寂滅, etc.). Rows that also need N4 (e.g. 反擊 needs the block-received sink) get only their N2 half, stated.
  3. Blood 護血 pool intake (血溢: lifesteal overflow at full health goes into the pool, cap 20% max health) as described in v0.4 (pool = the magnitude of our own effect on the player, not an engine AV; the draining on being hit is N4). If this needs a per-tick step, the only allowed shape is a background timer thread posting one main-thread task per tick; self-rescheduling tasks freeze the game.
  4. Papyrus: remove the magnitude pre-write and rewrite paths now owned by the DLL, in the same slice (the 分期斷裂 rule: a Papyrus path is deleted only in the slice where its DLL representation goes live). Target-side multipliers stay in the Papyrus difference patch until N3; the status layer is untouched in N2 (its known ghost bug is N3's job).
  5. Scope honesty for nodes: v0.4 may name nodes that do not exist yet in the ESP. Implement the ones that exist; list every N2-tagged row that could not be implemented because its perk/record does not exist yet, or because it depends on a later slice, in a table in the ledger and in the final report. Do not invent placeholder perks to make a row look done.
  6. Tests: extend the native tests honestly — the magnitude formula against a hand-computed table for the production inputs, the engine-read-to-input wiring with mocks, and (new in N2, required by design-latency section 6) the distribution of 1000+ rolls for one input (random B, lightning best-of-N, crit rate) checked against the expected distribution with a stated tolerance. Report counts per group and what each proves.
  7. Player-visible: at debug level >= 2 the per-proc notification also shows the computed magnitude (and "暴擊" when it crits), rate-limited as now. Update `build/native-verification.md` with a new N2 section (APIs used, verified against the pinned CommonLib source) and write a new probe card `build/fix20-probes.md` in the same style as `build/fix19-probes.md`: no step needs the log, short console commands, expected on-screen result, pass/fail, about ten minutes total; include at least: magnitude varies between hits, power attack larger, crit notification appears at roughly the stated rate, blood lifesteal and 護血 intake, element-less siphon/dispel/silence, night/day bonus (with the `set gamehour to` command), rain (with a short `fw` weather command — look up a real vanilla rain weather FormID).

Passing = `python build_v03.py` exits 0 with every check line ok and meaningful; the native tests pass; the release ESP still has zero entry-51 entries of ours; append a changelog to 實作紀錄.md; the ledger lists each outcome item 0-7 with what you did or why not.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\** and D:\Game\Other\SKSE\MO2\mods\**. You may run python, the build, MSVC/CMake from the installed Visual Studio 2022, and read-only disassembly tools.
  - Write set: native/** (ours; dependencies stay pinned), src/*.psc, build_v03.py, settings.json, state-schema.lock.json, build/*.py, build/** generated outputs, build/native-verification.md, build/fix20-probes.md, package/Elements Spellblade/** (generated), 實作紀錄.md (append only), .codex/pre-fix20-snapshot/** (take it FIRST, before any product edit: src, build_v03.py, settings.json, build/v03-formids.json, native/src, native/tests, native/CMakeLists.txt), .codex/impl-fix-round20.html.
  - Network: not needed; dependencies are pinned under native/deps. Fetch only a pinned version if one is genuinely missing.
  - MUST NOT: write under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE; edit 元素魔戰士規劃-v0.3.md, 元素魔戰士規劃-v0.3_alter.md, 元素魔戰士規劃-v0.4.md, design-*.md, README.md, art-book/**, .strategic-advance/**, CLAUDE.md; use git to commit, reset, checkout or stash (the commander commits); hook, trampoline, vtable-patch or write game memory (event sinks and library APIs only); add ESP masters beyond Skyrim.esm; store state in the DLL or add save serialization (design state lives in engine effects).
  - MUST: keep file encodings and line endings as they are (build_v03.py is CRLF; the design docs are UTF-8 no BOM). If a design row is ambiguous or contradicts an API limit, pick the reading closest to the design's intent, record the decision in the ledger, and keep going; stop only for something that would change the design itself.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

When done, report (Traditional Chinese): VERDICT, what you did per outcome item 0-7, test counts and what they prove, the table of N2 rows not implemented and why, anything the player will feel differently, and the files the commander should review first.

Supervision (supervised long task):
  - Progress ledger: D:\Game\Other\SKSE\.codex\elemental-spellblade\.codex\impl-fix-round20.html (skeleton created by the commander; in your write set). Update it after reading the contract and code (short plan + the N2 row inventory), after each outcome item, after each build/test run (with exit code), and on any blocker. Auditable decision summaries, not private reasoning. The commander patrols every 5 minutes; a stale ledger during active edits is a warning sign.
  - Milestone order so an interruption leaves a usable checkpoint: (1) snapshot + item 0; (2) item 1 core magnitude pipeline with tests; (3) items 2 and 3; (4) item 4 Papyrus removal; (5) items 6-7 tests, docs, probe card; (6) full build and final report. If resumed after an interruption, read the ledger first and continue from the last completed milestone.

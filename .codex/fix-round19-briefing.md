Goal and acceptance:
Round 19 for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade: the first slice of phase 2N — replace the on-hit element proc, currently delivered by perk entry point 51 (`ESSB_P_HitProc`), with a native SKSE plugin (C++ DLL). Report in Traditional Chinese (code, identifiers, paths verbatim).

Why: measured in game this week (evidence .codex/smoke8-*, smoke9-*, smoke10-* logs; build/entry51-compat-audit.md): entry 51 applies ONE spell per hit across all perks the attacker has (lowest priority wins; equal priority means the perk added to the actor first wins), so our proc competes with Ordinator, For Honor, Stealth Kill Detection Fix, Darenii "X之軀" and others. The user decided (2026-09-22, verbatim "單純走丙"): go native SKSE only, with NO entry-51 fallback. When the DLL is absent, disabled or refuses to run, the mod simply has no element proc on hit — the user knows and accepts this.

Specification: design-latency-2026-09-20.md section 4.11 「丙的架構」 (DLL responsibilities, what stays in ESP/Papyrus, `ESSB_NativeHit`, manifest.json, `ESSBNative` native functions, risks and safeguards) — EXCEPT that every mention of keeping entry 51 as a fallback is void per the user decision above (the design organ is updating the document in parallel; this briefing wins where they differ). Its 「無法從本機檔案確認、需要查證的」 table lists API names that were written from memory: treat all of them as unverified.

Required outcome:
  1. Verify first, write build/native-verification.md: for every row of the 4.11 verification table, the real library (choose CommonLibSSE-NG or powerof3's CommonLibSSE and say why for 1.5.97 + SKSE 2.0.20), the real header/type/function names and signatures you will use, and where you confirmed them (repo + path + commit). Anything you could not confirm is marked so. Nothing in the DLL may rely on an unconfirmed name.
  2. A C++ project under native/ (CMake + MSVC from the installed Visual Studio 2022 17.14; dependencies pinned to exact versions/commits and recorded), producing ElementsSpellblade.dll for Skyrim SE 1.5.97. The generator build (`python build_v03.py`) must copy the DLL and a generated `SKSE/Plugins/ElementsSpellblade/manifest.json` (local FormIDs of every proc spell + selection keys) into package/Elements Spellblade/, or fail loudly if the DLL is missing or stale relative to its sources.
  3. DLL behaviour for this slice = exactly what `ESSB_P_HitProc`'s 74 entry-51 segments do today, no more: on a weapon/unarmed hit where the attacker is the player, `ESSB_Enabled`==1, `ESSB_FormActive`==1, element>=1, target is a living non-teammate non-commanded actor not blocking, pick the same proc spell those segments pick (element, normal/power, wind sneak, blood HP% zone, weapon-type rules, lightning five-band random at N=1) and apply it with the player as caster the same way DoCombatSpellApply does (so resistances, 0x1D magnitude perks and external bonuses behave as before). Put the selection in a pure function and prove, with an offline unit test compiled natively, that it returns the same spell as the round-18 HITPROC truth table for every combination that table covers (the table's source is in build_v03.py / build/fix18_*.py). Lightning: reproduce the band probabilities and test the distribution.
  4. Remove the entry-51 proc segments from `ESSB_P_HitProc` in the release ESP (the release must contain zero entry-51 entries of ours). Keep everything else round 18 delivered: the Papyrus difference patch (base x (M_target - 1)) and the whole status layer stay exactly as they are and must keep working with the DLL-applied base proc. Keep an equivalent of the HITPROC equality check (engine_base + script_bonus == old ApplyProc magnitude), with engine_base now meaning the DLL-applied spell.
  5. Safeguards (4.11 風險與保險, minus the fallback): version check at load (runtime 1.5.97, Address Library present), otherwise log and do nothing; the event handler cannot throw into the engine (catch/SEH as the library allows), any fault logs, sets `ESSB_NativeHit` 0 and stops handling; ignore hits whose source is a spell/enchantment (no recursion); MCM toggle plus `ESSBNative.SetNativeHit(Bool)`, `IsNativeHitActive()`, `NativeVersion()` native functions; the MCM general page shows DLL status; rename the MCM 「附傷開關」 label so it is clearly the mod's master switch. The DLL logs to its own log file under the SKSE log folder, levels driven by `ESSB_DebugLevel`: L2 writes one `[ESSB][hit][L2]` line per proc applied, L0 writes only load/version/fault lines.
  6. A probe card for the player, build/fix19-probes.md: five minutes, step by step, pass/fail per step, covering: DLL loaded (log line), a fire-form hit applies the fire proc immediately, switching element changes the next hit, power attack and sneak variants, blood HP zones, lightning spread, Ordinator/For Honor coexistence spot check (the player runs both), MCM toggle off gives no proc, and the Vancian Magic spell-count check (probe N-5 in the plan). Where the player must wait for a log line, the plugin or scripts must ALSO show an on-screen notification — the player cannot see the log in game.
  7. `python build_v03.py` exits 0 with every existing check line ok (update checks that asserted entry-51 segments so they now assert their absence and the DLL path instead — say which and why), plus new check lines for: DLL present and fresh, manifest matches ESP, zero entry-51 entries of ours, selection unit test passed.

Passing = all of the above with evidence in 實作紀錄.md and the progress ledger. FormID diffs versus .codex/pre-fix19-snapshot are report-only (the user keeps no old saves) but list them.

Permission boundary:
  - Network: ALLOWED, only to fetch the C++ build dependencies (CommonLibSSE / CommonLibSSE-NG, spdlog, fmt, binary_io and their transitive deps, via vcpkg, git or CMake FetchContent) and to read their documentation/source (GitHub, vcpkg registry, the libraries' own docs). No other sites, no uploads, no accounts, no telemetry opt-ins.
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\** and D:\Game\Other\SKSE\MO2\mods\**; executing PapyrusCompiler.exe, MSVC, CMake, vcpkg, git and your own tools is allowed.
  - Write set (exact): native/** (new), src/*.psc, build_v03.py, settings.json, state-schema.lock.json, build/*.py helpers, build/** generated outputs, build/native-verification.md, build/fix19-probes.md, package/Elements Spellblade/** (generated), 實作紀錄.md (append), .codex/pre-fix19-snapshot/** (take it FIRST), .codex/impl-fix-round19.html (progress ledger; create before product edits), and a dependency cache directory inside native/ (keep it out of package/).
  - MUST NOT: write anything under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE (the commander deploys); edit 元素魔戰士規劃-v0.3.md, 元素魔戰士規劃-v0.3_alter.md, design-latency-2026-09-20.md, review-*.md, .strategic-advance/**; hook or patch engine code (no trampolines, no writing to game memory) — event sinks and library APIs only, as the plan specifies; add ESP masters beyond Skyrim.esm.
  - MUST: keep file encodings and line endings as they are (build_v03.py is CRLF).

Background (MAY):
  - Visual Studio 2022 Community 17.14 is installed (CMake ships inside it under Common7\IDE\CommonExtensions\Microsoft\CMake), plus clang-cl and git. SKSE for 1.5.97 (skse64_1_5_97.dll) is in D:\Game\Other\SKSE\SkyrimSE; Address Library for 1.5.97 is MO2\mods\Address Library All in One\SKSE\Plugins\version-1-5-97-0.bin. PO3 Papyrus Extender is installed and uses the same hit event our script listens to.
  - Standing authority as in round 18: acceptance wording that proves unsatisfiable is yours to resolve and record; anything the player can feel (numbers, which spell a hit gets, the status layer) is not.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

Goal and acceptance:
Fix every defect recorded in four review files of a Skyrim SE 1.5.97 mod (Papyrus scripts + a Python ESP generator) at D:\Game\Other\SKSE\.codex\elemental-spellblade, without changing the design. Report in Traditional Chinese (keep code, identifiers, paths verbatim).

The authoritative work list (read all four first; they are inside your read scope):
  - review-2026-09-17.md      — 6 commander findings (P0-1 pushes, P0-2 spell delivery, P1-3..6) + 7 builder-review findings R1–R7 + 3 advisories
  - review-astra-A.md         — 15 findings (element node effects)
  - review-astra-B.md         — 11 findings + one supplement to P1-4 (kill attribution)
  Two pairs overlap (death-curse multiplier A7 = B4; 血斷 A10 = B8) — fix once.

Passing =
  1. `python build_v03.py` exits 0 from the campaign root, and its own checks pass: ESP readback masters == ['Skyrim.esm'], record_count == manifest, 0 duplicate FormIDs, CSF 13/13 valid, plan coverage 0 unmapped / 0 PENDING, every script compiles with 0 errors (list any warnings).
  2. Every work-list item is either FIXED (file + function + one-line description of the change) or NOT FIXED with a concrete technical reason. No silent skips. Write this table to 實作紀錄.md as a new dated entry "fix round（審查修正）" and also return it in your final report.
  3. Design truth is 元素魔戰士規劃-v0.3.md: where a review item cites node text, the node text in build/plan-tree-nodes.json decides. Never edit the plan or the review files.
  4. Behaviour you did not intend to change must not change: keep FormIDs stable (ESSB_DebugLevel stays 0x000811; do not renumber any existing record), keep the PERK EDID scheme, keep the public function names other scripts call unless you update every caller.
  5. For P0-2 (delivery): every hostile spell that scripts apply to enemies with DoCombatSpellApply becomes delivery Contact (1) in BOTH the SPEL SPIT and its MGEF DATA; spells applied to the player become Self (0). List the affected EDIDs.
  6. For P0-1 (pushes): no ApplyHavokImpulse on a non-ragdolled actor. Use PushActorAway with positive force (blow back) or negative force from the centre reference (pull); lift = ragdoll first, then one delayed upward impulse via a single-shot update (no Utility.Wait loops). Keep the existing per-target cooldowns and immunity list.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade.
  - Write set (exact): src/*.psc, build_v03.py, plan_coverage.py, plan_trees.py, settings.json, 實作紀錄.md (append only), build/** (generated), package/Elements Spellblade/** (generated by the build), vendor/imports/*.psc (only to add missing compile stubs), .codex/impl-fix-round.html (your progress ledger).
  - MUST NOT: write anywhere outside the campaign root; touch D:\Game\Other\SKSE\MO2\** or D:\Game\Other\SKSE\SkyrimSE\** in any way other than reading; edit 元素魔戰士規劃-v0.3.md, review-*.md, fx_extract.py, anything under .strategic-advance/; add a master other than Skyrim.esm; ESL-flag the plugin; use Spell.Cast for damage (Ordinator's Vancian magic listens to cast events); DamageActorValue an enemy's health outside the existing true-damage spell path; introduce Utility.Wait loops, RegisterForUpdate (non-single), or per-frame polling; access the network.
  - MUST: write .psc files as UTF-8 (the sources contain Traditional Chinese comments; the compiler accepts them in src/ but NOT BOM-less CJK inside vendor/imports — keep import stubs ASCII). Reason: a previous heredoc write corrupted a CJK-heavy file.
  - MUST: keep a progress ledger at D:\Game\Other\SKSE\.codex\elemental-spellblade\.codex\impl-fix-round.html — create it before the first product edit; update it after each group of fixes (current stage, files changed, items done/remaining, failures and retries, validation results). It is a progress signal only.

Background (MAY):
  - 實作紀錄.md documents every deliberate deviation and the architecture: ESSBController (player alias: hit path, registry of 8 marked targets, ApplyDamage/ApplyDamageRaw/ApplyTrueDamage wrappers that set magnitude on the mod's own spells then DoCombatSpellApply, domains, pushes), ESSBStatus (per-target status host with ring buckets, 30 s host swapped every ~25 s via Export/Import), ESSBReactions (base 開印/終焉 per element), ESSBElem / ESSBElem2 / ESSBElem3 (node effects for elements 1–3 / 4–7 / 8–11), ESSBNodes (common tree), ESSBNoForm (no-form tree), ESSBTrees (CSF trees, rank cache slots 0/1/2), ESSBGuard (player-as-victim hits and kills).
  - Tree ids: 0..10 fire, frost, lightning, earth, wind, blood, divine, poison, water, darkness, astral; 11 no-form; 12 common. Element ids 1..11 in the same order. Routes 0/1/2 = 持續/開啟/關閉; tiers 0..4; mainline rank 0..15; branches 0-based.
  - The build compiles with the Nemesis-bundled PapyrusCompiler; build_v03.py already wires it. A full build takes about two minutes.
  - An FX front just finished editing build_v03.py and src/ESSBController.psc (effect shaders, impact sets, sounds, explosions at 終焉/融斷, StartDomain visuals, sync-stage glow). Do not undo that work.
  - Suggested order: P0 items → engine correctness (dual marks, cooldowns, host swap, kill attribution/dedupe, global mirrors, domain scan) → per-element ordering bugs (frost, lightning, darkness pending drain) → multipliers that are computed then dropped → formula mismatches → the rest. Build after each group.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

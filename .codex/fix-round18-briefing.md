RESUMPTION (third start) — The zero-GetStack acceptance criterion that stopped the previous run has been RULED ON in the acceptance section below; read it, it grants you standing authority so this class of stop does not repeat. Everything the previous two runs produced (snapshot, ledger, FIX8 fix, build/fix18-zero-difference-preflight.json) stands — keep it.

RESUMPTION — a previous run of this round stopped on purpose and correctly. It found a real contradiction in the plan: phase 1 said to shrink `ApplyProc`, while the same phase requires the status layer to keep working exactly as today, and the existing `ApplyProc` multiplies the proc by TARGET status whose engine representation lands in phases 2-3. That contradiction has now been RULED ON and design-latency-2026-09-20.md section 6 has been rewritten: 第 1 期 work item 2 now specifies a difference-patch (engine applies the base proc, script tops up `base × (M_target − 1)` only, early-exit at zero difference), a new 「手感（不粉飾）」 section lists every multiplier the script still owns in phase 1 and where each one disappears, and 熱度/聖佑 moved from phase 3 to phase 2. Re-read section 6 in full before doing anything; it is the ruling, not a suggestion.
Already done by that run, do NOT redo or overwrite: `.codex/pre-fix18-snapshot/` (the clean baseline — keep it as it is), `.codex/impl-fix-round18.html` (append to it), and the FIX8 whitelist/content-hash fix in build_v03.py. Product source, settings.json, all 3941 FormIDs and schema 6 were left untouched, and the baseline build was re-run green (19 scripts, 0 errors).
Additional acceptance for this resumption: the harness must assert that for the same state, `engine_base + script_bonus` equals the old `ApplyProc` magnitude within 1e-4 (the worked example: heat 4 stays 15.246, with 11.55 from the engine and 3.696 from the script), and a fast-path assertion, RULED as follows because the original wording was unsatisfiable (a status may have expired, and today's code learns that only by running the host's lazy Tick, so zero reads cannot be a general requirement):
  - Zero cross-script reads are required ONLY when the player owns no target-side multiplier node at all. That must be decidable from the controller-local Rank/Br mirror with no cross-instance call. This is the case worth optimising and the assertion applies to it.
  - When target-side nodes ARE owned, reading is expected. Use exactly today's path (`GetStack`, which runs the host's lazy Tick) so that expiry settlement keeps happening as it does now. Do NOT switch to `ExportInts` to dodge the Tick: it returns the unsettled value and that is a silent change to the status layer, which this round forbids.
  - If the resulting difference is <= 0, apply no spell.
  The equality assertion above is the real guard; keep both in the build's check lines.

STANDING AUTHORITY, so you do not have to stop a third time: you have already stopped twice and both times you were right, because my acceptance criteria were wrong, not because the design was. From here on, when an acceptance criterion I wrote is unsatisfiable or self-contradictory but the DESIGN INTENT in design-latency-2026-09-20.md is unambiguous, you may resolve the criterion yourself in the way that serves that intent, implement it, and record the resolution and its reasoning in 實作紀錄.md and the ledger. Keep stopping for anything that changes gameplay, numbers, the status layer, or the design itself. In other words: wording and acceptance mechanics are yours to fix; mechanics the player can feel are not.

Goal and acceptance:
Round 18 for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade. This is phase 1 of the redesign described in design-latency-2026-09-20.md section 6: move the on-hit damage and the form/switch layer onto engine perk entry points, plus the cheap fixes, WITHOUT touching the target status layer. Report in Traditional Chinese (code, identifiers, paths verbatim).

Two documents are the specification and they disagree with nothing: 元素魔戰士規劃-v0.3_alter.md is the design truth (what the mechanics are and every number), design-latency-2026-09-20.md is the implementation plan (how to represent them and in what order). Read design-latency-2026-09-20.md section 6 "第 1 期（round 18）" first: it lists, file by file, what this round changes, and sections 4.1-4.4, 4.5, 5, 8, 10 and 12 give the representation rules, the lightning five-spell approximation, the generator record list, the invariants that must hold and the acceptance. Every number comes from 元素魔戰士規劃-v0.3_alter.md; where the two documents both mention a value, the alter document wins. 元素魔戰士規劃-v0.3.md is the previous design generation: read it for background if you want, NEVER edit it.

Required outcome:
  1. Everything listed under 第 1 期 items 1-6, to the acceptance in section 10.1 (offline: build + harness). Item 6 is the state_schema_version bump; follow the round-9 rule in 實作紀錄.md exactly (old quest FormIDs stay as inert stubs, no VMAD, flags 0, no aliases) and let the lock file regenerate.
  2. The status layer keeps working exactly as it does today, bugs included. Phase 2 replaces it; do not start it, do not half-start it. If a phase-1 change would force a status-layer change, stop and report instead of widening.
  3. The per-hit path must not get more expensive than round 17 left it (representative outgoing hit: 165 scripted+native calls, measured by build/papyrus_harness.py). Report the new figure. Moving work onto perk entry points should reduce it.
  4. Do not weaken any guard added in rounds 9-17: stale-instance / StateBroken / Ready, dead-target and once-only settlement, real-time deadlines, the StartDeferredKill divine-protection safety, the nine native protection windows, and the array rule (never `arr == None`, always `!arr`).

  5. design-latency-2026-09-20.md section 11 lists engine behaviours the design could not confirm from files. For each one this round touches: if it can be settled offline by inspecting vanilla Skyrim.esm, Ordinator or Phenderix records (e.g. whether several perk entries of the same type all apply, or what unit a cloak radius is in), settle it and record the record IDs as evidence in 實作紀錄.md. If it genuinely needs the running game, implement the documented fallback and list it as a probe for the player to run, with what to look for. Do not guess silently either way.

Passing = `python build_v03.py` exits 0 with every existing check line ok (READBACK masters ['Skyrim.esm'], records == manifest, CSF 13/13, DELIVERY ok, LAYOUT ok, DOT ok, MCM ok, MCM DEFAULTS ok, SCHEMA ok, FIX10-FIX17 ok, PLAN COVERAGE 0 unmapped, all scripts 0 errors) plus the new `HITPROC ok` truth-table check specified in 第 1 期 item 1; versus .codex/pre-fix18-snapshot/v03-formids.json no existing FormID changes other than those the schema bump is defined to move (ESSB_DebugLevel stays 0x000811). You run the build yourself.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\Data\*.esm and D:\Game\Other\SKSE\MO2\mods\** as the build needs; executing the PapyrusCompiler.exe that build_v03.py invokes is allowed.
  - Write set (exact): src/*.psc, build_v03.py, settings.json, state-schema.lock.json, build/*.py verification helpers, 實作紀錄.md (append only), build/** and package/Elements Spellblade/** (generated), .codex/pre-fix18-snapshot/** (take it FIRST: cp -r src build_v03.py settings.json build/v03-formids.json), .codex/impl-fix-round18.html (progress ledger; create before editing product files, update per item).
  - MUST NOT: write anything under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE (the game may be running); edit 元素魔戰士規劃-v0.3.md, 元素魔戰士規劃-v0.3_alter.md, design-latency-2026-09-20.md, any review-*.md, any earlier .codex/*-snapshot/**, fx_extract.py, or .strategic-advance/** (append-only commander ledger — never assert on its hash); add masters; introduce Utility.Wait loops, non-single RegisterForUpdate or per-frame polling; access the network.
  - MUST: keep file encodings and line endings as they are (build_v03.py is CRLF).

Background (MAY):
  - The redesign exists because the old per-hit Papyrus path produced both felt latency and impossible cross-target state (actor 180126 carried poison and astral values from a session where only wind was used). design-latency-2026-09-20.md section 2 has the diagnosis and section 8 the invariants that make it structurally impossible in the new architecture.
  - Perk entry point "Apply Combat Hit Spell" (index 51) applies a spell natively on every weapon hit; vanilla TrickShot/HackAndSlash, Ordinator (129 entries) and Phenderix all use it the same way.
  - PO3's OnWeaponHit does not reliably carry a Projectile for arrow hits; bows are identified by weapon type 7/9 only (round 11 defect, fixed).

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

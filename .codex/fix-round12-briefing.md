Goal and acceptance:
Round 12 for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade: the skill tree menu opens and closes again instantly, and the MCM needs a restore-defaults button. Report in Traditional Chinese (code, identifiers, paths verbatim).

Work items (the review report review-2026-09-18.md is the authoritative list for items 0a-0b; read it in full first — it carries the file:line, the reproduction, the design citation and the minimal fix for each):

  0a. Fix review findings 1-5 (all P1, all high confidence):
      1. Continuous hits starve the controller tick (every hit pushes NextTickAt to now+1s, so the per-second tick, domains, host swap and timed effects never run while the player keeps swinging). A hit may only bring an existing deadline EARLIER, never later.
      2. An ordinary game load can permanently stop form upkeep and the magicka-empty auto-close, depending on event order after load. Distinguish "not ready yet" from "stale generation" and re-arm the existing FormRules effect after every Setup.
      3. Persisted `Utility.GetCurrentRealTime()` timestamps freeze cooldowns and the XP window after a game restart (uptime resets, stored values are larger, differences go negative). Rebase or reset short-term deadlines on load; never reset tree levels or points.
      4. Direct hits do not exclude teammates, summons and reanimated servants, although the area scans do and design 2.9 requires both.
      5. The identity guard runs 152 times per ordinary hit. Keep every external event entry guarded, but inside one already-verified transaction use internal lookups, with one more guard immediately before damage is applied.
  0b. Then apply the review's reduction plan (section G, items 1-6) as far as it does not change behaviour: cache derived values on form switch / point purchase / respec / level up / MCM change / load; read each unchanging GLOB once per hit; update only the resource mirror that actually changed; pass an already-resolved slot into internal functions; index the cache tables directly and skip the lift scan when no lift is queued; gate logging before composing the string. Report the before/after per-hit counts for the review's exact representative scenario (currently 532 scripted + 388 native = 920, target 320) using the same harness the review used (build/papyrus_harness.py). If a cache must be persisted, bump state_schema_version per the round-9 rule — do not bypass the schema or the stale-instance protection to save calls.

Work items:
  1. The tree menu flashes open and shuts. The user's words: "樹閃一下就關掉". Evidence in this session's log (quoted from Papyrus.0.log, 2026-09-18 23:18-23:19): "[ESSB][Settings][L1] open tree common" at 23:18:23; "[ESSB][Settings][L1] open tree common" AND "open tree current" inside the same second at 23:18:43; and every later open first reconciles a STALE PendingTree ("refresh tree=11" + "reconcile tree=11" right after a "open tree common"), which means the previous menu's close event never arrived. The commander verified the menu name is right: the string "StatsMenu" is present in the installed CustomSkills.dll, and ESSBTrees.MENU_NAME is "StatsMenu".
     Commander's hypothesis — verify or refute it, do not assume: ESSBSettingsEffect.OnEffectStart calls the blocking `SettingsMenu.Show()` (a MessageBox) and then, in the SAME script frame, calls ESSBTrees.OpenTree -> CustomSkills.OpenCustomSkillMenu. Opening a menu while the message box is still being torn down makes the engine close the new menu immediately, and OnMenuClose for it may never reach us. A deferred open (a single-shot update of ~0.1-0.3 s after the message box result, no Utility.Wait) would let the message box finish first. Investigate what actually happens, then fix so that: one press opens exactly one tree menu, it stays open, its close event arrives and reconciles that same tree, and a second press while a menu is pending does not open a second one.
     Also make the settings power itself robust: it must not run its menu twice for one cast, and pressing it before the controller has finished initialising must show a short "尚未就緒" notice rather than opening anything (the log shows the user pressing at 23:18:23, two seconds before "[ESSB][Init] ready" at 23:18:25).
     Keep the existing safety net (reconciling a stale PendingTree on the next open) — it is what protected the user's points — but it must stop being the normal path.
  2. DEFERRED to the next round, do not do it now: MCM restore-defaults. Add to the 平衡 page a confirm-then-reset control that writes every tunable back to its build-time default: the six ESSB_Mult* sliders, ESSB_BaseDamageMult, ESSB_NodeScale, ESSB_MultUpkeep, and the two DoT coefficient sliders on the 一般 page. Defaults come from settings.json at build time — generate them, never hardcode a second copy that can drift; add a build check that the MCM defaults equal the GLOB defaults. Show each slider's default in its MCM text so the user can see the factory value while adjusting. Confirm before applying, and notify afterwards.
  2b. Debug logging costs too much even when it is switched OFF, and the user turned logging off because of lag. In Papyrus the arguments are evaluated before the call, so `LogThrottled(3, "damage", akTarget.GetFormID() + " element=" + aiElement + " amount=" + afAmount)` builds the whole string and makes a call (which releases the caller's lock) on EVERY hit regardless of ESSB_DebugLevel; the level test happens inside the callee, too late. `ESSBLog.Log` is worse: it resolves the global with `Game.GetFormFromFile` on every call. Fix so that when the level is below the line's level, no string is built and no cross-script call is made: cache the current level in a cheap member Int (refreshed when the level changes and on load/init), and guard every call site with that comparison before composing the message. Apply it to every log call in the per-hit, per-tick and per-kill paths — the cold paths may stay as they are if you say which. Measure and report: the number of string concatenations and cross-script calls per hit at debug level 0, before and after. Do not change what is logged when the level IS high enough, and keep the 6.1 throttle behaviour.

  3. 實作紀錄.md entry "fix round 12（選單與 MCM 預設）": FIXED / NOT FIXED table, the root cause you actually established for item 1 with the evidence, and the defaults table for item 2.

Passing = `python build_v03.py` exits 0 with every existing check line ok (READBACK masters ['Skyrim.esm'], records == manifest, CSF 13/13, DELIVERY ok, LAYOUT ok, DOT ok, MCM ok, SCHEMA ok, FIX9/FIX10/FIX11 ok, PLAN COVERAGE 0 unmapped, all scripts 0 errors) plus a check that the MCM defaults match the GLOB defaults; versus .codex/pre-fix12-snapshot/v03-formids.json no existing FormID changes (ESSB_DebugLevel 0x000811) unless a member-variable change forces a state_schema_version bump — avoid one if you can, and if you cannot, bump per the round-9 rule and say why. Offline regressions for both items. You run the build yourself.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\Data\*.esm and D:\Game\Other\SKSE\MO2\mods\** (the installed Custom Skills Framework ships Source/Scripts and its DLL; MCM Helper ships its own scripts); executing the PapyrusCompiler.exe that build_v03.py invokes is allowed.
  - Write set (exact): src/*.psc, build_v03.py, settings.json, state-schema.lock.json, build/*.py verification helpers, 實作紀錄.md (append only), build/** and package/Elements Spellblade/** (generated), .codex/impl-fix-round12.html (progress ledger; create first, update per item).
  - MUST NOT: write anything under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE; edit 元素魔戰士規劃-v0.3.md (record deviations instead), review-*.md (a separate review agent owns review-2026-09-18.md), fx_extract.py, .strategic-advance/** (the commander's campaign ledger is append-only — never assert on its hash); add masters; introduce Utility.Wait loops, non-single RegisterForUpdate or per-frame polling; access the network.
  - MUST: keep file encodings and line endings as they are (build_v03.py is CRLF).

Background (MAY):
  - Rounds 1-11 are accepted and installed. Round 10 stopped an old script instance from double-dispatching events; if item 1's double-run turns out to be the same class of problem, say so.
  - ESSBTrees already registers for the menu, snapshots the tree before opening, and reconciles branch purchases on close; ESSBMCM is the MCM bridge and gameplay never references it.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

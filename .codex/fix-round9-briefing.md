Goal and acceptance:
HOTFIX round for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade. In the user's second in-game session (build = rounds 1-8, loaded on a save that still held the round-1 script instances) the mod flooded Papyrus.0.log to 179 MB / 1.9 M lines and the mod did nothing (zero [ESSB] gameplay lines). Evidence: .codex/smoke2-essb-excerpt.log (first 400 ESSB lines + a histogram of error messages). Report in Traditional Chinese (code, identifiers, paths verbatim).

Two distinct defects:
  A. `If <arrayVar> == None` is not a valid None-test for Papyrus arrays: every evaluation logs "Error: Cannot cast from None to Int[] / Actor[] / Bool[] / essbstatus[] / essbmark[]". Round 4 rewrote the init guards of ESSBController.InitFixState / InitRegistry (63 guards) into this form, and those functions run on every ArmUpdate / Setup, so this spams forever even on a brand-new save. The valid idiom is `If !arrayVar` (an array casts to False when None or empty). Fix every array-vs-None comparison in src/*.psc (both `== None` and `!= None`), and make the init functions cheap after the first success (one Bool "initialised" fast-path) so the hot path does not re-test 60 arrays per call.
  B. Save upgrade: "Error: Cannot create an array into a non-array variable" (105 k times at InitFixState). Papyrus cannot create an array into a member variable that was added to a script AFTER the instance was stored in the save; the variable stays None forever and every retry logs an error. Every future update that adds an array to a persisted script would break existing saves the same way. Required outcome: a player can update the mod on an existing save WITHOUT a clean-save procedure and without losing tree progress (tree levels / perk points / settings live in GLOBs and perks, which persist), and a broken state can never spam the log.
     Preferred design (you may choose another if you can prove it works offline and explain why): a `state_schema_version` integer in settings.json that participates in the FormID allocation of the script-carrying quest(s) (ESSB_MainQuest with its player alias and ESSBController/ESSBTrees/ESSBGuard…; check whether ESSB_MCMQuest and any other record holding persisted script state needs the same). Bumping the version gives those quests NEW FormIDs (so the game creates fresh script instances with correctly typed variables) and leaves each previous FormID as an inert stub quest (no VMAD, not start-game-enabled, no aliases) so old saves only drop the orphaned instances. Everything that references the quest (VMAD properties, MCM config refs, SEQ file, CSF if any) is generated, so it must follow automatically. The "existing FormIDs never change" rule is deliberately relaxed for exactly these quest records and only on a version bump; every other record keeps its ID. Bump the version now (the current saves hold round-1 instances). Document in 實作紀錄.md exactly when a maintainer must bump it (any added/removed/retyped member variable in a persisted script) and add a build-time check that fails when the persisted scripts' member-variable signature changed but the version did not (store the signature hash in a tracked file under the campaign root, e.g. state-schema.lock.json).
     Safety net regardless of the design: if after initialisation any required array is still None, set a Bool "StateBroken", stop all ticking/hit processing (early return at every entry point: OnUpdate, hit events, kill events, form powers), and show ONE message box telling the player the save holds an incompatible older state and how to recover. No retries, no spam.
  C. Also fix whatever else in the excerpt has an ESSB stack (e.g. "Cannot call value() on a None object" 101x, IsDead() on None, InDomain / DomainFlag / TickTimers None-array accesses) — most will be consequences of B, say which.
  D. After the fix the mod must again pick up abilities on load: the player keeps the old powers/abilities from the previous quest instance (they are separate SPEL records and stay valid) — make sure Setup on the new instance does not double-add or lose them, and that an active form from the old save is reconciled (close it cleanly on first Setup: globals ESSB_FormActive / CurrentElement reset, form abilities removed) so state and visuals cannot disagree.

Passing = `python build_v03.py` exits 0 with every existing check line ok (READBACK masters ['Skyrim.esm'], records == manifest, CSF 13/13, DELIVERY ok, LAYOUT ok, DOT ok, MCM ok, FIX7/FIX8 ok, PLAN COVERAGE 0 unmapped, all scripts 0 errors) plus a new "SCHEMA ok" line; versus .codex/pre-fix9-snapshot/v03-formids.json the only changed/added IDs are the re-allocated quest(s) and their stubs (list them; ESSB_DebugLevel stays 0x000811, all GLOB / PERK / SPEL / MGEF IDs unchanged); `grep -nE "[A-Za-z0-9_]+ (==|!=) None" src/*.psc` shows no array variable on the left side (list what remains and why it is a non-array). You run the build yourself.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\Data\*.esm and D:\Game\Other\SKSE\MO2\mods\** as the build needs; executing the PapyrusCompiler.exe that build_v03.py invokes is allowed.
  - Write set (exact): src/*.psc, build_v03.py, settings.json, state-schema.lock.json (new, campaign root), build/*.py verification helpers, 實作紀錄.md (append only), build/** and package/Elements Spellblade/** (generated), .codex/impl-fix-round9.html (progress ledger; create first, update per item).
  - MUST NOT: write anything under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE (the game is running); edit 元素魔戰士規劃-v0.3.md, review-*.md, fx_extract.py, .strategic-advance/**; add masters; renumber any record other than the script-carrying quests; introduce Utility.Wait loops or non-single RegisterForUpdate; access the network.
  - MUST: keep file encodings and line endings as they are.

Background (MAY):
  - Papyrus facts: arrays max 128 elements; `!arr` is the None/empty test; member variables added to a script after an instance was saved are left untyped/None in that instance; a quest record with a new FormID gets brand-new alias/script instances; a saved instance whose script is no longer attached to its form is discarded on load with a one-time warning.
  - Round 2 already removed `SwapInts = None` (same error class). Rounds 1-8 are otherwise accepted; do not undo them.
  - The player alias is alias 0 of ESSB_MainQuest; many VMAD properties point at the quest via own(ID_QUEST).

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

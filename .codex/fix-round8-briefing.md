Goal and acceptance:
Eighth round for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade: three user decisions (form upkeep, a baseline for the no-form state, and the last undecided node class). Report in Traditional Chinese (code, identifiers, paths verbatim).

Work items (user-approved):
  1. Form upkeep (design 1.1 deviation, user-approved). Today only 暗 (1% max magicka/s) and 血 (health by 血位) pay upkeep; the user wants every form to cost something, Phenderix-style but gentler:
     - Every active form drains max magicka per second: base 1.0% (暗 2.0%), multiplied by (1 − 0.7 × level/100) of that element's tree (100 level → 0.3% / 0.6%). 血 keeps its health rule AND pays the magicka upkeep like the others.
     - No per-hit magicka cost (the mod's mana-return mechanics — 破魔, 雷, 暗 — must stay meaningful).
     - New GLOB `ESSB_MultUpkeep` (default 1.0) applied once; MCM 平衡 page slider 維持費 0.0–3.0 step 0.1 (0 = free). settings.json keys `upkeep_base_pct`, `upkeep_dark_pct`, `upkeep_level_relief` (0.7).
     - When current magicka reaches 0 while a form is active: after a 2 s grace the form closes through the normal close path (so 融斷 fires). Notification once. The existing open threshold (magicka > 10% max) is unchanged.
     - Implement in the existing ESSBFormRules per-second update (it already ticks only while a form is active); no new update loops.
  2. No-form baseline (user-approved; today the no-form state does nothing until nodes are bought): with no form active, every valid hit (same eligibility rules as elemental procs) adds true damage = `noform_base_true` (settings.json, default 5) × G(L_noform) × ESSB_BaseDamageMult, power attacks ×1.5, through ApplyTrueDamage with G applied once. It awards no extra XP beyond what the hit already awards. It stacks with, not replaces, the 純武藝 / 破魔 node effects. Show it in the plan-coverage table as a documented addition (not a plan node).
  3. Node scale for the last undecided rows of build/fix6-classification.md: apply `ESSB_NodeScale` to the 11 "開印效果 +3%／點" mainlines (all elements) and to noform 純武藝 傳奇 "終結 +3%／點"; for blood 持續 新手 scale only the "流血每層傷害 +2%／點" part (the 放血係數 part stays). earth 關閉 專精 "地震耐力削減 +3%／點" stays unscaled (it is a drain, covered by ESSB_MultDrain). Update generated node text accordingly.
  4. 實作紀錄.md entry "fix round 8（維持費、無形態基礎、開印節點）": FIXED / NOT FIXED table, upkeep numbers per level (1/25/50/75/100) for a 300-magicka character, the auto-close path, and the once-only proofs for G and the sliders.

Passing = `python build_v03.py` exits 0 with every existing check line ok (READBACK masters ['Skyrim.esm'], records == manifest, CSF 13/13, DELIVERY ok, LAYOUT ok, DOT ok, MCM ok with the new refs, PLAN COVERAGE 0 unmapped, all scripts 0 errors); no existing FormID changes versus .codex/pre-fix8-snapshot/v03-formids.json (ESSB_DebugLevel 0x000811; new GLOBs appended only). You run the build yourself.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\Data\*.esm and D:\Game\Other\SKSE\MO2\mods\** as the build needs; executing the PapyrusCompiler.exe that build_v03.py invokes is allowed.
  - Write set (exact): src/*.psc, build_v03.py, settings.json, plan_coverage.py (for the documented addition row), 實作紀錄.md (append only), build/** and package/Elements Spellblade/** (generated), .codex/impl-fix-round8.html (progress ledger; create first, update per item).
  - MUST NOT: write anything under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE (the game may be running); edit 元素魔戰士規劃-v0.3.md (record deviations instead), review-*.md, fx_extract.py, .strategic-advance/**; add masters; renumber records; introduce Utility.Wait loops, non-single RegisterForUpdate or new per-frame polling; DamageActorValue enemy health; access the network.
  - MUST: keep file encodings and line endings as they are.

Background (MAY):
  - Rounds 1-7 landed; round 7 added the five ESSB_Mult* sliders and the MCM 平衡 page (generated by build_v03.py). Do not undo them.
  - Design 1.1 says only 暗/血 pay upkeep and "MCM 可選：調整維持費" — this round realises the latter with the user's new rule.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

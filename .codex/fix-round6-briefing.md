Goal and acceptance:
Sixth round (node value rebalance) for the Skyrim SE 1.5.97 mod at D:\Game\Other\SKSE\.codex\elemental-spellblade. The user judged the trees too conservative after playing. Numbers only — no mechanic changes. Report in Traditional Chinese (code, identifiers, paths verbatim).

Work items (user-approved):
  1. Pure-percentage mainline nodes x3. In build/plan-tree-nodes.json, 56 mainline nodes are plain "+N%／點" bonuses to damage-like quantities (distribution today: 44 at 1%, 7 at 2%, 4 at 3%, 1 at 5%, plus the 0.2% and 0.5% ones). Multiply the per-point value of exactly that class by 3 (1% -> 3%, 15 points = +45%). NOT in scope: chance nodes (e.g. 千刃 5%／點), range/duration/cap/cooldown nodes, heal/regen nodes (e.g. 水 長流), resist-shred nodes, and every branch node. Produce the explicit list (tree / route / tier / old text / new value / code site) and get it from data, not by eye: classify all 195 mainlines and show the classification table so the commander can audit borderline cases; when unsure, leave the node unchanged and list it under "not changed — needs a decision".
     Implementation: one tunable `node_percent_scale` in settings.json (default 3.0) -> float GLOB `ESSB_NodeScale` (appended ID) bound as a script property, applied through one helper (e.g. ESSBNodes.Pct(rank, basePerPoint)) that every in-scope call site uses, so the 56 sites cannot drift. In-game node descriptions (PERK DESC / CSF text generated by build_v03.py) must show the scaled numbers for the default scale, including any "（8% → 11%）"-style ranges.
  2. 破魔 (no-form tree, route 1) feels too small: today drain = 5 + rank (max 20 magicka per hit, not level-scaled), so enemy mages with hundreds of magicka are never emptied and 沉默 never fires; true damage starts near 3 per hit. New numbers:
     - drain per hit = (20 + 4 x rank) x G(L_noform), G(L) = 1 + 0.05 x tree level (rank 15 at level 100 = 480; rank 1 at level 10 = 36). [user doubled the commander's first proposal] 大師 "+5%／點 vs casters" still multiplies on top.
     - 蝕魔 branch: "目標最大魔力 2%" -> 10% (still "取高者").
     - true damage = actual drained amount x ratio (ratio node unchanged: 0.5 -> 0.95). The drained amount now already carries G(L), so make sure G(L) is applied exactly once on this path (ApplyTrueDamage currently multiplies G itself — add a no-scale path or pre-divide; prove it in the report).
     - 枯竭 branch: 5% of the player's current magicka -> 20%.
     - 反咒 and 沉默 durations unchanged. Magicka restored to the player stays equal to the actual drained amount.
     Put these constants in settings.json too (`manabreak_base`, `manabreak_per_rank`, `manabreak_maxmag_pct`, `manabreak_dry_pct`).
  2b. Water tree numbers are also too small to notice (長流 heals 0.3% of max health per second: under 1 HP/s on a 300 HP character). Numbers only:
     - 長流 base regen 0.3% -> 2.0% per second [user doubled the first proposal] (health and stamina, same rule as today about what it restores and when).
     - 熟練 mainline "+0.02%／點（0.3% → 0.6%）" -> +0.2%／點 (2.0% -> 5.0%). 大師 (+0.05%／點 per sync stage) and 傳奇 (+0.05%／點) stay, so the absolute ceiling at sync 3 is 5.0 + 2.25 + 0.75 = 8.0% per second; compare it with what the 神聖 tree can restore in sustained combat (design 5.11 says water's total healing stays under divine) — report both numbers and flag it if water now overtakes divine; do not silently cap.
     - 浸濕 base slow 10% -> 15% (user's final number; the +1%／點 mainline stays, 15% -> 30%; 水牢 branch unchanged).
     - Frost base slow (開印「霜結」: 減速 15% 3 秒) -> 25% (user's final number); frost nodes that add to it are unchanged.
     - Add one shared cap: total movement slow applied by this mod to a single target never exceeds 70% (soft control only; enemies must never be rooted). Say where you enforce it.
     - 清流 branch: stamina on hit 5 -> 30. 回流 branch: stamina on open 15 -> 80.
     - Everything else in the water tree unchanged (its "+N%／點" damage mainlines already get item 1's x3).
     Put the new constants in settings.json (`water_flow_base_pct`, `water_flow_per_rank_pct`, `water_wet_slow_pct`, `frost_open_slow_pct`, `slow_cap_pct`, `water_clear_stamina`, `water_open_stamina`) and update generated node text.
  2c. Inventory only — do NOT change these values: list every fixed (non-percentage, non-level-scaled) number in all 13 trees and the base reaction table that a high-level character would not notice: flat heals, flat stamina/magicka restores or drains, flat damage, flat armour/resist amounts, flat shields, durations under 2 s. Source of truth is the code (src/*.psc literals and settings.json), cross-referenced to the node text in build/plan-tree-nodes.json. Write it as one table to build/flat-values-audit.md (and summarise in the report): tree / route / tier / node or branch name / what it does / current value / does it scale with tree level or G(L)? (yes/no) / code site (file:function) / your suggested value with a one-line reason, judged against a level-40+ character with roughly 300-500 health, 200-400 stamina and magicka, fighting enemies with 500-2000 health. Sort by how far below noticeable each value is. Exclude what items 2 and 2b already change.
  3. 實作紀錄.md entry "fix round 6（節點數值）": FIXED / NOT FIXED table, the full classification table from item 1, before/after for item 2, and the resulting worst-case multiplier check: for each element, M_mod at level 100 with every in-scope mainline maxed and sync stage 3 (so the user can see the new ceiling next to the old ~x1.7).

Passing = `python build_v03.py` exits 0 with every existing check line ok (READBACK masters ['Skyrim.esm'], records == manifest, CSF 13/13, DELIVERY ok, LAYOUT ok, MCM ok if present, PLAN COVERAGE 0 unmapped, all scripts 0 errors); no existing FormID changes versus .codex/pre-fix6-snapshot/v03-formids.json (ESSB_DebugLevel 0x000811; new GLOBs appended only). You run the build yourself.

Permission boundary:
  - Read scope: everything under D:\Game\Other\SKSE\.codex\elemental-spellblade; read-only D:\Game\Other\SKSE\SkyrimSE\Data\*.esm and D:\Game\Other\SKSE\MO2\mods\** as the build needs; executing the PapyrusCompiler.exe that build_v03.py invokes is allowed.
  - Write set (exact): src/*.psc, build_v03.py, settings.json, plan_coverage.py / plan_trees.py (only if generated node text must follow the new numbers), 實作紀錄.md (append only), build/** and package/Elements Spellblade/** (generated), MCM config generated by the build, .codex/impl-fix-round6.html (progress ledger; create first, update per item).
  - MUST NOT: write anything under D:\Game\Other\SKSE\MO2 or D:\Game\Other\SKSE\SkyrimSE; edit 元素魔戰士規劃-v0.3.md (record deviations instead), review-*.md, fx_extract.py, .strategic-advance/**; add masters; renumber records; change any mechanic, cap, cooldown or branch; access the network.
  - MUST: keep file encodings and line endings as they are. If the MCM from round 5 exists, add `ESSB_NodeScale` to its 一般 page as a slider (1.0-5.0, step 0.5).

Background (MAY):
  - Rounds 1-5 landed today (review fixes; log noise + layout; FX; B table / DoT / base multiplier; longer DoT layers + MCM). Do not undo them.
  - The design doc's plan-tree parsing (plan_trees.py) reads node text from the design doc; the doc must not change, so scaled display text has to be produced at generation time.

You may change tools, commands, and technical approaches at will,
within the granted permissions and write set.
A single tool or approach being unavailable means only that this
strategy failed — switch approaches and retry. Report blocked only
when the acceptance goal is genuinely unreachable, or when you need
permissions or scope beyond what was granted.

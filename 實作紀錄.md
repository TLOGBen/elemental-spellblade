# 實作紀錄（append-only）

規劃檔 `元素魔戰士規劃-v0.3.md` 是設計真相，不修改。任何與規劃不同的實作選擇都記在這裡。

---

## 2026-09-16 — form-layer-core（CORE_PACKAGE_BUILT）

新進入點 `build_v03.py`，產出 `package/Elements Spellblade/Elements Spellblade.esp`
（masters 只有 `Skyrim.esm`、一般 ESP 不設 ESL 旗標、113 筆記錄）與 5 個 PEX。
`build_core.py` / `build_melee.py` / `build_scripts.py` 保留不動，僅作歷史；它們掛
`Phenderix Elements.esp` 為 master，已不代表現行包。

### 保留的 FormID 區段（`build/v03-formids.json` 內有完整表）

| 區段 | 用途 | 現況 |
|---|---|---|
| 0x000800–0x00080F | QUST 與任務層 | 已用 0x000800（ESSB_MainQuest） |
| 0x000810–0x00082F | GLOB 全域變數 | 已用 0x000810–0x000815 |
| 0x000830–0x00083F | KYWD 通用關鍵字 | 已用 0x000830（ESSB_Proc）、0x000831（ESSB_TrueDamage） |
| 0x000840–0x00084F | KYWD 元素關鍵字 ×11 | 已用 0x000840–0x00084A |
| 0x000850–0x00085F | KYWD 印記關鍵字 ×11 | 已用 0x000850–0x00085A（引擎前線使用） |
| 0x000860–0x00088F | KYWD 保留（狀態、反應、真傷細分） | 保留 |
| 0x000890–0x0008FF | MESG／設定層 | 已用 0x000890–0x000892 |
| 0x000900–0x00091F | MGEF 形態力量效果 ×11 | 已用 0x000900–0x00090A |
| 0x000920–0x00093F | SPEL 形態力量 ×11 | 已用 0x000920–0x00092A |
| 0x000940–0x00095F | MGEF 形態能力效果 ×11 | 已用 0x000940–0x00094A |
| 0x000960–0x00097F | SPEL 形態能力 ×11 | 已用 0x000960–0x00096A |
| 0x000980–0x0009FF | 形態規則與保留 | 已用 0x000980（MGEF）、0x000981（SPEL） |
| 0x000A00–0x000AFF | MGEF 附傷效果 ×11 | 已用 0x000A00–0x000A0A |
| 0x000B00–0x000BFF | SPEL 附傷法術 ×22 | 已用 0x000B00–0x000B15 |
| 0x001000–0x001FFF | 保留：印記、元素狀態、反應、同調 | 引擎前線 |
| 0x002000–0x002FFF | 保留：PERK 與技能樹 | 機制前線 |
| 0x003000–0x003FFF | 保留：EFSH／RFCT／SNDR 等特效素材 | 特效前線 |

TES4 HEDR 的 next object id 設在 0x004000，之後前線在保留段內配置即可，不需重新編號。

### 與規劃不同或需要留底的決定

1. **附傷套用方式：Papyrus `DoCombatSpellApply`，不觸發施法事件；perk 進入點留待實測。**
   規劃 2.13 首選 perk 進入點「Apply Combat Hit Spell」。本前線先走 Papyrus 路徑
   （PO3 `OnWeaponHit` → `Actor.DoCombatSpellApply`），因為它同時涵蓋弓弩的箭矢命中與
   潛行射擊視為重擊的判定，而 perk 進入點的條件式難以區分這兩者。兩者都不呼叫
   `Spell.Cast`，所以 Ordinator 瓦希安魔法的法術槽不會被消耗。本前線沒有建立任何 PERK 記錄。

2. **11 個附傷 MGEF 對應 22 個 SPEL。** 規劃只說 22 個附傷法術。普通／重擊共用同一個 MGEF，
   差別放在 SPEL 的 EFIT 強度（暫定 8 / 12，來自 `settings.json`）。這樣元素關鍵字、學派、
   抗性與技能使用倍率只有一處來源，機制前線改各元素點傷範圍時只動 SPEL。

3. **開形態的魔力門檻只擋「開啟」，不擋「切換」。** 規劃 1.1 明寫「開形態需要魔力高於上限的
   10%。切換不需要」。前線指示的措辭讀起來像是切換也要檢查，這裡以規劃為準：
   `ToggleForm` 在 `FormActive == 1` 時直接進 `SwitchForm`，不檢查魔力。

4. **`ESSBLog` 的節流拆成兩半。** Papyrus 的全域函式不能持有狀態，所以：
   - `ESSBLog.Log(level, mechanism, msg)` 只做等級判定與 `Debug.Trace`，供等級 1 的低頻事件使用；
     等級由 `Game.GetFormFromFile(0x00000811, "Elements Spellblade.esp")` 讀回，因此
     `ESSB_DebugLevel` 的本地 FormID 0x000811 已成為對外契約，不可更動。
   - 每秒 20 行上限與丟棄計數放在 `ESSBController.LogThrottled()`（等級 2 以上一律走這裡），
     計數器是腳本變數。呼叫端在組字串前先比對 `DebugLevel`，符合規劃 6.1「不得組大字串再判斷等級」。
   - 「同一目標同一訊息 0.5 秒內不重複」尚未實作，等引擎前線有目標表時再加。

5. **形態維持費的喚醒方式。** `ESSB_FormRulesAbility` 由控制器在初始化時加給玩家並常駐。
   `ESSBFormRules` 只在形態啟用時跑 `RegisterForSingleUpdate(1.0)`；形態關閉時就不再排程，
   由控制器的 SKSE 模組事件 `ESSB_FormChanged` 重新喚醒。目前只實作血 0.5% 最大生命／秒與
   暗 1% 最大魔力／秒，其餘形態不扣。

6. **形態能力 MGEF 用 Script 原型但不掛腳本。** 11 個形態能力只是關鍵字載體。已確認原版
   Skyrim.esm 有 29 個「Script 原型 + 常駐 + 自身 + 無 VMAD」的 MGEF（例如 `AbOnFire`），
   這個作法是原版既有模式。命中著色器與視覺留空，等特效前線綁定。

7. **附傷法術沿用原版 Novice 半價天賦。** 沿用 `build_core.py` 當初的發現：現行天賦總成會檢查
   原本施法天賦的身分，不只看學派。因為只掛 Skyrim.esm，取的是原版
   `DestructionNovice00` / `RestorationNovice00` / `ConjurationNovice00`。

8. **腳本原始碼以 UTF-8 BOM 寫入，字串含繁體中文。** 沿用原型的作法（已編譯通過）。
   遊戲內的中文顯示尚未驗證，屬於遊戲內驗收項目，靜態檢查不能替代。

9. **`src/ESSBPlayerAlias.psc` 保留在 src/ 作歷史，但不再編譯或部署。** 建置時會把
   `package/.../Scripts/ESSBPlayerAlias.pex` 與 `Source/Scripts/ESSBPlayerAlias.psc` 刪除。
   `src/ESSBSettingsEffect.psc` 已改寫為新選單（附傷開關／除錯等級 0→1→2→3→0），
   因此 `build_melee.py` 不再與它相容。

### 尚未驗證（靜態檢查不能替代）

遊戲內載入、QUST 啟動與腳本綁定、PO3 `OnWeaponHit` 實際旗標、形態切換手感、
存讀檔後的 `OnPlayerLoadGame` 重新註冊、中文字串顯示。

---

## 2026-09-16 08:09 — reaction-engine CHECKPOINT（partial，指揮官暫停）

引擎前線（印記／元素狀態／同調／融斷／開印終焉）**尚未完成**。本次在「可建置的一致
狀態」停手：`build_v03.py` 未被修改，`python build_v03.py` 仍 exit 0、113 筆記錄、
masters 只有 `Skyrim.esm`、5 個腳本全部編譯 0 錯誤。包的行為與上一個里程碑完全相同。

### 已完成（已落地）

1. `vendor/imports/PO3_SKSEFunctions.psc`：從 MO2 的 powerofthree's Papyrus Extender
   Source 複製進來（只讀來源不動）。引擎需要 `GetActorsByProcessingLevel`、
   `GetWeatherType`、`IsVIP`、`GetCommandedActors`。純新增匯入，不影響現有編譯。
2. `src/ESSBMark.psc`：印記 ActiveMagicEffect，**已寫完但尚未納入編譯白名單**
   （`build_v03.py` 的 `SCRIPTS` 未動，所以不會被複製／編譯／部署）。
   設計：AME 本身不判斷結束原因，只回報 `OnMarkStart` / `OnMarkFinish` 給控制器，
   由控制器登記表決定是切掉／融斷／逐出／自然過期，解掉規劃 8「需分辨 Dispel 與自然結束」。

### 半成品（設計已定，程式未寫）

`src/ESSBStatus.psc` 寫入時中斷，檔案**不存在**，沒有殘留。下列決定已經定案，
續作時直接照做即可，不需重新推導：

**FormID 配置（全部落在引擎前線自有的 0x001000–0x001FFF；不動 0x000811）**

| 範圍 | 內容 |
|---|---|
| 0x001000–0x00100A | MGEF `ESSB_MarkEffect_<X>` ×11（Script 原型，8 秒／水 10 秒，隱藏無傷） |
| 0x001020–0x00102A | SPEL `ESSB_MarkSpell_<X>` ×11（type 0、casting 1、delivery 3 target actor） |
| 0x001040–0x00104A | MGEF `ESSB_ReactEffect_<X>` ×11（反應傷害，掛 `ESSB_React` 關鍵字，不掛 `ESSB_Proc` 以免遞迴） |
| 0x001060–0x00106A | SPEL `ESSB_React_<X>` ×11（執行期以 `SetNthEffectMagnitude(0, dmg)` 設值後 `DoCombatSpellApply`） |
| 0x001080 / 0x001081 | MGEF / SPEL `ESSB_StatusHost`（30 秒，Script 原型，掛 `ESSBStatus`） |
| 0x001082 / 0x001083 | MGEF / SPEL `ESSB_Engaged`（30 秒已交戰標記，規劃 2.9） |
| 0x001090–0x001097 | MGEF ×8：Slow(AV30)、ArmorDebuff(AV39)、DrainMagicka(AV25)、DrainStamina(AV26)、RestoreHealth、RestoreMagicka、RestoreStamina、BleedTick(resist −1) |
| 0x0010A0–0x0010A7 | 對應 SPEL ×8（magnitude 與 duration 都用 `SetNthEffectMagnitude` / `SetNthEffectDuration` 執行期設定） |
| 0x001F00–0x001F02 | KYWD `ESSB_StatusHost` / `ESSB_Engaged` / `ESSB_React` |
| 0x001F10–0x001F15 | GLOB `ESSB_SyncT1`(5) / `T2`(15) / `T3`(30) / `ESSB_EnvWet` / `ESSB_EnvStormy` / `ESSB_EnvNight` |

**已決定但尚未寫進程式的實作選擇（續作時一併補進本紀錄的「與規劃不同」段）**

1. **KYWD 與 GLOB 放在 0x001Fxx 而不是原本的 0x00083x／0x00081x 段。** 前線指示明寫
   「allocate in 0x001000–0x001FFF」，所以引擎自有的關鍵字與全域變數不佔用共用段，
   避免跨前線搶號。
2. **EDID 命名避開既有關鍵字。** `ESSB_Mark_<X>` 已是 0x000850 段的 KYWD，所以印記的
   MGEF／SPEL 改名 `ESSB_MarkEffect_<X>` / `ESSB_MarkSpell_<X>`。
3. **反應傷害一律走自有法術 + `SetNthEffectMagnitude`**，不用 `DamageActorValue` 打生命，
   讓 Ordinator 天賦成立；只對本模組自有、玩家專用的 record 改 magnitude。
4. **狀態容器換宿主要搬家。** `ESSB_StatusHost` 只有 30 秒，重掛會換一個 AME 實體，
   所以 `ESSBStatus` 提供 `ExportInts/ExportFloats/ImportState`，由控制器在補掛時搬移層數，
   並且補掛只在 `GetTimeElapsed() > 25` 時做（每目標約 25 秒一次，不是每次命中）。
5. **狀態實體尚未就緒時的緩衝。** 第一次命中同一幀內 `ESSB_StatusHost` 的 AME 還沒啟動，
   所以控制器登記表每格帶一組 `RegPendElem` / `RegPendAmt`，狀態註冊時再補上。
6. **環狀桶尺寸**：毒層 6 格（每層獨立 6 秒）、血痕 5 格（命中刷新全部＝併回最新格）、
   星痕 2 格（2 秒後自行引爆）。`RingSum` 相加、每秒丟最舊一格。
7. **天氣**：用 `Weather.GetCurrentWeather().GetClassification()`（2 雨／3 雪）決定
   `ESSB_EnvStormy`，PO3 `GetWeatherType` 的回傳值只在等級 3 紀錄供進遊戲校準
   （`Game.GetCurrentWeather` 不存在，正確 API 是 `Weather.GetCurrentWeather()` global）。
   夜晚讀 Skyrim.esm 0x38 `GameHour`（20:00–6:00）。濕：天氣為雨／雪，或玩家 `IsSwimming()`
   / PO3 `IsRefUnderwater`。
8. **明確延後給機制前線（要寫進 `build/engine-coverage.json`）**：風的拉近／吹飛／吹上天
   與土的跌倒推力（規劃 8 明寫需實測）、聖印的「受聖傷 +20%」與死咒的「無法被治療」
   （需要 PERK 進入點，PERK 段 0x002000 屬機制前線）、岩甲「被打 −1」（需要玩家受擊事件）、
   風勢到門檻的風刃（樹機制）。
9. **裁決對亡靈魔族的讀法**：規劃 2.6 寫 ×2.0、對亡靈魔族 ×3；2.1 又寫附傷 ×1.5。
   決定：裁決基準 K=2.0，對 `ActorTypeUndead`(0x13796)／`ActorTypeDaedra`(0x13797) 再 ×3。

### 下一步（具體，照順序）

1. 寫 `src/ESSBStatus.psc`（內容如上，約 450 行；上次就是寫到這裡被中斷）。
2. 寫 `src/ESSBReactions.psc`：`Open(ESSBController, Int, Actor)` 與
   `End(ESSBController, Int, Actor, Int reason, Float mult)`，11 個元素照規劃 2.6 基礎表。
   （`End` 不是 Papyrus 保留字，但第一次編譯要確認。）
3. 改寫 `src/ESSBController.psc`：8 格登記表、開印／終焉各 1 秒冷卻、第 9 目標逐出、
   同調計數與 `OnSyncStage`、`OnFormClosed` 的融斷、1 秒 tick（自身狀態衰減）與 5 秒環境檢查、
   `IsValidTarget` / `ScanTargets`、`ApplyDamage` 等法術套用封裝、`DumpRegistry`。
4. `src/ESSBSettingsEffect.psc` 加第三個選項「除錯：印出目標表」，MESG 0x000890 加一條 ITXT，
   VMAD 補 `Controller` 屬性。
5. `build_v03.py`：加上面的記錄、把 `ESSBMark` / `ESSBStatus` / `ESSBReactions` 加進 `SCRIPTS`
   白名單、更新 `ALLOCATION` 表、產出 `build/engine-coverage.json`（2.2–2.10 每一條對應
   到腳本與函式，或標 `DEFERRED-to-mechanics` 並寫原因）。
6. 跑 `python build_v03.py`，確認 exit 0、`build/v03-compile-results.json` 全 0、
   回讀 masters 與 record_count 相符，然後把本段的「已決定」搬成正式的「與規劃不同」條目。

---

## 2026-09-16 20:02 — reaction-engine（ENGINE_BUILT）

引擎前線完成：印記、元素狀態、同調、融斷、開印與終焉全部落地。
`python build_v03.py` exit 0、**186 筆記錄**（上一個里程碑 113 筆，+73）、masters 只有
`Skyrim.esm`、一般 ESP 不設 ESL 旗標、**8 個腳本全部 0 error 0 warning**。
新增覆蓋表 `build/engine-coverage.json`：規劃 2.2–2.10 共 **103 條**，83 條已落地、
21 條標 `DEFERRED-to-mechanics` 並附原因，沒有任何一條沒有對應。

新腳本三支：`src/ESSBStatus.psc`（狀態容器）、`src/ESSBReactions.psc`（開印與終焉）、
改寫的 `src/ESSBController.psc`（登記表、同調、融斷、環境、範圍掃描）。
`src/ESSBMark.psc` 從上一個檢查點的「寫完但未納入白名單」正式進入編譯白名單。
`src/ESSBSettingsEffect.psc` 加上第三個選項「除錯：印出目標表」。

### 本前線新增的 FormID（全部落在 0x001000–0x001FFF，沒有動 0x000811）

| 區段 | 內容 | 現況 |
|---|---|---|
| 0x001000–0x00100A | MGEF `ESSB_MarkEffect_<X>` ×11（Script 原型、隱藏、無傷，掛 `ESSB_Mark_<X>`） | 已用 |
| 0x001020–0x00102A | SPEL `ESSB_MarkSpell_<X>` ×11（type 0、casting 1、delivery 3，EFIT 時長 8） | 已用 |
| 0x001040–0x00104A | MGEF `ESSB_ReactEffect_<X>` ×11（掛 `ESSB_React` 與元素關鍵字，不掛 `ESSB_Proc`） | 已用 |
| 0x001060–0x00106A | SPEL `ESSB_React_<X>` ×11（執行期 `SetNthEffectMagnitude(0, dmg)`） | 已用 |
| 0x001080 / 0x001081 | MGEF `ESSB_StatusHostEffect` / SPEL `ESSB_StatusHostSpell`（30 秒，掛 `ESSBStatus`） | 已用 |
| 0x001082 / 0x001083 | MGEF `ESSB_EngagedEffect` / SPEL `ESSB_EngagedSpell`（30 秒已交戰標記，規劃 2.9） | 已用 |
| 0x001090–0x001097 | MGEF ×8：緩速(AV30)、碎甲(AV39)、削魔(AV25)、削耐(AV26)、回血、回魔、回耐、放血(resist none) | 已用 |
| 0x0010A0–0x0010A7 | SPEL ×8 對應上表（強度與時長都在執行期設定） | 已用 |
| 0x0010B0–0x001EFF | 保留：引擎前線後續（雙印、領域、真傷細分） | 保留 |
| 0x001F00–0x001F02 | KYWD `ESSB_StatusHost` / `ESSB_Engaged` / `ESSB_React` | 已用 |
| 0x001F10–0x001F15 | GLOB `ESSB_SyncT1`(5) / `T2`(15) / `T3`(30) / `ESSB_EnvWet` / `ESSB_EnvStormy` / `ESSB_EnvNight` | 已用 |

### 已實作的 11 條基礎開印／終焉（規劃 2.6 基礎表）

`B` 表該元素的 `B_max`（規劃 2.1）；`G(L) = 1`（見下方第 19 條）；
`M_mod` 一律經 `ESSBController.GetDamageMult()`；所有傷害走自有的 `ESSB_React_<X>` 法術。

| 元素 | 開印（已實作） | 終焉（已實作） | 函式 |
|---|---|---|---|
| 火焰 | 點燃：熱度 +2，立即 B ×0.5 火傷 | 爆燃：B ×1.0 ×(1 + 熱度 ×0.2)，消耗熱度 | `Open(1)` / `EndFire` |
| 冰霜 | 霜結：凍結 +3（暴風雪 ×2），減速 15% 3 秒 | 碎冰：冰封且 <20% 生命 → 處決（首領改 B ×5.0）；冰封 → B ×2.5 加碎甲 10% 5 秒；未冰封 → B ×1.0 | `Open(2)` / `EndFrost` |
| 雷電 | 感電：你 +2 電荷，目標魔力 −B ×1.0 | 放電：電荷 ×30% B，削魔 = 傷害 50%，跳 2 人各 40%；滿格 ×2；唯一吃重擊倍率的終焉 | `Open(3)` / `EndShock` |
| 大地 | 裂痕：護甲 −30（8 秒）、耐力 −10，你回 10 耐力並 +2 岩甲 | 地震：3 公尺 B ×1.5 土傷、削耐 B ×2、減速 30% 3 秒（跌倒推力延後） | `Open(4)` / `EndEarth` |
| 風 | 風痕：你風勢 +2，施加失衡（拉近推力延後） | 吹飛：目標與附近 2 人各 B ×1.0；首領與大型改減速 30% 3 秒（吹飛推力延後） | `Open(5)` / `EndWind` |
| 鮮血 | 血痕：流血 2 層，依血位吸血 | 血潮：結算剩餘流血總傷 + 目標當前生命 10%（首領 3%），治療你 = 傷害 × 吸血比例 ×2 | `Open(6)` / `EndBlood` |
| 神聖 | 聖印：聖印 1 層，你回血 B ×0.5（受聖傷 +20% 延後） | 裁決：B ×2.0 聖傷，對亡靈魔族 ×3，治療你 B ×1.0 | `Open(7)` / `EndDivine` |
| 毒素 | 淬毒：+3 毒層，立即向 3 公尺內一名敵人傳 1 層 | 催毒：不結清，8 秒內每秒跳兩次、擴散改每秒一次，關閉形態後仍工作 | `Open(8)` / `EndPoison` |
| 水 | 浸濕：減速 10%，印記時長改 10 秒 | 導引：接管元素的下一次終焉 ×1.5，你 +5 同調 | `Open(9)` / `EndWater` |
| 黑暗 | 詛咒：2 層詛咒，吸魔 B ×1.0（削目標魔力並回你等量） | 死咒：3 秒後 B ×2.0 + 目標已損失生命 15%（無法被治療延後） | `Open(10)` / `EndDark` |
| 星界 | 星痕：+1 層，2 秒後自行引爆 B ×1.0 | 星落：B ×2.0 星傷，接管元素的這次開印 ×1.5 | `Open(11)` / `EndAstral` |

終焉理由 `aiReason`：0 被切掉、1 融斷（乘 K_sync）、2 自然過期、3 第 9 目標逐出（視同過期）。
「接管元素」類效果（導引、星落）只在 `aiReason == 0` 時設定，符合規劃 2.6。

### 與規劃不同或需要留底的決定（接續上一段的 1–9）

10. **KYWD 與 GLOB 放在 0x001Fxx，不放原本的 0x00083x／0x00081x 段。**
    前線指示明寫「allocate in 0x001000–0x001FFF」，所以引擎自有的關鍵字與全域變數不佔用
    共用段，避免跨前線搶號。共用段 0x000860–0x00088F 仍然保留空著。

11. **EDID 命名避開既有名稱。** `ESSB_Mark_<X>` 已是 0x000850 段的 KYWD，所以印記的
    MGEF／SPEL 改名 `ESSB_MarkEffect_<X>` / `ESSB_MarkSpell_<X>`。同理，KYWD
    `ESSB_StatusHost` 與 `ESSB_Engaged` 已佔用名稱，法術改名
    `ESSB_StatusHostSpell` / `ESSB_EngagedSpell`（檢查點的表寫的是 `ESSB_StatusHost`，
    建置時的重複 EDID 檢查擋下來，以 KYWD 名稱為準）。

12. **反應傷害一律走自有法術 + `SetNthEffectMagnitude`，不用 `DamageActorValue` 打生命。**
    讓 Ordinator 等天賦照常成立；只對本模組自有、玩家專用的 record 改 magnitude，
    不動任何外部記錄。削魔、削耐、減速、碎甲、回復、放血同理，走 0x001090 段的 8 個自有法術。

13. **狀態容器換宿要搬家。** `ESSB_StatusHost` 只有 30 秒，重掛會換一個 AME 實體，
    所以 `ESSBStatus` 提供 `ExportInts` / `ExportFloats` / `ImportState`，
    由 `ESSBController.SwapHosts()` 在 `GetTimeElapsed() > 25` 時搬移層數
    （每目標約 25 秒一次，不是每次命中）。搬家順序是「匯出 → `PrepareSwap()` → `Dispel()`
    → 重掛 → `OnStatusStart` 匯入」，`PrepareSwap()` 讓舊實體的 `OnEffectFinish`
    不回報控制器，登記表不會被搬家動作清掉。

14. **狀態實體尚未就緒時的緩衝。** 第一次命中同一幀內 `ESSB_StatusHost` 的 AME 還沒啟動，
    所以登記表每格帶一組 `RegPendElem` / `RegPendAmt`，`ESSBController.AddStack()` 先緩衝，
    `OnStatusStart` 再補上。同種類會累加；不同種類會覆蓋並在等級 3 留一行紀錄。

15. **環狀桶尺寸與語意。** 毒層 6 格（每層獨立 6 秒、命中不刷新舊層）、血痕 5 格
    （命中刷新全部＝`RingMerge` 併回最新格）、星痕 2 格（掉出桶的那一格即引爆）。
    最新的一秒固定在 `[0]`，`RingAge()` 每秒往後推一格並回傳掉出去的層數。
    `RingSum` 相加得層數總和，記憶體固定、負載不隨層數增加。

16. **天氣與時間。** 用 `Weather.GetCurrentWeather().GetClassification()`（2 雨／3 雪）
    同時決定 `ESSB_EnvWet` 與 `ESSB_EnvStormy`（`Game.GetCurrentWeather` 不存在，
    正確 API 是 `Weather.GetCurrentWeather()` global）。**雷雨與暴風雪在分類上無法細分**，
    兩者都落在 Stormy；PO3 `GetWeatherType` 的回傳值只在除錯等級 3 紀錄，供進遊戲校準。
    夜晚讀 Skyrim.esm 0x38 `GameHour`（20:00–6:00）。濕：天氣為雨／雪，或玩家
    `IsSwimming()` / PO3 `IsRefUnderwater`。規劃 2.10「室內、地城無環境加成」用
    `GetParentCell().IsInterior()` 實作：室內先把 wet／stormy 歸零，游泳仍算濕。
    環境檢查每 5 秒一次，只在值改變時留等級 1 紀錄。

17. **明確延後給機制前線的項目，全部寫進 `build/engine-coverage.json`（21 條）。**
    主要是：風的拉近／吹飛／吹上天與土的跌倒推力（規劃 8 明寫需實測）、聖印的
    「受聖傷 +20%」與死咒的「無法被治療」（需要 PERK 進入點，PERK 段 0x002000 屬機制前線）、
    岩甲「被打 −1」（需要玩家受擊事件）、風勢到門檻的風刃（樹機制）、真實傷害（無元素樹）、
    萬象上限、各樹分支與「臨」「領域」類範圍效果、同調光暈與 TrueHUD（特效前線）。

18. **裁決對亡靈魔族的讀法。** 規劃 2.6 寫 ×2.0、對亡靈魔族 ×3；2.1 又寫附傷 ×1.5。
    決定：裁決基準 K = 2.0，對 `ActorTypeUndead`(0x13796) 或 `ActorTypeDaedra`(0x13797)
    再 ×3。兩個關鍵字以 VMAD 屬性指向 Skyrim.esm，不新增記錄。

19. **`G(L) = 1`。** 規劃 2.7 的 `G(L) = 1 + 0.05 × L` 需要各元素樹等級，技能樹屬機制前線，
    本前線沒有等級來源，所以公式裡的 `G(L)` 恆為 1，並在 `engine-coverage.json` 標
    `DEFERRED-to-mechanics`。實作上 `G(L)` 不是常數乘在程式裡，而是「之後升級時更新自有法術
    magnitude」，所以機制前線接手時不需要改 `ESSBReactions` 的任何一行。

20. **`M_mod` 只有一個掛勾 `ESSBController.GetDamageMult(element)`。** 本前線放進去的只有
    規劃 2.10 的環境加成（夜晚黑暗 +20%、白天神聖 +20%）與規劃 1.1 的血位命中倍率。
    節點加成之後接在同一個函式裡，附傷、持續傷、反應、融斷四條公式都不必再改。
    血位曲線以規劃 1.1 的四個點線性內插實作：命中倍率 100% ×1.3、70% ×1.1、30% ×0.8、
    10% ×0.6（10% 以下夾住）；吸血比例 5%／15%／35%／50%。

21. **自燃與冰封的數值是本前線補的。** 規劃 2.3 只寫「熱度 10 滿即自燃」「冰封 3 秒後量表歸零」，
    沒寫自燃打多少。決定：自燃比照爆燃在滿層時的倍率，即 `B_max ×1.0 ×(1 + 10 ×0.2) = ×3`，
    打完歸零；冰封（凍結 ≥5）3 秒後量表歸零，未冰封仍是 6 秒未命中歸零。
    過熱滿 10 對自己爆最大生命 10% 的火傷（規劃 1.1 有寫），走自有火法術而非 `DamageActorValue`。

22. **`LogThrottled` 補上「同一目標同一訊息 0.5 秒內不重複」。** 上一段第 4 條列為待辦，
    本前線已實作（`LogLastKey` / `LogLastTime`），規劃 6.1 的節流規則現在全部到齊。
    `DumpRegistry()` 是唯一不受除錯等級限制的輸出，因為它是玩家主動從設定選單觸發的。

23. **終焉的 1 秒冷卻被拒時，印記仍然解除，只是不跑反應。** 規劃 2.6 說冷卻限制的是反應，
    不是印記本身；若冷卻時連印記都留著，會出現「印記還在但 AME 已消失」的孤兒狀態。
    開印被冷卻拒絕時同理：登記表仍記下新元素，只是不跑開印效果，並留等級 3 紀錄。

24. **範圍掃描用 PO3 `GetActorsByProcessingLevel(0)`，回傳固定 5 格陣列。**
    規劃 2.9 的人數上限就是 5，所以 `ScanTargets` 一律回 5 格、近的優先、未用到的格是 `None`，
    不做動態配置也不做視線判定。等級 3 只印一行「radius／max／pool／found」，
    不逐一列印掃描到的每個目標（規劃 6.1 明令禁止）。
    目標資格 `IsValidTarget` = （`IsHostileToActor` 或 與玩家交戰中 或 帶 `ESSB_Engaged` 標記）
    且 非同伴 且 非受命 且 活著；`IsHostileToActor` 已涵蓋「敵對陣營但尚未察覺你」。

25. **印記與反應法術都不掛 `ESSB_Proc`。** 附傷的 `OnWeaponHit` 判定只認 `ESSB_Proc`，
    所以反應傷害、DoT、放血、輔助效果都不會反過來觸發附傷，規劃 2.6 的「不留印記、
    不觸發任何反應」自然成立，不需要額外的遞迴保護旗標。

26. **輔助法術不掛學派（skill = -1）。** 減速、碎甲、削魔、削耐、回復、放血這 8 個效果被多個
    元素共用，掛任一學派都會讓 Ordinator 的加成落在錯的樹上。放血另外把抗性 AV 設為 none，
    對應規劃 2.7 的「放血不吃 G(L) 與抗性」。

27. **狀態容器與已交戰標記不設敵對旗標。** 兩者都是內部容器（隱藏、無強度、無範圍），
    設成敵對會在原版行為之外多製造敵意與犯罪。印記本身仍是敵對有害，因為它是玩家看得到的減益。

28. **`vendor/imports/` 新增 7 個型別佔位檔。** 引入 `PO3_SKSEFunctions.psc` 後，編譯器在解析它的
    標頭時找不到 `Debris`、`FootstepSet`、`Furniture`、`Hazard`、`LightingTemplate`、
    `MaterialObject`、`VisualEffect`，導致所有引用它的腳本編譯失敗。七個佔位檔都是
    `Scriptname X extends Form Hidden` 一行，純 ASCII（這些 vendor 檔沒有 BOM，
    編譯器讀不了無 BOM 的 UTF-8 中文），本模組不呼叫這些型別的任何函式。

### 效能守則的落實

- 沒有任何每幀迴圈，沒有 `Utility.Wait` 忙等。
- 玩家參照在 `Setup()` 快取為 `PlayerRef`，之後一律走 `ThePlayer()`。
- 每個帶印記的目標只有一個每秒腳本（`ESSBStatus.OnUpdate`），最多 8 個，與規劃 2.2 的估算一致；
  放血、毒傷、流血、星痕引爆、擴散、死咒倒數、狀態過期全部在同一個 tick 裡做。
- 控制器的 tick 在形態關閉且登記表空時降頻成 5 秒，環境檢查以真實時間節流成每 5 秒一次。
- 毒的擴散每個帶毒目標每次只找一個對象，不做全場掃描。

### 尚未驗證（靜態檢查不能替代）

遊戲內載入與腳本綁定、PO3 `OnWeaponHit` 實際旗標、`DoCombatSpellApply` 對自身回復法術的行為、
`SetNthEffectMagnitude` / `SetNthEffectDuration` 在執行期改自有 record 的即時性與存檔持久性、
狀態容器換宿是否真的不掉層、印記 `OnEffectFinish` 在刷新（同法術重複套用）時是否會誤報、
處決對首領判定（`IsEssential` / `IsProtected` / `IsUnique`）的實際覆蓋、
雷雨與暴風雪的天氣細分、中文字串顯示、8 個每秒腳本同時運作的實際負載。

---

## 2026-09-16 20:39 — skill-trees（TREES_BUILT）

技能樹前線完成：規劃第 5 章的 13 張表全部落地成 PERK 與 CSF 設定檔。
`python build_v03.py` exit 0、**3491 筆記錄**（上一個里程碑 186 筆，+3305）、masters 只有
`Skyrim.esm`、一般 ESP 不設 ESL 旗標、**9 個腳本全部 0 error 0 warning**、
13 個 CSF 設定檔全部通過驗證（`build/csf-validate.json`）。

新檔：`plan_trees.py`（規劃第 5 章解析器）、`src/ESSBTrees.psc`（技能樹腳本）、
`build/plan-tree-nodes.json`（節點真相來源）、`build/csf-validate.json`（CSF 驗證證據）、
`package/Elements Spellblade/SKSE/Plugins/CustomSkills/ESSB_<tree>.json` ×13。
改寫：`src/ESSBSettingsEffect.psc`（選單加四個選項）、`src/ESSBController.psc`
（`Trees` 屬性、關形態命中也走經驗、開印／終焉發經驗）、`build_v03.py`。
`vendor/imports/` 新增 `CustomSkills.psc` 與 `CustomSkills_AliasExt.psc`（純 ASCII，無 BOM）。

### 本前線新增的 FormID

| 區段 | 內容 | 現況 |
|---|---|---|
| 0x002000-0x0023FF | PERK 分支節點：195 階 × 4 格預留，實際 300 個 | 已用 0x002000-0x002309 |
| 0x002400-0x00240C | GLOB `ESSB_Lvl_<tree>` ×13（CSF 等級，預設 1） | 已用 |
| 0x002420-0x00242C | GLOB `ESSB_Ratio_<tree>` ×13（CSF 進度比，float） | 已用 |
| 0x002440-0x00244C | GLOB `ESSB_Pts_<tree>` ×13（CSF perkPoints，預設 1） | 已用 |
| 0x002460-0x00246C | GLOB `ESSB_Color_<tree>` ×13（RGB int，FNAM 'l'） | 已用 |
| 0x002480-0x00248C | GLOB `ESSB_ShowLvl_<tree>` ×13（升級訊息旗標） | 已用 |
| 0x0024A0-0x0024AC | GLOB `ESSB_Respec_<tree>` ×13（洗點冷卻時戳，遊戲日，預設 -10） | 已用 |
| 0x0024C0 / 0x0024C1 | GLOB `ESSB_ShowMenu` / `ESSB_XPPerHit` | 已用 |
| 0x0024D0-0x002FFF | 保留：技能樹前線後續（節點進入點、額外全域變數） | 保留 |
| 0x004000-0x004B6C | PERK 主線階級鏈：195 節點 × 15 階 = 2925 | 已用 |
| 0x004B6D-0x004FFF | 保留：主線鏈擴充 | 保留 |

**TES4 HEDR 的 next object id 從 0x004000 移到 0x005000。** 主線階級鏈需要連續 2925 個 ID，
PERK 段 0x002000-0x002FFF（4096 個）放不下「分支 + 主線 + 13 棵樹的 6 組全域變數」，
所以依前線指示另開 0x004000-0x004FFF 給主線鏈，0x002000-0x0023FF 只放分支、
0x002400-0x0024FF 放全域變數。特效前線的 0x003000-0x003FFF 沒有被動到。

### 記錄數

| 類別 | 數量 |
|---|---|
| PERK 主線階級 | 2925（195 節點 × 15） |
| PERK 分支 | 300（1 個 95 階、2 個 95 階、3 個 5 階） |
| GLOB 技能樹 | 80（13 × 6 + ShowMenu + XPPerHit） |
| 合計新增 | 3305 |

### 與規劃不同或需要留底的決定（接續上一段的 1–28）

29. **主線 15 階用 15 筆 PERK 以 NNAM 串接，不用單一 perk 的 Num Ranks。**
    原版的多階天賦（`Armsman00`→`Armsman80`）就是這個做法：每一筆 PERK 的 DATA
    `Num Ranks` 都是 1，階級關係只靠 NNAM。CSF 的設定檔只填鏈的**第一個** perk。
    因此 `ESSB_P_<tree>_<route>_<tier>_M1` 是對外契約，機制前線加進入點時要逐階加。

30. **節點的取得走 `Game.GetFormFromFile` 算式定址，不用 VMAD 屬性也不用 FormList。**
    3225 個 perk 不可能當屬性掛。FormID 是算出來的：
    主線 `0x004000 + ((tree × 3 + route) × 5 + tier) × 15 + rank`、
    分支 `0x002000 + ((tree × 3 + route) × 5 + tier) × 4 + n`（樹順序見
    `plan_trees.TREE_ORDER`：0–10 元素同 `ELEMENTS`、11 無元素、12 通用）。
    **這條算式是跨前線契約，改配置表就要同時改 `ESSBTrees.psc` 的 `MAIN_BASE` /
    `BRANCH_BASE` / `BRANCH_SLOTS`。** 未配置的分支格回 `None`，所以每階分支數
    不必另外記錄。

31. **每階預留 4 個分支格。** 規劃寫「一到兩個分支」，但實際有 5 個階有 3 個分支
    （5.1 破魔熟練、5.1 破魔傳奇、5.5 雷電開啟大師、5.7 風持續大師、5.8 血關閉大師）。
    `plan_trees.py` 硬性檢查「每階 ≤ 4 個分支」，超過就建置失敗，不會默默截斷。

32. **分支的 5 點由腳本執行，不由 CSF 執行。** CSF 每買一個節點固定扣 1 點。
    `ESSBTrees.OpenTree()` 在開選單前拍下該樹 15 階的分支位元遮罩，選單關閉後
    `Reconcile()` 對每個新取得的分支再扣 4 點；點數不夠就 `RemovePerk` 退回該分支、
    把 CSF 扣掉的那 1 點也還回去，並跳「點數不足」通知。

33. **CSF 的選單名稱是原版 `StatsMenu`（不確定項，已降低風險）。**
    `CustomSkills.psc` 與 `CustomSkills_AliasExt.psc` 都沒有公開選單名稱。
    從 `CustomSkills.dll` 取出的字串裡有 `StatsMenu`、`StatsMenu.SkillStatsA` 與
    `CustomSkills::MenuSetup::MenuConstructorPatch` / `CloseMenuPatch`，
    可見 CSF 是修補原版 StatsMenu 而不是自建選單，所以用
    `RegisterForMenu("StatsMenu")` + `OnMenuClose`。**萬一名稱不對**，
    `OnMenuClose` 不會觸發、`PendingTree` 會留著，下一次 `OpenTree()` 開頭會先
    結清上一次（保險路徑），所以最壞情況只是「補扣延後到下次開樹」，不會漏扣或重複扣。
    進遊戲要驗的就是這一條。

34. **經驗放在 `ESSBController.OnWeaponHit`／`OpenMark`／`EndMark` 三個既有路徑上。**
    規劃 4 的六條來源：開形態命中、開印、終焉、融斷 → 元素樹 + 通用樹；
    關形態命中 → 無元素樹。`OnWeaponHit` 的 `FormActive != 1` 早退被往後挪到
    武器與目標資格檢查之後，所以關形態的有效命中也會發經驗，但不做附傷與印記
    （行為與上一個里程碑完全相同）。**真實傷害 → 無元素樹**還沒有來源（真傷屬機制前線後續），
    列為未完成。每秒經驗上限用 `XPBudget()` 做成每秒 10 次（規劃 4「每秒經驗有上限」）。

35. **經驗曲線是暫定值，放在 `settings.json`。** `xp_per_hit` 1.0、
    `csf_use_mult` 1.0、`csf_use_offset` 0.0、`csf_improve_mult` 0.05、
    `csf_improve_offset` 2.0（約 14000 次有效使用到 100 級）。
    規劃 4 明寫「換算公式尚未定，進遊戲再調」，所以不寫死在程式裡。

36. **階級門檻照規劃字面取 10／25／50／75／100。** 規劃 3 寫「新手 10／熟練 25／
    專精 50／大師 75／傳奇 100」，所以**新手階也要樹等級 10**，1–9 級只能存點。
    條件式是 PERK 的 CTDA `GetGlobalValue(ESSB_Lvl_<tree>) >= 門檻`；分支再加一條
    `HasPerk(該階主線第 1 階) == 1`，對應規劃 3「階內主線至少投 1 點才能點該階分支」。
    **CSF 是否會評估 PERK 的 CTDA 尚未驗證**；若不評估，門檻只是失效（不會壞），
    但要在遊戲內補上腳本檢查。

37. **`ESSBTrees` 掛在同一個 Player 別名上，是該別名的第二個腳本。**
    QUST VMAD 的 alias 0 現在 `scriptCount = 2`（`ESSBController` + `ESSBTrees`）。
    這樣 `ESSBTrees` 才能拿到 `Alias` 型別給
    `CustomSkills_AliasExt.RegisterForCustomSkillIncrease(Self)`，也才有
    `OnPlayerLoadGame`。兩個腳本互相以別名屬性 `(QUST FormID, aliasID 0)` 指向對方。

38. **節點階數快取只留三棵樹。** Papyrus 陣列上限 128 格，13 × 15 = 195 放不下。
    `CacheTree[3]` / `CacheRank[45]` / `CacheBranch[45]` 以輪替方式保留最近三棵
    （實際只會用到「當前元素樹 + 通用樹 + 無元素樹」）。沒被快取的樹一律即時查，
    所以 API 對呼叫端是透明的。

39. **洗點的冷卻時戳預設 -10 天。** `Utility.GetCurrentGameTime()` 新開遊戲從 0 起算，
    預設 0 會讓第一個遊戲日洗不了點。`RespecAll()` 一次跑 13 棵樹約 1800 次原生呼叫，
    是玩家主動觸發的一次性動作，沒有做分批；若進遊戲發現卡頓再改成分幀。

40. **設定選單擴成 8 個按鈕。** 順序＝`ESSBSettingsEffect` 的 `choice` 編號：
    0 切換附傷開關、1 除錯等級循環、2 除錯：印出目標表、3 開啟目前元素的技能樹、
    4 開啟通用樹、5 洗點：目前元素樹、6 洗點：全部、7 關閉。
    「開啟目前元素的技能樹」在未開形態時開的是無元素樹（`ESSBTrees.CurrentTree()`）。

41. **本前線沒有加任何 perk 進入點或效果。** 3225 個 PERK 都只有 EDID／FULL／DESC／
    CTDA／DATA（／NNAM），沒有 PRKE 區段。上一個里程碑 `engine-coverage.json` 裡
    21 條 `DEFERRED-to-mechanics` 仍然是延後狀態，本前線只是把「節點本身」建好。

### 給機制前線的階數快取 API（`ESSBTrees`，別名 0 的第二個腳本）

```
ESSBTrees trees = (myQuest.GetAlias(0) as ESSBTrees)

Int  CurrentTree()                                   ; 開形態＝元素-1，未開＝11（無元素）
Int  CommonTree()                                    ; 12
Int  GetMainRank(tree, route, tier)                  ; 0–15，固定 4 次 HasPerk（二分搜尋）
Perk GetMainPerk(tree, route, tier, rank)            ; rank 1 起算
Perk GetBranch(tree, route, tier, n)                 ; n 0–3，未配置回 None
Bool HasBranch(tree, route, tier, n)
Function RefreshTree(tree)                           ; 把該樹 15 階算進快取（約 120 次原生呼叫）
Int  CachedMainRank(tree, route, tier)               ; 命中快取 O(1)，未命中自動即時查
Bool CachedBranch(tree, route, tier, n)
Function OpenTree(tree) / OpenCommonTree()
Function Respec(tree) / RespecAll()
Function OnValidHitXP(element) / OnOpenXP(element) / OnEndXP(element)
Function DumpTree(tree)                              ; 不受除錯等級限制
```

`tree` 0–10 = `ELEMENTS`（火冰雷土風血聖毒水暗星）、11 無元素、12 通用；
`route` 0 持續／1 開啟／2 關閉（無元素樹是 0 純武藝／1 破魔／2 融斷）；
`tier` 0 新手／1 熟練／2 專精／3 大師／4 傳奇。
規劃 2.7 的 `G(L) = 1 + 0.05 × L` 現在有等級來源了：`LvlGlobals[tree]`。

### 尚未驗證（靜態檢查不能替代）

CSF 是否真的讀得到 13 個設定檔並開得出 13 個選單、CSF 是否評估 PERK 的 CTDA
（階級門檻與「主線先投 1 點」）、選單名稱 `StatsMenu` 是否收得到 `OnMenuClose`、
`OnCustomSkillIncrease` 是否送到別名上的第二個腳本、8 個按鈕的 MESG 在原版
訊息框的顯示（原版上限 10，未實測 8 個的排版）、3225 筆 PERK 對載入時間與
存檔大小的影響、洗點全部一次跑 13 棵樹的卡頓、中文節點名稱在 CSF 選單的顯示。

---

## 2026-09-16 21:27 — element-mechanics round 1（通用、無元素、火、冰、雷）

節點效果框架落地，規劃第 5 章五棵樹（5.2 通用、5.1 無元素、5.3 火、5.4 冰、5.5 雷）
的 192 個節點全部接上機制。`python build_v03.py` exit 0、**3543 筆記錄**（上一個里程碑
3491 筆，+52）、masters 只有 `Skyrim.esm`、一般 ESP、**14 個腳本全部 0 error 0 warning**、
回讀無重複 FormID、`build/plan-coverage.json` 511 列且 unmapped = 0。
`pyright` 對 `build_v03.py` / `plan_coverage.py` / `plan_trees.py` / `tes.py` 0 error 0 warning。

新檔：`src/ESSBNodes.psc`（框架 + 通用樹）、`src/ESSBNoForm.psc`（無元素樹 + 真實傷害）、
`src/ESSBElem.psc`（火冰雷）、`src/ESSBCounter.psc`（破魔印 AME、反咒監聽）、
`src/ESSBSilence.psc`（沉默 AME）、`plan_coverage.py`、`build/plan-coverage.json`。
改寫：`src/ESSBController.psc`、`src/ESSBReactions.psc`、`src/ESSBStatus.psc`、
`src/ESSBTrees.psc`、`build_v03.py`、`plan_trees.py`。

### 開工前先修的靜態問題

前線交辦的 `xp_per_hit`（約 544 行）與 `plan`（約 548 行）在技能樹前線收尾時就已經定義好了
（`xp_per_hit` 在 `build_esp` 第 284 行、`plan` 是 `build_esp(plan)` 的參數），
pyright 1.1.414 現在對 `build_v03.py` 回報 0 error。真正還在的靜態錯誤只有一個，
在 `plan_trees.py` 第 116 行：`re.match(...).group(1)` 沒有判 `None`
（`reportOptionalMemberAccess`）。已改成先取 `bold = re.match(...)` 再判斷，
行為完全相同（路線格一定是粗體，只是型別上補齊）。修完四個 Python 檔 0 error 0 warning。

---

### 42. 三種效果樣式（這是本輪最重要的產出，其餘八棵樹照抄）

節點效果只分三類，每一類一個固定寫法，寫在 `ESSBNodes.psc` 的檔頭註解裡：

| 樣式 | 用在哪 | 寫法 |
|---|---|---|
| **A 主線數值** | 附傷倍率、開印／終焉倍率、狀態上限、衰減秒數、範圍 | 在計算的地方讀 `ESSBNodes.Rank(ctl, tree, route, tier)`（0–15），乘進公式項 |
| **B 分支旗標** | 開印／終焉／每秒／命中／形態事件裡的條件效果 | 在對應的處理函式裡讀 `ESSBNodes.Br(ctl, tree, route, tier, n)` |
| **C 引擎側** | 武器傷害、受到的傷害、護甲、抗性、移速、耐力消耗 | `build_v03.py` 在**同一筆** PERK 記錄上加 `PRKE/DATA/EPFT/EPFD/PRKC/CTDA` |

`G(L) = 1 + 0.05 × L` 由 `ESSBTrees.TreeG(tree)` 提供，四條傷害路徑各只乘一次：
`ApplyDamage`（反應、DoT、融斷）、`ApplyProc`（附傷）、`ApplyTrueDamage`（真傷）、
`ApplyDamageRaw`（自爆這類以玩家自身數值為基準的，**不**乘）。
`ESSBReactions.ReactDamage` 因此改成不乘 G(L)，避免重複。

每個效果處理函式都留紀錄：等級 1 是事件（開印、終焉、融斷、化身、處決、沉默、反咒、
領域、冰心、連鎖冰封），等級 2 是節點（`[ESSB][node][L2]`），等級 3 是公式細項
（`[ESSB][proc][L3]` 印出元素、magnitude、是否重擊、是否開印）。全部走既有的
`ESSBController.LogThrottled`，沿用每秒 20 行與 0.5 秒去重的節流。

### 43. 樣式 C 的橋：腳本狀態鏡射成全域變數（0x005000 段）

PERK 進入點的 CTDA 讀不到 Papyrus 變數，所以戰意、電荷、過熱、冰盾、同調段數與
各種護盾秒數都鏡射成 18 個 GLOB（`ESSB_SyncStage`、`ESSB_Resolve`、`ESSB_Overheat`、
`ESSB_Charge`、`ESSB_IceShield`、`ESSB_Molten`、`ESSB_Ember`、`ESSB_Quench`、
`ESSB_PrevElement`、`ESSB_TwinElement`、`ESSB_DomainFire`、`ESSB_DomainFrost`、
`ESSB_ShockRecent`、`ESSB_GuardSwitch`、`ESSB_GuardBurst`、`ESSB_GuardIce`、
`ESSB_Combo`、`ESSB_FreeOpen`）。寫入集中在 `PushSelf()`、`PushSyncStage()`、
`SetGlobal()` 三個地方，而且**只在值真的改變時**寫，不在每秒 tick 裡盲寫。

### 44. 進入點索引是實測值，不是憑印象

`build_v03.py` 的 `EP_*` 常數全部是掃本機 `Skyrim.esm` 的 PERK 記錄對照出來的：

| 索引 | 原版對照 | 用途 |
|---|---|---|
| 0x23 | `Armsman00` / `Barbarian00`（EPFD 1.2） | 武器傷害倍率 |
| 0x24 | `DeftMovement` / `MQBladesDragonInfusion` | 受到的傷害倍率 |
| 0x25 | `Skullcrusher30`（EPFD 0.75 = 無視 25% 護甲） | 目標護甲倍率（本輪未用） |
| 0x1B | `ChampionsStance` / `FightingStance`（EPFD 0.75） | 重擊耐力消耗倍率 |

條件分頁 `PRKC`：0 = 天賦擁有者，1 = 對手（`MQBladesDragonInfusion` 在分頁 1 判攻擊者的
keyword）。條件函式：74 `GetGlobalValue`、448 `HasPerk`、693 `HasMagicEffectKeyword`
（`AugmentedFlames60` 用同一個）。**主線 15 階鏈的進入點沿用原版 `Armsman00 → Armsman20`
的手法**：每一階的進入點條件加一條 `HasPerk(下一階) == 0`，所以只有玩家持有的最高階生效，
不會 15 階一起疊。第 15 階沒有下一階，條件就省略。本輪一共寫進 **135 段進入點**。

進入點的值是固定的，所以「每層 +X%」這種依變數的只能用「每層一段條件」表達：
戰意武器傷害寫成 5 段 `ESSB_Resolve >= 1..5`，疾攻寫成 5 段 `ESSB_Combo >= 1..5`。
數學上是乘法疊加（5 層 ×1.005^5 而不是 ×1.025），差距在個位數百分比，接受並記錄。

### 45. 新增的 FormID（0x005000-0x005FFF），HEDR next object id 移到 0x006000

| 區段 | 內容 | 現況 |
|---|---|---|
| 0x005000-0x005011 | GLOB 狀態鏡射 ×18 | 已用 |
| 0x005020 / 0x005021 | MGEF／SPEL 真實傷害 | 已用 |
| 0x005022 / 0x005023 | MGEF／SPEL 破魔印（ESSBCounter） | 已用 |
| 0x005024 / 0x005025 | MGEF／SPEL 沉默（ESSBSilence + MagickaRateMult -100） | 已用 |
| 0x005030-0x005032 | MGEF 自有常駐能力（抗咒、溫血、感應） | 已用 |
| 0x005038-0x00503A | SPEL 自有常駐能力 ×3 | 已用 |
| 0x005050 / 0x005051 | KYWD `ESSB_ManaBreak` / `ESSB_Silence` | 已用 |
| 0x005080-0x005089 | MGEF 輔助效果第 8–17 號 | 已用 |
| 0x0050A0-0x0050A9 | SPEL 輔助法術第 8–17 號 | 已用 |
| 0x0050C0-0x005FFF | 保留：round 2／3 的其餘八棵元素樹 | 保留 |

`ESSB_DebugLevel` 仍然是 0x000811，沒有動。分支與主線 PERK 也沒有重新編號，
進入點是加在既有記錄上（`ESSB_P_<tree>_<route>_<tier>_M<rank>` / `_B<n>` 的 EDID 契約不變）。

輔助效果原本只配 0x001090-0x00109F（16 格 MGEF），本輪要加到 18 個放不下，
所以第 8 號以後移到 0x005080／0x0050A0，索引仍然連續，由 `util_effect_id()` /
`util_spell_id()` 兩個函式對應。`ESSBController.ApplyUtil` 的索引語意不變。

新的輔助效果：8 魔抗+、9 疾行（SpeedMult+）、10 魔力回復+、11 耐力回復+、
12 停止回魔（沉默的第二個效果）、13 火抗削、14 冰抗削、15 電抗削、16 魔抗削、17 攻擊削弱。
AV 索引是掃 `Skyrim.esm` MGEF 對出來的：MagickaRateMult = 156、StaminaRateMult = 157、
HealRateMult = 155、MagicResist = 44、SpeedMult = 30、MeleeDamage = 34
（**不是** 80/81/82，這一條差點寫錯）。

### 46. 順手修掉的既有問題：有持續時間的值修正沒有 Recover 旗標

引擎前線的 `MGEF_DEBUFF_FLAGS = 0x8815` 配原型 0（Value Modifier）。原版
`FrostSlowFFContact` 是原型 **34**（Peak Value Modifier）加 **Recover（0x2）**，
`AlchFortify*` 也一樣。原型 0 + 持續時間 + 沒有 Recover 會讓 AV **永久**被改掉，
減速與碎甲會越疊越慘且不會還原。已改成 `MGEF_DEBUFF_FLAGS = 0x8817`、
`MGEF_BUFF_FLAGS = 0x8812`，且「有持續時間」的輔助效果一律用原型 34；
瞬間的削減與回復維持原型 0（同 `AlchDamageMagicka`）。

### 47. 每擊隨機 B 落地，附傷改為執行期設 magnitude

規劃 2.1 的 11 組上下限寫進 `ESSBReactions.BaseMin` / `BaseMax`，`RollBase` 取
`Utility.RandomFloat(low, high)`。`ESSBController.ApplyProc` 在
`DoCombatSpellApply` 前算好 `B × R × G(L) × M_mod` 再 `SetNthEffectMagnitude`，
取代原本寫死的 8／12。規劃 8 的「三檔法術擇一 或 施放前設 magnitude」選後者，
因為法術是本模組自有、玩家專用，改 magnitude 不影響任何別人。

重擊 R = 1.5（弓弩的潛行射擊已由既有邏輯換算成重擊）。
規劃 1.1 的風潛行 ×3 也在同一處（讀 HitData 旗標 2048）。

### 48. 真實傷害（2.8）

`MGEF ESSB_TrueEffect`：`skill = -1`（不掛學派）、`resist = -1`（Resist Value = None）、
AV 24、只掛 `ESSB_TrueDamage` keyword、**不掛** `ESSB_Proc`（不會反過來觸發附傷）。
`ESSBController.ApplyTrueDamage` 算 `基準 × G(L_無元素) × M_mod(無元素樹)` 後
`SetNthEffectMagnitude` + `DoCombatSpellApply`，並在同一處做兩件事：
給無元素樹經驗（規劃 2.8「造成真實傷害練無元素樹」，補上技能樹前線列為未完成的那一條），
以及「逆流」分支的 50% 轉生命。這是本模組唯一會扣生命的路徑，其餘一律不 `DamageActorValue` 生命。

### 49. 沉默與反咒的實作

**沉默**：一個法術兩個效果。第一個是 `ESSBSilence`（腳本，每秒把目標當前魔力扣到 0，
只動魔力）；第二個是 `MagickaRateMult` 的有害值修正 100（＝ -100% 回魔）。
NPC 沒有魔力就不會施法，等同沉默；不碰 AI、不上硬直、不上麻痺，符合低衝突原則（6）。
首領秒數減半（規劃 8 的「對首領可縮短」）。掛 `ESSB_Silence` keyword 給「靜寂」當條件。

**反咒**：破魔印（`ESSB_ManaBreakEffect`，8 秒）的 AME `ESSBCounter` 在
`OnEffectStart` 對目標 `RegisterForAnimationEvent("MRh_SpellFire_Event")` 與
`MLh_SpellFire_Event`，`OnEffectFinish` 取消註冊，所以監聽只存在於印記的 8 秒內。
觸發時依哪一手讀 `GetEquippedSpell(1/0)`，用 `GetEffectiveMagickaCost(目標)` 取該次
施法對他的實際消耗，乘 `1 + 0.02 × 無元素樹等級` 造成真實傷害。
**沒取得「反咒」分支的人連註冊都不做**，零成本。

**斷咒**：Skyrim 沒有可靠的「打斷施法」API，也不能用硬直。做法是
`GetAnimationVariableBool("IsCastingRight/Left/Dual")` 判定正在施法，然後把魔力清空
並給 1 秒沉默——NPC 會中止當前施法改用近戰。每 5 秒一次。

### 50. 領域（火域、冰原）：不放任何 ObjectReference

規劃 2.9 的例外表要求「融斷後留下的領域：中心為目標、3 公尺、5 秒」。
CastEx 的雲形法術與 hazard 都要新的美術或 hazard 記錄，而本輪不加資產，
所以改用**控制器側的幾何領域**：`StartDomain()` 記下中心座標（`GetPositionX/Y/Z`）、
元素與剩餘秒數，`InDomain(actor, element)` 做平方距離比較（3 公尺 = 210 單位），
`TickDomain()` 掛在既有的每秒 tick 上。

這是對前線指示（「cloak-style area spell via PO3 CastEx 或 explosion + hazard 二選一」）
的**刻意偏離**，理由三條：一、完全不需要新資產與新記錄；二、不必管 ObjectReference 的
生命週期與存檔殘留；三、判定可以直接在命中路徑上讀（火域的「熱度累積 ×2」與
「內部敵人受火傷 +20%」本來就是查詢，不是範圍法術）。
代價是**沒有視覺**——領域的特效留給特效前線，屆時只要在 `StartDomain` 多放一次
一次性的視覺法術即可，判定不必改。玩家位置也鏡射成 `ESSB_DomainFire` /
`ESSB_DomainFrost` 兩個全域變數，之後要接 PERK 進入點不必改結構。

### 51. 碎冰處決改用自有高強度傷害法術，不用 `Kill()`

`ESSBController.Execute` 原本是 `akTarget.Kill(player)`。改成
`ApplyDamageRaw(element, 當前生命 ×10 + 1000, target)`：擊殺歸屬、死亡事件、
外部天賦的「擊殺時…」、神聖化灰與黑暗復生的判定都與一般擊殺一致。
首領與必要角色仍然由呼叫端改走 `B_max ×5.0`，不會走到這裡。

### 52. 雙印與疊印共用同一個「副印記格」

登記表每格多了 `RegElem2 / RegSeq2 / RegSecondUntil / RegMark2`。
「雙印」把**較新**的元素放進副格、較舊的留在主格（所以被切掉時結算的是較舊的那個，
符合規劃 5.2 的字面），而且不觸發終焉；「疊印」則是終焉之後把剛結清的元素放進副格 4 秒。
融斷時主格與副格各結算一次。副印記不占 8 個名額（規劃 2.2 明寫雙印不占名額）。
主印記結束而副格還在長效期（雙印）時，副格會遞補成主格，維持「一格一個主元素」的不變式。

### 53. 快取槽改成固定分工，不再輪替

技能樹前線的 `RefreshTree` 用輪替挑槽（決定 38），但機制前線每一次計算都同時要
「當前元素樹 + 無元素樹 + 通用樹」，輪替會在切換元素時把通用樹擠掉。
改成 `SlotFor()`：樹 11 固定槽 1、樹 12 固定槽 2、其餘（元素樹）固定槽 0。
`RefreshActive(tree)` 一次刷三棵，只在**形態開／切／關**與**技能樹選單關閉**時呼叫，
不在命中路徑上。命中路徑一律走 `CachedMainRank` / `CachedBranch`（O(1)）。

### 54. 「廣印」的擴散鏈用 `InSpread` 旗標擋住

廣印的開印本身也會觸發 `AfterOpen`，不擋就會一路傳染到場上所有人。
一次命中最多多開一個目標。

### 55. 對規劃字面的解讀與偏離（進遊戲要確認的）

1. **終結（無元素傳奇主線）**「重擊無視護甲 +3%／點」：原版有 0x25（Skullcrusher）
   可以做護甲穿透，但那是被動、沒有「重擊」條件，做不到「戰意滿層的重擊」。
   改以**等量的真實傷害**表達「無視護甲」（`武器基準 × 0.03 × 階數`），語意相同、
   條件精準，而且剛好用上本輪新建的真傷類別。
2. **暴擊率（無元素專精主線）** 依規劃寫的「自有判定」實作：`RandomFloat` 擲骰，
   命中就補一次武器基準的真實傷害，**沒有**同時掛 0x01（引擎暴擊率）進入點，避免雙重計算。
3. **電弧（雷持續熟練分支）**「放電時向附近 2 人跳躍 40%」與規劃 2.6 的基礎放電字面重複。
   解讀為加成：沒點時跳躍 40%，點了 80%。
4. **冰盾（冰持續大師分支）**「被打消耗一層抵消該次 30% 傷害」：層數累積與 -30% 減傷
   已落地（PERK 0x24 讀 `ESSB_IceShield`），但「被打消耗一層」要等玩家受擊事件，
   目前是「有層數就減傷」。round 2 接上受擊事件後改成逐層消耗。
5. **浴火（火持續傳奇分支）** 用 PERK 0x24 的**分頁 1（攻擊者）** 判
   `HasMagicEffectKeyword(ESSB_Mark_Fire)`。分頁 1 的語意是照
   `MQBladesDragonInfusion` 抄的，但那是 `HasKeyword` 不是 `HasMagicEffectKeyword`，
   需進遊戲確認。最壞情況是條件永遠不成立＝分支不生效，不會誤傷。
6. **不移（通用持續熟練分支）**「被打斷或擊倒不失去同調」：本模組的同調只在切換與關閉時
   歸零，本來就不因打斷或擊倒扣，所以這個分支是**行為保證**而不是新事件，沒有另加程式碼。
7. **定神（通用持續專精分支）**「同調三段時免疫減速」：守衛函式 `SelfSlowImmune` 已落地，
   目前唯一會減玩家速度的自有來源是冰原（也已接）。風／土樹的自我減速在 round 2／3 接上。
8. **化身（通用持續傳奇主線）** 觸發的「當前元素持續傳奇效果」逐元素定義：
   火＝對最近的帶熱度目標立即自燃、冰＝對最近的冰印記目標直接冰封並上強減速、
   雷＝對最近的感電目標做一次範圍放電。其餘八個元素在 round 2／3 補。
9. **凍結累積 +5%／點** 是小數，量表是整數。用 `RoundStochastic`：整數部分保證給，
   小數部分擲骰。不四捨五入，也不截掉。

### 56. 本輪 DEFERRED 的 7 個節點（全部有具體技術理由）

| 節點 | 理由 |
|---|---|
| `ESSB_P_noform_0_0_B1` 穩步 | 「格擋耐力消耗」在本機 `Skyrim.esm` 找不到任何原版進入點用例，索引無法實測確認；需進遊戲對 CK 的進入點清單逐一試 |
| `ESSB_P_noform_0_1_B1` 反擊 | 需要「格擋成功」事件 |
| `ESSB_P_noform_1_1_B2` 反噬 | 需要「受到法術傷害」事件 |
| `ESSB_P_noform_1_3_B1` 破護 | 需要「受到披風反傷」事件與外部披風效果辨識 |
| `ESSB_P_fire_0_3_B1` 灼身 | 需要「被近戰命中」事件 |
| `ESSB_P_frost_0_3_B2` 寒反 | 需要「被攻擊」事件 |
| `ESSB_P_lightning_0_1_B2` 靜電 | 需要「被近戰命中」事件 |

後六個是同一個缺口：**玩家「被打」的事件目前沒有掛勾**。
土樹的岩甲「被打 -1」（`engine-coverage.json` 第 13 條）也是同一條。
建議 round 2 開一個 `ESSBGuard` 別名腳本，掛 PO3 的 `OnHit` / `OnMagicHit`
（玩家為受害者），一次把這七條連同岩甲全部接上。

### 57. `build/plan-coverage.json`

`plan_coverage.py` 是節點狀態的真相來源，`build_v03.write_plan_coverage()` 產生檔案並
**在缺狀態時讓建置失敗**。三道硬性檢查：
(1) `plan-tree-nodes.json` 的每一個節點都必須查得到狀態，缺一個就 `SystemExit`；
(2) 每一列的 `status` 必須是四種之一，且 `where` / `note` / `text` 不可為空；
(3) 每一列的 `edid` 必須真的存在於這一次寫出去的 ESP 的 manifest；
(4) round 1 的五棵樹不可以出現 `PENDING-*`（只能 IMPLEMENTED 或 DEFERRED）。

本輪數字：511 列 = 495 個節點 + 16 條共通機制（規劃 1.1／2.1／2.3／2.7／2.8／2.9／2.13／6.1／8）。
節點狀態：IMPLEMENTED 185、DEFERRED 7、PENDING-round2 155、PENDING-round3 148。
輪次分配：round 2 = 大地、風、鮮血、神聖；round 3 = 毒素、水、黑暗、星界。

`engine-coverage.json` 的 21 條 `DEFERRED-to-mechanics` 本輪結清 7 條
（雙印、無元素狀態、萬象上限、收束、開啟／關閉路線放大、G(L)、真實傷害），
剩 14 條，其中「例外表：領域與臨」已改成部分落地並在 note 裡寫清楚剩下的是誰。

### 58. 效能

- 沒有新的每幀迴圈、沒有 `Utility.Wait`。
- 節點階數在命中路徑上一律讀快取（`CachedMainRank` / `CachedBranch`，O(1)）；
  `RefreshTree` 只在形態開／切／關與技能樹選單關閉時跑（每次約 120 次原生呼叫）。
- 控制器的秒計時器（熔身、餘燼、淬火、疾電、三種護盾、領域、同調保留）全部在
  **同一個既有的每秒 tick** 裡減一，沒有另開 `RegisterForUpdate`。
  只有在這些計時器還在跑時才把 tick 維持在 1 秒，否則仍然降頻成 5 秒。
- 新增的每秒腳本只有一種：沉默（每個被沉默的目標一個，最長 4 秒）。
  破魔印的反咒是動畫事件監聽，不是輪詢，而且只在有點分支時註冊。
- 連鎖終焉與大協奏用 `abChain = True` 參數擋住遞迴（連鎖出去的終焉不再往外連鎖）。
- 廣印用 `InSpread` 旗標擋住擴散鏈。

### 尚未驗證（靜態檢查不能替代）

PERK 進入點是否真的生效（0x23／0x24／0x1B 的索引與 `PRKC` 分頁語意）、
`HasMagicEffectKeyword`（693）在進入點條件裡的 run-on 對象、
15 階鏈的「只有最高階生效」條件在 CSF 買點後是否成立、
`GetEquippedWeapon(true)` 對雙持同型武器的分辨（雙生可能誤判為右手）、
`MRh_SpellFire_Event` / `MLh_SpellFire_Event` 對 NPC 是否可靠送達、
`GetEffectiveMagickaCost` 對 NPC 的回傳值、
`GetAnimationVariableBool("IsCasting*")` 的可靠度、
AV 156／157（MagickaRateMult／StaminaRateMult）在 Peak Value Modifier 下的行為、
沉默對「沒有魔力的敵人」（野獸、亡靈）等於無效這件事的手感、
處決改用高強度傷害法術是否真的能一擊致死（護甲與抗性已由 magnitude 蓋過，
但 `DoCombatSpellApply` 對無敵旗標的目標行為未測）、
火葬用「套用後生命 ≤0」判定擊殺的時序、
領域的幾何判定在垂直落差大的地形上的手感、
18 個新全域變數對存檔大小的影響。

---

## 2026-09-16 22:16 — element-mechanics round 2（大地、風、鮮血、神聖）+ 玩家受擊事件

規劃第 5 章的 5.6 大地、5.7 風、5.8 鮮血、5.9 神聖四棵樹共 155 個節點全部接上機制，
同時補上 round 1 缺的「玩家被打」事件，把當時列為 DEFERRED 的 7 個節點結清 6 個。
`python build_v03.py` exit 0、**3580 筆記錄**（上一個里程碑 3543 筆，+37）、
masters 只有 `Skyrim.esm`、一般 ESP、**16 個腳本全部 0 error 0 warning**、
回讀無重複 FormID、`build/plan-coverage.json` 527 列且 unmapped = 0、
**零 PENDING-round2**。`pyright` 對 `build_v03.py` / `plan_coverage.py` / `plan_trees.py`
0 error 0 warning。進入點從 135 段增為 **158 段**。

新檔：`src/ESSBGuard.psc`（玩家受擊與擊殺事件）、`src/ESSBElem2.psc`（土風血聖四棵樹）。
改寫：`src/ESSBController.psc`、`src/ESSBElem.psc`、`src/ESSBReactions.psc`、
`src/ESSBStatus.psc`、`src/ESSBNodes.psc`、`src/ESSBFormRules.psc`、
`build_v03.py`、`plan_coverage.py`。

---

### 59. `ESSBGuard`：第三個玩家別名腳本，一次補齊「被打」與「擊殺」

`ESSB_MainQuest` 的別名 0 現在掛三個腳本：`ESSBController`、`ESSBTrees`、`ESSBGuard`。
`ESSBGuard` 只註冊兩個 PO3 事件，沒有任何每幀或每秒迴圈：

| 事件 | 用途 |
|---|---|
| `RegisterForHitEventEx` → `OnHitEx` | 玩家是受害者。參數帶 `abPowerAttack` / `abSneakAttack` / `abBashAttack` / `abHitBlocked`，還有 `akSource`（法術或武器）與 `akProjectile`，所以「格擋成功」「受到法術傷害」「被近戰命中」三種都分得出來 |
| `RegisterForActorKilled` → `OnActorKilled` | 全場擊殺事件，過濾 `akKiller == 玩家`。飲血、血承、不死、化灰、連殺、無魔都改掛這裡 |

每個攻擊者的反制效果都有 **3 秒冷卻**（規劃 5.3／5.4／5.5 的「每 3 秒一次」），
用 4 格環狀表記錄攻擊者與時戳，滿了覆蓋最舊一格，不配置新陣列、不做全場掃描。

round 1 的 7 個 DEFERRED 結清 6 個：

| 節點 | 這一輪的做法 |
|---|---|
| 反擊 | `abHitBlocked` → `SetRiposte(3)`，下一次命中 `ApplyProc` 乘 1.3 |
| 反噬 | `akSource as Spell` 成立 → 回魔 `B_max(無元素) × G(L)` |
| 破護 | 見決定 65 |
| 灼身 | 近戰命中 → 攻擊者熱度 +1 並吃一次 `B_max ×1.0` 火傷 |
| 寒反 | 近戰命中 → 攻擊者減速 30% 3 秒 |
| 靜電 | 近戰命中 → 走感電的基礎效果（你 +1 電荷、他 -B_max 魔力） |

同一個事件也補上：土的岩甲「被打 -1」（`engine-coverage` 第 13 條）、
冰盾的「被打消耗一層」（round 1 決定 55 第 4 點欠的那一條）、風的殘影、土的反震、聖的神佑。

**擊殺事件沒有取代舊路徑**：控制器每秒 tick 裡「登記表中的目標已死就跑擊殺掛勾」的後援
仍然保留（死於 DoT、死在事件送達之前都靠它），`OnKillEvent` 結算後會 `ClearSlot`，
所以同一個目標不會被結算兩次。

### 60. 本輪新增的 FormID（0x0050C0-0x0050FF，加上輔助效果與能力的續編）

| 區段 | 內容 | 現況 |
|---|---|---|
| 0x00508A-0x00508E | MGEF 輔助效果第 18–22 號（岩甲護甲、血盾臨時生命、治療削減、耐力凝滯、消音） | 已用 |
| 0x0050AA-0x0050AE | SPEL 輔助法術第 18–22 號 | 已用 |
| 0x005033-0x005034 | MGEF 自有常駐能力第 4–5 號（風行 SpeedMult、風隱 Muffle） | 已用 |
| 0x00503B-0x00503C | SPEL 自有常駐能力第 4–5 號 | 已用 |
| 0x0050C0-0x0050CA | GLOB 狀態鏡射第二批 ×11 | 已用 |
| 0x0050D0 / 0x0050D1 | MGEF／SPEL 化灰 | 已用 |
| 0x0050D2 | SPEL 無聲（Muffle 1.0 + 移速 +20%，沿用既有 MGEF） | 已用 |
| 0x0050E0-0x0050E6 | MGEF 血承 ×7（火冰電毒魔抗、護甲、最大生命） | 已用 |
| 0x0050E7 | SPEL 血承 | 已用 |
| 0x0050F0 | PERK `ESSB_P_BaseRules`（基礎規則，開局 AddPerk） | 已用 |
| 0x005100-0x005FFF | 保留：round 3（毒素、水、黑暗、星界） | 保留 |

`ESSB_DebugLevel` 仍然是 0x000811，沒有動。HEDR next object id 仍是 0x006000。
第二批鏡射全域變數：`ESSB_RockArmor`、`ESSB_Wind`、`ESSB_HolyShield`、`ESSB_Bloodthirst`、
`ESSB_GuardWind`、`ESSB_GuardDivine`、`ESSB_DomainEarth`、`ESSB_DomainBlood`、
`ESSB_DomainDivine`、`ESSB_NoBloodCost`、`ESSB_CloakGuard`。

### 61. 又兩個實測出來的進入點索引，和一個推翻 round 1 假設的發現

掃本機 `Skyrim.esm` 的 PERK 記錄，本輪新確認兩個索引：

| 索引 | 原版對照 | 用途 |
|---|---|---|
| 0x21 | `TowerOfStrength`（EPFD 0.5＝被擊退幅度減半）、`MQGreybeardsFus`（0.75） | 受到的擊退幅度 → 土的「不動」以 ×0 表達免疫擊退 |
| 0x29 | `ElementalProtection`（0.5）、`InvulnerableActorZeroIncomingDamage`（0.0） | 受到的法術強度 → 聖盾、破護 |

**更重要的是一個推翻性的發現**：攻擊類進入點的條件分頁 1 是**武器**，不是目標。
`Armsman00`（0x23）分頁 1 的條件是 `HasKeyword(WeapTypeSword/Greatsword/…)`，
`DevastatingBlow`（0x1C）、`Skullcrusher90`（0x25）、`AssassinsBlade`（0x12）全部一樣。
只有受傷類（0x24，`MQBladesDragonInfusion`）的分頁 1 才是攻擊者。
**所以「目標帶某狀態時你的武器傷害 ×N」在原版進入點語意下是寫不出來的**，
`空中追擊`（5.7 傳奇分支）因此改成只放大**本模組自己造成的傷害**（附傷、反應、真傷），
引擎的武器傷害那一份沒有放大。round 1 的「浴火」用 0x24 分頁 1 判攻擊者，語意仍然成立。

`ESSB_P_BaseRules` 是本輪新增的一筆**非節點** PERK：不可玩、隱藏，開局由
`ESSBController.Setup` 呼叫 `AddPerk` 一次，永不移除。裝的是「不依賴投點的引擎側規則」，
目前兩條：血形態重擊耐力消耗 ×0（0x1B，條件 `ESSB_FormActive == 1` 且
`ESSB_CurrentElement == 6`）、岩甲每層物理減傷 -1%（0x24 ×5 段，條件 `ESSB_RockArmor >= 1..5`）。

### 62. 物理推力：四個動作、兩條冷卻、一張免疫名單

規劃 2.6／2.9／8 要求的跌倒、拉近、吹飛、吹上天全部集中在控制器的四個函式，
`ESSBElem2` 只決定「要不要推、推多遠」：

| 函式 | 做法 | 冷卻 |
|---|---|---|
| `Knockdown` | `PushActorAway(目標, 3.0)`，同吼聲的小力道，不走硬直動畫 | 每目標 8 秒 |
| `BlowBack` | 由玩家往外的水平 `ApplyHavokImpulse`，z 分量 0.15，強度 = 公尺 × 70 | 每目標 8 秒 |
| `PullIn` / `PullTo` | 由目標往中心的水平衝量，z 分量 0.1 | 每目標 3 秒 |
| `LiftUp` | 純向上衝量 + 2 秒浮空狀態 | 每目標 8 秒 |

冷卻用 8 格環狀表（`KnockActor` / `KnockTime` / `PullTime`），跌倒與拉近各記一個時戳。
免疫名單（`CanRagdoll`）：`ActorTypeDragon`（0x035D59）、`IsOnMount()`、
`IsVIPTarget()`（essential／protected／unique）、`GiantRace`（0x0131F9）與
`MammothRace`（0x0131FF）。四個函式一律回傳 Bool，**推不動時呼叫端改上減速 30% 3 秒**，
規劃 5.7「龍、騎乘、必要角色、大型生物改為減速」因此是一條路徑，不是四份判斷。

`ActorTypeGiant`（0x10E984）沒有掛在 `GiantRace` 上（掃出來 `GiantRace` 的 KWDA 只有
`ActorTypeCreature`），所以巨人與猛獁改用 RACE 直接比對，不用 keyword。

### 63. 浮空：自有的 2 秒狀態，不讀物理

照規劃 8 的字面：**不讀物理狀態**。`LiftUp` 推完就在狀態容器裡寫
`AirLeft = 2` 與 `AirDamage`（= 落地傷害），`ESSBStatus.Tick` 每秒減一，
歸零的那一秒呼叫 `ESSBController.OnLanding` 結算落地傷害。
換宿的 `ExportInts` / `ExportFloats` 跟著加一格（24 → 25 個 Int、11 → 12 個 Float）。

規劃寫「1.5 秒」，這裡取 **2 秒**：狀態容器是整數秒的 tick，1.5 會被截成 1，
而 1 秒的滯空在落地傷害還沒結算前就過了。這是刻意的取整，進遊戲要看手感。

「風刃命中空中目標再推高 1 公尺並延長滯空」直接在 `WindBladeOne` 裡呼叫 `LiftUp`，
`SetAirborne` 會覆寫 `AirLeft`，所以刷新滯空與推高是同一次呼叫。

### 64. 血位曲線與「重擊改扣生命」

規劃 1.1 的三條曲線現在都在控制器裡，四點線性內插：

| 曲線 | 函式 | 100% / 70% / 30% / 10% |
|---|---|---|
| 命中效果 | `BloodHitCurve` | ×1.3 / ×1.1 / ×0.8 / ×0.6 |
| 吸血比例 | `BloodLeechCurve` | 5% / 15% / 35% / 50% |
| 損血（維持每秒） | `BloodDrainCurve(power = False)` | 1.0% / 0.6% / 0.2% / 0 |
| 損血（重擊一次） | `BloodDrainCurve(power = True)` | 8% / 5% / 2% / 0 |

round 1 的 `ESSBFormRules` 是固定每秒 0.5%，本輪換成曲線；折扣（血氣減半、
血臨強化／血約 8–10 秒歸零、不死在同調三段歸零）集中在 `BloodCostScale()`。
`PayBloodCost` **永遠留 1 點生命**，維持費與重擊費都不會自己把玩家打死。

重擊那一半是兩件事合起來：耐力用 `ESSB_P_BaseRules` 的 0x1B ×0 進入點，
生命在 `OnWeaponHit` 判到 `element == 6 && power` 時扣。

「逆流」（5.8 持續專精分支）只反轉**查表用的百分比**（`BloodPercent()` 回
`1 - 當前百分比`），命中與吸血兩條曲線跟著反轉；損血曲線走原始百分比，不反轉，
符合規劃「損血曲線不變」的字面。

### 65. 破護：認出披風那一跳，開一個 2 秒的法術免疫視窗

「不受元素披風反傷」是本輪最難落地的一條。掃 `Skyrim.esm` 的結果：
原版披風的**傷害**法術（`FireCloakDmg` 0x02B38A、`FrostCloakDmg`、`LightningCloakDmg`…）
**完全沒有掛任何 keyword**，它們的 MGEF 只掛 `MagicDamageFire/Frost/Shock`，
而那三個 keyword 每一顆火球都有。所以 0x29 的條件分頁 1（讀入射效果的 keyword，
由 `ElementalProtection` 實測）分不出「披風一跳」和「一發火球」。

做法：`ESSBGuard` 認出「來源是法術 + 沒有投射物 + 攻擊者身上帶原版 `MagicCloak` 效果」
的那一跳，開 2 秒的 `ESSB_CloakGuard` 視窗，分支天賦上的 0x29 ×0 進入點在視窗內把
受到的法術強度歸零；披風每秒一跳，所以每一跳都會刷新視窗。
**代價是第一跳仍然會吃到**，之後只要還貼著就完全免疫。這是刻意的偏離，進遊戲要確認手感。

### 66. 化灰：DATA 照抄原版，觸發點分兩條

原版沒有「Disintegrate」這個 MGEF 原型。掃出來的事實是：
`PerkDisintegrateFFAimed`（0x0F3F0D）是**原型 0（Value Modifier）、AV 24、skill 20**、
flags `0x10209805`，比一般的 `ShockDamageFFAimed75`（`0x10200805`）多了 `0x1000` 與 `0x8000`
兩個位元；`Disintegrate` 那筆 PERK 本身**沒有任何進入點**，只是條件天賦。

所以 `ESSB_AshEffect` 的 DATA 逐欄照抄 `PerkDisintegrateFFAimed`，只把抗性改成 none
（裁決的化灰不該被電抗擋掉）。觸發走兩條：
一、`OnActorKilled` 時若「最後一次本模組傷害是神聖」（`LastDamageElement == 7`，
在 `ApplyDamageRaw` 裡記）或「淨土」分支成立，就對屍體施放化灰法術；
二、聖的「處決」分支走 `Execute()`，前面先 `NoteDamageElement(7)`，讓化灰跟著成立。
龍與必要角色不化灰（規劃 8 與 2.9 的例外欄）。

**對屍體施放是否真的會產生灰燼堆，靜態檢查驗不出來**，是本輪最需要進遊戲確認的一條。
最壞情況是沒有視覺效果，聖灰與淨灰的回饋仍然由本模組自己的記帳觸發，不會壞掉。

### 67. 死靈施法者：三個候選全取聯集

規劃 8 說「候選為職業、陣營、或身邊存在復生物；需實測選一個」。本輪不選一個，
取三者的聯集（`IsNecromancer`），因為三個都是 O(1) 的查詢，而且互補：

- 職業 `CombatMageNecro`（Skyrim.esm 0x0C969F）
- 陣營 `NecromancerFaction`（Skyrim.esm 0x034B74）
- `PO3_SKSEFunctions.GetCommandedActors(目標)` 非空（正在指揮復生僕從）

前兩個涵蓋「他是設定上的死靈法師」，第三個涵蓋「他正在做死靈的事」。

### 68. SpeedMult 的重新整理方式（規劃 8 的待定項）

**本模組所有移速修正都走 MGEF**，與原版 `FrostSlowFFContact`（AV 30 SpeedMult、
原型 34 Peak Value Modifier）同款，從來不用 `SetActorValue("SpeedMult", …)`，
所以規劃 8 擔心的「改了不生效」在正常情況下不會發生。

保險起見另加一個**負重微調法**：`RefreshSpeed()` 做
`ModActorValue("CarryWeight", +0.1)` 再 `-0.1`（淨值為零），強迫引擎重算衍生數值。
只在形態開／切／關、以及潛行狀態改變時各跑一次，兩次原生呼叫，不在命中路徑上。

風形態的移速強度隨節點變動（基礎 +10%、專精主線 +0.5%／點、疾風分支再 +10%），
而常駐能力的強度是記錄裡的固定值，所以 `RefreshWindAbilities()` 在 `AddSpell` **之前**
先 `SetNthEffectMagnitude(0, 總量)`，和附傷的做法一致。

潛行狀態（無聲 / Muffle 0.5）用**既有的每秒 tick** 讀一次 `IsSneaking()`，
變了才動能力，沒有每幀迴圈。

### 69. 岩甲的護甲：自有增益，層數變動時重下、歸零時 Dispel

規劃 8 說「岩甲用原版石膚同款的護甲加成」。做法是新的輔助效果 18
（`ESSB_Util_ArmorBuff`，AV 39 DamageResist、原型 34、30 秒），
`SyncRockArmor()` 在層數真的變動時重下一次（magnitude = 層數 × 15，磐石分支 25），
層數歸零時直接 `player.DispelSpell(armorSpell)`，不留殘影。
`RockArmorShown` 記住上一次的值，值沒變就完全不呼叫，所以連續命中不會每次都重下。

### 70. 單一領域格繼續沿用，本輪多了三種

round 1 的領域是控制器側的幾何判定（中心座標 + 剩餘秒數 + 每秒 tick），
本輪**沒有加第二格**，四種新領域共用同一格：地裂帶／泥沼（元素 4）、血池（6）、
聖域／神聖領域（7），加上開印的「裂地」減速地帶（也是 4）。
**後開的領域會取代先開的**，這是刻意的簡化，理由與 round 1 相同（不放 ObjectReference、
不必管生命週期），代價是同時只有一個領域生效，進遊戲要看會不會踩到。

鏡射給 PERK 進入點的是「**玩家在不在領域裡**」而不是「領域存不存在」
（`ESSB_DomainBlood` / `ESSB_DomainDivine` 只在玩家位於領域內時才是 1），
因為聖域的「你在其中受傷 -20%」是對玩家的條件。

### 71. 對規劃字面的解讀與偏離（接續 round 1 的決定 55）

10. **聖印「目標受聖傷 +20%」**（規劃 2.6 基礎表）與「聖印每層 +1%／點」（5.9 持續新手主線）
    合併成一個函式 `HolyVulnerability(目標)`，附傷（`DivineHitMult`）與裁決（`Judge`）共用。
    沒有走 PERK 進入點，理由見決定 61（攻擊類進入點讀不到目標）。
11. **血咒「受治療 -50%」**：Skyrim 沒有「受到的治療」修正這種 AV。
    改以 `HealRateMult -50`（輔助效果 20）表達，語意變成「生命回復速率 -50%」，
    **別人對他施放的治療法術不受影響**。這是能力範圍內最接近的表達，記錄在案。
12. **神佑「致命傷改為留 1 血並清除所有負面」**：`DispelAllSpells()` 會把玩家自己的增益
    一起洗掉，違反規劃 8 對「洗淨」的守則。改成「補回 1 點生命 + 2 秒受傷 ×0 的無敵視窗」
    （0x24 ×0 進入點讀 `ESSB_GuardDivine`），讓玩家有時間脫離，效果等價但不誤傷自己的增益。
    「每場戰鬥一次」用 `IsInCombat()` 在每秒 tick 裡判定脫戰重置。
13. **殘影「30% 機率無效化」**：擲骰發生在「已經被打到」之後，沒有辦法回溯抵消那一擊。
    改成「擲中就消耗風勢並開 2 秒受傷 ×0 的視窗，抵掉**下一次**攻擊後立刻關閉」。
    語意從「這一擊無效」變成「下一擊無效」，次數守恆。
14. **疾風「衝刺耐力消耗 -50%」沒有實作**：本機 `Skyrim.esm` 找不到任何衝刺耐力的
    進入點用例可以比對索引（和 round 1 的「穩步／格擋耐力」同一個問題）。
    移速 +10% 那一半已落地，節點狀態仍記為 IMPLEMENTED，但這半條要進 CK 逐一試。
15. **空中追擊「受你的所有傷害 ×1.5」**改為只放大本模組造成的傷害，理由見決定 61。
16. **聖痕「開印目標對亡靈魔族判定」**照字面實作：帶本模組聖印記的目標在
    `IsHolyPrey()` 裡視同亡靈魔族，所以裁決對他也是 ×3。這條很強，是 5 點的大師分支，
    數值上進遊戲要盯。
17. **浮空 1.5 秒改為 2 秒**，理由見決定 63。
18. **`SetEndBoost` 補上讀取端**：round 1 把「終焉後 5 秒接管元素附傷 +1%／點」
    寫進去了但沒有人讀。本輪加 `EndBoostLeft` / `EndBoostAmount`，由 `GetHitMult` 讀，
    冰、雷、聖三棵樹的這一階這才真的生效。這是順手修掉的 round 1 缺口。
19. **風勢是量表**，封頂在門檻（4，亂舞 3），到門檻就送出風刃並歸零。
    round 1 只封頂不送刀，本輪補上（`CheckWindGauge`）。潛行攻擊直接補滿（規劃 1.1）。
20. **地震不消耗裂痕**（規劃 2.6 明寫），所以 `EndEarth` 不再 `ClearStack(3)`。
    round 1 的實作清掉了，本輪改正。

### 72. `build/plan-coverage.json`

本輪數字：527 列 = 495 個節點 + 32 條共通機制（round 1 的 16 條 + 本輪新增 16 條）。
節點狀態：**IMPLEMENTED 346、DEFERRED 1、PENDING-round3 148、PENDING-round2 0**。
逐樹：通用 38、無元素 39 + 1 DEFERRED、火 39、冰 38、雷 37、
**土 36、風 40、血 39、聖 40**（以上九棵全部結清），
毒 37、水 36、暗 39、星 36 留給 round 3。

`write_plan_coverage` 的硬性檢查從「round 1 的五棵樹不可以有 PENDING」
擴大成「round 1 + round 2 的九棵樹不可以有 PENDING」。

`engine-coverage.json` 的 14 條 `DEFERRED-to-mechanics` 本輪結清 9 條
（岩甲被打 -1、風勢送風刃、失衡 +30%、吹上天與落地傷害、地震跌倒、風的拉近、
風的吹飛、聖印 +20%、血承；另有「必要角色的跌倒與化灰豁免」一併補齊），剩 **5 條**：
水壓（round 3）、同調光暈與 HUD（特效前線）、死咒無法被治療（round 3）、
例外表的瘴氣複製與星界範圍成長（round 3）、星界範圍 ×1.5（round 3，化灰已在 note 裡結清）。

### 73. 本輪 DEFERRED（只剩 round 1 留下的那一個）

| 節點 | 理由 |
|---|---|
| `ESSB_P_noform_0_0_B1` 穩步 | 「格擋耐力消耗」在本機 `Skyrim.esm` 找不到任何原版進入點用例，索引無法實測確認；需進遊戲對 CK 的進入點清單逐一試。5.7 疾風的「衝刺耐力消耗 -50%」是同一個問題，但那個節點的另一半已落地，所以記為 IMPLEMENTED 並在 note 裡註明 |

### 74. 效能

- 沒有新的每幀迴圈、沒有 `Utility.Wait`、沒有新的 `RegisterForUpdate`。
- `ESSBGuard` 是純事件驅動：兩個 PO3 註冊，沒有計時器。
- 潛行狀態、神佑的脫戰重置、四種新領域的每秒效果全部掛在**既有的每秒 tick** 上。
- 推力冷卻與反制冷卻都是固定長度的環狀表（8 格 / 4 格），不配置、不成長。
- 岩甲護甲只在層數真的改變時重下（`RockArmorShown` 比對），連續命中不重複施放。
- 新增的秒計時器 10 個（嗜血、殘影視窗、神佑視窗、披風視窗、不扣血、順勢、反擊、
  接管加成、連殺潛行、以及原有的），全部在 `TickTimers` 裡各減一，`TimersActive()`
  同步擴充，所以沒有目標時仍會降頻成 5 秒。

### 尚未驗證（靜態檢查不能替代）

`OnHitEx` 的 `abHitBlocked` 對「格擋成功」與「格擋失敗但擋了一下」的分辨、
`OnActorKilled` 的 `akKiller` 對「死於本模組 DoT」的回報值、
0x21（受到的擊退幅度）與 0x29（受到的法術強度）兩個索引的實際語意、
`PushActorAway(3.0)` 與 `ApplyHavokImpulse(公尺 × 70)` 的實際距離是否接近規劃的數字、
拉近會不會把人拉穿牆、吹上天的高度是不是「胸口 1 公尺」讓近戰打得到、
浮空 2 秒與落地傷害的時序、
化灰法術對**已經死亡**的屍體是否真的會產生灰燼堆、
`PreventActorDetection` 對「不解除潛行」的效果與副作用、
`ModActorValue("CarryWeight", ±0.1)` 是否真的會重算移動速度、
`DispelSpell` 對自有護甲增益的即時性、
血位曲線在實戰中的震盪手感（尤其重擊扣 8% 最大生命在高血位的痛感）、
單一領域格被後開的領域取代時的體感、
`ESSB_P_BaseRules` 用 `AddPerk` 掛在玩家身上對其他天賦模組的相容性、
新增 11 個全域變數與 37 筆記錄對存檔大小的影響。

### CHECKPOINT（round 2 完成）

- **已完成**：5.6／5.7／5.8／5.9 四棵樹 155 個節點全部 IMPLEMENTED；
  round 1 的 6 個 DEFERRED 結清；`engine-coverage` 結清 9 條。
  `python build_v03.py` exit 0、16 個腳本 0 error 0 warning、
  `plan-coverage.json` 零 PENDING-round2、unmapped = 0。
- **半成品（節點已落地但字面沒有全滿）**：疾風的「衝刺耐力消耗 -50%」、
  空中追擊的「引擎武器傷害那一份」、破護的「第一跳仍會吃到」、
  血咒的「受治療 -50%」語意被縮成「生命回復速率 -50%」、
  神佑的「清除所有負面」換成無敵視窗。五條都在決定 65／71 裡寫明理由。
- **下一步（round 3 的四棵樹：毒素、水、黑暗、星界）**：
  1. FormID 從 **0x005100** 起配，HEDR next object id 仍是 0x006000（沒動）。
  2. 照 `ESSBElem2` 的樣子開 `ESSBElem3`，在 `ESSBElem` 的八個分派點加
     `ElseIf aiElement >= 8 && aiElement <= 11` 一行即可，共通框架不必改。
  3. 控制器的 `ElementHitHook` 要加 8–11 的分支（目前只處理 4–7）。
  4. 還沒有人用的既有掛勾：`ESSBStatus` 的水壓（kind 9）、死咒（`DeathCurseLeft`）、
     星痕環狀桶都已就緒；毒的擴散與催毒 round 1 就做完了。
  5. 剩下的 5 條 `engine-coverage`：水壓、死咒無法被治療（要新的 PERK 進入點，
     0x29 或 `HealRateMult`）、瘴氣複製、星界範圍成長 ×1.5、同調光暈（特效前線）。
  6. **務必先讀決定 61**：攻擊類進入點的條件分頁 1 是武器不是目標，
     任何「目標帶某狀態時你的武器傷害 ×N」都要改成腳本側的自有傷害放大。

---

## 2026-09-17 20:45 — element-mechanics round 3（MECHANICS_BUILT）

規劃第 5 章的 5.10 毒素、5.11 水、5.12 黑暗、5.13 星界四棵樹共 **148 個節點**全部接上機制，
十三棵樹 495 個節點至此只剩 1 個 DEFERRED。`python build_v03.py` exit 0、
**3614 筆記錄**（上一個里程碑 3580 筆，+34）、masters 只有 `Skyrim.esm`、一般 ESP、
**17 個腳本全部 0 error 0 warning**、回讀無重複 FormID（3614 個 id 全不同，最大 0x00512D）、
`build/plan-coverage.json` **541 列、unmapped = 0、PENDING-round2 = 0、PENDING-round3 = 0**、
`build/engine-coverage.json` 103 條只剩 1 條 DEFERRED（同調光暈與 HUD，屬特效前線）。
進入點從 158 段增為 **167 段**。`pyright` 對 `build_v03.py` / `plan_coverage.py` /
`plan_trees.py` 0 error 0 warning。

新檔：`src/ESSBElem3.psc`（毒水暗星四棵樹）。
改寫：`src/ESSBElem.psc`、`src/ESSBController.psc`、`src/ESSBStatus.psc`、
`src/ESSBReactions.psc`、`src/ESSBGuard.psc`、`build_v03.py`、`plan_coverage.py`。

---

### 75. 本輪新增的 FormID（0x005035、0x00503D、0x00508F-0x005093、0x0050AF-0x0050B3、0x005100-0x00512D）

| 區段 | 內容 | 現況 |
|---|---|---|
| 0x005035 / 0x00503D | MGEF／SPEL 自有常駐能力第 6 號（毒免：毒抗 +50%） | 已用 |
| 0x00508F-0x005093 | MGEF 輔助效果第 23–27 號（毒抗強化、毒抗削減、治療強化、疾病抗性、攻擊強化） | 已用 |
| 0x0050AF-0x0050B3 | SPEL 輔助法術第 23–27 號 | 已用 |
| 0x005100-0x005107 | GLOB 狀態鏡射第三批 ×8 | 已用 |
| 0x005120 / 0x005121 | MGEF／SPEL 恐懼（原型 7 Demoralize） | 已用 |
| 0x005122 / 0x005123 | MGEF／SPEL 瘋狂（原型 8 Frenzy） | 已用 |
| 0x005124 / 0x005125 | MGEF／SPEL 亡者歸來（原型 22 Reanimate） | 已用 |
| 0x005126-0x005129 | MGEF 洗淨（原型 2 + Dispel Keywords）、治病（3）、淨化（2）、解毒（29） | 已用 |
| 0x00512A / 0x00512B | SPEL 洗淨、淨化 | 已用 |
| 0x00512C / 0x00512D | MGEF／SPEL 沖刷（對目標的 Dispel） | 已用 |
| 0x005130-0x005FFF | 保留（機制前線三輪到此結束） | 保留 |

`ESSB_DebugLevel` 仍然是 **0x000811**，沒有動。**HEDR next object id 仍是 0x006000**，
沒有超過也沒有 bump（最大 id 0x00512D）。第三批鏡射全域變數：`ESSB_WaterMirror`、
`ESSB_GuardDark`、`ESSB_GuardAstral`、`ESSB_GuardStar`、`ESSB_DomainPoison`、
`ESSB_DomainWater`、`ESSB_DomainDark`、`ESSB_DomainAstral`。

### 76. 原型（archetype）編號也是實測值，只有一個是推出來的

掃本機 `Skyrim.esm` 的 MGEF DATA，本輪確認五個原型：

| 原型 | 原版對照 | 用途 |
|---|---|---|
| 7 Demoralize | `InfluenceConfDownFFAimed`（skill 21 幻術、flags 0x00201903） | 恐懼（夢魘、暗臨強化、亡魂） |
| 8 Frenzy | `InfluenceAggUpFFAimed`（skill 21） | 瘋狂（瘋狂、群魔） |
| 22 Reanimate | `ReanimateFFTargetActor0`（flags 0x00401002、skill 19、delivery 3） | 亡者歸來 |
| 3 Cure Disease | `CureDiseaseEffect` | 洗淨／淨化的治病那一份 |
| 29 Cure Poison | `AlchCurePoison` | 淨化的解毒那一份 |

**唯一不是實測的是原型 2（Dispel）**：本機 `Skyrim.esm` 的 MGEF **沒有任何一筆**用這個原型
（0 = Value Modifier、1 = Script、3 = Cure Disease、4 = Absorb 都有實測點，中間只剩 2），
所以 2 是由這四個點位推出來的，**進 CK 要確認**。最壞情況是洗淨／淨化／沖刷三條不生效，
不會誤傷。

復生的數值語意也是掃出來的：**magnitude 就是可復生的最高等級**
（`VampireRaiseThrall01`–`04` 分別是 6／13／21／30 級、duration 60 秒；
`DeathThrall` 是 40 級、duration 86313600 秒＝永久），恐懼與瘋狂的 magnitude 同樣是等級上限。
所以本模組的六階表完全不引用它們的法術，只用同一組語意：
`SetNthEffectMagnitude(0, 等級上限)` + `SetNthEffectDuration(0, 秒數)` 後 `DoCombatSpellApply`。

### 77. 領域從 1 格擴充為固定 3 格（前線交辦的 B 項）

round 1／2 的單一領域格在本輪會實際踩到：毒霧（8）、潮池（9）、死域（10）、星域（11）
四種新領域加上舊的六種，而「融斷留下領域 → 切換形態 → 再融斷」在正常玩法下**一定**會
讓兩種元素的領域同時存在。所以改成固定 3 格陣列：

- `DomainElem[3] / DomainLeft[3] / DomainX[3] / DomainY[3] / DomainZ[3] / DomainR[3]`
- `StartDomain(元素, 中心, 秒數, 半徑 = 210.0)`：同元素直接取代自己那一格，
  否則取空格，全滿時換掉**剩餘秒數最少**的那一格。
- `InDomain(目標, 元素)` 掃三格做平方距離比較；`DomainActive()` 給 `TimersActive()` 用。
- `TickDomain()` 三格各自倒數，但**全場只掃一次 5 人**，再對每一格做幾何判定，
  所以格數變多不會增加掃描次數。
- 半徑改成每格自己記，因為星域的半徑隨關閉專精主線成長（3 → 15 公尺）並在夜晚 ×1.5，
  不再是固定 3 公尺。

代價：同時最多三種領域。這仍然是**刻意的上限**（理由與 round 1 決定 50 相同：
不放 ObjectReference、不必管生命週期），但 3 格足以涵蓋「前一個元素的領域 + 當前元素的領域
+ 一個溢出」。

### 78. 星界的範圍：一個函式，所有範圍都走它

規劃 2.9 例外表的最後一列（「星界的所有範圍：基礎相同，主線每點 +1 公尺」）與 2.10 的
「夜晚 → 星界所有範圍 ×1.5」合併成 `ESSBElem3.Range(ctl, 基礎公尺, 每點公尺, 階數)`，
夜晚在這裡乘 1.5，回傳遊戲單位。四個入口：`AdventRadius`（星臨 2 + 1／點）、
`FallRadius`（星落與星域 3 + 0.8／點）、`ConstellationRadius`（群星 1 + 1／點）、
`ScatterRadius`（星散 15 + 1／點）。`ESSBElem.OnFormOpened` 對元素 11 改讀 `AdventRadius`，
其餘元素維持 2 + 0.2／點的通式。**人數上限仍然是 5**（規劃 2.9），靠人數限制不靠距離。

### 79. 亡者歸來：六階表、召喚上限、與化灰互斥

`ESSBElem3.Reanimate` 的判定順序（每一步都會留等級 1 紀錄）：

1. 分支「亡者歸來」有沒有投；沒有就完全不做事（零成本）。
2. 機率：25%；「死靈主」在同調三段時改為 100%。
3. `ESSBController.CanReanimate`：必要角色／首領（`IsVIPTarget`）、龍
   （`ActorTypeDragon`）、帶原版 `MagicNoReanimate`（0x06F6FB，灰堆與不可復生的屍體）
   一律不復生。
4. 等級 → 階數：≤6／≤13／≤21／≤30／≤60／任意，持續 120／120／120／180／180／永久。
   **60 級以上只有死靈主能復生**；死靈主在「身邊一個僕從都沒有」時把這一名升成第六階永久。
5. 「亡者強化」持續 ×2（永久那一階不再加倍），並把死前的詛咒層數轉成攻擊加成
   （輔助效果 27，AV 34，每層 10 點）。
6. 召喚上限：`SummonCap()` = 1，玩家持有原版雙魂（`TwinSouls` 0x0D5F1C）時 2；
   永久的第六階上限固定 1。現有僕從數用 `PO3_SKSEFunctions.GetCommandedActors(玩家).Length`。
7. 與化灰互斥：`ESSBElem3.OnKill` 先問 `ESSBElem2.ShouldAsh`，化灰要收就不復生
   （規劃 5.12 明寫「化灰先於復生判定」）。

觸發點是 `ESSBGuard.OnActorKilled → ESSBController.OnKillEvent → ESSBElem3.OnKill`，
條件是 **`LastDamageWasElement(10)`**——也就是 round 2 為化灰做的「最後一次本模組傷害的元素」
追蹤，死咒的傷害走 `ApplyDamage(10, …)` 所以也算。控制器每秒 tick 的後援路徑同樣會呼叫
`ESSBElem3.OnKill`（死於 DoT 或事件沒送到時），`ClearSlot` 保證不會結算兩次。

### 80. 恐懼與瘋狂：等級上限與免疫，不碰 AI 套件

做法同原版幻術：自有 MGEF（原型 7／8）＋ `skill = 21`（讓幻術天賦照常加成）＋
原版 `MagicInfluence` / `MagicInfluenceFear` / `MagicInfluenceFrenzy` 三個 keyword，
magnitude 由 `ESSBController.ApplyFear` / `ApplyFrenzy` 在施放前設成
**`CharmCap()` = 10 + 暗樹等級**（100 級暗樹 → 110 級以下都控得到）。
`CanCharm()` 擋掉首領與必要角色、龍、亡靈魔族，並在等級超過上限時直接不施放
（省掉一次無效的 `DoCombatSpellApply`）。不動 AI 套件、不上麻痺、不上硬直，符合低衝突原則。

### 81. 洗淨／淨化／沖刷：限定關鍵字的 Dispel

規劃 8 明寫「不用原版 Dispel 原型對自己施放（會連自己的增益一起洗掉）」。
本輪的做法是 **原型 2 + MGEF 旗標 0x100（Dispel Keywords）+ KWDA 列出類別**：

| 法術 | 效果 | KWDA |
|---|---|---|
| `ESSB_CleanseSpell`（洗淨） | 限定關鍵字 Dispel + Cure Disease | `MagicAlchHarmful`、`MagicDamageFire/Frost/Shock`、`MagicSlow` |
| `ESSB_PurgeSpell`（淨化、洗滌、水臨強化） | 同上再加麻痺與幻術影響類 + Cure Disease + Cure Poison | 上列 + `MagicParalysis`、`MagicInfluence`、`MagicInfluenceFear`、`MagicInfluenceFrenzy` |
| `ESSB_StripSpell`（沖刷、洗滌的對敵那一半） | 對目標的無限定 Dispel（原型 2、delivery 3） | 無（只清有時限的法術效果） |

「淨化＝清除**全部**負面」因此被解讀為「關鍵字範圍更大的洗淨」，不是無限定 Dispel——
無限定版本會把玩家自己的形態能力、血承、岩甲一起洗掉。沖刷對**敵人**才用無限定版本，
因為那正是它的目的（驅散護甲術、披風、隱身），而且原型 2 不影響能力型常駐效果。
沖刷每目標 10 秒一次，沿用推力的 8 格環狀表（新的 kind 2，窗口 10 秒），不另配置陣列。

### 82. 對規劃字面的解讀與偏離（接續 round 2 的決定 71）

21. **死咒「期間目標無法被治療」**（規劃 2.6、`engine-coverage` 第 72 條）
    不走 PERK 進入點，改用 `HealRateMult -100%` 的自有減益（輔助效果 20，3 秒，
    「不治」分支 6 秒）。語意與血咒同一個偏離：**是「生命回復速率歸零」，
    別人對他施放的治療法術不受影響**。理由是進入點 0x29 只管「受到的法術強度」，
    分不出治療與傷害；而 `HealRateMult` 是原版就有的表達。
22. **「腐朽」（詛咒目標受治療 -50%）** 同上，`HealRateMult -50%`。
23. **「幻影」（目標對你的命中率 -30%）**：引擎沒有命中率修正這種 AV。
    改以攻擊 -30%（AV 34）3 秒表達，語意從「打不中」變成「打得比較輕」。
24. **「夢魘」的 1.5 秒取整為 2 秒**：效果時長是整數秒（同 round 2 決定 63 的浮空）。
25. **「蝕魔」（命中吸魔 ×2）**：規劃沒有定義暗形態的「基礎每擊吸魔」。
    解讀為基礎 `B_max ×0.5`、分支把它加倍成 `×1.0`（開印的吸魔是 `×1.0`，所以
    分支後的每擊吸魔剛好等於開印那一下）。這是本輪最需要數值回饋的一條。
26. **「星軌終焉」（星終焉後接管元素的開印 ×1.5）** 與規劃 2.6 的基礎星落字面重複
    （基礎星落本來就給接管元素 ×1.5）。解讀為加深：沒點 ×1.5，點了 ×2.0，
    同 round 1 決定 55 第 3 點對「電弧」的處理。
27. **「星痕延遲 -0.1 秒／點」只有兩檔**（2 秒與 1 秒）：星痕是兩格環狀桶，
    整數秒 tick 做不出 1.9 秒。10 點以上縮成 1 秒（整桶一起引爆），
    點滿（15 點）的「0.5 秒」沒有再細分，記錄在案。
28. **「基礎擴散間隔 -0.1 秒／點（2 → 0.5 秒）」同樣受整數秒限制**：
    10 點以下 2 秒、10 點以上 1 秒，點滿（15 點）以「每秒傳**兩個**目標」表達 0.5 秒的效果量。
29. **「劇毒」（催毒期間目標毒抗視為 0）**：引擎沒有「視為 0」。改成下一個**等量於目標
    當前毒抗**的削減（同 round 1 碎冰的碎甲用 `DamageResist × 比例` 的做法）。
    目標毒抗為負時不做事。
30. **「亡者強化」的「每層 +10%」以 AV 34 的定值表達**（每層 10 點），
    與既有的「攻擊削弱 -20%」同一個慣例（那一條也是把 20.0 當成 AV 34 的定值），
    不是真的百分比。兩條要一起進遊戲校準。
31. **「星輝」（星形態魔力回復 +20%）與雷樹「感應」效果完全相同**，
    沿用同一個能力記錄（`ESSB_Ability_Induction`），不新增 MGEF／SPEL。
    `RefreshAbilities` 的條件改成「元素 3 且有感應」或「元素 11 且有星輝」。
32. **「長流」用既有的每秒 tick，不用持續型法術**：每秒 `ApplyUtil(回血／回耐)`
    一個百分比，符合規劃 8「不動回復速率 AV，因此不受戰鬥中回復減半影響」的要求，
    而且不必新增記錄、不另開計時器。
33. **「百毒不侵」的免疫用每秒重下 100% 毒抗與疾病抗**（輔助效果 23／26，3 秒，原型 34），
    不是真的免疫旗標。抗性 100% 在原版等同免疫。
34. **「深淵」的「抗性可侵蝕到負值」不必另做**：AV 抗性本來就可以是負數，
    侵蝕是 Peak Value Modifier 的負向修正，點滿 10 層 ×5% = -50% 自然會壓到負值。

### 83. `build/plan-coverage.json`（三輪的最終狀態）

本輪數字：**541 列 = 495 個節點 + 46 條共通機制**（round 1 的 16 條 + round 2 的 16 條 +
本輪 14 條）。節點狀態：**IMPLEMENTED 494、DEFERRED 1、PENDING-round2 0、PENDING-round3 0**。
逐樹：通用 38、無元素 39 + 1 DEFERRED、火 39、冰 38、雷 37、土 36、風 40、血 39、聖 40、
**毒 37、水 36、暗 39、星 36**（十三棵全部結清）。

`plan_coverage.rows()` 的 `default` 本輪改成 `None`：**任何樹缺一個節點狀態都會讓建置失敗**
（不再有「沒寫就當 PENDING」的退路）。`write_plan_coverage` 另加一道檢查：
只要還有任何 `PENDING-*` 列就 `SystemExit`。

`engine-coverage.json` 的 5 條 `DEFERRED-to-mechanics` 本輪結清 4 條：
水壓、死咒無法被治療、瘴氣複製、星界範圍成長與夜晚 ×1.5。
**剩 1 條：同調光暈、武器發光、TrueHUD 同調條——素材與 HUD 屬特效前線（規劃 2.11），
本前線不碰。**

### 84. 本輪 DEFERRED（全部三輪加起來只剩 1 個節點）

| 節點 | 理由 |
|---|---|
| `ESSB_P_noform_0_0_B1` 穩步 | 「格擋耐力消耗」在本機 `Skyrim.esm` 找不到任何原版進入點用例，索引無法實測確認；需進遊戲對 CK 的進入點清單逐一試。5.7 疾風的「衝刺耐力消耗 -50%」是同一個缺口，但那個節點的另一半（移速 +10%）已落地，所以記為 IMPLEMENTED 並在 note 裡註明 |

round 3 的 148 個節點**沒有新增任何 DEFERRED**。

### 85. 效能

- 沒有新的每幀迴圈、沒有 `Utility.Wait`、沒有新的 `RegisterForUpdate`。
- 新增的每秒工作全部掛在**既有的**兩個 tick 上：控制器的每秒 tick（長流、以毒攻毒、
  百毒不侵、三格領域）與狀態容器的每秒 tick（毒層擴散與瘟疫、星痕引爆與預知、死咒）。
- 命中路徑上的新查詢**一律先看投點**再碰狀態容器：`TargetDamageMult` 的三條
  （詛咒滿層、星鎖、星域）、`WaterHitMult` 的水壓、`OnPoisonHit` 的兩個門檻分支。
  只有暗樹的抗性侵蝕是無條件查一次（它是詛咒狀態本身的效果，同血樹的 `OnBloodHit`）。
- 三格領域**每秒只掃一次 5 人**（和一格時完全一樣），再對每格做平方距離比較。
- 沖刷的每目標冷卻沿用既有的 8 格推力環狀表（新增 kind 2），不配置新陣列。
- 新增的秒計時器 3 個（影身視窗、預知視窗、星光視窗）全部在 `TickTimers` 裡各減一，
  `TimersActive()` 同步擴充（含 `DomainActive()` 與水鏡層數），所以沒有目標時仍會降頻成 5 秒。
- 恐懼／瘋狂／復生／洗淨／沖刷都是**事件觸發的一次性法術**，沒有輪詢。

### 86. 節點索引的自動交叉檢查（順手抓到一個真錯）

`ESSBNodes.Rank(ctl, 樹, 路線, 階)` / `Br(ctl, 樹, 路線, 階, 分支)` 的索引寫錯不會編譯失敗，
只會讓節點**永遠不生效**（或誤讀別的節點）。本輪收尾時把所有腳本裡的這兩種呼叫
用正則抓出來，逐一對 `build/plan-tree-nodes.json` 驗證「這棵樹的這一階真的有這個分支」，
並檢查每個檔案只碰自己負責的樹。

**386 個呼叫，抓到 1 個真錯**：`ESSBElem3.PoisonFormTick` 的「百毒不侵」原本寫成
`Br(7, 0, 4, 1)`，但毒樹持續傳奇階只有一個分支（index 0），所以那一條永遠不成立。
已修正為 `Br(7, 0, 4, 0)`。修完 386 個呼叫全部有對應節點、沒有跨樹誤用。
（這個檢查目前是一次性腳本，沒有寫進 `build_v03.py`；建議特效前線或下一輪把它收成
建置期檢查。）

### 尚未驗證（進遊戲才知道）——三個前線累積的完整清單

**載入與綁定（form-layer-core、reaction-engine、skill-trees）**
遊戲內載入、QUST 啟動與三個腳本綁定（`ESSBController` / `ESSBTrees` / `ESSBGuard` 掛同一個
別名）、存讀檔後的 `OnPlayerLoadGame` 重新註冊、中文字串顯示、
PO3 `OnWeaponHit` 的實際旗標、`DoCombatSpellApply` 對自身回復法術的行為、
`SetNthEffectMagnitude` / `SetNthEffectDuration` 在執行期改自有 record 的即時性與存檔持久性、
狀態容器換宿是否真的不掉層（本輪 `ExportInts` 由 25 增為 27 格）、
印記 `OnEffectFinish` 在刷新時是否誤報、雷雨與暴風雪的天氣細分、
8 個每秒腳本同時運作的實際負載。

**技能樹（skill-trees）**
CSF 是否讀得到 13 個設定檔、CSF 是否評估 PERK 的 CTDA（階級門檻與「主線先投 1 點」）、
`StatsMenu` 是否收得到 `OnMenuClose`、`OnCustomSkillIncrease` 是否送到別名上的第二個腳本、
8 個按鈕的 MESG 排版、3226 筆 PERK 對載入時間與存檔大小的影響、洗點一次跑 13 棵樹的卡頓、
中文節點名稱在 CSF 選單的顯示。

**進入點（round 1／2／3）**
167 段進入點是否真的生效；索引 0x23／0x24／0x25／0x1B／0x21／0x29 的實際語意；
`PRKC` 分頁語意（決定 61：攻擊類的分頁 1 是**武器**，受傷類的分頁 1 才是攻擊者）；
`HasMagicEffectKeyword`（693）在進入點條件裡的 run-on 對象（浴火、水膜、影甲三條都靠它）；
15 階鏈的「只有最高階生效」條件在 CSF 買點後是否成立；
「格擋耐力消耗」與「衝刺耐力消耗」兩個找不到用例的索引。

**命中與事件（round 1／2）**
`GetEquippedWeapon(true)` 對雙持同型武器的分辨（雙生可能誤判右手）、
`MRh_SpellFire_Event` / `MLh_SpellFire_Event` 對 NPC 是否可靠送達、
`GetEffectiveMagickaCost` 對 NPC 的回傳值、`GetAnimationVariableBool("IsCasting*")` 的可靠度、
`OnHitEx` 的 `abHitBlocked` 對「格擋成功」與「擋了一下」的分辨、
`OnActorKilled` 的 `akKiller` 對「死於本模組 DoT」的回報值。

**數值與手感（round 1／2）**
AV 156／157（MagickaRateMult／StaminaRateMult）在 Peak Value Modifier 下的行為、
沉默對沒有魔力的敵人等於無效的手感、處決用高強度傷害法術是否真的一擊致死
（`DoCombatSpellApply` 對無敵旗標的目標未測）、火葬用「套用後生命 ≤0」判定擊殺的時序、
領域的幾何判定在垂直落差大的地形上的手感、血位曲線的震盪（尤其重擊扣 8% 最大生命）、
`PushActorAway(3.0)` 與 `ApplyHavokImpulse(公尺 × 70)` 的實際距離、拉近會不會把人拉穿牆、
吹上天的高度是不是「胸口 1 公尺」、浮空 2 秒與落地傷害的時序、
化灰法術對**已經死亡**的屍體是否真的產生灰燼堆、`PreventActorDetection` 的副作用、
`ModActorValue("CarryWeight", ±0.1)` 是否真的重算移動速度、`DispelSpell` 對自有增益的即時性、
單一領域格被取代時的體感、`ESSB_P_BaseRules` 用 `AddPerk` 對其他天賦模組的相容性。

**round 3 新增的未驗證項（本輪最需要盯的）**
1. **原型 2（Dispel）的編號**是推出來的，不是實測（見決定 76）。洗淨、淨化、沖刷三條
   全部靠它；最壞情況是三條不生效。**進 CK 第一件事就是確認這個。**
2. **MGEF 旗標 0x100（Dispel Keywords）** 是否真的把 Dispel 限制在 KWDA 列出的類別；
   如果不是，洗淨會變成無限定 Dispel，**會洗掉玩家自己的增益**（這是本輪最危險的一條，
   如果實測不對就要改成逐個 `DispelSpell` 自有減益）。
3. **對屍體施放 Reanimate 是否成立**（和化灰同一個問題：`DoCombatSpellApply` 對已死目標）。
4. **復生僕從是否真的計入原版召喚上限**：本模組是自己用 `GetCommandedActors` 計數再擋，
   但引擎自己的上限（`SummonCap`）會不會另外把先前的召喚物解散，未測。
5. **恐懼／瘋狂的 magnitude 是不是等級上限**（原版是，但自有 MGEF 的行為要確認），
   以及 `CharmCap` = 10 + 樹等級 的手感（100 級時控得到 110 級以下，可能過強）。
6. **瘋狂會不會讓敵人攻擊玩家自己**（原版 Frenzy 會；「攻擊最近的任何人」是規劃的字面，
   但實戰中可能反而幫敵人）。
7. **AV 45（ResistDisease）與 AV 40（PoisonResist）在 Peak Value Modifier 下的行為**。
8. **三格領域被第四種領域擠掉時的體感**，以及星域 15 公尺半徑在室內的觀感。
9. **水鏡／影身／殘影三個「下一擊無效」視窗同時開著時**會不會互相吃掉彼此的抵消次數。
10. **毒層無上限在實戰中的上限**：瘴氣（原層數複製）＋毒霧（每秒 +1）＋瘟疫（每秒擴散）
    三條疊起來的層數成長速度與 DoT 負載。
11. **`ApplyTrueDamage` 的第三個參數（樹）** 只有星斷用；真傷仍然給無元素樹經驗
    （規劃 2.8 的字面），星樹練不到，這是刻意的。
12. **「亡魂」（死咒殺死目標時附近敵人恐懼）** 用「死咒傷害套用後生命 ≤0」判定擊殺，
    和 round 1 的火葬是同一個時序問題（`DoCombatSpellApply` 不保證同一幀結算完）。
    最壞情況是漏觸發，不會誤觸發。

### 特效前線要知道的事（交接）

**一、需要命中特效／衝擊組／音效的 MGEF（本前線只建了機制，全部沒有美術）**

| 類別 | EDID | 說明 |
|---|---|---|
| 附傷 ×11 | `ESSB_HitEffect_<元素>` | 每次武器命中都會跳，是最常見的一顆，優先做 |
| 反應傷害 ×11 | `ESSB_ReactEffect_<元素>` | 開印／終焉／DoT／領域的所有傷害都走它 |
| 印記 ×11 | `ESSB_MarkEffect_<元素>` | 持續 8 秒，**適合掛 EffectShader**（目標身上的元素光） |
| 真實傷害 | `ESSB_TrueEffect` | 無學派無抗性，建議給一個明顯不同於元素的白色調 |
| 破魔印／沉默 | `ESSB_ManaBreakEffect` / `ESSB_SilenceEffect` | 兩個都是 8 秒／4 秒的狀態，需要目標身上的標記 |
| 化灰 | `ESSB_AshEffect` | DATA 照抄原版 `PerkDisintegrateFFAimed`，**原版的崩解視覺應該會跟著來**，要確認 |
| round 3 恐懼／瘋狂 | `ESSB_FearEffect` / `ESSB_FrenzyEffect` | 原版幻術的紫色調；已掛 `MagicInfluence*` keyword |
| round 3 亡者歸來 | `ESSB_ReanimateEffect` | 原版復生的紫色調；已掛 `MagicSummonUndead` |
| round 3 洗淨／淨化／沖刷 | `ESSB_CleanseEffect` / `ESSB_PurgeEffect` / `ESSB_StripEffect` | 洗淨與淨化是對自己、沖刷是對敵人 |
| 輔助效果 ×28 | `ESSB_UtilEffect_*` | 減速、碎甲、削魔、削耐、回血回魔回耐、放血、抗性升降、岩甲護甲、血盾、治療削減、耐力凝滯、消音、毒抗升降、治療強化、疾病抗性、攻擊強化 |
| 自有常駐能力 ×6 | `ESSB_AbilityEffect_*` | 抗咒、溫血、感應（雷／星共用）、風行、風隱、毒免 |

**二、領域的視覺要掛在哪**

`ESSBController.StartDomain(元素, 中心, 秒數, 半徑)` 是**十一種領域的唯一入口**
（火域、冰原、地裂／泥沼、血池、聖域／神聖領域、毒霧、潮池、死域、星域）。
判定完全在腳本側（三格、中心座標 + 半徑 + 剩餘秒數），**沒有任何 ObjectReference，
所以也沒有視覺**。特效前線只要在 `StartDomain` 裡多放一次一次性的視覺法術／爆炸／hazard
即可，**判定不必改**，半徑參數 `afRadius` 已經是遊戲單位（1 公尺 = 70 單位）。
每格的元素也鏡射成全域變數（`ESSB_DomainFire`／`Frost`／`Earth`／`Blood`／`Divine`／
`Poison`／`Water`／`Dark`／`Astral`），要做「玩家站在領域裡」的螢幕效果可以直接讀。

**三、形態能力住在哪**

每個形態一個 `ESSB_FormAbility_<元素>`（SPEL 0x000960-0x00096A，常駐型），
由 `ESSBController.SwitchForm` 在切換的同一幀 `RemoveSpell` 舊的、`AddSpell` 新的。
**形態的身體光暈、武器附魔光、同調三段的武器發光都應該掛在這 11 個能力上**
（加一個 MGEF 效果即可，控制器不必改）。同調段數與所有腳本狀態都已鏡射成
0x005000／0x0050C0／0x005100 三段共 **37 個全域變數**（`ESSB_SyncStage` 就是同調段數 0–3），
所以條件式的視覺可以直接用 CTDA 讀，不必碰 Papyrus。

**四、還沒有人做的那一條**

`engine-coverage.json` 最後一條 `DEFERRED-to-mechanics`：
**同調光暈（一、二、三段各一層）、三段時武器發光、TrueHUD 同調條**。
資料面已經齊了（`ESSB_SyncStage` 全域變數 + `ESSB_SyncStage` 模組事件 + 11 個形態能力），
缺的只有素材與掛點。規劃 2.11 要求美術從載入順序中的法術包**複製記錄進本模組 ESP**、
不掛 master；`fx_extract.py` 與 `build/fx-catalog.json` 已經把候選素材掃好了。

### CHECKPOINT（round 3 完成，機制前線結束）

- **已完成**：5.10／5.11／5.12／5.13 四棵樹 148 個節點全部 IMPLEMENTED；
  `engine-coverage` 結清 4 條，只剩特效前線那 1 條；
  `plan-coverage.json` 541 列、零 PENDING、unmapped = 0、DEFERRED 1。
  `python build_v03.py` exit 0、17 個腳本 0 error 0 warning、回讀無重複 FormID。
- **半成品（節點已落地但字面沒有全滿）**：決定 82 的 21–34 十四條，
  其中最需要進遊戲改的是「原型 2 是否為 Dispel」「Dispel Keywords 旗標是否限定範圍」
  「蝕魔的基礎吸魔量」「亡者強化的 AV 34 定值 vs 百分比」。
- **下一步（特效前線）**：見上面的四點交接。FormID 從 **0x005130** 起配（或用預留的
  0x003000-0x003FFF 放 EFSH／RFCT／SNDR），HEDR next object id 仍是 0x006000。
## 2026-09-17 21:24 — fx-assets（FX_BOUND）

規劃 2.11／2.12 的美術與音效全部落地：**238 筆借用記錄複製進本模組 ESP**（EDID 前綴
`ESSBFX_`、FormID 0x003000–0x0030ED 連號），masters **仍然只有 `Skyrim.esm`**、一般 ESP。
`python build_v03.py` exit 0、**3870 筆記錄**（上一個里程碑 3614 筆，+256）、
17 個腳本 0 error 0 warning、回讀無重複 FormID（3870 個 id 全不同，最大 0x005147）、
`build/engine-coverage.json` 103 條 **DEFERRED 歸零**（最後一條「同調光暈」本輪結清，
TrueHUD 條改記 DEFERRED-optional）、`build/plan-coverage.json` 541 列、零 PENDING、
DEFERRED 仍是那 1 個節點。`pyright` 對 `build_v03.py` / `plan_coverage.py` /
`plan_trees.py` / `fx_extract.py` **0 error 0 warning**（前兩個檔在 IDE 上被報的那兩條
是 round 3 的舊診斷，實測已經不存在；真正有錯的只有 `fx_extract.py` 的 5 條）。
`python fx_extract.py --selftest` 仍然 PASS（131 筆記錄 round trip 0 failure）。

新檔：無。改寫：`build_v03.py`、`fx_extract.py`、`plan_coverage.py`、`src/ESSBController.psc`。
新輸出：`build/fx-bindings.json`。**沒有複製任何 nif／dds／wav**；複製的記錄指向原模組
既有的資源路徑（規劃 2.11 的做法），原模組移除時只會少特效。

---

### 87. 本輪新增的 FormID（0x003000-0x0030ED、0x005130-0x005147）

| 區段 | 內容 | 現況 |
|---|---|---|
| 0x003000-0x0030ED | 借用的特效素材 238 筆（EFSH 66、SNDR 59、SOUN 55、ARTO 22、IPDS 16、EXPL 11、IPCT 5、LIGH 1、HAZD 1、DEBR 1、TXST 1） | 已用（連號） |
| 0x0030EE-0x003FFF | 保留（特效素材段還剩 3858 格） | 保留 |
| 0x005130-0x005132 | MGEF 同調光暈 ×3（一段石膚、二段鐵膚、三段黑檀膚） | 已用 |
| 0x005134-0x00513E | MGEF 同調三段武器發光 ×11 | 已用 |
| 0x005144-0x005147 | MGEF 重擊附傷 ×4（火／冰／雷／暗，改用 Upgraded 衝擊組） | 已用 |
| 0x005148-0x005FFF | 保留 | 保留 |

`ESSB_DebugLevel` 仍然是 **0x000811**，沒有動。**HEDR next object id 仍是 0x006000**，
沒有超過也沒有 bump（最大 id 0x005147）。0x003000-0x003FFF 是 form-layer-core 當初就
留給特效前線的段，本輪第一次用到。

### 88. 借用的做法：`fx_extract.export` 一次搞定，原始檔唯讀

`build_v03.fx_selectors()` 列出 **224 個選擇器**，全部寫成 `"Plugin.esp|EDID"` 而不是裸
EDID——Darenii 各包共用 EDID（`DAR_RedFXS` 在 Vulcano／Natura／Bloodmoon 各有一筆），
裸 EDID 會撞成 ambiguous。閉包展開後 **238 筆**（多出來的 14 筆是 EXPL 連出去的
IPCT／LIGH／DEBR／TXST／HAZD 與音效描述子）。選擇器 224 → 記錄 238，
missing 0、ambiguous 0、EDID 撞名 0。

選擇器的三大類：

1. **Phenderix Elements 的逐元素成套家族**（11 個元素全部齊備，已逐一核對）：
   `ZZShader_<X>Form`、`ZZShader_<X>`、`ZZImpactSet_<X>`（火冰雷暗另有 `Upgraded`）、
   `ZZExplosion_<X>Hand1`、`ZZArt_<X>`、`ZZArt_<X>Cloak`、
   `ZZSoundDescriptor_{FormActive,Release,DrawSheathe,Charge,OnHit}_<X>`
   以及對應的 **`ZZSound_*_<X>`（SOUN）**。
2. **規劃 2.11 逐元素來源表**的三欄（形態光環／同調三段武器光／印記命中特效）。
3. **備選與狀態素材**（`FX_SPARE`，14 筆）：過熱、旋風疊層、放血、聖光鎖鏈、毒光環、
   催毒、Shadow Form、詛咒、星界兩個備選，加 Darenii 通用色版 5 色。

**原始檔全程唯讀**：`fx_extract` 只用 `tes.read_plugin`（mmap ACCESS_READ）。收尾把
21 個來源插件重新算 sha256 與 `build/fx-catalog.json` 記的值逐一比對，**21/21 相同**；
Phenderix Elements、所有 Darenii 包、Venomancy、Atromancy、StormCalling、
`SkyrimSE/Data`（299 個檔）的最新修改時間全部早於本 session，沒有一個檔被動過。

### 89. 兩個「不能搬」的參照，兩種處置（都在匯出路徑上做，不改原始檔）

`fx_extract.export()` 本輪加了 `drop_subrecords` 參數。理由：xEdit 定義裡 SNDR 的
`CTDA` 與 IPDS 的 `PNAM` 都是**沒有計數欄的無序陣列**，抽掉一個成員對其餘完全無損；
而 `unmapped='null'` 對它們沒用（材質寫 0 會讓那一對變成壞資料）。

| 處置 | 對象 | 筆數 |
|---|---|---|
| `unmapped='null'`（欄位寫 0） | 10 個 `ZZExplosion_<X>Hand1` 的 Object Effect（指向 Phenderix 的 ENCH，沒複製）＋ `ZZHazard_DarknessUpgraded` 的 Spell | 11 |
| `drop_subrecords=('CTDA',)` | 8 個 `ZZSoundDescriptor_DrawSheathe_<X>` 的 CTDA（GetIsRace，參數 `dawnguard.esm\|00283A`） | 8 |
| `drop_subrecords=('PNAM',)` | `ZZImpactSet_Earth／Divine／Water` 各一對 PNAM（材質 `dragonborn.esm\|018C9C`） | 3 |

回讀驗證：**238 筆複製記錄的所有參照欄位，沒有任何一個 master index > 1**
（0 = Skyrim.esm、1 = 本插件），三個衝擊組各剩 78 對 PNAM、外來材質 0 對，
`ESSBFX_ZZSoundDescriptor_DrawSheathe_Fire` 的子記錄變成
`EDID／CNAM／GNAM／ANAM／ONAM／LNAM／BNAM`（CTDA 消失）。
`extra_masters` 在修正後是空的，`masters == ['Skyrim.esm']`。

爆炸少掉 Object Effect 的後果：爆炸本身照放，只是不帶附魔（Phenderix 的手部爆炸
附魔是給它自己的法術算傷害用的，本模組的傷害完全走自己的反應法術，所以沒有損失）。

### 90. MGEF DATA 的特效欄位：位移逐欄數出來，不憑印象

`mgef_data()` 本輪擴充 11 個具名參數，位移全部逐欄數自
`vendor/wbDefinitionsTES5.pas` 的 `wbMGEFData`（結構總長 152 位元組，與既有實作相符）：

| 位移 | 欄位 | 位移 | 欄位 |
|---|---|---|---|
| 24 | Casting Light | 96 | Hit Effect Art |
| 32 | Hit Shader | 100 | Impact Data |
| 36 | Enchant Shader | 116 | Enchant Art |
| 72 | Projectile | 120 | Hit Visuals（RFCT） |
| 76 | Explosion | 124 | Enchant Visuals（RFCT） |
| 92 | Casting Art | | |

音效不是 `SNDD`「leftover, unused」那一條——那是 STAT 的註解。掃本機 `Skyrim.esm`
的 **950 筆 MGEF，每一筆都有 `SNDD`**，內容是 `(型別 uint32, SNDR FormID)` 的陣列
（例：`BanishDmgHealthFFTargetActor` = 型別 0 → `MAGLightDrawSheatheLPMSD`、
型別 3、型別 4），型別枚舉 0 Draw/Sheathe、1 Charge、2 Ready、3 Release、
4 Concentration Cast Loop、5 On Hit。參照的是 **SNDR 不是 SOUN**（實測三個都對到 SNDR）。
子記錄順序照定義擺在 `DATA` 之後、`DNAM` 之前。

### 91. 逐元素綁定表（實際建出來的樣子）

| 元素 | 形態光環（Hit+Enchant Shader） | 同調三段武器光 | 命中／印記／反應 Hit Shader | 衝擊組（普攻／重擊） | 開印 Art |
|---|---|---|---|---|---|
| 火焰 | `ESSBFX_DAR_MoltenFXShader` | `ESSBFX_ZZShader_FireForm`※ | `ESSBFX_DAR_MoltenSpellLavaMistShader` | `ZZImpactSet_Fire` / `_FireUpgraded` | `ESSBFX_ZZArt_Fire` |
| 冰霜 | `ESSBFX__IP_FrostIceFormFXShader02` | `ESSBFX__ATRO_FrostBuffFXShader2` | `ESSBFX__IP_FrostFXShader` | `_Frost` / `_FrostUpgraded` | `ESSBFX_ZZArt_Frost` |
| 雷電 | `ESSBFX_DAR_ArcFXShader` | `ESSBFX__ATRO_StormBuffFXShader2` | `ESSBFX__SC_ShockStormFXShader` | `_Lightning` / `_LightningUpgraded` | `ESSBFX_ZZArt_Lightning` |
| 大地 | `ESSBFX_NAT_MagicArmorStoneFleshFXS` | `ESSBFX_NAT_ArmorRatingSFXS` | `ESSBFX_ZZShader_Earth`※ | `_Earth` / `_Earth` | `ESSBFX_ZZArt_Earth` |
| 風 | `ESSBFX__AV_EffectShader` | `ESSBFX_ZZShader_Wind` | `ESSBFX_NAT_WindHitFX` | `_Wind` / `_Wind` | `ESSBFX_ZZArt_Wind` |
| 鮮血 | `ESSBFX_DAR_RedFXS` | `ESSBFX_ZZShader_Blood` | `ESSBFX_BLO_BloodDamageImpactMist` | `_Blood` / `_Blood` | `ESSBFX_ZZArt_Blood` |
| 神聖 | `ESSBFX_INQ_HealingLightFXS` | `ESSBFX_INQ_EnchHolySwordFXS` | `ESSBFX__LIP_ConsecrateDeadFXSA` | `_Divine` / `_Divine` | `ESSBFX_ZZArt_Divine` |
| 毒素 | `ESSBFX__VENOM_PoisonCloakFXShader` | `ESSBFX__VENOM_PoisonWeaponFXShader` | `ESSBFX__VENOM_PoisonMistFXShader` | `_Poison` / `_Poison` | `ESSBFX_ZZArt_Poison` |
| 水 | `ESSBFX_ZZShader_WaterForm` | `ESSBFX_NAT_WaterShader` | `ESSBFX_ZZWaterloggedShader1` | `_Water` / `_Water` | `ESSBFX_ZZArt_Water` |
| 黑暗 | `ESSBFX_ABY_ShadowFXS` | `ESSBFX_NEC_EnchNecroticSwordFXS` | `ESSBFX_ABY_ShadowDamageImpactMist` | `_Darkness` / `_DarknessUpgraded` | `ESSBFX_ZZArt_Darkness` |
| 星界 | `ESSBFX_DAR_AstralFXShader` | `ESSBFX_LUN_BlueFXS` | `ESSBFX_DAR_AstralSpellStarMistShader` | `_Astral` / `_Astral` | `ESSBFX_ZZArt_Astral` |

※ 兩格退回 Phenderix 的保底，理由見決定 94。音效每元素五條：
`ZZSoundDescriptor_OnHit_<X>`（掛附傷 MGEF 的 SNDD 型別 5）、
`ZZSoundDescriptor_Charge_<X>`（掛印記 MGEF 的 SNDD 型別 1）、
`ZZSound_FormActive_<X>／Release_<X>／DrawSheathe_<X>／Charge_<X>`
（SOUN，由 `ESSBController.PlayFormSound` 用 `Sound.Play(玩家)` 播）。

掛法一覽（全部在 `build/fx-bindings.json` 有機器可讀版）：

| 記錄 | 旗標變動 | 綁了什麼 |
|---|---|---|
| `ESSB_FormAbilityEffect_<X>` | `+0x1000` FX Persist | Hit Shader ＝ Enchant Shader ＝ 形態光環 |
| `ESSB_HitEffect_<X>` | 不變 | Hit Shader + Impact Data + SNDD(5) |
| `ESSB_HitEffect_<X>_Power`（火冰雷暗 4 顆，新記錄） | 不變 | 同上但 Impact Data 換 `Upgraded`；`ESSB_Hit_<X>_Power` 改指這一顆 |
| `ESSB_MarkEffect_<X>` | `+0x1000` FX Persist | Hit Shader（印記期間持續）+ Hit Effect Art（`ZZArt_<X>` 閃現）+ SNDD(1) |
| `ESSB_ReactEffect_<X>` | 不變 | 只給 Hit Shader |
| `ESSB_TrueEffect` | 不變 | Hit Shader ＝ 白光 |
| `ESSB_ManaBreakEffect`／`ESSB_SilenceEffect` | `+0x1000` FX Persist | Hit Shader ＝ 白光（狀態期間持續） |
| `ESSB_AshEffect` | 不變 | Hit Shader ＝ `ESSBFX__LIP_ConsecrateDeadFXSA` |
| `ESSB_ReanimateEffect`／`FearEffect`／`FrenzyEffect`／`CureDiseaseEffect`／`CurePoisonEffect` | 不變 | 原版既有特效，**master 參照不複製**（見決定 93） |

### 92. 同調光暈：三段條件式效果掛在形態能力上，零 Papyrus

引擎覆蓋表最後一條 `DEFERRED-to-mechanics` 本輪結清。做法：

- 三個共用的 MGEF `ESSB_SyncGlowEffect_1／2／3`，Hit Shader ＝ Enchant Shader ＝
  原版 `MagicArmorStoneFleshFXS`（0x094161）／`MagicArmorIronFleshFXS`（0x094163）／
  `MagicArmorEbonyFleshFXS`（0x094162）——就是規劃 2.11「一段石膚、二段鐵膚、
  三段黑檀膚」的字面，三筆都在 `Skyrim.esm`，**一筆都不必複製**。
- 每元素一個 `ESSB_SyncWeaponEffect_<X>`，Hit Shader ＝ 2.11 第 2 欄的武器光。
- 四個效果都加進 11 個 `ESSB_FormAbility_<X>`，條件寫在**法術效果層的 CTDA**
  （`EFID／EFIT／CTDA` 是 xEdit `wbEffect` 的合法順序）：
  `GetGlobalValue(ESSB_SyncStage) >= 1／2／3／3`。回讀確認每個形態能力
  **5 個效果、4 條 CTDA**（op 0x60 ＝ 大於等於、函式 74、param1 = 0x01005000）。
- 常駐視覺的旗標照抄原版 `AbOnFire`（掃 `Skyrim.esm` 出來：flags 0x1200 ＝
  FX Persist 0x1000 + No Duration 0x200、Hit Shader ＝ Enchant Shader、原型 1）。
  本模組的形態能力效果旗標從 `0x8E00` 變成 `0x9E00`（多 FX Persist）。
- `ESSB_SyncStage` 全域變數（0x005000）在 `PushSyncStage()` 就已經寫好，所以升段／
  降段／融斷歸零全部自動反映到光暈，**控制器不必為視覺多寫一行**。
  `OnSyncStage` 只多播一次 `Charge_<X>`（規劃 2.12「同調升段：Charge_X」）。

**TrueHUD 同調條記 DEFERRED-optional**：規劃把它和光暈寫在同一列，但光暈是規格、
HUD 條是加分；要做需要 TrueHUD 的 SKSE API 與自訂條 DLL，會多一個硬依賴，
而且資料面（`ESSB_SyncStage` 全域變數 + `ESSB_SyncStage` 模組事件）已經齊了，
任何 HUD 模組隨時可以讀。理由寫在 `build/engine-coverage.json` 的 `deferred_optional`，
不擋任何交付。

### 93. 爆炸與音效：唯一需要 Papyrus 的兩件事

規劃 2.12 的效能守則是硬約束：「每次命中的特效只靠附傷 MGEF 自帶的衝擊組與命中著色，
引擎處理，不額外施放視覺法術，不跑腳本」「爆炸記錄只用在終焉、融斷、自燃、放電、
處決這類事件；普通命中禁止用爆炸」「範圍事件最多播 5 個目標的特效」。

`ESSBController` 只多了兩個函式：

- `PlaceFx(元素, 位置)`：`PlaceAtMe(ESSBFX_ZZExplosion_<X>Hand1)`，
  **0.5 秒窗口最多 5 個**的預算（`FxBudget`／`FxBudgetTime`）。呼叫點只有三個：
  `EndMark`（終焉，reason 1 就是融斷）、`EndSecondMark`（副印記終焉）、
  `StartDomain`（領域開場一次）。融斷一次最多跑 16 次 `EndMark`，
  預算保證只會放出 5 個——規劃「範圍事件最多播 5 個目標」直接由這個預算實現。
  **普通命中不經過 `EndMark`，所以爆炸不可能出現在普通命中上。**
- `PlayFormSound(音效庫, 元素)`：`Sound.Play(玩家)`。呼叫點四個：
  `OnFormOpened`（FormActive）、`OnFormClosed`（Release）、
  `OnFormSwitched`（DrawSheathe 舊元素，`SwitchForm` 的順序剛好是先
  `OnFormSwitched` 再 `OnFormOpened`，所以自然變成「DrawSheathe_舊 接 FormActive_新」）、
  `OnSyncStage`（Charge）。

**領域的判定完全沒動**：`StartDomain` 還是三格、中心座標 + 半徑 + 剩餘秒數，
沒有任何 `ObjectReference`，只是多放一次一次性的爆炸（`akCenter` 本來就是 Actor）。

**沒有新的每秒工作、沒有新的計時器、沒有 `RegisterForUpdate`、沒有 `Utility.Wait`。**
命中路徑上一行 Papyrus 都沒加——附傷的衝擊組、命中著色、命中音效全部是引擎播的。

### 94. 與規劃不同（接續 round 3 的決定 82）

35. **火的「同調三段武器光」退回 `ZZShader_FireForm`**。2.11 寫「Vibrant Weapons
    火系武器光」。`Vibrant Weapons EAE` 確實裝了，但掃出來 **EFSH 0 筆**（它是
    ARTO 附魔藝術路線），而且 master 帶 `EnchantmentArtExtender.esl`，
    借它就會多一個 master，違反「masters 只有 Skyrim.esm」。所以退回 Phenderix 保底。
36. **大地的「印記／命中特效」退回 `ZZShader_Earth`**。2.11 寫「Phenderix EDM 補丁
    『Earthquake』爆炸」，本機沒有那個補丁；而且用爆炸當每次命中的著色器會直接
    違反 2.12 的效能守則（普通命中禁止用爆炸）。
37. **真實／破魔的白光用 `_BL_EffectShaderWhiteShockGlow`**。2.11 寫「Apocalypse
    `WB_FXS_White` 類」。Apocalypse 裝了（889 筆 FX 記錄、167 筆 EFSH），
    但**沒有任何 EDID 含 White**，`WB_FXS_White` 不存在。改用同一列也點名的
    Dark Hierophant `_BL_EffectShaderWhiteShockGlow`。
38. **鮮血的光環用 Natura 的 `DAR_RedFXS`，不是 Bloodmoon 的**。兩筆是不同記錄但
    **EDID 相同**，複製進來會撞成同一個 `ESSBFX_DAR_RedFXS`。Darenii 通用色版
    統一取 Natura 那一套（Red／Orange／Green／Blue／Purple／WhiteMist 六色齊全），
    紅色那一筆同時當血光環與通用紅。`BLO_BleedingShader` 照樣複製進來備用。
39. **終焉的爆炸改由 Papyrus `PlaceAtMe`，不走「終焉 MGEF 的爆炸欄位」**。
    2.12 寫「由終焉 MGEF 的爆炸欄位播放」，但終焉走的 `ESSB_ReactEffect_<X>` 這 11 顆
    同時承載 DoT、領域、毒層、星痕、死咒的**每秒**傷害——爆炸欄位會讓每一跳都炸一次，
    直接違反同一節的效能守則。所以爆炸交給控制器在 `EndMark` 放，帶 5 個／0.5 秒的預算。
40. **元素狀態的「每升一層著色加深」本輪沒做**。2.12 第 8 列要求熱度／凍結／詛咒
    逐層加深著色。要做得逐層 `EffectShader.Play/Stop`，那是命中路徑上的腳本工作，
    與同一節的效能守則衝突。素材（`DAR_MoltenHeartFXS` 過熱、`_VENOM_RotfleshFXS`
    催毒、`DAR_EldritchShadowMist` 詛咒、`ABY_ShadowPriestFleshFXS` 影身）都已經
    複製進 ESP，列在 `build/fx-bindings.json` 的 `unbound_spares`，CK 裡隨時可掛。
41. **重擊只有火冰雷暗四個元素有專屬 MGEF**。2.12 說「附傷法術分普攻與重擊兩個版本，
    各掛自己的衝擊組」，但 Phenderix 只有這四個元素有 `Upgraded` 衝擊組，
    其餘七個元素的重擊法術繼續指原本那一顆（同衝擊組）。「同上音量 +3 dB」沒做：
    SNDR 的音量欄是原模組的資料，改它等於改借來的記錄，不值得。
42. **洗淨／淨化的 SELF 路徑改成「安全建構」，不再依賴原型 2 與旗標 0x100**
    （這是 round 3 尚未驗證清單第 1、2 條，也是當時標記「本輪最危險」的那一條）。
    新做法三段，每一段最壞情況都只是「少清幾個」，不可能洗掉玩家自己的增益：
    1. `SelfCleanseSpells`（11 個）＝ 本模組所有「有時限且有害」的輔助法術
       （緩速、碎甲、停止回魔、火／冰／電／魔抗削減、攻擊削弱、治療削減、
       耐力凝滯、毒抗削減），逐個 `HasMagicEffect` 後 `DispelSpell`。
    2. `ESSB_CleanseSpell` 只留 **Cure Disease（原型 3）**、`ESSB_PurgeSpell` 留
       **Cure Disease + Cure Poison（原型 29）**——兩個原型在 `Skyrim.esm` 都有實測對照
       （`CureDiseaseEffect`、`AlchCurePoison`）。**原型 2 的 MGEF 不再掛進任何法術。**
    3. 淨化另跑 `DispelHostileEffects`：PO3 `GetActiveEffects` 掃現行效果，
       只挑 `IsEffectFlagSet(1)`（敵對）或 `(4)`（有害）的，
       再用 PO3 `GetMagicEffectSource` 回推來源法術後 `DispelSpell`。
       形態能力、血承、岩甲、抗咒這些自有增益都不帶這兩個旗標，所以掃不到。
    `ESSB_CleanseEffect`（0x005126）與 `ESSB_PurgeEffect`（0x005128）**記錄保留在 ESP
    裡待命、FormID 不動**，但沒有任何法術引用；進 CK 確認原型 2 與旗標 0x100 為真之後
    掛回去即可。**TARGET 路徑（沖刷）維持原型 2**，因為驅散敵人的護甲術／披風／隱身
    正是它的目的；`ApplyStrip` 另加兩道守衛：目標等於玩家直接拒絕（留等級 1 紀錄）、
    非法目標（`IsValidTarget`）直接拒絕。
43. **`ESSBNodes.Rank/Br` 的索引交叉檢查收進建置期**（round 3 決定 86 的建議）。
    `build_v03.check_node_calls()` 用正則抓 17 個腳本裡的呼叫，逐一對
    `build/plan-tree-nodes.json` 驗「這棵樹的這一階真的有這個分支」，
    超出範圍就 `SystemExit`。本輪跑出 **421 個呼叫、0 個超界**
    （比 round 3 的 386 多，因為這次也抓 `ESSBNodes.psc` 自己不帶前綴的呼叫）。
    另加一道 `MIN_NODE_CALLS = 300` 的下限：抓到的數量掉到 300 以下就讓建置失敗，
    免得正則哪天失效變成靜默通過。
44. **IDE 報的那兩條診斷是舊的**。`build_v03.py:1897`（tuple unpack 2 vs 3）與
    `plan_coverage.py:685`（`R2`／`R3` possibly unbound）在 round 3 收尾時就已經修掉了，
    實跑 `npx pyright` 兩個檔都 0 error。真正還有錯的是 `fx_extract.py`
    的 5 條（`RECORDS: dict[str, object]` 讓 `record.ss／ref／edid／sig` 全部不可見）。
    修法：`RECORDS: dict[str, Record]`（從 `tes` import `Record`）＋
    `export()` 裡加一行 `if target is None: continue`（`ref()` 只在 FormID 0 回 None，
    而那條路已經被 `if not fid` 擋掉，pyright 看不出來）。行為完全沒變，
    `python fx_extract.py --selftest` 仍然 PASS。

### 95. 效能

- **命中路徑上零新增 Papyrus**：附傷的衝擊組、命中著色、命中音效都是引擎播的
  （MGEF 自帶的 Impact Data／Hit Shader／SNDD），規劃 2.12 第 4 列的字面。
- 沒有新的每秒迴圈、沒有新計時器、沒有 `RegisterForUpdate`、沒有 `Utility.Wait`。
- 爆炸總量由 `PlaceFx` 的預算封頂：**每 0.5 秒最多 5 個**，來源只有終焉、融斷、
  領域開場三處。融斷最壞情況（8 格 × 主副印記 = 16 次結算）也只放 5 個。
- 音效每次形態動作最多 1 聲（開／關／切換／升段各一），沒有輪詢。
- 同調光暈是引擎評估 CTDA，不是腳本輪詢；`ESSB_SyncStage` 本來就只在變動時寫一次。
- ESP 從 3614 筆長到 3870 筆（+256），檔案 861,238 bytes。238 筆借來的記錄全部是
  被動資料（著色器、衝擊組、音效描述子），不會增加每幀成本。

### 96. `build/fx-bindings.json`

機器可讀的綁定表，欄位：`totals`（選擇器 224、記錄 238、逐型別計數、來源插件 21、
nulled 11、dropped 11、Skyrim.esm 外部參照 144、修正後的 extra masters = 空）、
`nulled`（11 條，逐筆寫 EDID／欄位／原目標／原因）、`dropped`（11 條）、
`elements`（11 列，每列含形態能力、光環、武器光、命中著色、普攻／重擊衝擊組、
重擊 MGEF、印記 MGEF、開印 Art、反應 MGEF、爆炸、六個音效）、
`shared`（同調光暈、白光、化灰、原版既有、領域、終焉六組）、
`unbound_spares`（14 筆複製但還沒掛的素材）、`records`（238 筆，EDID／型別／
本地 id／來源 `Plugin|原 id`）。`assets_copied: false` 明寫沒有搬任何資源檔。

### 尚未驗證（進遊戲才知道）——本前線新增，建議照這個順序跑

以下清單假設 **除錯等級 3**（Z 選單第 2 個按鈕循環到 3）。前三個前線累積的清單
仍然有效（見 round 3 的「尚未驗證」四大段），這裡只列特效相關、且**有順序**的。

**A. 載入與基本綁定（2 分鐘）**
1. 新遊戲或乾淨存檔載入，確認沒有缺 master 的警告（本模組只依賴 `Skyrim.esm`）。
2. `~` 主控台 `help ESSBFX_ 0` 應該列得出 238 筆 `ESSBFX_*` 記錄。
   抽查 `help ESSBFX_ZZImpactSet_Fire 0` 與 `help ESSBFX_ZZSound_FormActive_Fire 0`。
3. **原模組還在的情況**下才有貼圖與音檔。若把 Phenderix Elements 停用，
   預期是「特效消失但不崩潰」（規劃 2.11 的承諾）——這一條可以最後再測。

**B. 形態光環與音效（每元素 15 秒，11 個元素）**
4. 按 Z 開火形態：身上應該出現岩漿皮膚（`DAR_MoltenFXShader`），
   同時聽到一聲 `FormActive_Fire`。紀錄應該有 `form open element=1`。
5. Z 切到冰形態：先聽到一聲 `DrawSheathe_Fire`，接著 `FormActive_Frost`，
   光環換成冰形。**這是最容易出問題的一條**（兩個 SOUN 同幀播）。
6. 再按同一顆 Z 關形態：聽到 `Release_<當前元素>`，光環消失。
7. 11 個元素各開一次，確認每一個都有光環（沒有哪個元素是空的）。
   大地的光環是石膚系、水的是 Phenderix 水形態、星界是 Stellaris 星光。

**C. 命中特效（引擎側，最重要）**
8. 開火形態，普攻打一個敵人：應該有火花衝擊組 + 目標短暫火光著色 +
   `OnHit_Fire` 音效。**這一條驗證 MGEF 的 Impact Data／Hit Shader／SNDD 三欄
   在自有 MGEF 上是否真的生效**——如果無聲無衝擊，其餘命中特效都不用測了。
9. 同一目標重擊：衝擊組應該換成 `FireUpgraded`（火花更大）。
   火冰雷暗四個元素測這一條；其餘七個元素預期與普攻相同（決定 41）。
10. 開印那一下（第一次命中或印記過期後重新開印）：目標身上應該閃一次
    `ZZArt_Fire` 並聽到 `Charge_Fire`，之後 8 秒內目標持續帶火光著色。
    **這一條驗證 Hit Effect Art 與 FX Persist 旗標。**
11. DoT／毒層／星痕的每秒傷害：目標應該每跳閃一次元素著色，
    **但不應該有衝擊組音效、更不應該有爆炸**。如果每跳都炸，
    就是反應 MGEF 被誤掛了爆炸欄位（本輪刻意沒掛）。

**D. 同調光暈（本輪結清的那一條）**
12. 開形態後連續命中累同調。到一段（預設 5）：身上應該多一層石膚光。
    紀錄有 `sync stage=1`，同時聽到一聲 `Charge_<元素>`。
13. 二段（15）：石膚光之上再加一層鐵膚光（更亮／更金屬）。
14. 三段（30）：再加黑檀膚光，**並且武器上出現該元素的光**
    （火＝Phenderix 火形態光、冰＝Atromancy 冰光、星界＝Lunaris 藍光…）。
15. Z 融斷：同調歸零，三層光暈與武器光**應該同時消失**。
    這一條驗證「CTDA 條件在常駐能力上會被重新評估」——
    **如果光暈不消失，就是引擎只在 AddSpell 時評估一次條件**，
    那就要改成由控制器在 `OnSyncStage` 用 `AddSpell/RemoveSpell` 換四個能力。
    **這是本輪最需要確認的一條。**
16. 切換形態後再升到三段：武器光應該換成新元素的顏色（不是舊元素的）。

**E. 爆炸（終焉、融斷、領域）**
17. 單一目標的終焉（印記到期或主動結清）：目標位置應該炸一次該元素的手部爆炸。
18. 帶印記的目標湊到 6 個以上再按 Z 融斷：**最多只應該看到 5 次爆炸**
    （`PlaceFx` 的 0.5 秒 5 個預算）。紀錄的 `burst targets=` 可能大於 5，
    這是刻意的——傷害全算，特效只播 5 個。
19. 領域類技能（火域、冰原、地裂、血池、聖域、毒霧、潮池、死域、星域）：
    開場應該炸一次，之後**領域期間不再有任何視覺**（領域沒有 ObjectReference，
    這是刻意的；要做「站在領域裡」的螢幕效果得讀 `ESSB_Domain<X>` 全域變數）。
20. 確認**普通命中一次爆炸都沒有**。若有，就是 `PlaceFx` 被誤呼叫。

**F. 洗淨／淨化的安全性（決定 42 的驗收）**
21. 開水形態，先讓自己吃到緩速或碎甲（本模組的減益），再觸發洗淨：
    紀錄應該有 `cleanse self purge=False removed=N`，N ≥ 1。
22. **關鍵安全測試**：開任一形態（身上有形態能力光環 + 可能有血承／岩甲），
    觸發淨化（水樹持續大師分支）。**光環與形態能力必須還在**、
    同調段數不能歸零、岩甲層數不能歸零。紀錄的 `removed=` 只該算到減益。
23. 吃疾病（例如被熊抓）後觸發洗淨：疾病應該消失（Cure Disease 原型 3 的實測）。
24. 沖刷：對一個有護甲術的法師敵人開印，它的護甲術應該消失。
    **對自己絕不會發生**——`ApplyStrip` 有硬性守衛，若紀錄出現
    `strip refused: never dispel the player` 就是守衛擋下來了（不該發生，但擋得住）。

**G. 相容與收尾**
25. 停用 Phenderix Elements 再載入：預期「特效與音效消失、遊戲照跑、紀錄照寫」。
    若崩潰，就是複製記錄裡還有指向原模組的參照（本輪回讀驗證是 0，但值得實測）。
26. 連續戰鬥 5 分鐘觀察掉幀：重點看融斷與領域密集時。若卡，先把
    `PlaceFx` 的預算從 5 降到 3。

### CHECKPOINT（fx-assets 完成，特效前線結束）

- **已完成**：規劃 2.11 逐元素來源表 11×3 格全部有來源（兩格用保底，理由在決定 94）；
  2.12 事件→視覺→音效表的 10 列裡 8 列落地、1 列刻意改做法（決定 39）、
  1 列延後且素材已就位（決定 40）；`engine-coverage.json` **DEFERRED 歸零**、
  多一條 DEFERRED-optional（TrueHUD，不擋）；`plan-coverage.json` 541 列、零 PENDING。
  `python build_v03.py` exit 0、3870 筆、masters `['Skyrim.esm']`、
  17 個腳本 0 error、回讀 0 重複、`pyright` 四個檔 0 error、
  `fx_extract --selftest` PASS、21 個來源插件 sha256 全部未變。
- **半成品**：元素狀態的逐層著色（決定 40，素材已複製待掛）；
  火的武器光與大地的命中著色用保底（決定 35、36）。
- **下一步（install-enable）**：`package/Elements Spellblade` 複製進
  `MO2/mods/Elements Spellblade`、在 normal profile 啟用、三個清單檔一致；
  然後照上面 A→G 的順序跑進遊戲煙霧測試（除錯等級 3）。
  FormID 從 **0x0030EE**（特效段）或 **0x005148**（自有段）起配，
  HEDR next object id 仍是 0x006000。


## 2026-09-17 22:13 Asia/Taipei — fix round（審查修正）

本輪依三個實體檔案中的四份審查修正：review-2026-09-17.md（commander 與 builder review）、review-astra-A.md、review-astra-B.md。先讀完整審查，再以未修改的 元素魔戰士規劃-v0.3.md 與 build/plan-tree-nodes.json 核對節點。A7/B4、A10/B8 各合併一次；B 的 P1-4 歸屬補充併入該項。40 個不重複修正項目均已完成；另將審查列出的五個 P2 實機／既有風險明列為 NOT FIXED，不把編譯成功當作遊戲驗證。

產品修改：src/ESSBController.psc、src/ESSBElem.psc、src/ESSBElem2.psc、src/ESSBElem3.psc、src/ESSBGuard.psc、src/ESSBNoForm.psc、src/ESSBReactions.psc、src/ESSBStatus.psc、src/ESSBTrees.psc、build_v03.py。所有 .psc 保持 UTF-8；未新增 import stub。settings.json、plan_coverage.py、plan_trees.py 未修改；本檔只追加。計畫、審查、fx_extract.py 保持原始 SHA-256；未寫入 .strategic-advance/、MO2 或 SkyrimSE，未存取網路。

### 建置與驗證

- campaign root 執行 `python build_v03.py`：最後一輪 exit 0；17 支出貨腳本全部編譯，0 errors、0 warnings。完整輸出：build/fix-round-build-final.log、build/v03-compile-results.json。
- ESP 回讀 masters == ['Skyrim.esm']；record_count == manifest == 3878；重複 FormID 0；未 ESL-flag。原有 3870 筆 EDID/FormID 配對全部保留，ESSB_DebugLevel 固定 0x000811，PERK EDID 集合不變。
- CSF 13/13 valid；495 nodes。plan coverage 541 列：540 IMPLEMENTED、1 原有 DEFERRED、0 unmapped、0 PENDING。既有 DEFERRED 是 ESSB_P_noform_0_0_B1「穩步」的原版格擋耐力進入點待實機確認，本輪未改設計或把它改標完成。
- SPEL SPIT 與每個引用 MGEF DATA 的 delivery 回讀全部相符：Contact (1) 71 支、Self (0) 49 支。敵方法術與同伴目標法術為 Contact；玩家法術為 Self。逐筆結果：build/fix-round-delivery.json。
- build/fix_round_regression.py：23 組測試通過，執行實際 Papyrus 函式／事件內容並替代原生與外部呼叫；涵蓋順序、快照、插層、世代交錯、去重、冷卻、重入、宿主緩衝、領域五跳及推拉。此為離線邏輯驗證，未執行 Skyrim 引擎。
- build/fix_round_audit.py：99 項檢查通過；包含保護檔案雜湊、FX 關鍵函式不變、既有 public function 名稱、禁用 API、UTF-8、陣列上限、FormID、產物與覆蓋率。報告：build/fix-round-audit.json。
- 建置自帶 420 處 Rank/Br 呼叫索引核對，0 out of range。FX 238 筆複製仍在，effect shader／impact／sound／explosion 與 StartDomain、PushSyncStage 等既有 FX 邏輯保留；沒有增加 master。
- 測試工具初跑出現兩個 fixture 缺陷（切片失去 Length、缺 TreeOf 替身），修正後全數重跑通過；稽核曾誤納未出貨歷史 ESSBPlayerAlias.psc，已改核對出貨白名單並確認歷史檔完全未變。產品每輪完整建置均通過。

### 逐項修正表

| 項目 | 狀態 | 檔案 | 函式／位置 | 修改或未修正原因 |
|---|---|---|---|---|
| P0-1 | FIXED | src/ESSBController.psc | BlowBack / PullTo / LiftUp / OnUpdate | 正負 PushActorAway；吹上天先 ragdoll，再單次延遲且檢查 knock state 才補向上衝量，保留冷卻／免疫並緩衝落地狀態。 |
| P0-2 | FIXED | build_v03.py；src/ESSBController.psc | build_esp / validate_delivery；ApplyUtil / ApplyDamageRaw | 敵方 SPEL／MGEF 同改 Contact；玩家同改 Self；另分同伴治療／強化與過熱自傷法術，逐效果回讀。 |
| P1-3 | FIXED | src/ESSBController.psc | Execute | 處決直接套 TrueSpell，不乘 G(L)、破魔倍率或元素抗性。 |
| P1-4＋B 歸屬補充 | FIXED | src/ESSBController.psc；src/ESSBGuard.psc | OnKillEvent / CaptureDeath；OnActorKilled | 八名死者去重；只接受 akKiller 為玩家，Tick 僅保存死亡狀態快照，不推定歸屬。 |
| P1-5 | FIXED | src/ESSBController.psc | ApplyMark | 只有 aiElement == 9 的水印記基礎為 10 秒，其餘 8 秒。 |
| P1-6 | FIXED | src/ESSBController.psc；src/ESSBNoForm.psc | ApplyTrueDamage；OnMartialHit / OnManaBreak | 新增 abGrantXP；直接命中延伸真傷傳 False，反咒／星斷等保留發經驗。 |
| R1 | FIXED | src/ESSBElem3.psc | WaterHitMult | 刪除兩條長流回復主線的附傷加成，保留水壓與真正的開印附傷主線。 |
| R2 | FIXED | src/ESSBController.psc；src/ESSBStatus.psc | FinishMark；ReleaseWetLock / ClearStack | 水印記結束解除 WetLock；非汪洋保留者清除鎖定浸濕，汪洋另保留有限時間浸濕／副印記。 |
| R3 | FIXED | src/ESSBElem3.psc；src/ESSBGuard.psc | OnShadowBody；OnHitEx | 近戰進入 OnShadowBody 先清抵銷視窗，再判定新 proc；箭矢不再關閉該視窗。 |
| R4 | FIXED | src/ESSBController.psc；src/ESSBStatus.psc | SpreadPoison；Tick | 一次掃描取得多名不同目標，每人一層，避免同一最近目標吃兩層。 |
| R5 | FIXED | src/ESSBController.psc | TickDomain | 先結算領域每秒效果，再倒數；五秒領域跑足五次。 |
| R6 | FIXED | src/ESSBController.psc | SetStarLock / SetWetLock / OnStatusStart | 宿主未就緒時緩衝星鎖與汪洋之始，綁定後補交，清槽時取消。 |
| R7 | FIXED | src/ESSBController.psc | ApplyReanimate / ServantCount | 套復生前鎖定交易並預留名額，與已生成僕從去重計數；同幀擊殺不突破永久上限。 |
| A1 | FIXED | src/ESSBElem.psc；src/ESSBStatus.psc | Shatter / EndFrostNodes / ShatterArea；SetFrozen | 冰河使用終焉前冰封快照；先清舊凍結，再對未處決者設獨立 2 秒冰棺。 |
| A2 | FIXED | src/ESSBReactions.psc；src/ESSBElem.psc；src/ESSBController.psc | End / EndShock；EndShockNodes；ConsumeEndCharge | 基礎放電與雷殛共用原電荷快照，分支結算完只消耗一次，不依賴蓄餘。 |
| A3 | FIXED | src/ESSBReactions.psc | End | 先兌現原有 PendingDrain，再執行會建立新 pending 的元素終焉掛勾。 |
| A4 | FIXED | src/ESSBReactions.psc；src/ESSBElem2.psc；src/ESSBStatus.psc | Open；OpenWind；AddAstral | 冰凍結／減速、風勢／拉近、毒層實際套倍率；整數採既有隨機補小數，星痕保存加權傷害。 |
| A5 | FIXED | src/ESSBReactions.psc；src/ESSBStatus.psc | EndPoison / EndWater；SetCatalyze / Tick / ExportFloats / ImportState | 催毒期間保存並套用終焉倍率；水導引含傳入倍率，宿主未就緒亦緩衝。 |
| A6 | FIXED | src/ESSBElem3.psc | Fall / FallHit | 中心與流星雨目標共用結算；星斷融斷皆為 ×0.6 真傷。 |
| A7＝B4 | FIXED | src/ESSBReactions.psc；src/ESSBStatus.psc | EndDark；SetDeathCurse / Tick / ExportFloats / ImportState | 固定項與失血項相加後統一乘保存的終焉／死咒倍率，搬家保留。 |
| A8 | FIXED | src/ESSBReactions.psc | EndBlood | 傳入 abChain；血海子結算禁止再掃描擴散，範圍固定於原中心。 |
| A9 | FIXED | src/ESSBStatus.psc | Tick | 血／毒桶快照與老化在無外部呼叫的一段完成，再執行傷害，途中新增層留新桶。 |
| A10＝B8 | FIXED | src/ESSBReactions.psc | EndBlood | 傳入 aiReason；血斷額外 50% 治療僅在 aiReason == 1。 |
| A11 | FIXED | src/ESSBElem2.psc；src/ESSBStatus.psc；src/ESSBReactions.psc | BleedPerLayer；Tick；EndBlood | 一般流血與剩餘流血總傷共用包含萬象／止血／主線的每層公式。 |
| A12 | FIXED | src/ESSBElem.psc | IgniteMult | 自燃獨立基礎 ×2，業火滿點 ×3.5，不再套爆燃每層公式。 |
| A13 | FIXED | src/ESSBElem2.psc | LandingDamage | 落地倍率為 0.5 + 0.05 × rank；上天只把基礎替換為 1.0。 |
| A14 | FIXED | src/ESSBElem2.psc | WindHitMult | 移除拉近距離主線誤附的命中傷害，PullDistance 成長保留。 |
| A15 | FIXED | src/ESSBElem3.psc；src/ESSBStatus.psc | OpenAstral / DetonateAstral；DetonateAstralNow | 延遲與星耀共用倍率及引爆回饋；星耀只改立即並額外 ×2。 |
| B1 | FIXED | src/ESSBController.psc | InstallMark / ApplyMark / OnMarkStart / OnMarkFinish / FinishMark | 主副印記依元素分配 AME；副元素命中只刷新，第三元素只替換最舊者並保留遞補實體。 |
| B2 | FIXED | src/ESSBController.psc；src/ESSBReactions.psc | OpenSecond / EndBothMarks / EndLinkedMark / FinishMark；ChainEnd | 主副印記與連鎖共用每目標冷卻；連鎖消耗印記，雙印融斷共用一次交易許可。 |
| B3 | FIXED | src/ESSBController.psc | SwapHosts / OnStatusStart / ClearSlot / SaveSwapData | 換宿核對 Actor＋世代，清槽取消；三秒重試、九秒解鎖，逐槽備份仍保留，匯入後再核身分。 |
| B5 | FIXED | src/ESSBController.psc | BloodDrainPerSecond / BloodCostScale | 不死的零扣血判定移到維持費函式，重擊生命成本仍照原曲線。 |
| B6 | FIXED | src/ESSBController.psc | Tick / SyncRockArmor | 電荷／風勢衰減立刻 PushSelf；岩甲增減同步 GRockArmor。 |
| B7 | FIXED | src/ESSBController.psc | TickDomain / ScanDomainTargets | 候選池只掃一次，每領域先檢查成員再按中心距離取五人。 |
| B9 | FIXED | src/ESSBController.psc | PushTrio | 記錄各元素最近終焉時間，排除超過十秒者，重複元素不延長其他元素資格。 |
| B10 | FIXED | src/ESSBController.psc | OnFormClosed / SwitchForm | 永續用獨立 PerpetualKeep，直到下次開形態才消耗；連斷仍維持五秒期限。 |
| B11 | FIXED | src/ESSBTrees.psc | Reconcile / RespecTree / RespecAll | 買點結算與洗點後刷新能力；全樹洗點結束只刷新一次。 |
| advisory：預知 | FIXED | src/ESSBStatus.psc | AddAstral / Tick | 一秒星痕在加層時啟動預知，兩秒星痕保留引爆前一秒提醒。 |
| advisory：處刑 | FIXED | src/ESSBElem3.psc | AfterDeathCurse | 首領處刑只補 ×2，與已造成的 ×1 合計 ×3。 |
| advisory：GuardKill | FIXED | src/ESSBGuard.psc | 檔頭事件說明 | 移除不存在的 GuardKill 旗標說明，改記玩家歸屬、八名去重及 Tick 快照。 |
| P2：CSF 條件／選單事件 | NOT FIXED | src/ESSBTrees.psc；build_v03.py | Reconcile / OnMenuClose；csf_config | CTDA 是否由 CSF 選單評估及 StatsMenu 事件須在 Skyrim／CSF 執行期觀察；本輪未安裝或啟動遊戲。 |
| P2：同幀 magnitude | NOT FIXED | src/ESSBController.psc | ApplyProc / ApplyDamageRaw / ApplyUtil | 共用自有 Spell record 的 magnitude 仍可能被同幀雙擊覆寫；審查明示可接受，未改既有傷害架構。 |
| P2：Unique 免疫範圍 | NOT FIXED | src/ESSBController.psc | IsVIPTarget / CanRagdoll | 仍依 ActorBase.IsUnique() 判首領；本輪明確要求保留免疫名單，未縮窄範圍。 |
| P2：化灰／血承實機 | NOT FIXED | src/ESSBController.psc | ApplyAsh / ApplyInherit | 投遞記錄已修正，但死者化灰及玩家 Self DoCombatSpellApply 的實際引擎效果需進遊戲驗證。 |
| P2：經驗曲線手感 | NOT FIXED | settings.json；src/ESSBTrees.psc；build_v03.py | Award / XPBudget；csf_config | improveMult 0.05、improveOffset 2 與每秒上限保留；缺少遊戲內節奏測試，不能用靜態編譯決定平衡值。 |

### 實作邊界與相容性

PushActorAway 的 force 使用正值吹走、以中心 reference 負值拉近；沿用原冷卻與免疫判定。吹上天先進入 ragdoll，單次更新延遲後確認 knock state 才施加一次向上 ApplyHavokImpulse；未進入 ragdoll 就不施加。未新增 Utility.Wait 迴圈、持續 RegisterForUpdate 或每幀輪詢。推力的引擎實際距離仍需實機量測。

傷害仍走自有法術 DoCombatSpellApply；處決直接走原 TrueSpell，不發 Spell.Cast 傷害事件、不直接扣敵人 Health。玩家過熱另分 Self 法術；同伴回復／強化另分 Contact 法術，避免共用同一 SPEL 的互斥投遞設定。

八槽登記表架構保留；新增逐槽換宿備份與 Actor／世代核對，超時可重試，不把資料備份隨交易解鎖刪除。宿主浮點匯出由 12 欄增至 17 欄，匯入保留舊 12 欄格式預設值；外部 public function 名稱保持，新增參數提供預設或更新全部呼叫端。

### 新增記錄（原記錄不重編）

| 本地 FormID | EDID | 類型 |
|---|---|---|
| 0x005150 | ESSB_UtilTargetEffect_RestoreHealth | MGEF |
| 0x005151 | ESSB_UtilTarget_RestoreHealth | SPEL |
| 0x005152 | ESSB_UtilTargetEffect_RestoreStamina | MGEF |
| 0x005153 | ESSB_UtilTarget_RestoreStamina | SPEL |
| 0x005154 | ESSB_UtilTargetEffect_MeleeBuff | MGEF |
| 0x005155 | ESSB_UtilTarget_MeleeBuff | SPEL |
| 0x005156 | ESSB_OverheatSelfEffect | MGEF |
| 0x005157 | ESSB_OverheatSelfSpell | SPEL |

### P0-2 完整 SPEL／MGEF EDID 清單

以下列出所有已回讀法術，包含原已正確而保留的 Self 法術，避免只列簡稱而漏掉效果。Contact 的 71 支為本輪改為 Contact 或新增 Contact；Self 中 ESSB_Util_*、ESSB_InheritSpell 為投遞修正，ESSB_OverheatSelfSpell 為新增，其餘保留 Self。每列所有 MGEF 與 SPEL 的 delivery 都相同。

| SPEL EDID | delivery（SPIT／DATA） | MGEF EDID |
|---|---|---|
| ESSB_Ability_AntiMagic | Self (0) | ESSB_AbilityEffect_AntiMagic |
| ESSB_Ability_Induction | Self (0) | ESSB_AbilityEffect_Induction |
| ESSB_Ability_Muffle | Self (0) | ESSB_AbilityEffect_Muffle |
| ESSB_Ability_PoisonResist | Self (0) | ESSB_AbilityEffect_PoisonResist |
| ESSB_Ability_WarmBlood | Self (0) | ESSB_AbilityEffect_WarmBlood |
| ESSB_Ability_WindSpeed | Self (0) | ESSB_AbilityEffect_WindSpeed |
| ESSB_CleanseSpell | Self (0) | ESSB_CureDiseaseEffect |
| ESSB_FormAbility_Astral | Self (0) | ESSB_FormAbilityEffect_Astral、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Astral |
| ESSB_FormAbility_Blood | Self (0) | ESSB_FormAbilityEffect_Blood、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Blood |
| ESSB_FormAbility_Darkness | Self (0) | ESSB_FormAbilityEffect_Darkness、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Darkness |
| ESSB_FormAbility_Divine | Self (0) | ESSB_FormAbilityEffect_Divine、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Divine |
| ESSB_FormAbility_Earth | Self (0) | ESSB_FormAbilityEffect_Earth、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Earth |
| ESSB_FormAbility_Fire | Self (0) | ESSB_FormAbilityEffect_Fire、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Fire |
| ESSB_FormAbility_Frost | Self (0) | ESSB_FormAbilityEffect_Frost、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Frost |
| ESSB_FormAbility_Lightning | Self (0) | ESSB_FormAbilityEffect_Lightning、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Lightning |
| ESSB_FormAbility_Poison | Self (0) | ESSB_FormAbilityEffect_Poison、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Poison |
| ESSB_FormAbility_Water | Self (0) | ESSB_FormAbilityEffect_Water、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Water |
| ESSB_FormAbility_Wind | Self (0) | ESSB_FormAbilityEffect_Wind、ESSB_SyncGlowEffect_1、ESSB_SyncGlowEffect_2、ESSB_SyncGlowEffect_3、ESSB_SyncWeaponEffect_Wind |
| ESSB_FormPower_Astral | Self (0) | ESSB_FormPowerEffect_Astral |
| ESSB_FormPower_Blood | Self (0) | ESSB_FormPowerEffect_Blood |
| ESSB_FormPower_Darkness | Self (0) | ESSB_FormPowerEffect_Darkness |
| ESSB_FormPower_Divine | Self (0) | ESSB_FormPowerEffect_Divine |
| ESSB_FormPower_Earth | Self (0) | ESSB_FormPowerEffect_Earth |
| ESSB_FormPower_Fire | Self (0) | ESSB_FormPowerEffect_Fire |
| ESSB_FormPower_Frost | Self (0) | ESSB_FormPowerEffect_Frost |
| ESSB_FormPower_Lightning | Self (0) | ESSB_FormPowerEffect_Lightning |
| ESSB_FormPower_Poison | Self (0) | ESSB_FormPowerEffect_Poison |
| ESSB_FormPower_Water | Self (0) | ESSB_FormPowerEffect_Water |
| ESSB_FormPower_Wind | Self (0) | ESSB_FormPowerEffect_Wind |
| ESSB_FormRulesAbility | Self (0) | ESSB_FormRulesEffect |
| ESSB_InheritSpell | Self (0) | ESSB_InheritEffect_41、ESSB_InheritEffect_43、ESSB_InheritEffect_42、ESSB_InheritEffect_40、ESSB_InheritEffect_44、ESSB_InheritEffect_39、ESSB_InheritEffect_24 |
| ESSB_OverheatSelfSpell | Self (0) | ESSB_OverheatSelfEffect |
| ESSB_PurgeSpell | Self (0) | ESSB_CureDiseaseEffect、ESSB_CurePoisonEffect |
| ESSB_SettingsPower | Self (0) | ESSB_SettingsEffect |
| ESSB_SilentAbility | Self (0) | ESSB_AbilityEffect_Muffle、ESSB_UtilEffect_Haste |
| ESSB_Util_ArmorBuff | Self (0) | ESSB_UtilEffect_ArmorBuff |
| ESSB_Util_DiseaseResistBuff | Self (0) | ESSB_UtilEffect_DiseaseResistBuff |
| ESSB_Util_Haste | Self (0) | ESSB_UtilEffect_Haste |
| ESSB_Util_HealRateBuff | Self (0) | ESSB_UtilEffect_HealRateBuff |
| ESSB_Util_MagicResistBuff | Self (0) | ESSB_UtilEffect_MagicResistBuff |
| ESSB_Util_MagickaRateBuff | Self (0) | ESSB_UtilEffect_MagickaRateBuff |
| ESSB_Util_MeleeBuff | Self (0) | ESSB_UtilEffect_MeleeBuff |
| ESSB_Util_MuffleBuff | Self (0) | ESSB_UtilEffect_MuffleBuff |
| ESSB_Util_PoisonResistBuff | Self (0) | ESSB_UtilEffect_PoisonResistBuff |
| ESSB_Util_RestoreHealth | Self (0) | ESSB_UtilEffect_RestoreHealth |
| ESSB_Util_RestoreMagicka | Self (0) | ESSB_UtilEffect_RestoreMagicka |
| ESSB_Util_RestoreStamina | Self (0) | ESSB_UtilEffect_RestoreStamina |
| ESSB_Util_StaminaRateBuff | Self (0) | ESSB_UtilEffect_StaminaRateBuff |
| ESSB_Util_TempHealth | Self (0) | ESSB_UtilEffect_TempHealth |
| ESSB_AshSpell | Contact (1) | ESSB_AshEffect |
| ESSB_EngagedSpell | Contact (1) | ESSB_EngagedEffect |
| ESSB_FearSpell | Contact (1) | ESSB_FearEffect |
| ESSB_FrenzySpell | Contact (1) | ESSB_FrenzyEffect |
| ESSB_Hit_Astral_Normal | Contact (1) | ESSB_HitEffect_Astral |
| ESSB_Hit_Astral_Power | Contact (1) | ESSB_HitEffect_Astral |
| ESSB_Hit_Blood_Normal | Contact (1) | ESSB_HitEffect_Blood |
| ESSB_Hit_Blood_Power | Contact (1) | ESSB_HitEffect_Blood |
| ESSB_Hit_Darkness_Normal | Contact (1) | ESSB_HitEffect_Darkness |
| ESSB_Hit_Darkness_Power | Contact (1) | ESSB_HitEffect_Darkness_Power |
| ESSB_Hit_Divine_Normal | Contact (1) | ESSB_HitEffect_Divine |
| ESSB_Hit_Divine_Power | Contact (1) | ESSB_HitEffect_Divine |
| ESSB_Hit_Earth_Normal | Contact (1) | ESSB_HitEffect_Earth |
| ESSB_Hit_Earth_Power | Contact (1) | ESSB_HitEffect_Earth |
| ESSB_Hit_Fire_Normal | Contact (1) | ESSB_HitEffect_Fire |
| ESSB_Hit_Fire_Power | Contact (1) | ESSB_HitEffect_Fire_Power |
| ESSB_Hit_Frost_Normal | Contact (1) | ESSB_HitEffect_Frost |
| ESSB_Hit_Frost_Power | Contact (1) | ESSB_HitEffect_Frost_Power |
| ESSB_Hit_Lightning_Normal | Contact (1) | ESSB_HitEffect_Lightning |
| ESSB_Hit_Lightning_Power | Contact (1) | ESSB_HitEffect_Lightning_Power |
| ESSB_Hit_Poison_Normal | Contact (1) | ESSB_HitEffect_Poison |
| ESSB_Hit_Poison_Power | Contact (1) | ESSB_HitEffect_Poison |
| ESSB_Hit_Water_Normal | Contact (1) | ESSB_HitEffect_Water |
| ESSB_Hit_Water_Power | Contact (1) | ESSB_HitEffect_Water |
| ESSB_Hit_Wind_Normal | Contact (1) | ESSB_HitEffect_Wind |
| ESSB_Hit_Wind_Power | Contact (1) | ESSB_HitEffect_Wind |
| ESSB_ManaBreakSpell | Contact (1) | ESSB_ManaBreakEffect |
| ESSB_MarkSpell_Astral | Contact (1) | ESSB_MarkEffect_Astral |
| ESSB_MarkSpell_Blood | Contact (1) | ESSB_MarkEffect_Blood |
| ESSB_MarkSpell_Darkness | Contact (1) | ESSB_MarkEffect_Darkness |
| ESSB_MarkSpell_Divine | Contact (1) | ESSB_MarkEffect_Divine |
| ESSB_MarkSpell_Earth | Contact (1) | ESSB_MarkEffect_Earth |
| ESSB_MarkSpell_Fire | Contact (1) | ESSB_MarkEffect_Fire |
| ESSB_MarkSpell_Frost | Contact (1) | ESSB_MarkEffect_Frost |
| ESSB_MarkSpell_Lightning | Contact (1) | ESSB_MarkEffect_Lightning |
| ESSB_MarkSpell_Poison | Contact (1) | ESSB_MarkEffect_Poison |
| ESSB_MarkSpell_Water | Contact (1) | ESSB_MarkEffect_Water |
| ESSB_MarkSpell_Wind | Contact (1) | ESSB_MarkEffect_Wind |
| ESSB_React_Astral | Contact (1) | ESSB_ReactEffect_Astral |
| ESSB_React_Blood | Contact (1) | ESSB_ReactEffect_Blood |
| ESSB_React_Darkness | Contact (1) | ESSB_ReactEffect_Darkness |
| ESSB_React_Divine | Contact (1) | ESSB_ReactEffect_Divine |
| ESSB_React_Earth | Contact (1) | ESSB_ReactEffect_Earth |
| ESSB_React_Fire | Contact (1) | ESSB_ReactEffect_Fire |
| ESSB_React_Frost | Contact (1) | ESSB_ReactEffect_Frost |
| ESSB_React_Lightning | Contact (1) | ESSB_ReactEffect_Lightning |
| ESSB_React_Poison | Contact (1) | ESSB_ReactEffect_Poison |
| ESSB_React_Water | Contact (1) | ESSB_ReactEffect_Water |
| ESSB_React_Wind | Contact (1) | ESSB_ReactEffect_Wind |
| ESSB_ReanimateSpell | Contact (1) | ESSB_ReanimateEffect |
| ESSB_SilenceSpell | Contact (1) | ESSB_SilenceEffect、ESSB_UtilEffect_MagickaRateDrain |
| ESSB_StatusHostSpell | Contact (1) | ESSB_StatusHostEffect |
| ESSB_StripSpell | Contact (1) | ESSB_StripEffect |
| ESSB_TrueDamageSpell | Contact (1) | ESSB_TrueEffect |
| ESSB_UtilTarget_MeleeBuff | Contact (1) | ESSB_UtilTargetEffect_MeleeBuff |
| ESSB_UtilTarget_RestoreHealth | Contact (1) | ESSB_UtilTargetEffect_RestoreHealth |
| ESSB_UtilTarget_RestoreStamina | Contact (1) | ESSB_UtilTargetEffect_RestoreStamina |
| ESSB_Util_ArmorDebuff | Contact (1) | ESSB_UtilEffect_ArmorDebuff |
| ESSB_Util_BleedTick | Contact (1) | ESSB_UtilEffect_BleedTick |
| ESSB_Util_DrainMagicka | Contact (1) | ESSB_UtilEffect_DrainMagicka |
| ESSB_Util_DrainStamina | Contact (1) | ESSB_UtilEffect_DrainStamina |
| ESSB_Util_FireResistDebuff | Contact (1) | ESSB_UtilEffect_FireResistDebuff |
| ESSB_Util_FrostResistDebuff | Contact (1) | ESSB_UtilEffect_FrostResistDebuff |
| ESSB_Util_HealRateDebuff | Contact (1) | ESSB_UtilEffect_HealRateDebuff |
| ESSB_Util_MagicResistDebuff | Contact (1) | ESSB_UtilEffect_MagicResistDebuff |
| ESSB_Util_MagickaRateDrain | Contact (1) | ESSB_UtilEffect_MagickaRateDrain |
| ESSB_Util_MeleeDebuff | Contact (1) | ESSB_UtilEffect_MeleeDebuff |
| ESSB_Util_PoisonResistDebuff | Contact (1) | ESSB_UtilEffect_PoisonResistDebuff |
| ESSB_Util_ShockResistDebuff | Contact (1) | ESSB_UtilEffect_ShockResistDebuff |
| ESSB_Util_Slow | Contact (1) | ESSB_UtilEffect_Slow |
| ESSB_Util_StaminaRateDebuff | Contact (1) | ESSB_UtilEffect_StaminaRateDebuff |

產物位於 package/Elements Spellblade/。本輪未安裝至 MO2、未啟動遊戲；P2 表列的五項界限保留。進度訊號位於 .codex/impl-fix-round.html。

## 2026-09-17 22:21 Asia/Taipei — fix round 驗收（指揮官）

- 結果：通過。重跑 `python build_v03.py` exit 0；masters `['Skyrim.esm']`、3878／3878 筆、17 支腳本 0 errors、CSF 13/13、coverage 0 unmapped。
- FormID：以 `.codex/pre-fix-snapshot/v03-formids.json` 逐筆比對，原 3870 筆 0 變動、0 消失，新增 8 筆（0x005150–0x005157）；`ESSB_DebugLevel` 仍為 0x000811。
- 獨立讀回（不經產生器自帶檢查）：SPEL delivery 為 Contact 71／Self 49，Target Actor 0；SPEL 與所屬 MGEF 的 casting／delivery 全數一致。
- 禁用寫法掃描：出貨腳本無 `Utility.Wait`、無持續性 `RegisterForUpdate`、無 `Spell.Cast`；`ApplyHavokImpulse` 只剩延遲浮空一處，且先以 `GetActorKnockState`（已確認存在於本機安裝的 PO3 DLL）確認目標在 ragdoll 狀態。
- 範圍：MO2／SkyrimSE Data 未被觸碰；寫入皆在允許清單內。
- 驗收時另修一筆舊缺陷（非本輪引入）：`ESSB_SilentAbility`（Constant Effect 能力）第二個效果原接 Fire and Forget 的 `ESSB_UtilEffect_Haste`，施放類型不符會被引擎略過；改接同為 Constant Effect 的 `ESSB_AbilityEffect_WindSpeed`（AV 30，+20）。未新增記錄。
- 備註：ESSBElem2／ESSBElem3／ESSBGuard 的行尾由 LF 變為 CRLF，實質差異分別為 22／55／10 行；編譯不受影響。


## 2026-09-17 23:20 Asia/Taipei — fix round 2（首次實機回饋）

本輪依 `.codex/smoke1-Papyrus.0.log` 完整事件區塊盤點：僅納入 stack 觸及 ESSB 腳本的 Error，共 **78 次、9 條錯誤路徑**。`Int[]`／`Float[]` 各 15 次，合計 30 次（與任務描述的 12 次不同，以實際 log 為準）；沒有其他 ESSB Error／Warning 類別。其他模組不處理。

以下 FIXED 表示來源碼修復及離線驗證完成；完整編譯與實機結果尚未完成，不能視為整輪驗收通過。

| 狀態 | 項目／錯誤路徑 | 次數／結果 | 原因與修正／未完成原因 |
|---|---|---:|---|
| FIXED | CancelSwap：Cannot cast from None to Int[] | 15 | 未使用的舊陣列被指定 None；移除該指派，保留每目標備份。 |
| FIXED | CancelSwap：Cannot cast from None to Float[] | 15 | 同上；實際狀態仍由 BackupInts／BackupFloats 分槽保存。 |
| FIXED | ESSBStatus.OnEffectFinish → UnregisterForUpdate | 25 | AME native 已解除綁定；先設 Finished，不再在 finish 回呼呼叫 native unregister。 |
| FIXED | ESSBStatus.OnUpdate → RegisterForSingleUpdate | 7 | Tick 期間效果可能結束；入口與 Tick 返回後檢查 Finished／Migrating。 |
| FIXED | ESSBMark.OnEffectFinish → OnMarkFinish → EndMark → FinishMark → Dispel | 7 | 結束回呼再次消除自身；傳遞 akFinishing，略過該 AME 的 self-dispel，保留 End／XP／FX。 |
| FIXED | ESSBStatus.OnEffectStart → RegisterForSingleUpdate | 5 | 控制器回呼期間效果可能結束／換宿；回呼返回後重新檢查旗標。 |
| FIXED | ESSBMark.OnEffectStart → OnMarkStart → Dispel | 2 | 遲到的起始回呼拒收已結束印記；改呼叫 DispelIfActive。 |
| FIXED | ESSBStatus.OnEffectStart → OnStatusStart → PrepareSwap → UnregisterForUpdate | 1 | 拒收已結束宿主仍取消 native 排程；PrepareSwap 僅設 Migrating，殘留單次 tick 直接退出。 |
| FIXED | ESSBStatus.OnEffectStart → OnStatusStart → Dispel | 1 | 拒收已結束宿主仍消除 native；所有 status 消除路徑改用 DispelIfActive。 |
| FIXED | 13 棵 CSF 技能樹、版面斷言 | 13/13 | x=-4.275..4.275、y=0..4、最小間距 0.95；三路並排，同階分支橫排，第三分支保留額外欄。links／節點／perk 僅座標外全數不變。 |
| FIXED | UTF-8／原行尾、設定按鈕標籤保留 | 3 scripts | ESSBMark 保持 LF；ESSBStatus／ESSBController 保持 CRLF；build_v03.py 的短按鈕標籤未改動。 |
| NOT FIXED | 完整 python build_v03.py 驗收、17 支 PEX 重編 | 未執行 | 既有建置需唯讀 Skyrim.esm、MO2 來源插件及執行 MO2 PapyrusCompiler.exe，超出本輪 read scope；campaign root 無編譯器，未越權存取。已請求此必要例外。 |
| NOT FIXED | 修正後實機重測 | 未執行 | 遊戲仍在執行；未安裝至 MO2，未操作遊戲。不能宣稱新 log 已零錯誤。 |

驗證證據：

- `python build/fix2_regression.py`：11/11 通過；實際 Papyrus function bodies 搭配 native mocks，涵蓋控制器／Tick 期間結束與換宿、遲到 callback、主／副印記自然終焉、主動終焉與 8 槽備份保留；並以故意破壞 bounds／spacing／links／階序確認版面斷言會拒絕。不是 Skyrim runtime。
- `python build/fix2_prior_regression.py`：前輪 23/23 情境通過；只將舊假 AME 的 `Dispel` 介面配合改成 `DispelIfActive`，測試條件不變。
- 範圍內獨立執行 `write_csf`：`LAYOUT ok: trees=13/13 x=[-4.5,4.5] y=[0,4.5] min_spacing=0.95 >= 0.9; tier order + route separation + mainline/branch links valid`；CSF 13 個皆通過原有參照與 JSON 檢查。
- 目前 `build/v03-formids.json` 對 `.codex/pre-fix2-snapshot/v03-formids.json`：3878 筆完全相同，`ESSB_DebugLevel=0x000811`。這是完整重建前的比對；重建後仍須通過新增的 snapshot 斷言。
- 沒有新增 master／record，也沒有新增 `Utility.Wait`、持續性 `RegisterForUpdate`、`Spell.Cast` 或敵人 health `DamageActorValue`。
- 全部操作限 campaign root；沒有存取網路、MO2 或 SkyrimSE，未動規劃、review、`fx_extract.py`、`.strategic-advance/**`。三支 src 的 UTF-8 與原行尾以位元組檢查保留。

產物／狀態：`src/ESSBController.psc`、`src/ESSBMark.psc`、`src/ESSBStatus.psc`、`build_v03.py` 已修改，13 份 package CSF JSON 已更新；**package 的 PEX 尚未重編，因此目前不是本輪可安裝成品**。待前述唯讀依賴授權後執行完整建置，補上 READBACK／DELIVERY／PLAN COVERAGE／17 scripts 結果。進度及 13 棵座標預覽在 `.codex/impl-fix-round2.html`；錯誤清單、回歸結果分別見 `build/fix2-log-errors.json`、`build/fix2-regression.json`、`build/fix2-prior-regression.json`。

## 2026-09-17 23:22 Asia/Taipei — fix round 2 驗收（指揮官）

- Astra 依授權邊界未執行建置（建置需讀 `Skyrim.esm` 與 MO2 內的編譯器，超出當輪 read scope）；由指揮官重建：exit 0、17 支 0 errors、`LAYOUT ok`（13/13，x ±4.5、y 0–4.5、最小間距 0.95）、READBACK／CSF／DELIVERY／COVERAGE 全數通過。
- FormID：與 `.codex/pre-fix2-snapshot` 比對 3878 筆 0 變動。行尾維持原樣。
- 指揮官另補兩筆（首次實機回饋）：設定選單八個按鈕標籤縮短（原標籤橫排超出畫面）；`ESSB_ShowMenu` 預設值 1→0，並在 `ESSBTrees.OnPlayerLoadGame／Setup` 強制歸 0（CSF 把非 0 視為開樹請求，造成一進遊戲就跳技能樹；舊存檔也會被修正）。
- 實機發現（與模組內容無關）：指揮官安裝時手改 `plugins.txt` 的那一場，SKSE 讀取翻譯檔 0 筆（MCM 顯示 $ 字串）；MO2 重寫該檔後恢復 62 筆。此後不再手改 `plugins.txt／loadorder.txt`，啟用交給使用者在 MO2 內勾選。

## fix round 3（特效回饋）

本輪已自行執行 `python build_v03.py`，exit 0；產物在 `package/Elements Spellblade/`。依首次實機回饋將 11 個形態光環改回 Phenderix 原始 form shader；印記與命中特效拆開。建置、EFSH 數值及腳本生命週期檢查已通過，尚未宣稱遊戲內觀感已驗收。

### 形態光環逐元素對照

下表是本插件內 EDID；每筆「後」的來源 selector 都是 `Phenderix Elements.esp|ZZShader_<Element>Form`。11 筆原本已齊全，沒有補匯入或改寫其 EFSH DATA。退下來的借用光環仍保留原 FormID，實際未綁定者列在 `build/fx-bindings.json` 的 `unbound_spares`（依 MGEF 實際引用計算）。

| 元素 | Aura 前 | Aura 後 |
|---|---|---|
| 火焰 `Fire` | `ESSBFX_DAR_MoltenFXShader` | `ESSBFX_ZZShader_FireForm` |
| 冰霜 `Frost` | `ESSBFX__IP_FrostIceFormFXShader02` | `ESSBFX_ZZShader_FrostForm` |
| 雷電 `Lightning` | `ESSBFX_DAR_ArcFXShader` | `ESSBFX_ZZShader_LightningForm` |
| 大地 `Earth` | `ESSBFX_NAT_MagicArmorStoneFleshFXS` | `ESSBFX_ZZShader_EarthForm` |
| 風 `Wind` | `ESSBFX__AV_EffectShader` | `ESSBFX_ZZShader_WindForm` |
| 鮮血 `Blood` | `ESSBFX_DAR_RedFXS` | `ESSBFX_ZZShader_BloodForm` |
| 神聖 `Divine` | `ESSBFX_INQ_HealingLightFXS` | `ESSBFX_ZZShader_DivineForm` |
| 毒素 `Poison` | `ESSBFX__VENOM_PoisonCloakFXShader` | `ESSBFX_ZZShader_PoisonForm` |
| 水 `Water` | `ESSBFX_ZZShader_WaterForm` | `ESSBFX_ZZShader_WaterForm` |
| 黑暗 `Darkness` | `ESSBFX_ABY_ShadowFXS` | `ESSBFX_ZZShader_DarknessForm` |
| 星界 `Astral` | `ESSBFX_DAR_AstralFXShader` | `ESSBFX_ZZShader_AstralForm` |

### 印記：資料解析、選擇與可讀性

依 `vendor/wbDefinitionsTES5.pas:7043–7182` 逐欄解析 66 個已複製 EFSH。DATA 位移：fill full time=24、fill persistent alpha=32、edge RGB=56、edge full time=64、edge persistent alpha=72、fill/edge full alpha=84/88、粒子 full time=116、持續粒子數=128、Ambient Sound=308、Flags=384。schema 回歸測試獨立從 Pascal 欄位順序算出相同位移。312-byte 舊記錄沒有 offset 384 的 Flags，報告為 null，不能擅自當成 0；這些記錄只作色彩來源，不作持續邊緣的證據。

火、血、暗、星的舊命中霧效 flags 含 `0x1 No Membrane Shader`；風亦停用膜層。它們可有粒子持續，但無法提供可靠的目標輪廓。神聖舊命中效果 persistent fill/edge 均為 0；大地 persistent edge=0。冰霜的舊效果持續 fill=0.75，外觀偏全身覆蓋；水的舊效果 fill=1 且 RGB 偏暗。雷原效果已具合格持續邊緣（fill=0.01、edge=0.75），故用它作所有新印記的幾何／混合模板，保留原件不動。

新增 11 個 `Elements Spellblade.esp|ESSBFX_Mark_<Element>`：模板選 `StormCalling.esl|_SC_ShockStormFXShader`，元素 RGB 則逐一取下表的實際來源，再等比例放大至最大通道 255，保持色相比較容易辨認。這是明確新增的本模組衍生 shader，並非宣稱來源原件本來就有這些數值。所有印記固定 fill persistent/full alpha=0，edge persistent/full alpha=1；初始 full-alpha time=0.05 秒，之後 persistent alpha 仍為 1；fade-in/out=0、pulse=0，沒有週期性淡出；edge falloff=1。

新記錄 flags=`0x00000408`（No Particle Shader + No Weapons），不設 No Membrane、Affect Skin Only 或 Edge Inverse；持續粒子數=0、Addon Models=0、Ambient Sound=0。印記仍由既有 MGEF `FX Persist` 綁住 8 秒，Water 沿用控制器原本的 10 秒設定；自然到期、切換、終焉與 dispel 的遊戲流程完全不變。不新增 ARTO，原本開印閃現與 Charge 音效保留。

數值欄格式：**Fp/Ep；Ft/Et；Ffull/Efull**，分別是 fill/edge 的持續 alpha、full-alpha 秒數、full-alpha ratio。Flags 是 EFSH DATA 的完整 flags，含粒子旗標；新記錄一律 `0x408`，舊表 `0x10000`/`0x20000` 分別表示粒子 grayscale color/alpha，`0x8` 表示無粒子。完整粒子時間、數量、壽命、旗標與來源在 `build/fx-visibility.json`。

| 元素 | Mark 前（本插件 EDID） | 前：Fp/Ep；Ft/Et；Ffull/Efull | 前 edge RGB／flags | Mark 後（本插件 EDID） | 後：Fp/Ep；Ft/Et；Ffull/Efull | 後 edge RGB／flags |
|---|---|---|---|---|---|---|
| 火焰 `Fire` | `ESSBFX_DAR_MoltenSpellLavaMistShader` | 0 / 0; 0.5 / 0.5; 0 / 0 | 110,220,50 / `0x00010001` | `ESSBFX_Mark_Fire` | 0 / 1; 0.05 / 0.05; 0 / 1 | 255,114,19 / `0x00000408` |
| 冰霜 `Frost` | `ESSBFX__IP_FrostFXShader` | 0.75 / 0.8; 0.075 / 0.075; 0.5 / 0.5 | 255,255,255 / `0x00000042` | `ESSBFX_Mark_Frost` | 0 / 1; 0.05 / 0.05; 0 / 1 | 0,191,255 / `0x00000408` |
| 雷電 `Lightning` | `ESSBFX__SC_ShockStormFXShader` | 0.01 / 0.75; 0.05 / 0.05; 0.1 / 1.5 | 102,114,193 / `0x00000008` | `ESSBFX_Mark_Lightning` | 0 / 1; 0.05 / 0.05; 0 / 1 | 135,151,255 / `0x00000408` |
| 大地 `Earth` | `ESSBFX_ZZShader_Earth` | 1 / 0; 0.1 / 1; 1 / 0 | 131,90,61 / `0x0001808E` | `ESSBFX_Mark_Earth` | 0 / 1; 0.05 / 0.05; 0 / 1 | 255,175,119 / `0x00000408` |
| 風 `Wind` | `ESSBFX_NAT_WindHitFX` | 0.25 / 0.75; 0.05 / 0.05; 0.25 / 1.5 | 150,255,0 / `0x00030001` | `ESSBFX_Mark_Wind` | 0 / 1; 0.05 / 0.05; 0 / 1 | 145,255,208 / `0x00000408` |
| 鮮血 `Blood` | `ESSBFX_BLO_BloodDamageImpactMist` | 0 / 0; 0.5 / 0.5; 0 / 0 | 110,0,220 / `0x00010001` | `ESSBFX_Mark_Blood` | 0 / 1; 0.05 / 0.05; 0 / 1 | 255,51,51 / `0x00000408` |
| 神聖 `Divine` | `ESSBFX__LIP_ConsecrateDeadFXSA` | 0 / 0; 2.4 / 0.5; 1 / 1 | 255,255,255 / `0x00000142` | `ESSBFX_Mark_Divine` | 0 / 1; 0.05 / 0.05; 0 / 1 | 255,228,168 / `0x00000408` |
| 毒素 `Poison` | `ESSBFX__VENOM_PoisonMistFXShader` | 0 / 0; 0.5 / 0; 1 / 1 | 255,255,255 / `0x00020412` | `ESSBFX_Mark_Poison` | 0 / 1; 0.05 / 0.05; 0 / 1 | 180,255,69 / `0x00000408` |
| 水 `Water` | `ESSBFX_ZZWaterloggedShader1` | 1 / 1; 1 / 1; 1 / 1 | 5,26,58 / `0x00000000` | `ESSBFX_Mark_Water` | 0 / 1; 0.05 / 0.05; 0 / 1 | 137,220,255 / `0x00000408` |
| 黑暗 `Darkness` | `ESSBFX_ABY_ShadowDamageImpactMist` | 0 / 0; 0.5 / 0.5; 0 / 0 | 110,0,220 / `0x00010001` | `ESSBFX_Mark_Darkness` | 0 / 1; 0.05 / 0.05; 0 / 1 | 131,49,255 / `0x00000408` |
| 星界 `Astral` | `ESSBFX_DAR_AstralSpellStarMistShader` | 0 / 0; 0.5 / 0.5; 0 / 0 | 110,220,50 / `0x00010001` | `ESSBFX_Mark_Astral` | 0 / 1; 0.05 / 0.05; 0 / 1 | 254,213,255 / `0x00000408` |

| 元素 | 新印記色彩取自（selector） | 原 RGB → 新 RGB | 新 FormID |
|---|---|---|---|
| 火焰 `Fire` | `Phenderix Elements.esp&#124;ZZShader_Fire` | 255,114,19 → 255,114,19 | `0x0030EE` |
| 冰霜 `Frost` | `Atromancy.esp&#124;_ATRO_FrostBuffFXShader2` | 0,150,200 → 0,191,255 | `0x0030EF` |
| 雷電 `Lightning` | `StormCalling.esl&#124;_SC_ShockStormFXShader` | 102,114,193 → 135,151,255 | `0x0030F0` |
| 大地 `Earth` | `Phenderix Elements.esp&#124;ZZShader_Earth` | 131,90,61 → 255,175,119 | `0x0030F1` |
| 風 `Wind` | `Aero.esl&#124;_AV_EffectShader` | 132,232,189 → 145,255,208 | `0x0030F2` |
| 鮮血 `Blood` | `Natura.esp&#124;DAR_RedFXS` | 200,40,40 → 255,51,51 | `0x0030F3` |
| 神聖 `Divine` | `Phenderix Elements.esp&#124;ZZShader_Divine` | 242,216,159 → 255,228,168 | `0x0030F4` |
| 毒素 `Poison` | `Venomancy.esp&#124;_VENOM_PoisonCloakFXShader` | 142,201,54 → 180,255,69 | `0x0030F5` |
| 水 `Water` | `Phenderix Elements.esp&#124;ZZShader_WaterForm` | 62,99,115 → 137,220,255 | `0x0030F6` |
| 黑暗 `Darkness` | `Phenderix Elements.esp&#124;ZZShader_DarknessForm` | 54,20,105 → 131,49,255 | `0x0030F7` |
| 星界 `Astral` | `Phenderix Elements.esp&#124;ZZShader_Astral` | 254,213,255 → 254,213,255 | `0x0030F8` |

### 冰封、破魔印、沉默

| 狀態／宿主 | 前 | 後 | 持續性與清除 |
|---|---|---|---|
| 冰封／`ESSB_StatusHostEffect` + `ESSBStatus` | 只有凍結量表及減速，沒有專屬 shader，無 EFSH 數值可檢查 | `ESSBFX_Status_Frozen` `0x0030FA`；Fp/Ep=0.18/1、Ft/Et=0.05/0.05、Ffull/Efull=0.18/1；RGB=255,255,255；flags=0x408；ambient=0 | `Freeze >= 5` 開啟淡白膜＋白邊；與僅青藍邊的 Frost 印記區分。AddStack／SetStack／SetFrozen／ClearStack／ImportState 與既有 Tick 同步；不新增 timer。到期、清除、死亡、Dispel、OnEffectFinish、換宿都 Stop；舊宿主延遲結束不會 Stop 新宿主的 shader。 |
| 破魔印／`ESSB_ManaBreakEffect` | `ESSBFX__BL_EffectShaderWhiteShockGlow`；5 / 0.45; 0.3 / 0.6; 5 / 1；RGB=242,253,255；flags=0x00000000。原件不是零 alpha，但 fill=5 明顯高於 edge=0.45，改成較易讀輪廓 | `ESSBFX_Status_White` `0x0030F9`；0 / 1; 0.05 / 0.05; 0 / 1；RGB=242,253,255；flags=0x00000408；ambient=0 | 保留原 MGEF FX Persist 與法術時長；真實傷害仍用原白光，不連帶修改。 |
| 沉默／`ESSB_SilenceEffect` | `ESSBFX__BL_EffectShaderWhiteShockGlow`；5 / 0.45; 0.3 / 0.6; 5 / 1；RGB=242,253,255；flags=0x00000000。原件不是零 alpha，但 fill=5 明顯高於 edge=0.45，改成較易讀輪廓 | `ESSBFX_Status_White` `0x0030F9`；0 / 1; 0.05 / 0.05; 0 / 1；RGB=242,253,255；flags=0x00000408；ambient=0 | 保留原 MGEF FX Persist 與法術時長；真實傷害仍用原白光，不連帶修改。 |

冰封使用一個 `EffectShader` property 與 Play/Stop 狀態轉換，沒有 `Utility.Wait`、新增更新迴圈、`Spell.Cast` 或敵人 health `DamageActorValue`。原有 AME 結束／換宿防護保留。既有存檔中已存活的舊宿主不保證即時取得新增 property；新宿主或既有 30 秒換宿後會使用新綁定，這部分仍需實機確認。

### 如何換 shader 與重建

`settings.json` 的 `fx_aura` 與 `fx_mark` 都是英文元素名 → `Plugin|EDID` 的 map；省略 map 或其中某個元素會使用預設。插件名指來源 plugin，EDID 用來源原名；本模組新增 shader 則使用 `Elements Spellblade.esp|ESSBFX_Mark_<Element>`。

例如只將雷電印記改用原來源 shader，修改既有 map 中這一項（其餘項目保留）：

```json
"fx_mark": {
  "Lightning": "StormCalling.esl|_SC_ShockStormFXShader"
}
```

形態例：`"Frost": "Phenderix Elements.esp|ZZShader_FrostForm"` 放在 `fx_aura`。候選來源可查 `build/fx-catalog.json`；已匯出 shader 的 persistence 數值查 `build/fx-visibility.json`。支持 `fx_extract.SOURCES` 清單內的來源插件，不接受缺失來源或任意裸 EDID。

在 `D:\Game\Other\SKSE\.codex\elemental-spellblade` 執行：

```powershell
python build_v03.py
```

建置會拒絕未知元素、非 map／非字串、缺失或模糊選擇器、非 EFSH 記錄；`fx_mark` 另外要求 DATA Flags 明確、膜層啟用、非 skin-only／inverse edge、edge persistent 減 pulse 至少 0.35、full edge alpha > 0、RGB 最大通道至少 80。這是保守資料門檻，不是遊戲畫面保證。未通過的霧效不能直接當 `fx_mark`；需要時可繼續作命中特效。自訂選擇器直接綁定來源複製件，**不會自動正規化其顏色或 alpha**；只有預設 `ESSBFX_Mark_*` 是本輪衍生版。

成功後檢查 `build/fx-bindings.json` 的 `aura_source`、`mark_source`、`mark_shader`、`mark_visibility`。新來源由既有 `fx_extract.export` 路徑匯入，ID 固定沿用 snapshot／既有 manifest，新增一律超過目前最高 FX ID；已匯入但改選後不用的 FX 仍保留。不要刪除 `build/v03-formids.json` 與既有快照，否則未來新增的自訂 FX 無法保留其歷史配置。重建只產生 package，不自動部署到 MO2 或遊戲；退出遊戲後由使用者更新原 MO2 模組。

### 完整驗收與保留邊界

- `python build_v03.py`：exit 0，完整輸出 `build/fix3-build.log`。17 scripts 均 `0 error(s), 0 warning(s)`；總記錄 3891，READBACK records == manifest。
- masters 僅 `['Skyrim.esm']`；來源 closure 中的 Dawnguard／Dragonborn 參照仍沿用原本 drop/null 處理，並未新增 masters。
- CSF 13/13；LAYOUT 13/13、最小間距 0.95；DELIVERY ok；PLAN COVERAGE 541 rows、0 unmapped；FX ok（原 238 + 衍生 13 = 251；EFSH 79）。
- `.codex/pre-fix3-snapshot/v03-formids.json` 的 3878 筆全部保留，`ESSB_DebugLevel=0x000811`；只追加 `0x0030EE–0x0030FA` 共 13 筆 EFSH。`build/fix3-readback.json` 記錄完整比對。
- `build/fix3_regression.py` 7/7：獨立 EFSH offset、ESP 位元組保留、冰封門檻／時長／換宿／死亡／消除／結束、自訂 FX 追加與錯誤設定拒絕；前輪 `fix2_regression.py` 11/11、`fix2_prior_regression.py` 23/23 亦通過。Papyrus 行為測試是實際函式內容搭配 native mocks，並非 Skyrim runtime。
- 3878 個既有記錄中只有 24 筆 payload 有改變：10 aura MGEF（水原本即 Phenderix）、11 mark MGEF、2 白光狀態 MGEF、1 status host VMAD。238 個既有 FX、全部武器光／命中／反應效果、其餘遊戲資料位元組相同。
- settings、build_v03.py 與 ESSBStatus.psc 保留原 UTF-8／行尾；本文件以原檔位元組為前綴僅追加。`fx_extract.py`、規劃、review、`.strategic-advance/**` 未改。未複製 nif/dds/wav、未使用網路、未寫 MO2／SkyrimSE。
- 原有唯一 PLAN COVERAGE DEFERRED「穩步」CK entry-point 實機確認保持原狀，與本輪特效變更無關。

| 狀態 | 項目 | 結果 |
|---|---|---|
| FIXED | 11 元素形態光環 | 全部改回 Phenderix 原始 form shader；`fx_aura` 可設定；借用 FX 保留。 |
| FIXED | 11 元素印記 | 獨立持續元素色邊緣；`fx_mark` 可設定；8 秒／水 10 秒及命中特效保留。 |
| FIXED | 冰封、破魔印、沉默 | 冰封依狀態啟停；破魔印／沉默改持續白邊；數值與生命週期檢查通過。 |
| FIXED | FormID／前兩輪成果 | 舊 3878 筆零變動；追加 13 筆；原 FX 與武器／命中不變；前輪回歸全通過。 |
| FIXED | 完整建置與文件 | exit 0；17 scripts 0 errors；全部既有檢查 ok；逐元素 EFSH 表、設定方法、進度紀錄已完成。 |
| NOT FIXED | 修正後遊戲內視覺驗收 | 尚未實機重測；不同光線／ENB、雙印記與其他 shader 同時顯示、舊存檔宿主更新仍待確認。未安裝至 MO2。 |

## 2026-09-17 23:40 Asia/Taipei — fix round 3 驗收（指揮官）

- 通過。指揮官重建 exit 0；3891 筆 == manifest；與 `.codex/pre-fix3-snapshot` 比對舊 3878 筆 0 變動，新增 13 筆 EFSH（0x0030EE–0x0030FA：11 個 `ESSBFX_Mark_<元素>`、`ESSBFX_Status_White`、`ESSBFX_Status_Frozen`，皆為自製邊緣光，不依賴外部資源）；11 個形態光環改為 Phenderix `ZZShader_<元素>Form`；`settings.json` 新增 `fx_aura`／`fx_mark`。腳本僅 ESSBStatus 變動 35 行。
- 使用者追加決定（第四輪執行）：B 表上調（雷 1–25、火 10–12、水 5–7、風與毒 8–9、其餘 8–10）、毒層 1 級每層每秒 2.0、流血 1.2、全域附傷基礎倍率；設定介面改走 MCM（第五輪），能力選單只留開樹。


## fix round 4（平衡旋鈕）

本輪依使用者核准調整 design 2.1 的 B 範圍與 design 2.7 的 k_dot，屬明示設計偏離；`元素魔戰士規劃-v0.3.md` 完全不修改。保留前三輪 log guard、ShowMenu=0、技能樹 layout、FX aura/mark 設定。本輪僅產出本地套件，未寫入 MO2 或 SkyrimSE，未連線。

### FIXED / NOT FIXED

| 狀態 | 項目 | 驗證／界線 |
|---|---|---|
| FIXED | 1：毒／流血係數單一來源 | float GLOB、VMAD 屬性、逐秒與血潮剩餘量共用來源；毒 6 秒、流血 5 秒不變。 |
| FIXED | 1b：11 元素 B 表上調 | `settings.json.element_damage` → QUST 的 11 格 Float[] → BaseMin／BaseMax；所有消費者共用，雷 1–25 明確保留。 |
| FIXED | 2：BaseDamageMult | 普通命中、元素反應／DoT／領域／節點及 B-derived 星斷真傷各一次；無元素樹真傷排除；未新增 MessageBox 選項。 |
| FIXED | 3：初始化缺陷 | 65 個陣列各自 guard；OnInit／OnPlayerLoadGame 僅 RegisterForSingleUpdate；首次 OnUpdate 先 Setup，再進入正常排程。舊碼的部分初始化失敗可離線重現，新碼修復並保留既有陣列。 |
| FIXED | 4：建置、FormID、證據與紀錄 | `python build_v03.py` exit 0；17 scripts 0 errors；新舊身分、VMAD 回讀及 9 組離線回歸通過。 |
| NOT FIXED | 遊戲內重新開局／讀檔驗收 | 本輪沒有啟動 Skyrim；不宣稱原始 8 次 None-array log 已在遊戲內消失。雙重 OnInit／引擎 reset 的當次事件順序未由現有 log 證明。 |
| NOT FIXED | 既有 coverage 延後項 | `ESSB_P_noform_0_0_B1` 穩步的引擎進入點實測仍為原先 DEFERRED；本輪沒有擴大範圍修改。 |

### 設定與常數替換清單

- `ESSB_PoisonDotK` = `0x005158`；settings `poison_dot_k` = `0.21164021164021163` = 2 / (9 × 1.05)。
- `ESSB_BleedDotK` = `0x005159`；settings `bleed_dot_k` = `0.11428571428571428` = 1.2 / (10 × 1.05)。
- `ESSB_BaseDamageMult` = `0x00515A`；settings `base_damage_mult` = `1.0`。
- 以上全為 FNAM `f`，接在舊最高 `0x005157` 後。舊 3891 筆的整份 manifest record 身分均相同，`ESSB_DebugLevel` 維持 `0x000811`。
- `ESSBStatus.Tick` 的毒公式字面量 `5.0 * 0.05` → `ESSBReactions.BaseMax(Ctl, 8) * Ctl.PoisonDotK.GetValue()`。
- `ESSBElem2.BleedPerLayer` 的 `0.08` → `akCtl.BleedDotK.GetValue()`；`ESSBStatus.Tick` 與 `ESSBReactions.EndBlood` 都沿用這個函式，後者的舊 `0.08` 註解亦同步改為 `BleedDotK`。
- `ESSBReactions.BaseMin`／`BaseMax` 原本共 22 個數值 return 全部移除，改讀 controller 上由同一張設定表綁定的 `ElementDamageMin`／`ElementDamageMax`。完整旧→新如下；元素 ID、索引、其他 K 倍率不是 B 常數，不任意替換。

| 元素 | 舊 BaseMin / BaseMax 常數 | 新 settings.json element_damage |
|---|---|---|
| `Fire` | `8.0 / 10.0` | `[10, 12]` |
| `Frost` | `5.0 / 7.0` | `[8, 10]` |
| `Lightning` | `3.0 / 15.0` | `[1, 25]` |
| `Earth` | `7.0 / 9.0` | `[8, 10]` |
| `Wind` | `4.0 / 6.0` | `[8, 9]` |
| `Blood` | `6.0 / 8.0` | `[8, 10]` |
| `Divine` | `6.0 / 8.0` | `[8, 10]` |
| `Poison` | `3.0 / 5.0` | `[8, 9]` |
| `Water` | `2.0 / 4.0` | `[5, 7]` |
| `Darkness` | `5.0 / 7.0` | `[8, 10]` |
| `Astral` | `6.0 / 8.0` | `[8, 10]` |

`build_v03.py` 原本從 `normal_damage`／`power_damage` 取得共用法術初始 magnitude，亦改從相同 element_damage 的中點 × 普通／重擊係數生成。執行期仍逐擊 RollBase，重擊 R=1.5 不變；歷史 settings 鍵保留但 v0.3 不再讀取。新增 VMAD type 14（Float[]）序列化與設定值驗證，拒絕負數、非有限數、缺元素、顛倒範圍。沒有新增依賴或 masters。

目標數值以樹等級 1、沒有節點、BaseDamageMult=1、M_mod／外部倍率=1、抗性=0 校準。毒每層每秒 2.0；流血每層每秒 1.2 是血位倍率前的基準。原有滿血倍率 1.3 保留，故滿血流血為 1.56（尚未加放血）。不能在保留血位曲線時宣稱所有血位都恰為 1.2。放血的當前生命 0.003／首領三分之一等公式逐位保留。若未指定其他血位基準，本輪採此正規化基準。

### 項目 1 / 1b 呼叫點

公式入口 `ESSBReactions.BaseMin`、`BaseMax`、`RollBase`、`ReactDamage` 統一加入 controller 參數；所有實際消費點如下（包含回血等非傷害用途，避免 B_max 漂移）：

| 檔案／行 | 所屬函式 | 調整 |
|---|---|---|
| `src/ESSBStatus.psc:761` | `Tick` | `Ctl.ApplyDamage(6, bleed * ESSBElem2.BleedPerLayer(Ctl), Holder)` |
| `src/ESSBStatus.psc:769` | `Tick` | `Float venom = ESSBReactions.BaseMax(Ctl, 8) * Ctl.PoisonDotK.GetValue() * poison * Ctl.GetDamageMult(8) * ESSBNodes.OmniMult(Ctl) \` |
| `src/ESSBReactions.psc:43` | `RollBase` | `Float low = BaseMin(akCtl, aiElement)` |
| `src/ESSBReactions.psc:44` | `RollBase` | `Float high = BaseMax(akCtl, aiElement)` |
| `src/ESSBReactions.psc:54` | `ReactDamage` | `Return BaseMax(akCtl, aiElement) * afK * akCtl.GetDamageMult(aiElement)` |
| `src/ESSBReactions.psc:103` | `Open` | `akCtl.Leech(BaseMax(akCtl, 6) * akCtl.GetBloodLeechRatio() * mult)` |
| `src/ESSBReactions.psc:291` | `EndBlood` | `remaining = akStatus.BleedRemaining(ESSBElem2.BleedPerLayer(akCtl))` |
| `src/ESSBNodes.psc:90` | `OnSyncStage` | `Float amount = ESSBReactions.BaseMax(akCtl, akCtl.CurrentElement.GetValueInt()) * 2.0` |
| `src/ESSBNodes.psc:214` | `OnEndReward` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, aiElement), 0, player)` |
| `src/ESSBNoForm.psc:312` | `OnBurst` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, aiElement) * 0.5 * aiMarks, 0, player)` |
| `src/ESSBElem.psc:253` | `OpenFire` | `Float amount = ESSBReactions.BaseMax(akCtl, 1) * 0.5 * akCtl.GetDamageMult(1)` |
| `src/ESSBElem.psc:305` | `OpenShock` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 3), 0, player)` |
| `src/ESSBElem.psc:396` | `OnIgnite` | `Float amount = ESSBReactions.BaseMax(akCtl, 1) * IgniteMult(akCtl, aiHeat) * akCtl.GetDamageMult(1)` |
| `src/ESSBElem.psc:402` | `OnIgnite` | `akCtl.ApplyUtil(4, ESSBReactions.BaseMax(akCtl, 1) * 2.0, 0, player)` |
| `src/ESSBElem.psc:425` | `Detonate` | `Float amount = ESSBReactions.BaseMax(akCtl, 1) * afMult * mult * akCtl.GetDamageMult(1) * SignatureMult(akCtl, 1)` |
| `src/ESSBElem.psc:769` | `OnTick` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 3) * charge, 0, player)` |
| `src/ESSBElem2.psc:223` | `OnBloodHit` | `Float amount = ESSBReactions.BaseMax(akCtl, 6) * akCtl.GetBloodLeechRatio() * akCtl.GetDamageMult(6)` |
| `src/ESSBElem2.psc:235` | `OnDivineHit` | `akCtl.ApplyUtil(4, ESSBReactions.BaseMax(akCtl, 7) * 0.02 * heal, 0, player)` |
| `src/ESSBElem2.psc:252` | `OnDivineHit` | `HealAllies(akCtl, ESSBReactions.BaseMax(akCtl, 7))` |
| `src/ESSBElem2.psc:352` | `OpenBlood` | `Float heal = ESSBReactions.BaseMax(akCtl, 6) * 0.5` |
| `src/ESSBElem2.psc:382` | `OpenDivine` | `akCtl.ApplyUtil(4, ESSBReactions.BaseMax(akCtl, 7) * 0.05 * rank, 0, player)` |
| `src/ESSBElem2.psc:398` | `OpenDivine` | `HealAllies(akCtl, ESSBReactions.BaseMax(akCtl, 7))` |
| `src/ESSBElem2.psc:402` | `OpenDivine` | `akCtl.ApplyUtil(4, ESSBReactions.BaseMax(akCtl, 7), 0, player)` |
| `src/ESSBElem2.psc:464` | `OnFormOpened` | `akCtl.ApplyUtil(4, ESSBReactions.BaseMax(akCtl, 7) * 2.0, 0, player)` |
| `src/ESSBElem2.psc:465` | `OnFormOpened` | `HealAllies(akCtl, ESSBReactions.BaseMax(akCtl, 7) * 2.0)` |
| `src/ESSBElem2.psc:649` | `BleedPerLayer` | `Return ESSBReactions.BaseMax(akCtl, 6) * akCtl.BleedDotK.GetValue() * akCtl.GetDamageMult(6) \` |
| `src/ESSBElem2.psc:1008` | `OnAsh` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 7) * 2.0, 0, player)` |
| `src/ESSBElem3.psc:301` | `OnAstralHit` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 11), 0, player)` |
| `src/ESSBElem3.psc:343` | `OpenPoison` | `akCtl.ApplyUtil(4, ESSBReactions.BaseMax(akCtl, 8) * 0.5, 0, player)` |
| `src/ESSBElem3.psc:426` | `OpenAstral` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 11), 0, player)` |
| `src/ESSBElem3.psc:838` | `OnAstralDetonate` | `akCtl.ApplyUtil(19, ESSBReactions.BaseMax(akCtl, 11) * aiLayers, 10, player)` |
| `src/ESSBElem3.psc:842` | `OnAstralDetonate` | `akCtl.ApplyUtil(4, ESSBReactions.BaseMax(akCtl, 11) * aiLayers, 0, player)` |
| `src/ESSBElem3.psc:1068` | `PoisonFormTick` | `heal = heal + ESSBReactions.BaseMax(akCtl, 8) * 0.5` |
| `src/ESSBElem3.psc:1157` | `OnKill` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 10) * curse, 0, player)` |
| `src/ESSBElem3.psc:1199` | `OnShadowBody` | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 10) * 2.0, 0, player)` |
| `src/ESSBController.psc:993` | `OnWeaponHit` | `ApplyDamage(PrevElement, ESSBReactions.BaseMax(Self, PrevElement) * echo \` |
| `src/ESSBController.psc:1037` | `ApplyProc` | `Float magnitude = ESSBReactions.RollBase(Self, aiElement)` |
| `src/ESSBController.psc:1098` | `OnNoFormHit` | `ApplyDamage(EmberElem, ESSBReactions.BaseMax(Self, EmberElem) * ember * GetDamageMult(EmberElem), akTarget)` |
| `src/ESSBController.psc:3393` | `TickDomain` | `ApplyUtil(4, ESSBReactions.BaseMax(Self, 6), 0, player)` |
| `src/ESSBController.psc:3396` | `TickDomain` | `ApplyUtil(4, ESSBReactions.BaseMax(Self, 7), 0, player)` |
| `src/ESSBController.psc:3397` | `TickDomain` | `ApplyUtil(5, ESSBReactions.BaseMax(Self, 7), 0, player)` |
| `src/ESSBController.psc:3400` | `TickDomain` | `ApplyUtil(4, ESSBReactions.BaseMax(Self, 9), 0, player)` |
| `src/ESSBController.psc:3401` | `TickDomain` | `ApplyUtil(6, ESSBReactions.BaseMax(Self, 9), 0, player)` |
| `src/ESSBGuard.psc:78` | `OnHitEx` | `Ctl.ApplyUtil(5, ESSBReactions.BaseMax(Ctl, 11) * Ctl.GLevel(11), 0, player)` |

### 項目 2 呼叫點與一次倍率證據

只在 `ESSBController.ApplyProc`、`ESSBController.ApplyDamage`、`ESSBController.ApplyTrueDamage` 的 `aiTree == 10` 分支新增 `BaseDamageMult.GetValue()`。`ApplyDamageRaw` 不再次乘倍率，只更新註解。`ApplyTrueDamage(aiTree == 11)` 完全不讀倍率；現有非 11 的真傷呼叫只有星斷（10）。公式 helper、狀態序列化、DoT 剩餘量、連鎖傳參均不預乘。

| 傷害路徑 | B／係數來源 → 最終入口 | BaseDamageMult 次數 |
|---|---|---:|
| 普通、重擊、雙生附傷 | RollBase → ApplyProc → DoCombatSpellApply | 1 |
| 開印／終焉、反擊、自燃、連鎖、節點追加傷害 | BaseMax／ReactDamage → ApplyDamage → ApplyDamageRaw | 1 |
| 融斷爆傷 | OnFormClosed → EndMark → ESSBReactions.End（含 K_sync）→ 各元素終焉 → ApplyDamage → Raw | 1 |
| 毒 DoT（含催毒、毒霧加層） | Status.Tick → BaseMax(Poison) × PoisonDotK → ApplyDamage → Raw | 1 |
| 流血 DoT | Status.Tick → BleedPerLayer → ApplyDamage → Raw | 1 |
| 血潮剩餘流血 | EndBlood → BleedRemaining(BleedPerLayer) → ApplyDamage → Raw | 1 |
| 延遲死咒／處刑追加段 | EndDark 儲存未乘值 → Status.Tick → ApplyDamage；AfterDeathCurse 收到原始 amount，再各自 ApplyDamage | 每一傷害段 1 |
| 星痕／落地延遲傷害 | 未乘值／層數保留於狀態 → DetonateAstral／OnLanding → ApplyDamage → Raw | 1 |
| 傷害領域 | 火域 → OnIgnite；冰原／地裂 → 狀態及反應；毒霧 → 毒層；死域 → ApplyDamage；星域 → 目標增幅 | 每一傷害段 1 |
| 星斷（B_max 轉真傷） | Fall → FallHit → ApplyTrueDamage(aiTree=10) 的專用分支 | 1 |
| 無元素樹真傷 | ApplyTrueDamage(aiTree=11／預設值) | 0（依規劃 2.7 排除） |
| 放血當前生命百分比 | ApplyBleedDrain → ApplyUtil(7) | 0（原係數與血位效果保留） |
| 過熱自傷、處決 | AddSelf → ApplyDamageRaw（自身 HP 比例）；Execute → TrueSpell | 0（不源自 B） |
| 回血／回魔／回耐、削魔／削耐、控制效果 | ApplyUtil；B_max 類效果仍讀同一張表 | 0（不是元素生命傷害） |
血潮、死咒的元素生命傷害包含原有百分比項，整次元素傷害從 ApplyDamage 乘一次；這不改動獨立的「放血」ApplyBleedDrain。傷害倍率也不拿來放大純治療／資源效果。

完整 100 個公式／傷害／施法呼叫點（含函式名、行號、原始程式碼）：[build/fix4-damage-paths.md](build/fix4-damage-paths.md)；機器可讀版本：[build/fix4-offline-proof.json](build/fix4-offline-proof.json)。該表以 17 支出貨白名單建立；歷史 `ESSBPlayerAlias.psc` 未修改且不編譯／出貨。

### 初始化原因與修法

可由原始碼確認的根因是初始化完成條件不足：舊 `InitFixState` 的 `If !LiftActor` 先建立 LiftActor，再建立 LiftDue／LiftForce／LiftDamage，卻只以 LiftActor 判斷整組是否完成。若另一個入口在中間重入，或儲存狀態只剩主陣列存在，就跳過剩下三個陣列；`ArmUpdate` 已呼叫 InitFixState 仍不等於它取得了完整陣列組。同樣缺陷存在於 PendingAir、BackupValid、PendingStacks、RegGeneration 等群組，以及 InitRegistry 只檢查 RegActor 的 early return。

本輪離線直接執行快照的舊函式：給 LiftActor 有效值而 LiftDue=None，InitFixState 不補建，隨後 ArmUpdate 確實發生 None 存取。這證明缺陷本身；但不把這個可重現時序冒充原遊戲 log 的完整事件追蹤。提供的當次症狀只有 OnInit→ScheduleTick→ArmUpdate 與八次錯誤，無法單憑它分辨引擎 quest reset 或双重 OnInit。工作區的 smoke1 log 也沒有該 ArmUpdate 錯誤證據。

修法：每個陣列各自 `== None` 檢查並只補缺失項；LiftActor 改為最後配置，LiftUp 排隊也最後發布 actor，先填 force／damage。OnInit 不再走 ScheduleTick／ArmUpdate，也不讀写任何陣列，只 `RegisterForSingleUpdate(2.0)`；OnPlayerLoadGame 同樣先延後，首次 OnUpdate 先 Setup→InitRegistry，完成後再 ScheduleTick。無 Wait、無忙等、無 RegisterForUpdate。離線測試涵蓋重複 OnInit 只排程、16/16 Lift 部分初始化狀態、65/65 陣列分別缺失、自身重入後保留原有陣列及值。

### 建置與驗收

`python build_v03.py` 已由本輪執行，exit 0。完整輸出：[build/fix4-build.log](build/fix4-build.log)。

- READBACK：masters `['Skyrim.esm']`，records=manifest=3894，17 scripts 0 errors。
- CSF：13/13；LAYOUT ok；DELIVERY ok（Contact 71／Self 49）；PLAN COVERAGE 541 rows、0 unmapped；FX ok（251 records）。
- FIX3：原 3878 records 與 13 筆追加 FX、持續標記／光暈回讀檢查保留通過。其「新增 FX」檢查僅排除三個已知新 GLOB，GLOB 由獨立 FIX4 嚴格檢查，沒有放鬆其他記錄限制。
- FIX4：既有 3891 筆完全不變，只追加 3 個 float GLOB；直接回讀 ESP 的 FLTV、QUST VMAD 屬性與 11 元素 Float[] 綁定。
- `python build/fix4-audit.py`：9 組回歸通過。實際執行所選 Papyrus 原始函式，原生呼叫 mock；覆蓋 11 元素普通／重擊、反應、1 與 2.5 倍、逐秒 DoT 5／6 次、血潮剩餘流血、真傷例外、放血及過熱例外。
- ESP Float32 回讀目標：毒 `1.9999999746680261`，流血 `1.2000000067055225`（正常 float 精度）。
- 修改檔 UTF-8／BOM 與各自 LF／CRLF 格式保留；本紀錄僅追加，舊內容逐位保留。未修改 plan_coverage.py（現有說明沒有舊 k_dot 常數）、設計文件、review、fx_extract.py 或 .strategic-advance。

### How to tune

修改 `settings.json` 的 `base_damage_mult` 後重建，可給高等級起步角色提高整體元素傷害（例如 2.0 = 本輪基準 ×2）。只調 DoT 時改 `poison_dot_k`／`bleed_dot_k`。新的目標每層秒傷 / (對應 B_max × 1.05) 即為等級 1、倍率前的 k；若指定满血流血實際值，再除以 1.3。變更 B_max 後，想維持固定 DoT 目標就同步重算 k，否則 DoT 隨 B_max 線性變動。

`element_damage` 用精確元素鍵 `[min,max]` 設定；全模組相關反應／節點同步採用 max，雷電 max=25 是使用者明確選擇。不要再改 `normal_damage`／`power_damage`。設定是建置輸入，遊戲不讀 JSON；重建後套件位於 `package/Elements Spellblade/`。已有存檔可能保存 GLOB／屬性值，單改 JSON 不保證覆寫存檔；三個 GLOB 已以屬性綁定，供下一輪 MCM 使用。本輪沒有增設選單按鈕，亦不自行安裝／修改存檔。

本輪修改檔：`build_v03.py`, `settings.json`, `src/ESSBController.psc`, `src/ESSBElem.psc`, `src/ESSBElem2.psc`, `src/ESSBElem3.psc`, `src/ESSBGuard.psc`, `src/ESSBNodes.psc`, `src/ESSBNoForm.psc`, `src/ESSBReactions.psc`, `src/ESSBStatus.psc`；另追加本紀錄、更新指定進度頁與 build／package 產物。修改前備份在 `build/fix4-before/`。

## 2026-09-17 23:54 Asia/Taipei — fix round 4 驗收（指揮官）

- 通過。指揮官重建 exit 0；3894 筆 == manifest；與 `.codex/pre-fix4-snapshot` 比對舊 3891 筆 0 變動，新增 3 個 GLOB（`ESSB_PoisonDotK` 0x005158、`ESSB_BleedDotK` 0x005159、`ESSB_BaseDamageMult` 0x00515A）。
- 數值核對：`poison_dot_k` 0.2116 = 2.0 ÷ (B_max 9 × G(1) 1.05)；`bleed_dot_k` 0.1143 = 1.2 ÷ (10 × 1.05)；B 表與使用者核定值一致（雷 1–25）。腳本內已無 `5.0 * 0.05` 之類寫死係數。`BaseDamageMult` 只出現在 ApplyProc、ApplyDamage、DoT 三個咽喉點。
- 使用者再追加（第五輪第 0 項）：毒層壽命 6→12 秒、流血 5→10 秒，每層每秒不變。


## fix round 5（MCM）

本輪先完成第 0 項並獨立執行 `python build_v03.py`（exit 0、18 支腳本），再進行 MCM 與能力選單。完整版本再次執行同一建置命令，exit 0、19 支出貨腳本均為 `0 error(s), 0 warning(s)`。

### FIXED / NOT FIXED

| 狀態 | 項目 | 結果與驗證界線 |
|---|---|---|
| FIXED | 毒層 6 → 12 秒、流血 5 → 10 秒 | `build_v03.py` 的 `POISON_LAYER_SECONDS`、`BLEED_LAYER_SECONDS` 為唯一壽命常數；生成 `ESSBState.psc` 的常數函式及 Papyrus 固定長度配置。`settings.json` 與第四輪快照逐位不變，每層每秒係數不變。 |
| FIXED | 換宿序列化與備份 | 27-int → 38-int；所有血／毒 bucket 及尾端狀態雙向對稱。整數備份改為 4 組 × 76 格，各存 2 個目標；8 個目標槽位互不覆寫。17-float 備份維持 2 組 × 68 格。 |
| FIXED | 舊存檔資料 | `InitRings` 擴充既有 5／6 格環狀桶、保留 bucket 年齡；`ESSBState.UpgradeInts` 與 `InitBackupInts` 支援原 27-int／108-int 備份。新增的高齡 bucket 初始為零。 |
| FIXED | 血潮與 pending | `BleedRemaining` 以 `BleedRing.Length - index` 算剩餘秒數，離線執行验证通過。`PendingStacks`／`PendingSet` 是 8 目標 × 12 狀態種類（96 格），並非每秒 bucket；維持 96 格。其他 pending 為每目標旗標／值，維持 8 格。 |
| FIXED | MCM 一般／技能樹／除錯 | UTF-8 繁體中文直接寫入 `config.json`。31 個 GLOB 直接綁定，13 棵樹的 26 個等級／點數欄位唯讀；3 個 CallFunction 動作由 `ESSBMCM` 橋接。 |
| FIXED | 洗點確認與目前元素樹 | 目前樹按鈕先保存 `CurrentTree()`，確認框說明會關閉形態；確認後且不在戰鬥／冷卻中才呼叫 `ESSBController.CloseForm()`，再呼叫 `ESSBTrees.Respec(原樹)`。避免「關形態後 CurrentTree 變無元素」使元素洗點不可用。取消不執行；全部洗點仍使用 `RespecAll` 的原限制與冷卻規則。 |
| FIXED | 能力選單 | 同一批既有 FormID，按鈕僅 `0 元素樹 / 1 通用樹 / 2 關閉`；更新說明及 `ESSBSettingsEffect` 索引。附傷／除錯切換仍保留為脚本函式，既有 `DumpRegistry`、`Respec`、`RespecAll` 未移除。 |
| FIXED | 執行期軟依賴 | 獨立 `ESSB_MCMQuest`（0x00515B），VMAD 掛 `ESSBMCM`；其玩家別名使用執行期既有 `SKI_PlayerLoadGameAlias` 呼叫 SkyUI 讀檔註冊流程。SEQ 包含主任務及 MCM 任務。所有遊戲機制腳本均無 `ESSBMCM` 參照，ESP masters 僅 `['Skyrim.esm']`。 |
| FIXED | 編譯依賴 | 本機 loose source 不存在，從 `MCMHelper.bsa`／`SkyUI_SE.bsa` 讀取 PEX 核對父類與函式簽章；僅在 `vendor/imports` 放 ASCII stubs。未把 stub 或第三方 PEX 放入套件。 |
| FIXED | FormID、既有驗收與封裝 | 舊 3894 筆身分完全不變，僅追加 1 筆 QUST。`ESSB_DebugLevel` 仍為 0x000811。READBACK／CSF／DELIVERY／LAYOUT／PLAN COVERAGE／FX 均通過，新增 `DOT ok` 與 `MCM ok`。 |
| NOT FIXED（未進遊戲驗證） | MCM 顯示、按鈕實際操作、新／舊存檔註冊、卸除軟依賴實跑 | 已做檔案、ESP、編譯、介面簽章與依賴方向驗證；本輪未啟動遊戲，也未寫入 MO2 或 SkyrimSE。以下清單供遊戲內驗收。 |
| NOT FIXED（驗證界線） | 引擎實際牆鐘時間 | 離線證明為每秒 ring tick 模型：t 加入的層在 t+12／t+10 那次 tick 移出，之前仍存在。Papyrus 排程抖動、命中落在 tick 中間、換宿回呼延遲未做遊戲測時；不把離線模型冒充引擎毫秒精度。 |
| NOT FIXED（既有範圍外項） | 無元素「穩步」原版格擋耐力進入點 | 仍為既有唯一 DEFERRED 計畫列，本輪未變更；PLAN COVERAGE 維持 0 unmapped。 |

### DoT 版面與離線證據

38-int 新版面：0–7 原獨立狀態；8–17 血痕 10 格；18–29 毒層 12 格；30–31 星痕；32 CatalyzeLeft；33 DeathCurseLeft；34 SpreadCounter；35 AirLeft；36 StarLockLeft；37 WetLock。血潮剩餘量隨 10 格計算。宿主仍在約 25 秒後走原 `PrepareSwap → Export → SaveSwapData → ImportState` 流程。

建置檢查直接執行實際 Papyrus 原始函式的受限子集合（非另外手寫一份序列化模型）：每格使用不同數值，確認 export→import→export 相同、8 槽整數／浮點備份各自相同、舊 27-int 及兩個 108-int bank 可轉換。22 個逐格壽命案例涵蓋 12+10 種 age；血潮剩餘傷害計算通過。掃描 19 支出貨 source 的 89 個陣列配置，最大為 96，全部 <=128。

係數維持 `ESSB_PoisonDotK = 0.21164021164021163`、`ESSB_BleedDotK = 0.11428571428571428`。第四輪等級 1、倍率 1 的中立基準每層每秒仍約毒 2.0／流血 1.2；不受其他倍率／抗性影響時，完整壽命總量因時間加倍而成約 24／12。原本的血位與其他加成不變。

證據：[第 0 項建置](build/fix5-item0-build.log)、[完整建置](build/fix5-final-build.log)、[DoT 離線檢查](build/fix5-dot-check.json)、[MCM 引用與身分檢查](build/fix5-mcm-check.json)、[stub 實際簽章](build/fix5-stub-signatures.json)、[編譯輸出](build/v03-compile-results.json)。

### 本輪套件新增檔案

以下是相對於 `package/Elements Spellblade/` 的新增檔案（其餘 ESP／SEQ、既有對應 PSC／PEX 為更新）：

- `MCM/Config/Elements Spellblade/config.json`
- `Scripts/ESSBMCM.pex`
- `Source/Scripts/ESSBMCM.psc`
- `Scripts/ESSBState.pex`
- `Source/Scripts/ESSBState.psc`

不使用 ModSettings，因此不產出 `settings.ini`。沒有 `Interface/Translations` 檔；所有中文直接存於 UTF-8 JSON，不受本安裝 `sLanguage=ENGLISH` 的翻譯檔命名影響。未新增任何會覆蓋其他模組的出貨檔名。

只供編譯、不出貨：`vendor/imports/MCM_ConfigBase.psc`、`vendor/imports/SKI_ConfigBase.psc`、`vendor/imports/SKI_QuestBase.psc`。`ShowMessage(String a_message, Bool a_withCancel, String a_acceptLabel, String a_cancelLabel) → Bool` 與 `ForcePageReset() → None` 均核對實際 PEX；呼叫時顯式傳入全部參數，不推測缺失的 source 預設值。

### MCM 綁定的全部 GLOB

下列 FormID 都是本 ESP 的 local FormID，JSON 使用 `Elements Spellblade.esp|HEX`。一般頁倍率範圍 0.5–5.0、step 0.1；兩個 DoT 係數範圍 0–1、step 0.001，defaultValue 保留第四輪精度；除錯 enum 為 0–3。

| EDID | FormID | 類型 |
|---|---|---|
| `ESSB_Enabled` | `0x000810` | GLOB |
| `ESSB_BaseDamageMult` | `0x00515A` | GLOB |
| `ESSB_PoisonDotK` | `0x005158` | GLOB |
| `ESSB_BleedDotK` | `0x005159` | GLOB |
| `ESSB_Lvl_fire` | `0x002400` | GLOB |
| `ESSB_Pts_fire` | `0x002440` | GLOB |
| `ESSB_Lvl_frost` | `0x002401` | GLOB |
| `ESSB_Pts_frost` | `0x002441` | GLOB |
| `ESSB_Lvl_lightning` | `0x002402` | GLOB |
| `ESSB_Pts_lightning` | `0x002442` | GLOB |
| `ESSB_Lvl_earth` | `0x002403` | GLOB |
| `ESSB_Pts_earth` | `0x002443` | GLOB |
| `ESSB_Lvl_wind` | `0x002404` | GLOB |
| `ESSB_Pts_wind` | `0x002444` | GLOB |
| `ESSB_Lvl_blood` | `0x002405` | GLOB |
| `ESSB_Pts_blood` | `0x002445` | GLOB |
| `ESSB_Lvl_divine` | `0x002406` | GLOB |
| `ESSB_Pts_divine` | `0x002446` | GLOB |
| `ESSB_Lvl_poison` | `0x002407` | GLOB |
| `ESSB_Pts_poison` | `0x002447` | GLOB |
| `ESSB_Lvl_water` | `0x002408` | GLOB |
| `ESSB_Pts_water` | `0x002448` | GLOB |
| `ESSB_Lvl_darkness` | `0x002409` | GLOB |
| `ESSB_Pts_darkness` | `0x002449` | GLOB |
| `ESSB_Lvl_astral` | `0x00240A` | GLOB |
| `ESSB_Pts_astral` | `0x00244A` | GLOB |
| `ESSB_Lvl_noform` | `0x00240B` | GLOB |
| `ESSB_Pts_noform` | `0x00244B` | GLOB |
| `ESSB_Lvl_common` | `0x00240C` | GLOB |
| `ESSB_Pts_common` | `0x00244C` | GLOB |
| `ESSB_DebugLevel` | `0x000811` | GLOB |

3 個 CallFunction 的目標均為 `Elements Spellblade.esp|00515B`（QUST `ESSB_MCMQuest`），scriptName 為 `ESSBMCM`；函式為 `RespecCurrent`、`RespecAll`、`DumpRegistry`。設定 JSON 共有 34 個 form reference，建置逐一檢查其 record 類型與 manifest 身分。

### 遊戲內驗收清單

1. 手動安裝本套件後，透過 SKSE 1.5.97 相容執行環境進遊戲；本輪沒有代替使用者安裝或修改現行 MO2 模組。MCM 需本機 SkyUI 與 MCM Helper 正常載入。
2. 開啟 MCM，應看到「元素魔戰士」，其下有「一般／技能樹／除錯」三頁；中文字應直接顯示，不能是 `$...` key。不應看到 MCM 開啟 CSF 樹的按鈕。
3. 一般頁切換附傷，將基礎倍率調至 2.0，再恢復 1.0；毒／流血係數修改後應立即反映到既有 GLOB，離开再開選單數值應保持。舊存檔保留已儲存 GLOB，不強制覆寫成出廠值。
4. 技能樹頁確認 13 棵樹皆有等級／未用點數，與能力開啟的 CSF 樹一致；欄位為唯讀、不能修改。洗點先按取消，點數／技能／形態應不變。
5. 脫戰、冷卻已結束時啟用欲洗的元素形態，按「洗點：目前元素樹」：確認框應顯示正確樹名並說明關閉形態；確認後關閉形態並退回該樹點數，不能誤洗無元素樹。戰鬥中或該樹冷卻中不應關閉形態。未開形態時此按鈕指向無元素樹。
6. 「洗點：全部」也應先確認；沿用原規則，需先脫戰並關閉形態，冷卻中的樹會略過。確認後重新繪製頁面以顯示退還點數。
7. 除錯等級可選 0／1／2／3。按「印出目標表」應顯示通知；啟用遊戲 Papyrus logging 後，log 應出現 `[ESSB][dump][L0]`。按鈕本身不修改遊戲 logging 設定。
8. 施放原設定能力（名稱更新為「元素魔戰士：技能樹」），只應看到「元素樹／通用樹／關閉」。前兩者開對應樹，關閉不開樹；沒有附傷、除錯、目標表或洗點按鈕。
9. 在單次加層、沒有刷新／追加的測試中檢查毒約 12 秒、流血約 10 秒；每秒傷害維持第四輪。連續命中仍刷新全部血痕，但不刷新舊毒層。持續測試超過 25 秒並觀察目標表，確認宿主換宿未遺失層數／星痕／其他狀態。實際時間受每秒 tick 與 Papyrus 排程影響。
10. 儲存、離開、重新載入，檢查 MCM 仍可開啟且值未被重設。若 MCM 條目未出現：先關閉所有選單，讓 SkyUI 完成註冊，再開 MCM；既有存檔新增任務可能需等待或另存後重讀，新遊戲則走開局任務與 SEQ 初始化。確認 ESP 啟用、JSON 路徑正確、MCM Helper DLL 為 1.5.97 相容版本、SkyUI 本身可用。必要時在測試存檔使用 SkyUI 的 `setstage SKI_ConfigManagerInstance 1` 重新註冊，再離開選單等待；不要只因未立即出现就清除存檔或重設本模組主任务。
11. 以另建的測試設定檢查缺少 SkyUI／MCM Helper 時，元素機制與能力開樹仍正常；只應缺少設定頁（CSF 開樹仍依其原有執行期需求）。本輪僅靜態驗證此依賴隔離，未動本機啟用清單。

修改範圍：`build_v03.py`、`src/ESSBStatus.psc`、`src/ESSBController.psc`、`src/ESSBElem3.psc`、`src/ESSBSettingsEffect.psc`、新增 `src/ESSBState.psc`／`src/ESSBMCM.psc`、上述 3 個編譯 stub，以及指定進度頁、build／package 產物與本紀錄追加內容。既有 source／build script 保持 UTF-8 無 BOM、CRLF；`settings.json` 原位元內容未變。規劃文件、review、`fx_extract.py`、`.strategic-advance/**` 未編輯，未存取網路，未寫入 MO2／SkyrimSE。

## 2026-09-18 00:15 Asia/Taipei — fix round 5 驗收與部署（指揮官）

- 通過。指揮官重建 exit 0；3895 筆 == manifest；與 `.codex/pre-fix5-snapshot` 比對舊 3894 筆 0 變動，新增 `ESSB_MCMQuest` 0x00515B；19 支腳本 0 errors（新增 ESSBMCM、ESSBState）；`DOT ok`（毒 12 秒、流血 10 秒、匯出 38 格、來回與到期檢查通過）、`MCM ok`（34 個引用全數解析）。
- 套件檢查：MCM／SKI 編譯 stub 未被打包；19 個 pex 與其他已安裝模組無同名；新增檔案只有 `MCM/Config/Elements Spellblade/config.json`。
- 部署：遊戲與 MO2 皆未執行時，把套件 54 檔鏡像複製到 `MO2/mods/Elements Spellblade`（逐檔 sha256 相符，保留 meta.ini）；舊安裝版備份於 `.codex/mo2-installed-backup-20260918-001453`。未觸碰 profile 三檔。
- 第六輪工作單已備妥未派：`.codex/fix-round6-briefing.md`（主線純百分比 ×3、破魔、水、固定數值盤點表）。待使用者決定：形態維持費是否加回。


## fix round 6（節點數值）

本輪產物：`package/Elements Spellblade/Elements Spellblade.esp` 與同資料夾內的 Scripts、Source、SKSE/Plugins/CustomSkills、MCM/Config；只生成工作區套件，沒有安裝到 MO2 或 SkyrimSE。原規劃文件維持原樣，本節記錄數值偏離。

### FIXED / NOT FIXED

| 狀態 | 工作 | 結果／邊界 |
|---|---|---|
| FIXED | 純百分比增傷 | 從 195 主線資料分類，105 節點 ×3；settings `node_percent_scale=3.0` → float GLOB `ESSB_NodeScale=0x00515C` → Controller.NodeScale → ESSBNodes.Pct；每個納入節點均有程式位置驗證。 |
| NOT FIXED — needs a decision | 題述的 56 與資料不一致 | 實際原文含 `+N%／點` 的節點共131；明確純增傷105，分布為 {'0.2': 1, '1': 47, '2': 41, '3': 13, '5': 1, '0.5': 2}。不以刪節點或任意挑56來湊數。14 個混合／語義有疑義的節點保留原值，見下表。 |
| FIXED | 顯示與調整 | 同一份生成資料產出全部 2925 主線 rank PERK DESC；CSF 節點引用這些 PERK 顯示文字，沒有另外一套數字。火每層 8%→17%、死咒15%→37.5%、落地×0.5→×2.75等範圍也重算。MCM「一般」增加1.0–5.0、step0.5滑桿。 |
| FIXED | 破魔 | (20+4×rank)×G，蝕魔10%、枯竭20%，實際削量封頂與等量回魔保留；比例0.5→0.95、施法者 +5%／點、沉默／反咒時長保留。 |
| FIXED | 水／霜 | 長流2.0%基礎、熟練+0.2%／點、浸濕15%、霜結25%、清流30、回流80；大師／傳奇長流各0.05%規則與水牢20%不變。 |
| FIXED | 共用減速上限 | ESSBController.ApplyUtil 的 aiIndex=0 在 SetNthEffectMagnitude 前夾到 SlowCapPct，且硬上界70；所有本模組移動減速共用原有 ESSB_UtilEffect_Slow（AV30、Peak Value Modifier 34），同款效果取最強，不是不同來源相加。沒有新增定身／麻痺。 |
| FIXED | 固定數值盤點 | `build/flat-values-audit.md` 共60列、13/13樹；來源交叉表 `build/flat-values-audit-source.json`，建議一律未套用。 |
| NOT FIXED（照要求保留） | 水治療超過神聖 | 常態單體持續戰鬥已反超原設計5.11的預期；未偷偷設治療上限，也未改神聖。多目標裁決可讓神聖瞬間治療更高，詳見比較。 |
| FIXED | 編譯、原有檢查與 FormID | `python build_v03.py` exit0；19腳本全數0 errors；既有3895筆 identity與 pre-fix6 快照一致；新增12筆float GLOB 0x00515C–0x005167；DebugLevel仍0x000811。 |
| NOT FIXED（原有） | 穩步與遊戲內實測 | PLAN COVERAGE原有穩步DEFERRED仍保留；本輪沒有改動或宣稱修復。未啟動遊戲，動作排程、引擎效果與MCM互動仍需實機驗證。 |

### 指定數值前後與 G(L) 證明

settings 中 `_pct` 的單位為百分點（例如10.0表示10%），腳本需要比例時乘0.01。所有12個新常數均有float GLOB與QUST VMAD屬性綁定；`node_percent_scale` 之外11個常數沒有新增MCM項目。

| 設定／路徑 | 原值 | 新值 | 位置 |
|---|---|---|---|
| node_percent_scale | 無（等同1） | 3.0 | ESSBNodes.psc:Pct |
| manabreak_base / manabreak_per_rank | 5 + rank，沒有G | (20 +4×rank)×G(noform) | ESSBNoForm.psc:OnManaBreak |
| manabreak_maxmag_pct | 最大魔力2% | 10%，仍與已乘G的固定削量取高者 | ESSBNoForm.psc:OnManaBreak |
| manabreak_dry_pct | 玩家當前魔力5% | 20% | ESSBNoForm.psc:OnManaBreak |
| 破魔真傷比例 | 0.5 +0.03×rank，cap0.95 | 不變 | ESSBNoForm.psc:OnManaBreak |
| 施法者削魔加成 | 1+0.05×rank | 不變，滿點×1.75 | ESSBNoForm.psc:OnManaBreak |
| water_flow_base_pct | 0.3%／秒 | 2.0%／秒 | ESSBElem3.psc:FlowPercent |
| water_flow_per_rank_pct | +0.02%／點 | +0.2%／點，熟練滿點5.0% | ESSBElem3.psc:FlowPercent |
| water_wet_slow_pct | 10% | 15%，原主線+1%／點仍在，滿點30% | ESSBElem3.psc:WetSlow |
| frost_open_slow_pct | 15%／3秒 | 25%／3秒 | ESSBReactions.psc:Open |
| slow_cap_pct | 無共用上限，部分路徑80／90 | 70% | ESSBController.psc:ApplyUtil |
| water_clear_stamina | 5／命中 | 30／命中 | ESSBElem3.psc:OnWaterHit |
| water_open_stamina | 15／開印 | 80／開印 | ESSBElem3.psc:OpenWater |

破魔順序：`fixed=(20+4r)G` → 蝕魔時 `max(fixed, MaxMagicka×0.10)` → 施法者 `×(1+0.05×pierce)` → `actual=min(requested,currentMagicka)` → 扣除actual、回玩家actual → `trueAmount=actual×ratio`。`ApplyTrueDamage(..., abLevelScaled=True)`跳過自己的G乘法；原本的TrueMult（低魔力傳奇）與靜寂加成仍照常套用。百分比蝕魔分支若勝出，其10%本來就是獨立下限，也不再額外乘G。以下驗證關掉TrueMult／靜寂，避免把額外節點誤算成G：

| 樹等級／rank／目標當前魔力 | actual | ratio | 真傷（含G一次） | 回玩家魔力 |
|---|---:|---:|---:|---:|
| L10／r1／1000 | 36 | 0.95 | 34.2 | 36 |
| L100／r15／1000 | 480 | 0.95 | 456 | 480 |
| L100／r15／100 | 100 | 0.95 | 95 | 100 |
| L100／r15／10000，蝕魔＋施法者主線滿點 | 1750 | 0.95 | 1662.5 | 1750 |

L100、r15的普通施法者固定削量是480×1.75=840，再由當前魔力封頂。r1、L10且比例未投點的真傷=36×0.5=18。滿點低魔力傳奇現為+9%／點，另乘2.35；不能把此合法節點倍率誤認為重複乘G。

枯竭沒有actual drain，所以保留原路徑的一次G：玩家當前魔力300、L100時，300×20%×6=360（其他傷害節點另計），不是72也不是2160。離線測試直接執行上述兩個Papyrus函式內容，並核對回魔、比例封頂與兩個G分支；沒有用另一套抄寫公式冒充遊戲驗證。

### 長流與神聖比較

長流滿點、同調3：2.0 +15×0.2 +15×0.05×3 +15×0.05 = **8.0%／秒**；原為3.6%。這是每次WaterFormTick的上限，NodeScale=1、3、5均不影響。未投點基礎：300HP角色由0.9HP/s變6HP/s；滿點300–500HP角色為24–40HP/s，200–400耐力為16–32/s。玩家與原有範圍同伴的套用條件、對象都不變。

神聖B_max=10，治療走ApplyUtil，**不吃G(L)**：持續專精滿點僅3HP／擊；白天開印基礎6×1.45=8.7，再加開啟新手滿點7.5，合計16.2／開印；裁決每名受影響目標12HP（最多主目標＋5人=72／次）；聖斷每個融斷目標再12HP；聖域站內10HP/s；聖引10、聖臨20是額外事件，不是常駐每秒。

可稽核的持續模型（白天、不算外部回復）：`神聖HP/s = 3×每秒命中 +16.2×每秒開印 +12×每秒被裁決目標數 +12×每秒聖斷目標數 +10×聖域覆蓋率 +10×每秒聖引 +20×每秒聖臨`；通用回饋等一次性事件另加。無開印／終焉事件的1擊/s單體戰鬥，神聖為3，假設聖域100%覆蓋也只有13；遠低於水的24–40。即便單體每10秒額外開印、裁決、聖斷各一次，也只再加4.02/s。

另外，原有通用「化身」滿點每15秒能額外呼叫一次WaterFormTick，所以計入此額外事件，長流平均為8×(1+1/15)=8.5333%/s（300–500HP為25.6–42.6667），再加潮池站內7/s，為32.6–49.6667。神聖化身單體相當於12/15=0.8HP/s、六目標最多4.8HP/s；群體反覆裁決才可能超越水。水斷本身仍按融斷倍率回血／回耐，未在本輪另調基準。

**設計偏離：新水在常態單體與同伴持續治療已明顯超越神聖；8%不是包含所有分支事件的「全樹總治療」上限。** 保留使用者指定數值，不設新治療cap。大量敵人的神聖範圍裁決仍可能出現更高的瞬間回血，不能宣稱水在所有情境都更高。

### 倍率最壞情境檢查（樹等級100、同調3）

數字直接執行修改前備份／目前腳本的`GetHitMult`、各樹`HitMult`及共用helper取得，明細在`build/fix6-check.json`。舊「約×1.7」只接近一般元素樹自己的1+15%+45%+15%=1.75，**不是完整M_mod上界**；新值為1+45%+135%+45%=3.25，通用樹重擊倍率亦由1.75→3.25，兩者相乘已由3.0625→10.5625。

為呈現上界，表中所有主線（含未調的層數上限）都投滿、開印增益有效、重擊；熱度取無添薪時觸發自燃前12層、冰封5、聖印8、水壓8（僅開必要的水壓分支）、詛咒13；目標滿詛咒，接管增益有效（舊1.15→新1.45），滿血血位1.3；聖取白天、暗取夜晚各1.2。其餘分支關閉；這欄未放入火過熱等分支／狀態加成。M_mod不含G、B、重擊R=1.5、抗性或外部模組。

最右欄是更保守的**全分支條件上界**：所有分支、嗜血1.2、萬象1.25、風失衡／浮空1.3×1.5、星鎖1.1、星域1.2、暗低魔施法者1.5、火17熱度＋過熱／熔身／火域等全部條件各取最大，星痕9層。用來暴露既有乘算的風險，不代表所有互斥或短視窗必能在同一擊同時出現；實際可達峰值需遊戲事件順序驗證。

| 元素 | 元素樹單獨：舊 → 新 | M_mod 主線條件值：舊 → 新 | 新 G(100) × M_mod | 全分支條件上界：舊 → 新 |
|---|---:|---:|---:|---:|
| Fire | 3.0700 → 5.2900 | 7.1051 → 36.1472 | 216.8834 | 43.5174 → 187.0533 |
| Frost | 2.0500 → 4.1500 | 4.7445 → 28.3575 | 170.1448 | 14.6547 → 87.5905 |
| Lightning | 1.7500 → 3.2500 | 4.0502 → 22.2077 | 133.2459 | 12.5101 → 68.5950 |
| Earth | 1.7500 → 3.2500 | 4.0502 → 22.2077 | 133.2459 | 12.5101 → 68.5950 |
| Wind | 1.6000 → 2.8000 | 3.7030 → 19.1327 | 114.7965 | 11.4378 → 59.0972 |
| Blood | 1.7500 → 3.2500 | 5.2652 → 28.8700 | 173.2197 | 16.2632 → 89.1735 |
| Divine | 3.1500 → 7.0500 | 8.7483 → 57.8082 | 346.8494 | 34.7424 → 216.5492 |
| Poison | 1.7500 → 3.2500 | 4.0502 → 22.2077 | 133.2459 | 12.5101 → 68.5950 |
| Water | 3.1500 → 5.8500 | 7.2903 → 39.9738 | 239.8427 | 26.0925 → 146.6878 |
| Darkness | 1.7500 → 3.2500 | 4.8602 → 26.6492 | 159.8951 | 22.5182 → 123.4710 |
| Astral | 1.7500 → 3.2500 | 4.0502 → 22.2077 | 133.2459 | 18.9439 → 87.5905 |

元素樹內火／聖／水本来就有逐層項，不能把整欄都稱作舊×1.7。M_mod以外的原有開印擊分支最多再×1.5×1.5，風潛行／連殺最多×5×2，反擊再×1.3，皆未改值；G(100)=6與重擊R=1.5仍各乘一次。表內不是所有反應、DoT與無上限毒層的全系統單一上限。

終焉／融斷亦不能只看附傷：不含分支的共用融斷節點乘積 `CommonBurst×NoFormBurst×CommonEnd×ElementEnd×ElementBurst` 原=6.861400、新=77.199850；同調3的K_sync=3另乘，單獨招式Signature對常見+3%主線則1.45→2.35。完整含K_sync與Signature的共同部分原=29.847090、新=544.258942，尚未乘該招式K、G、狀態層數、環境、目標增傷或分支。水斷不乘Signature、其基準14在此無分支條件下已是14×3×77.199850=3242.394回復／印記；因此盤點建議對水斷暫留，不再盲目提高基準。

無形態武器進入點維持原本結構：一般主線滿點1.15→1.45；每層戰意1.075→1.225，五層實作仍乘五次；淬火1.15→1.45。因此三者同時有效的武器倍率由1.898620→5.799846，不是五層戰意加法。既有條件與PRKE數目不變，僅EPFD預設值與SKSE SetNthEntryValue寫入值改動。

### 固定數值盤點摘要（未修改）

`build/flat-values-audit.md`的60列含所有13樹與基礎反應，依單次資源占比與觸發成本排序。最弱的一批：血命中基礎吸血約0.65–3／擊（有主線仍多為2.6–3.9）、神聖滿點3HP／擊、追風3耐／次、汲力滿點3.75耐／擊、毒血4.5HP／次、順風5耐／擊與深寒5削耐／秒。B_max型回血／回魔多數沒有G，不能因為傷害有G就推定它們也成長。雷神滿層150–350回魔、磐石滿層325護甲其實已可感受，表中明確建議保留。血盾是最大生命百分比，神佑1HP是保命語義，不當作可直接乘大的普通補血。表內建議沒有寫回settings或腳本。

### 未改 — 需要決策（14個）

| 樹 | 路線（index） | 階（index） | 分類／原因 | 原文 | 新數值／顯示文字 | 實際程式位置 |
|---|---|---|---|---|---|---|
| fire | 開啟（焰起）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult → ESSBReactions.Open |
| frost | 開啟（冰臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| lightning | 開啟（雷臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| earth | 開啟（地臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| earth | 關閉（地震）（2） | 專精（2） | 不改—需決策：削耐而非生命傷害，保留待決 | 地震耐力削減 +3%／點 | 地震耐力削減 +3%／點 | ESSBElem2.QuakeStamina |
| wind | 開啟（風臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| blood | 持續（血位）（0） | 新手（0） | 不改—需決策：複合兩種百分比：流血傷害與放血係數，保留待決 | 流血每層傷害 +2%／點，放血係數 +0.01%／點（0.3% → 0.45%） | 流血每層傷害 +2%／點，放血係數 +0.01%／點（0.3% → 0.45%） | ESSBElem2.BleedTickMult / BleedDrainPercent → ESSBStatus.Tick |
| blood | 開啟（血臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| divine | 開啟（聖臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| poison | 開啟（毒臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| water | 開啟（水臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| darkness | 開啟（咒縛）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| astral | 開啟（星臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| noform | 純武藝（0） | 傳奇（4） | 不改—需決策：文字是無視護甲；實作為補真傷，保留待決 | 終結：戰意滿層重擊無視護甲 +3%／點 | 終結：戰意滿層重擊無視護甲 +3%／點 | ESSBNoForm.OnMartialHit（真實傷害路徑） |

### 全195主線分類表（含原文、新數值、程式位置）

分類函式為`plan_trees.py:classify_main`：先排除指定不動的比例／施法者加成、機率、治療、減抗、移速／控制／防禦，再標記混合效果待決，最後以原文`+N%／點`與傷害語義分類。每個SCALED節點必須能映射到實際Pct呼叫，否則建置失敗；共享函式按樹展開比對，不能把函式個數當成節點數。完整資料版`build/fix6-classification.json`；105個改值節點另列`build/fix6-scaled-nodes.md`。其餘72個不改；4個SPECIFIC是破魔、長流熟練、浸濕、霜結說明。

| 樹 | 路線（index） | 階（index） | 分類／原因 | 原文 | 新數值／顯示文字 | 實際程式位置 |
|---|---|---|---|---|---|---|
| fire | 持續（烈焰）（0） | 新手（0） | ×3：純百分比傷害／傷害係數 | 熱度每層火附傷 +0.2%／點（8% → 11%） | 熱度每層火附傷 +0.6%／點（8% → 17%） | src\ESSBElem.psc:FireHitMult:67 |
| fire | 持續（烈焰）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 火附傷 +1%／點 | 火附傷 +3%／點 | src\ESSBElem.psc:FireHitMult:64 |
| fire | 持續（烈焰）（0） | 專精（2） | 不改：減抗／削甲 | 帶熱度目標火抗 -1%／點 | 帶熱度目標火抗 -1%／點 | ESSBElem.ApplyFireResistShred |
| fire | 持續（烈焰）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段火附傷 +1%／點 | 同調每段火附傷 +3%／點 | src\ESSBElem.psc:FireHitMult:70 |
| fire | 持續（烈焰）（0） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 業火：同調三段時自燃倍率 +0.1／點（×2 → ×3.5） | 業火：同調三段時自燃倍率 +0.1／點（×2 → ×3.5） | ESSBElem.IgniteMult |
| fire | 開啟（焰起）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印熱度 +1／每 5 點 | 開印熱度 +1／每 5 點 | ESSBElem.OpenStacks |
| fire | 開啟（焰起）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內火附傷 +1%／點 | 開印後 5 秒內火附傷 +3%／點 | src\ESSBElem.psc:FireHitMult:72 |
| fire | 開啟（焰起）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 火印記持續 +0.2 秒／點 | 火印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus → ESSBController.ApplyMark |
| fire | 開啟（焰起）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 火臨：開火形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 火臨：開火形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened → ESSBController.ForceOpenOn |
| fire | 開啟（焰起）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult → ESSBReactions.Open |
| fire | 關閉（爆燃）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| fire | 關閉（爆燃）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 火印記的融斷 +2%／點 | 火印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| fire | 關閉（爆燃）（2） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 爆燃每層熱度倍率 +0.01／點（0.2 → 0.35） | 爆燃每層熱度倍率 +0.01／點（0.2 → 0.35） | ESSBElem.DetonatePerLayer |
| fire | 關閉（爆燃）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 火印記的融斷再 +2%／點 | 火印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| fire | 關閉（爆燃）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 爆燃 +3%／點 | 爆燃 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| frost | 持續（凍結）（0） | 新手（0） | 不改：移動／控制／防禦／門檻 | 每次命中凍結累積 +5%／點 | 每次命中凍結累積 +5%／點 | ESSBElem.HitStacks |
| frost | 持續（凍結）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 冰附傷 +1%／點 | 冰附傷 +3%／點 | src\ESSBElem.psc:FrostHitMult:93 |
| frost | 持續（凍結）（0） | 專精（2） | ×3：純百分比傷害／傷害係數 | 冰封目標受冰附傷 +2%／點 | 冰封目標受冰附傷 +6%／點 | src\ESSBElem.psc:FrostHitMult:95 |
| frost | 持續（凍結）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段冰附傷 +1%／點 | 同調每段冰附傷 +3%／點 | src\ESSBElem.psc:FrostHitMult:97 |
| frost | 持續（凍結）（0） | 傳奇（4） | 不改：移動／控制／防禦／門檻 | 絕對零度：同調三段時冰封減速再 -2%／點（自有強減速，非麻痺），可影響首領 | 絕對零度：同調三段時冰封減速再 -2%／點（自有強減速，非麻痺），可影響首領 | ESSBElem.OnFrozenTick |
| frost | 開啟（冰臨）（1） | 新手（0） | 第2／2b項指定：第 2／2b 項指定數值 | 開印凍結 +1／每 3 點 | 開印凍結 +1／每 3 點（霜結基礎減速 25%，持續 3 秒；總減速上限 70%） | ESSBElem.OpenStacks |
| frost | 開啟（冰臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內冰附傷 +1%／點 | 開印後 5 秒內冰附傷 +3%／點 | src\ESSBElem.psc:FrostHitMult:99 |
| frost | 開啟（冰臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 冰印記持續 +0.2 秒／點 | 冰印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| frost | 開啟（冰臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 冰臨：開冰形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 冰臨：開冰形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| frost | 開啟（冰臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| frost | 關閉（碎冰）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| frost | 關閉（碎冰）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 冰印記的融斷 +2%／點 | 冰印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| frost | 關閉（碎冰）（2） | 專精（2） | ×3：純百分比傷害／傷害係數 | 終焉後 5 秒內接管元素附傷 +1%／點 | 終焉後 5 秒內接管元素附傷 +3%／點 | src/ESSBElem.psc:OnEnd:600 |
| frost | 關閉（碎冰）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 冰印記的融斷再 +2%／點 | 冰印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| frost | 關閉（碎冰）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 碎冰 +3%／點 | 碎冰 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| lightning | 持續（充能）（0） | 新手（0） | ×3：純百分比傷害／傷害係數 | 放電每格電荷傷害 +1%／點 | 放電每格電荷傷害 +3%／點 | src\ESSBElem.psc:Discharge:528 |
| lightning | 持續（充能）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 雷附傷 +1%／點 | 雷附傷 +3%／點 | src\ESSBElem.psc:ShockHitMult:107 |
| lightning | 持續（充能）（0） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 電荷上限 +1／每 3 點 | 電荷上限 +1／每 3 點 | ESSBElem.ChargeCap → ESSBController.AddSelf |
| lightning | 持續（充能）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段雷附傷 +1%／點 | 同調每段雷附傷 +3%／點 | src\ESSBElem.psc:ShockHitMult:108 |
| lightning | 持續（充能）（0） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 天雷：同調三段時放電改為對範圍內所有感電目標，範圍 2 公尺 +0.2 公尺／點 | 天雷：同調三段時放電改為對範圍內所有感電目標，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.DischargeAll |
| lightning | 開啟（雷臨）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印電荷 +1／每 5 點 | 開印電荷 +1／每 5 點 | ESSBElem.OpenStacks |
| lightning | 開啟（雷臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內雷附傷 +1%／點 | 開印後 5 秒內雷附傷 +3%／點 | src\ESSBElem.psc:ShockHitMult:110 |
| lightning | 開啟（雷臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 雷印記持續 +0.2 秒／點 | 雷印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| lightning | 開啟（雷臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 雷臨：開雷形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 雷臨：開雷形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| lightning | 開啟（雷臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| lightning | 關閉（放電）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| lightning | 關閉（放電）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 雷印記的融斷 +2%／點 | 雷印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| lightning | 關閉（放電）（2） | 專精（2） | ×3：純百分比傷害／傷害係數 | 終焉後 5 秒內接管元素附傷 +1%／點 | 終焉後 5 秒內接管元素附傷 +3%／點 | src/ESSBElem.psc:OnEnd:600 |
| lightning | 關閉（放電）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 雷印記的融斷再 +2%／點 | 雷印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| lightning | 關閉（放電）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 放電 +3%／點 | 放電 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| earth | 持續（裂甲）（0） | 新手（0） | 不改：減抗／削甲 | 裂痕護甲削減 +2／點（-30 → -60） | 裂痕護甲削減 +2／點（-30 → -60） | ESSBElem2.FissureArmor → ESSBReactions.Open(4) |
| earth | 持續（裂甲）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 土附傷 +1%／點 | 土附傷 +3%／點 | src\ESSBElem2.psc:EarthHitMult:73 |
| earth | 持續（裂甲）（0） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 命中削減目標耐力 +0.5／點 | 命中削減目標耐力 +0.5／點 | ESSBElem2.OnEarthHit |
| earth | 持續（裂甲）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段土附傷 +1%／點 | 同調每段土附傷 +3%／點 | src\ESSBElem2.psc:EarthHitMult:74 |
| earth | 持續（裂甲）（0） | 傳奇（4） | 不改：機率 | 地動：同調三段時重擊對耐力低於 30% 的目標跌倒，機率 5%／點 | 地動：同調三段時重擊對耐力低於 30% 的目標跌倒，機率 5%／點 | ESSBElem2.OnEarthHit → ESSBController.Knockdown |
| earth | 開啟（地臨）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印岩甲 +1／每 5 點 | 開印岩甲 +1／每 5 點 | ESSBElem2.OpenStacks(4) |
| earth | 開啟（地臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內土附傷 +1%／點 | 開印後 5 秒內土附傷 +3%／點 | src\ESSBElem2.psc:EarthHitMult:76 |
| earth | 開啟（地臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 土印記持續 +0.2 秒／點 | 土印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus → ESSBController.ApplyMark |
| earth | 開啟（地臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 地臨：開土形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 地臨：開土形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened → ESSBController.ForceOpenOn |
| earth | 開啟（地臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| earth | 關閉（地震）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| earth | 關閉（地震）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 土印記的融斷 +2%／點 | 土印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| earth | 關閉（地震）（2） | 專精（2） | 不改—需決策：削耐而非生命傷害，保留待決 | 地震耐力削減 +3%／點 | 地震耐力削減 +3%／點 | ESSBElem2.QuakeStamina |
| earth | 關閉（地震）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 土印記的融斷再 +2%／點 | 土印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| earth | 關閉（地震）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 地震 +3%／點 | 地震 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| wind | 持續（連斬）（0） | 新手（0） | ×3：純百分比傷害／傷害係數 | 風刃傷害 +2%／點 | 風刃傷害 +6%／點 | src\ESSBElem2.psc:WindBladeMult:551 |
| wind | 持續（連斬）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 風附傷 +1%／點 | 風附傷 +3%／點 | src\ESSBElem2.psc:WindHitMult:84 |
| wind | 持續（連斬）（0） | 專精（2） | 不改：移動／控制／防禦／門檻 | 風形態移速再 +0.5%／點（+10% → +17.5%） | 風形態移速再 +0.5%／點（+10% → +17.5%） | ESSBElem2.WindSpeedBonus → ESSBController.RefreshWindAbilities |
| wind | 持續（連斬）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段風附傷 +1%／點 | 同調每段風附傷 +3%／點 | src\ESSBElem2.psc:WindHitMult:85 |
| wind | 持續（連斬）（0） | 傳奇（4） | 不改：機率 | 千刃：同調三段時每次命中附帶風刃，機率 5%／點 | 千刃：同調三段時每次命中附帶風刃，機率 5%／點 | ESSBElem2.OnWindHit |
| wind | 開啟（風臨）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印風勢 +1／每 5 點 | 開印風勢 +1／每 5 點 | ESSBElem2.OpenStacks(5) |
| wind | 開啟（風臨）（1） | 熟練（1） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印拉近距離 +0.1 公尺／點（1.5 → 3 公尺） | 開印拉近距離 +0.1 公尺／點（1.5 → 3 公尺） | ESSBElem2.PullDistance → ESSBController.PullIn |
| wind | 開啟（風臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 風印記持續 +0.2 秒／點 | 風印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| wind | 開啟（風臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 風臨：開風形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 風臨：開風形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| wind | 開啟（風臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| wind | 關閉（吹飛）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| wind | 關閉（吹飛）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 風印記的融斷 +2%／點 | 風印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| wind | 關閉（吹飛）（2） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 吹飛距離 +0.2 公尺／點（3 → 6 公尺） | 吹飛距離 +0.2 公尺／點（3 → 6 公尺） | ESSBElem2.BlowDistance |
| wind | 關閉（吹飛）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 風印記的融斷再 +2%／點 | 風印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| wind | 關閉（吹飛）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 落地傷害 +5%／點（×0.5 → ×1.25） | 落地傷害 +15%／點（×0.5 → ×2.75） | src\ESSBElem2.psc:LandingDamage:619 |
| blood | 持續（血位）（0） | 新手（0） | 不改—需決策：複合兩種百分比：流血傷害與放血係數，保留待決 | 流血每層傷害 +2%／點，放血係數 +0.01%／點（0.3% → 0.45%） | 流血每層傷害 +2%／點，放血係數 +0.01%／點（0.3% → 0.45%） | ESSBElem2.BleedTickMult / BleedDrainPercent → ESSBStatus.Tick |
| blood | 持續（血位）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 血附傷 +1%／點 | 血附傷 +3%／點 | src\ESSBElem2.psc:BloodHitMult:92 |
| blood | 持續（血位）（0） | 專精（2） | 不改：治療／回復 | 吸血比例各血位 +1%／點 | 吸血比例各血位 +1%／點 | ESSBController.GetBloodLeechRatio |
| blood | 持續（血位）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段血附傷 +1%／點 | 同調每段血附傷 +3%／點 | src\ESSBElem2.psc:BloodHitMult:93 |
| blood | 持續（血位）（0） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 血海：同調三段時血潮改為範圍，範圍 1 公尺 +0.2 公尺／點 | 血海：同調三段時血潮改為範圍，範圍 1 公尺 +0.2 公尺／點 | ESSBElem2.SurgeRadius → ESSBReactions.EndBlood |
| blood | 開啟（血臨）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印流血 +1 層／每 5 點 | 開印流血 +1 層／每 5 點 | ESSBElem2.OpenStacks(6) |
| blood | 開啟（血臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內血附傷 +1%／點 | 開印後 5 秒內血附傷 +3%／點 | src\ESSBElem2.psc:BloodHitMult:95 |
| blood | 開啟（血臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 血印記持續 +0.2 秒／點 | 血印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| blood | 開啟（血臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 血臨：開血形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 血臨：開血形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| blood | 開啟（血臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| blood | 關閉（血潮）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| blood | 關閉（血潮）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 血印記的融斷 +2%／點 | 血印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| blood | 關閉（血潮）（2） | 專精（2） | 不改：治療／回復 | 血潮治療倍率 ×2 → 每點 +0.1 | 血潮治療倍率 ×2 → 每點 +0.1 | ESSBElem2.SurgeHealMult |
| blood | 關閉（血潮）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 血印記的融斷再 +2%／點 | 血印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| blood | 關閉（血潮）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 血潮 +3%／點 | 血潮 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| divine | 持續（審判）（0） | 新手（0） | ×3：純百分比傷害／傷害係數 | 聖印每層目標受聖傷 +1%／點 | 聖印每層目標受聖傷 +3%／點 | src\ESSBElem2.psc:HolyVulnerability:120 |
| divine | 持續（審判）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 聖附傷 +1%／點 | 聖附傷 +3%／點 | src\ESSBElem2.psc:DivineHitMult:103 |
| divine | 持續（審判）（0） | 專精（2） | 不改：治療／回復 | 命中回血 +2%／點 | 命中回血 +2%／點 | ESSBElem2.OnDivineHit |
| divine | 持續（審判）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段聖附傷 +1%／點 | 同調每段聖附傷 +3%／點 | src\ESSBElem2.psc:DivineHitMult:104 |
| divine | 持續（審判）（0） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 天啟：同調三段時裁決改為範圍，範圍 1 公尺 +0.2 公尺／點 | 天啟：同調三段時裁決改為範圍，範圍 1 公尺 +0.2 公尺／點 | ESSBElem2.JudgeArea |
| divine | 開啟（聖臨）（1） | 新手（0） | 不改：治療／回復 | 開印回血 +5%／點 | 開印回血 +5%／點 | ESSBElem2.OpenDivine |
| divine | 開啟（聖臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內聖附傷 +1%／點 | 開印後 5 秒內聖附傷 +3%／點 | src\ESSBElem2.psc:DivineHitMult:106 |
| divine | 開啟（聖臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 聖印記持續 +0.2 秒／點 | 聖印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| divine | 開啟（聖臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 聖臨：開聖形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 聖臨：開聖形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| divine | 開啟（聖臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| divine | 關閉（裁決）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| divine | 關閉（裁決）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 聖印記的融斷 +2%／點 | 聖印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| divine | 關閉（裁決）（2） | 專精（2） | ×3：純百分比傷害／傷害係數 | 終焉後 5 秒內接管元素附傷 +1%／點 | 終焉後 5 秒內接管元素附傷 +3%／點 | src/ESSBElem.psc:OnEnd:600 |
| divine | 關閉（裁決）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 聖印記的融斷再 +2%／點 | 聖印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| divine | 關閉（裁決）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 裁決 +3%／點 | 裁決 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| poison | 持續（疫毒）（0） | 新手（0） | ×3：純百分比傷害／傷害係數 | 毒層每層傷害 +2%／點 | 毒層每層傷害 +6%／點 | src\ESSBElem3.psc:PoisonTickMult:537 |
| poison | 持續（疫毒）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 毒附傷 +1%／點 | 毒附傷 +3%／點 | src\ESSBElem3.psc:PoisonHitMult:98 |
| poison | 持續（疫毒）（0） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 基礎擴散間隔 -0.1 秒／點（2 → 0.5 秒） | 基礎擴散間隔 -0.1 秒／點（2 → 0.5 秒） | ESSBElem3.SpreadInterval / SpreadTargets → ESSBStatus.Tick |
| poison | 持續（疫毒）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段毒附傷 +1%／點 | 同調每段毒附傷 +3%／點 | src\ESSBElem3.psc:PoisonHitMult:99 |
| poison | 持續（疫毒）（0） | 傳奇（4） | 不改：機率 | 瘟疫：同調三段時毒層每秒自動擴散到附近敵人，機率 5%／點 | 瘟疫：同調三段時毒層每秒自動擴散到附近敵人，機率 5%／點 | ESSBElem3.PoisonTickHook ← ESSBStatus.Tick |
| poison | 開啟（毒臨）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印毒層 +1／每 3 點 | 開印毒層 +1／每 3 點 | ESSBElem3.OpenStacks → ESSBReactions.Open |
| poison | 開啟（毒臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內毒附傷 +1%／點 | 開印後 5 秒內毒附傷 +3%／點 | src\ESSBElem3.psc:PoisonHitMult:101 |
| poison | 開啟（毒臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 毒印記持續 +0.2 秒／點 | 毒印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| poison | 開啟（毒臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 毒臨：開毒形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 毒臨：開毒形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| poison | 開啟（毒臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| poison | 關閉（催毒）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| poison | 關閉（催毒）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 毒印記的融斷 +2%／點 | 毒印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| poison | 關閉（催毒）（2） | 專精（2） | ×3：純百分比傷害／傷害係數 | 終焉後 5 秒內接管元素附傷 +1%／點 | 終焉後 5 秒內接管元素附傷 +3%／點 | src/ESSBElem.psc:OnEnd:600 |
| poison | 關閉（催毒）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 毒印記的融斷再 +2%／點 | 毒印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| poison | 關閉（催毒）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 催毒期間毒層傷害 +3%／點 | 催毒期間毒層傷害 +9%／點 | src\ESSBElem3.psc:PoisonTickMult:539 |
| water | 持續（潮汐）（0） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 浸濕持續 +0.3 秒／點 | 浸濕持續 +0.3 秒／點 | ESSBElem3.WetSeconds → ESSBStatus.Tick |
| water | 持續（潮汐）（0） | 熟練（1） | 第2／2b項指定：第 2／2b 項指定數值 | 長流每秒回復 +0.02%／點（0.3% → 0.6%） | 長流每秒回復 +0.2%／點（2.0% → 5.0%） | ESSBElem3.FlowPercent → WaterFormTick |
| water | 持續（潮汐）（0） | 專精（2） | ×3：純百分比傷害／傷害係數 | 水壓每層水附傷 +1%／點 | 水壓每層水附傷 +3%／點 | src\ESSBElem3.psc:WaterHitMult:117 |
| water | 持續（潮汐）（0） | 大師（3） | 不改：治療／回復 | 同調每段長流回復 +0.05%／點 | 同調每段長流回復 +0.05%／點 | ESSBElem3.FlowPercent |
| water | 持續（潮汐）（0） | 傳奇（4） | 不改：治療／回復 | 長河：同調三段時長流再 +0.05%／點，且長流同時作用於附近同伴 | 長河：同調三段時長流再 +0.05%／點，且長流同時作用於附近同伴 | ESSBElem3.FlowPercent / WaterFormTick |
| water | 開啟（水臨）（1） | 新手（0） | 第2／2b項指定：第 2／2b 項指定數值 | 浸濕減速 +1%／點 | 浸濕減速 +1%／點（15% → 30%；本模組總減速上限 70%） | ESSBElem3.WetSlow → ESSBReactions.Open |
| water | 開啟（水臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內水附傷 +1%／點 | 開印後 5 秒內水附傷 +3%／點 | src\ESSBElem3.psc:WaterHitMult:111 |
| water | 開啟（水臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 水印記持續 +0.2 秒／點 | 水印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| water | 開啟（水臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 水臨：開水形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 水臨：開水形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| water | 開啟（水臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| water | 關閉（導引）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| water | 關閉（導引）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 水印記的融斷 +2%／點 | 水印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| water | 關閉（導引）（2） | 專精（2） | ×3：純百分比傷害／傷害係數 | 終焉後 5 秒內接管元素附傷 +1%／點 | 終焉後 5 秒內接管元素附傷 +3%／點 | src/ESSBElem.psc:OnEnd:600 |
| water | 關閉（導引）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 水印記的融斷再 +2%／點 | 水印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| water | 關閉（導引）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 導引 +3%／點 | 導引 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| darkness | 持續（侵蝕）（0） | 新手（0） | 不改：減抗／削甲 | 詛咒每層抗性侵蝕 -0.2%／點（-2% → -5%） | 詛咒每層抗性侵蝕 -0.2%／點（-2% → -5%） | ESSBElem3.ApplyCurseErosion |
| darkness | 持續（侵蝕）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 暗附傷 +1%／點 | 暗附傷 +3%／點 | src\ESSBElem3.psc:DarkHitMult:127 |
| darkness | 持續（侵蝕）（0） | 專精（2） | ×3：純百分比傷害／傷害係數 | 詛咒滿層目標受所有傷害 +1%／點 | 詛咒滿層目標受所有傷害 +3%／點 | src/ESSBElem3.psc:TargetDamageMult:157 |
| darkness | 持續（侵蝕）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段暗附傷 +1%／點 | 同調每段暗附傷 +3%／點 | src\ESSBElem3.psc:DarkHitMult:128 |
| darkness | 持續（侵蝕）（0） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 深淵：同調三段時詛咒層上限 +1／每 3 點，滿層目標抗性可侵蝕到負值 | 深淵：同調三段時詛咒層上限 +1／每 3 點，滿層目標抗性可侵蝕到負值 | ESSBElem3.CurseCap → ESSBController.StackCap |
| darkness | 開啟（咒縛）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印詛咒 +1 層／每 5 點 | 開印詛咒 +1 層／每 5 點 | ESSBElem3.OpenStacks → ESSBReactions.Open |
| darkness | 開啟（咒縛）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內暗附傷 +1%／點 | 開印後 5 秒內暗附傷 +3%／點 | src\ESSBElem3.psc:DarkHitMult:130 |
| darkness | 開啟（咒縛）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 暗印記持續 +0.2 秒／點 | 暗印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| darkness | 開啟（咒縛）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 暗臨：開暗形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | 暗臨：開暗形態時對範圍內敵人各開印一次，範圍 2 公尺 +0.2 公尺／點 | ESSBElem.OnFormOpened |
| darkness | 開啟（咒縛）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| darkness | 關閉（死咒）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| darkness | 關閉（死咒）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 暗印記的融斷 +2%／點 | 暗印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| darkness | 關閉（死咒）（2） | 專精（2） | ×3：純百分比傷害／傷害係數 | 死咒的「已損失生命」係數 +0.5%／點（15% → 22.5%） | 死咒的「已損失生命」係數 +1.5%／點（15% → 37.5%） | src\ESSBElem3.psc:DeathCurseLostRatio:627 |
| darkness | 關閉（死咒）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 暗印記的融斷再 +2%／點 | 暗印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| darkness | 關閉（死咒）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 死咒 +3%／點 | 死咒 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| astral | 持續（星落）（0） | 新手（0） | ×3：純百分比傷害／傷害係數 | 星痕延遲傷害 +2%／點 | 星痕延遲傷害 +6%／點 | src\ESSBElem3.psc:AstralTickMult:820 |
| astral | 持續（星落）（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 星附傷 +1%／點 | 星附傷 +3%／點 | src\ESSBElem3.psc:AstralHitMult:142 |
| astral | 持續（星落）（0） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 星痕層數上限 +1／每 5 點 | 星痕層數上限 +1／每 5 點 | ESSBElem3.AstralCap → ESSBController.StackCap |
| astral | 持續（星落）（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調每段星附傷 +1%／點 | 同調每段星附傷 +3%／點 | src\ESSBElem3.psc:AstralHitMult:143 |
| astral | 持續（星落）（0） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 群星：同調三段時星落連鎖附近星痕目標，範圍 1 公尺 +1 公尺／點（滿點 16 公尺） | 群星：同調三段時星落連鎖附近星痕目標，範圍 1 公尺 +1 公尺／點（滿點 16 公尺） | ESSBElem3.FallChain / ConstellationRadius |
| astral | 開啟（星臨）（1） | 新手（0） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 星痕延遲 -0.1 秒／點 | 星痕延遲 -0.1 秒／點 | ESSBElem3.AstralDelay → ESSBStatus.Tick |
| astral | 開啟（星臨）（1） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 開印後 5 秒內星附傷 +1%／點 | 開印後 5 秒內星附傷 +3%／點 | src\ESSBElem3.psc:AstralHitMult:145 |
| astral | 開啟（星臨）（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 星印記持續 +0.2 秒／點 | 星印記持續 +0.2 秒／點 | ESSBElem.MarkDurationBonus |
| astral | 開啟（星臨）（1） | 大師（3） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 星臨：開星形態時對範圍內敵人各開印一次，範圍 2 公尺 +1 公尺／點（滿點 17 公尺） | 星臨：開星形態時對範圍內敵人各開印一次，範圍 2 公尺 +1 公尺／點（滿點 17 公尺） | ESSBElem.OnFormOpened → ESSBElem3.AdventRadius |
| astral | 開啟（星臨）（1） | 傳奇（4） | 不改—需決策：開印倍率同時放大層數／控制／回復，保留待決 | 開印效果 +3%／點 | 開印效果 +3%／點 | ESSBElem.OpenMult |
| astral | 關閉（星落）（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +2%／點 | 終焉 +6%／點 | src/ESSBElem.psc:EndMult:129 |
| astral | 關閉（星落）（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 星印記的融斷 +2%／點 | 星印記的融斷 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| astral | 關閉（星落）（2） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 星落與星域的範圍 +0.8 公尺／點（3 → 15 公尺；星落需「流星雨」才有範圍） | 星落與星域的範圍 +0.8 公尺／點（3 → 15 公尺；星落需「流星雨」才有範圍） | ESSBElem3.FallRadius |
| astral | 關閉（星落）（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 星印記的融斷再 +2%／點 | 星印記的融斷再 +6%／點 | src/ESSBElem.psc:BurstMult:135 |
| astral | 關閉（星落）（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 星落 +3%／點 | 星落 +9%／點 | src/ESSBElem.psc:SignatureMult:141 |
| noform | 純武藝（0） | 新手（0） | ×3：純百分比傷害／傷害係數 | 無形態時武器傷害 +1%／點 | 無形態時武器傷害 +3%／點 | src/ESSBNodes.psc:RefreshWeaponPercent:30；build_v03.py:main_entries（預設 EPFD） |
| noform | 純武藝（0） | 熟練（1） | 不改：減抗／削甲 | 重擊碎甲：自有減防 1%／點，5 秒 | 重擊碎甲：自有減防 1%／點，5 秒 | ESSBNoForm.OnMartialHit |
| noform | 純武藝（0） | 專精（2） | 不改：機率 | 無形態時暴擊率 +1%／點（自有判定） | 無形態時暴擊率 +1%／點（自有判定） | ESSBNoForm.OnMartialHit |
| noform | 純武藝（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 戰意：每層武器傷害 +0.5%／點 | 戰意：每層武器傷害 +1.5%／點 | src/ESSBNodes.psc:RefreshWeaponPercent:30；build_v03.py:main_entries（預設 EPFD） |
| noform | 純武藝（0） | 傳奇（4） | 不改—需決策：文字是無視護甲；實作為補真傷，保留待決 | 終結：戰意滿層重擊無視護甲 +3%／點 | 終結：戰意滿層重擊無視護甲 +3%／點 | ESSBNoForm.OnMartialHit（真實傷害路徑） |
| noform | 破魔（1） | 新手（0） | 第2／2b項指定：第 2／2b 項指定數值 | 破魔：命中削減目標魔力 5（+1／點），回復你等量魔力，造成削減量 ×0.5 的真實傷害（無視護甲與所有抗性），並施加「破魔印」8 秒 | 破魔：命中削減目標魔力（20 + 4／點）× G(L_無元素)，回復你實際削減的等量魔力，造成實際削減量 ×0.5 的真實傷害（此路徑不重複乘 G），並施加「破魔印」8 秒 | ESSBNoForm.OnManaBreak |
| noform | 破魔（1） | 熟練（1） | 不改：第 2 項明定比例／對施法者 +5% 不變 | 真實傷害比例 +3%／點（0.5 → 0.95） | 真實傷害比例 +3%／點（0.5 → 0.95） | ESSBNoForm.OnManaBreak |
| noform | 破魔（1） | 專精（2） | 不改：治療／回復 | 沉默：目標魔力被削至 0 時施加沉默 1 秒（+0.2 秒／點，最長 4 秒）；沉默中無法施法、不回復魔力 | 沉默：目標魔力被削至 0 時施加沉默 1 秒（+0.2 秒／點，最長 4 秒）；沉默中無法施法、不回復魔力 | ESSBNoForm.ApplySilence → ESSBController.ApplySilenceSpell |
| noform | 破魔（1） | 大師（3） | 不改：第 2 項明定比例／對施法者 +5% 不變 | 對施法者與帶魔法護盾、元素披風的敵人削魔量 +5%／點 | 對施法者與帶魔法護盾、元素披風的敵人削魔量 +5%／點 | ESSBNoForm.IsSpellUser |
| noform | 破魔（1） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 目標魔力低於 25% 時命中傷害 +3%／點 | 目標魔力低於 25% 時命中傷害 +9%／點 | src\ESSBNoForm.psc:TrueMult:40 |
| noform | 融斷（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 融斷 +2%／點 | 融斷 +6%／點 | src\ESSBNoForm.psc:BurstMult:271 |
| noform | 融斷（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 餘燼：關閉形態後 10 秒內無形態命中附帶前一元素附傷 +2%／點 | 餘燼：關閉形態後 10 秒內無形態命中附帶前一元素附傷 +6%／點 | src\ESSBNoForm.psc:EmberRatio:341 |
| noform | 融斷（2） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 融斷範圍 +0.3 公尺／點 | 融斷範圍 +0.3 公尺／點 | ESSBNoForm.BurstRadius |
| noform | 融斷（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 淬火：融斷後 10 秒內武器傷害 +1%／點 | 淬火：融斷後 10 秒內武器傷害 +3%／點 | src/ESSBNodes.psc:RefreshWeaponPercent:30；build_v03.py:main_entries（預設 EPFD） |
| noform | 融斷（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 融斷再 +3%／點 | 融斷再 +9%／點 | src\ESSBNoForm.psc:BurstMult:271 |
| common | 持續（0） | 新手（0） | 不改：移動／控制／防禦／門檻 | 同調門檻 -2%／點 | 同調門檻 -2%／點 | ESSBNodes.SyncThresholdScale → ESSBController.SyncStage |
| common | 持續（0） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 同調二段時附傷 +1%／點 | 同調二段時附傷 +3%／點 | src\ESSBNodes.psc:CommonHitMult:134 |
| common | 持續（0） | 專精（2） | 不改：移動／控制／防禦／門檻 | 同調三段時受傷 -0.5%／點 | 同調三段時受傷 -0.5%／點 | PERK ESSB_P_common_0_2_M<n> 進入點 0x24（受到的傷害） |
| common | 持續（0） | 大師（3） | ×3：純百分比傷害／傷害係數 | 同調三段時重擊附傷 +2%／點 | 同調三段時重擊附傷 +6%／點 | src\ESSBNodes.psc:CommonHitMult:137 |
| common | 持續（0） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 化身：同調三段時每 30 秒自動觸發一次當前元素的持續傳奇效果，冷卻 -1 秒／點 | 化身：同調三段時每 30 秒自動觸發一次當前元素的持續傳奇效果，冷卻 -1 秒／點 | ESSBNodes.AvatarCooldown → ESSBController.Tick → ESSBElem.OnAvatar |
| common | 開啟（1） | 新手（0） | ×3：純百分比傷害／傷害係數 | 所有元素附傷 +1%／點 | 所有元素附傷 +3%／點 | src\ESSBNodes.psc:CommonHitMult:131 |
| common | 開啟（1） | 熟練（1） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 印記持續 +0.2 秒／點 | 印記持續 +0.2 秒／點 | ESSBNodes.MarkDurationBonus → ESSBController.ApplyMark |
| common | 開啟（1） | 專精（2） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 開印時 +1 同調／每 5 點 | 開印時 +1 同調／每 5 點 | ESSBNodes.OpenSyncBonus |
| common | 開啟（1） | 大師（3） | ×3：純百分比傷害／傷害係數 | 所有元素附傷再 +1%／點 | 所有元素附傷再 +3%／點 | src\ESSBNodes.psc:CommonHitMult:131 |
| common | 開啟（1） | 傳奇（4） | 不改：非本輪百分比增傷（距離／時長／上限／冷卻／固定值／非百分比係數） | 每種元素狀態上限 +1 層／每 5 點 | 每種元素狀態上限 +1 層／每 5 點 | ESSBNodes.StatusCapBonus → ESSBController.StackCap |
| common | 關閉（2） | 新手（0） | ×3：純百分比傷害／傷害係數 | 終焉 +1%／點 | 終焉 +3%／點 | src\ESSBNodes.psc:CommonEndMult:144 |
| common | 關閉（2） | 熟練（1） | ×3：純百分比傷害／傷害係數 | 融斷 +1%／點 | 融斷 +3%／點 | src\ESSBNodes.psc:CommonBurstMult:149 |
| common | 關閉（2） | 專精（2） | ×3：純百分比傷害／傷害係數 | 切換後首次命中附帶前一元素附傷 +3%／點 | 切換後首次命中附帶前一元素附傷 +9%／點 | src\ESSBNodes.psc:EchoRatio:286 |
| common | 關閉（2） | 大師（3） | ×3：純百分比傷害／傷害係數 | 終焉再 +1%／點 | 終焉再 +3%／點 | src\ESSBNodes.psc:CommonEndMult:144 |
| common | 關閉（2） | 傳奇（4） | ×3：純百分比傷害／傷害係數 | 融斷再 +2%／點 | 融斷再 +6%／點 | src\ESSBNodes.psc:CommonBurstMult:149 |

### 驗證與保留界線

- 最終建置：`python build_v03.py` exit0。`build/fix6-build.log`包含DOT ok（12秒毒／10秒血、38 ints）、LAYOUT 13/13、19 compile OK、FIX3／FIX4／FIX6／MCM ok、READBACK masters=['Skyrim.esm'] records=3907 manifest=3907、CSF13/13、DELIVERY ok、PLAN COVERAGE unmapped=0。
- pre-fix6的3895筆FormID完全一致，僅附加12個GLOB；無新masters；ESSB_DebugLevel仍000811。VMAD每個常數僅綁一次，float回讀對照settings。MCM32個GLOB來源＋3個QUST動作參照均可解析。
- 全2925主線rank描述及300分支描述逐筆ESP回讀；僅四個明確核准分支描述變更。CSF節點直接引用同一PERK文字；MCM滑桿不會在遊戲內重寫ESP描述，描述顯示建置預設3.0；改settings重建會一起更新。
- 腳本數值透過讀取實際函式內容離線執行：Pct的rank0／1／15、scale1／1.5／3／5；全部45個武器rank PERK的105個entry更新；破魔固定削量／蝕魔／施法者／魔力不足／枯竭；長流不吃NodeScale；15–999的減速輸入均夾住。此工具只執行公式與mock delivery，沒有偽稱代替Skyrim引擎測試。
- 已核對24個原檔的BOM與LF／CRLF型態；既有來源編碼與換行保留。本紀錄以bytes append保留原有混合換行與全部舊內容。`build/fix6-before/`保留修改前來源，生成的`build/fix6-encoding-check.json`列出結果。
- 沒有修改設計原稿、review檔、fx_extract.py、.strategic-advance/**，沒有寫入MO2或SkyrimSE。第三輪FX、第四輪B／DoT／BaseDamageMult、第五輪狀態序列化與MCM既有功能均通過原檢查。
- 動態weapon百分比僅在既有Tick偵測NodeScale變更或讀檔後刷新全部rank entries；不改條件、觸發次數、buff時長、支線、傷害投遞方式。原有OnEnd對其他樹亦建立EndBoost的路徑保留舊0.01×rank，只有資料明定的冰／雷／聖／毒／水五個主線放大，避免共用函式越界改到非百分比節點。

封裝：`build/Elements-Spellblade-fix6.zip`，358734 bytes；SHA-256 `ab391e5df62b77dd3f8a97d9291197a04d0db2a6e60990d7d5a0769737c0ca53`。ZIP逐檔回讀與package完全一致。

## 2026-09-18 07:16 Asia/Taipei — fix round 6 驗收與部署（指揮官）

- 通過（建置與邊界）。指揮官重建 exit 0；3907 筆 == manifest；與 `.codex/pre-fix6-snapshot` 比對舊 3895 筆 0 變動，新增 12 個 GLOB（0x00515C–0x005167）。分類表 195 筆：105 個 ×3、72 個不改、14 個待決、4 個由第 2／2b 項指定；抽查分類原則一致（機率、範圍、時間、層數、回復、抗性未被納入）。
- 指揮官更正：先前對使用者說「純百分比主線 56 個、滿級節點倍率約 ×1.7」是低估。實際納入 105 個；元素樹、通用樹、接管增益等是**相乘**，舊的主線條件 M_mod 已是 ×4.1–8.7，`node_percent_scale = 3` 之後為 ×22–58（G(100)×M_mod = 133–347），融斷共用乘積 6.9 → 77。此為使用者指定數值的結果，已如實回報並建議預設改 2；MCM 有 1–5 滑桿。
- 設計偏離（使用者指定）：長流滿點 8%／秒，常態單體治療超過神聖；浸濕 15%、霜結 25%、總減速上限 70%；破魔削魔 (20+4r)×G，G 只乘一次（`ApplyTrueDamage(..., abLevelScaled=True)`）。
- 部署：遊戲與 MO2 未執行時鏡像複製到 MO2（逐檔 sha256 相符）；前一安裝版備份於 `mo2-installed-backup-20260918-071558`。
- 產出：`build/fix6-report.md`、`build/fix6-classification.md`、`build/flat-values-audit.md`（60 列，只列不改）。


## fix round 7（固定數值與平衡桿）

範圍：僅固定數值、G(L)、分類倍率與 MCM。未修改規劃、review 文件、fx_extract.py、MO2 或 SkyrimSE；沒有連網。來源 `build/flat-values-audit.md` 共 60 列，以出現順序編號 01–60。

| 狀態 | 項目 | 結果 |
|---|---|---|
| FIXED | 60 項固定數值 | 按核准建議套用；04、05、43、55、57–60 的基準保留。37 收割為 15／層。 |
| FIXED | 裁決／共用領域 | 每目標 25，單次裁決基準累計上限 100；29／30 共用聖域生命 25、魔力 20／秒。 |
| FIXED | G(L) | 55 個金額路徑各乘所屬樹 G 一次；5 個時長／保命項豁免。utility 不增乘 G。 |
| FIXED | 六類平衡倍率 | 傷害沿用 ESSB_BaseDamageMult；只新增其餘 5 個 float GLOB，各預設 1.0。 |
| FIXED | MCM | 一般＝開關、毒／血係數、除錯；平衡＝七支滑桿；技能樹原功能保留。 |
| FIXED | 建置及 FormID | 完整 python build_v03.py 成功；3907 舊記錄身分一致、3912 記錄回讀一致；19 支腳本 0 errors。 |
| NOT FIXED（核准豁免） | 毒／血層壽命滑桿 | 保留毒 12 格／12 秒、血 10 格／10 秒。固定每秒桶無法容納 3 倍壽命且保留原每秒老化與匯出含義；未改桶、tick 或匯出布局。 |
| NOT FIXED（驗證邊界） | 遊戲內實測 | 未操作或寫入正在運行的遊戲；編譯／資料回讀／離線公式測試不代表實際戰鬥或 MCM 畫面已驗證。 |

數值解讀：下表「新基準」不含新增 G 與分類倍率，保留原環境、層數、開印及終焉倍率。25 的上限採基準預算，最終治療為 `min(25×N,100) × environment × G(divine) × MultRecovery`；預算只限制回血，六目標傷害仍全數結算。11 汲力取 20 削耐的未分類金額一半，故兩類滑桿互不串乘。52 基準以既有土 B_max=10 ×4 表達，保留 settings 的 B 資料源。

保守解讀與邊界：時長採先乘倍率、正值最短 1 秒，整數法術／倒數器就近四捨五入；0 仍代表取消或永久，不轉成 1 秒。沉默「魔力鎖零／不回魔」、抵消命中與永久復生屬狀態語義，保留既有行為，不把它們變成可漏掉施法的比例削魔；沉默時長可調。冷卻排除 FX／logging／XP 限流及內部排程；每秒輪詢的擴散、雷雨充能和化身仍受原 tick 精度限制，不新增 subsecond tick。星痕引爆延遲依 59 保留。

| ID | 效果 | 舊基準 | 新基準 | 程式位置 |
|---|---|---|---|---|
| 01 | 血痕開印吸血 | 10 × 吸血比 × 開印倍率；基礎 0.5–5 | 50 × 原吸血比 × 開印倍率 | `src/ESSBReactions.psc:Open`（行 103） |
| 02 | 命中流血目標吸血（固定 B 基準） | B_max 10 × 吸血比 × 血位命中倍率；基礎滿血 0.65，低血 3；吸血主線滿點滿血 2.6、低血 3.9 | B_max ×5 × 原吸血比 × 血位命中倍率 | `src/ESSBElem2.psc:OnBloodHit`（行 223） |
| 03 | 切換受傷 -50% 視窗 | 1 秒 | 2 秒 | `src/ESSBController.psc:SwitchForm`（行 789） |
| 04 | 斷咒附帶沉默 | 1 秒（每 5 秒至多一次） | 1 秒（保留） | `src/ESSBNoForm.psc:OnManaBreak`（行 183） |
| 05 | 低點數沉默及首領折半 | 1+0.2×rank，四捨五入；rank1–2 實際 1 秒，首領最少1秒 | 1+0.2×rank，原四捨五入及首領折半（保留） | `src/ESSBNoForm.psc:ApplySilence`（行 194） |
| 06 | B_max 固定基準命中回血 | 10 × 0.02 × rank，滿點 3／擊 | 1 × rank | `src/ESSBElem2.psc:OnDivineHit`（行 235） |
| 07 | 風刃命中回耐 | 3／次 | 15／次 | `src/ESSBElem2.psc:WindBladeOne`（行 589） |
| 08 | 融斷每個印記回魔 | B_max ×0.5；依元素 = 3.5–12.5／印記 | B_max ×2／印記 | `src/ESSBNoForm.psc:OnBurst`（行 316） |
| 09 | 附近每名中毒敵人每秒回血 | 4.5／人／秒，最多 5 人 = 22.5 | 6／人／秒，最多 5 人 | `src/ESSBElem3.psc:PoisonFormTick`（行 1068） |
| 10 | 開印回血 | B_max 9 × 0.5 = 4.5 | 25 | `src/ESSBElem3.psc:OpenPoison`（行 343） |
| 11 | 命中削耐的一半回耐 | 0.25 × rank，滿點 3.75／擊 | 1.5 × rank | `src/ESSBElem2.psc:OnEarthHit`（行 178） |
| 12 | 開印固定回血 | 10 × 0.5 = 5；低血位 ×2 = 10 | 25，低血位 50 | `src/ESSBElem2.psc:OpenBlood`（行 352） |
| 13 | 聖印開印基礎回血 | B_max 10 ×0.5 ×環境 M ×開印 M；無分支滿點白天 8.7、夜晚 7.25 | 25 × 環境 M × 開印 M | `src/ESSBReactions.psc:Open`（行 108） |
| 14 | 熔身每秒回耐 | 5／秒 ×10 秒 = 50 | 20／秒 | `src/ESSBController.psc:TickTimers`（行 2892） |
| 15 | 冰封期間每秒削耐 | 5／秒 | 25／秒 | `src/ESSBElem.psc:OnFrozenTick`（行 515） |
| 16 | 命中回耐 | 5／擊 | 25／擊 | `src/ESSBElem2.psc:OnWindHit`（行 208） |
| 17 | 水域每秒回生命與耐力 | 各 B_max = 7／秒，5 秒各 35 | 生命與耐力各 15／秒 | `src/ESSBController.psc:TickDomain`（行 3527） |
| 18 | 開印額外回血 | 10 × 0.05 × rank，滿點 7.5 | 2 × rank | `src/ESSBElem2.psc:OpenDivine`（行 382） |
| 19 | 每次終焉回魔 | B_max；依元素 = 7–25 | B_max ×3 | `src/ESSBNodes.psc:OnEndReward`（行 241） |
| 20 | 每擊固定削耐 | 0.5 × rank；滿點 7.5 | 3 × rank | `src/ESSBElem2.psc:OnEarthHit`（行 178） |
| 21 | 引爆臨時生命護盾 | B_max 10 ×當次引爆層數，10 秒；同款 Peak Value Modifier 不逐次相加 | 25／引爆層 | `src/ESSBElem3.psc:OnAstralDetonate`（行 838） |
| 22 | 星痕引爆回血 | B_max 10 ×當次層數 | 20／引爆層 | `src/ESSBElem3.psc:OnAstralDetonate`（行 842） |
| 23 | 血池每秒回血 | B_max 10／秒，5 秒共 50 | 20／秒 | `src/ESSBController.psc:TickDomain`（行 3520） |
| 24 | 死咒結算吸血與吸魔 | 各 10 ×環境 M（夜晚 12） | 生命與魔力各 30 × 環境 M | `src/ESSBElem3.psc:AfterDeathCurse`（行 647） |
| 25 | 裁決回血（每名被裁決目標各一次） | B_max 10 ×環境 M，白天 12／次；範圍最多 6 次 = 72 | 25／目標，單次累計 min(25×目標數,100)，再乘環境 M | `src/ESSBElem2.psc:Judge`（行 709） |
| 26 | 命中治療每名同伴 | B_max 10／擊 | 25／同伴／擊 | `src/ESSBElem2.psc:OnDivineHit`（行 252） |
| 27 | 開印治療每名同伴 | B_max 10 | 40／同伴 | `src/ESSBElem2.psc:OpenDivine`（行 398） |
| 28 | 聖引預約下一開印治療玩家 | B_max 10 | 40 | `src/ESSBElem2.psc:OpenDivine`（行 402） |
| 29 | 聖域每秒回生命與魔力 | 各 B_max 10／秒，5 秒各 50 | 生命 25／秒、魔力 20／秒 | `src/ESSBController.psc:TickDomain`（行 3523） |
| 30 | 神聖領域同一回復路徑，延長時長 | 各 10／秒，8 秒各 80 | 同 29；同一領域，不另疊加 | `src/ESSBController.psc:TickDomain`（行 3524） |
| 31 | 聖印記融斷每目標額外治療 | 10 ×環境 M，白天 12 | 25 × 環境 M／目標 | `src/ESSBElem2.psc:EndDivineNodes`（行 867） |
| 32 | 連續三次命中回耐 | 25／三擊 | 60／三擊 | `src/ESSBNoForm.psc:OnCombo`（行 97） |
| 33 | 命中回魔 | B_max 10 | 25 | `src/ESSBElem3.psc:OnAstralHit`（行 301） |
| 34 | 開印回魔 | B_max 10 | 40 | `src/ESSBElem3.psc:OpenAstral`（行 426） |
| 35 | 詛咒開印吸魔／回魔 | 10 ×環境 M ×開印 M；滿點夜晚 17.4 | 40 × 原倍率 | `src/ESSBReactions.psc:Open`（行 121） |
| 36 | 命中詛咒目標吸魔／回魔 | B_max 10 ×環境 M，夜晚 12 | 30 × 環境 M | `src/ESSBElem3.psc:OnDarkHit`（行 259） |
| 37 | 詛咒目標死亡回魔 | B_max 10 ×詛咒層數，1–13 層 = 10–130 | 15／詛咒層 | `src/ESSBElem3.psc:OnKill`（行 1157） |
| 38 | 蝕魔終焉預約下一終焉吸魔／回魔 | 10 ×環境 M（夜晚 12） | 40 × 環境 M | `src/ESSBElem3.psc:OnAnyEnd`（行 1029） |
| 39 | 裂痕開印削耐 | 10 × 開印倍率（滿點 14.5） | 40 × 開印倍率 | `src/ESSBReactions.psc:Open`（行 92） |
| 40 | 裂痕開印回耐 | 10 | 40 | `src/ESSBReactions.psc:Open`（行 93） |
| 41 | 開印回耐 | 10 | 40 | `src/ESSBElem2.psc:OpenWind`（行 327） |
| 42 | 同調升段回生命與魔力 | 各 B_max ×2；依元素 = 14–50 | 生命與魔力各 B_max ×4 | `src/ESSBNodes.psc:OnSyncStage`（行 117） |
| 43 | 水印記融斷治療與回耐 | 各 B_max 7 ×2 ×afMult；倍率中含新通用／元素／無形態融斷節點 | 各 B_max ×2 ×afMult（保留基準 14） | `src/ESSBElem3.psc:EndWaterNodes`（行 971） |
| 44 | 開印回耐 | 15 | 50 | `src/ESSBElem.psc:OpenFire`（行 248） |
| 45 | 聖臨時玩家與附近同伴回血 | 各 B_max 10 ×2 = 20 | 玩家與同伴各 60 | `src/ESSBElem2.psc:OnFormOpened`（行 464） |
| 46 | 自燃回血 | B_max 12 ×2 = 24 | 60 | `src/ESSBElem.psc:OnIgnite`（行 402） |
| 47 | 影身觸發回魔 | B_max 10 ×2 = 20 | 50 | `src/ESSBElem3.psc:OnShadowBody`（行 1199） |
| 48 | 化灰回魔 | B_max 10 ×2 = 20 | 60 | `src/ESSBElem2.psc:OnAsh`（行 1014） |
| 49 | 感電開印削魔 | B_max 25 ×開印倍率，滿點 36.25 | 60 × 開印倍率 | `src/ESSBReactions.psc:Open`（行 87） |
| 50 | 被近戰命中時感電削魔 | B_max 25 | 50 | `src/ESSBGuard.psc:OnHitEx`（行 150） |
| 51 | 開印回魔 | B_max 25 | 60 | `src/ESSBElem.psc:OpenShock`（行 305） |
| 52 | 地震削耐基準及主線 | B_max 10 ×2 ×(1+0.03×rank) ×終焉 M；rank15 基準29 | 40 ×(1+0.03×rank) ×終焉 M | `src/ESSBElem2.psc:QuakeStamina`（行 490） |
| 53 | 裂痕削甲（主線追加） | 30 +2 ×rank；滿點 60，再乘開印 M；擴散目標不乘開印 M | 60+4×rank | `src/ESSBElem2.psc:FissureArmor`（行 412） |
| 54 | 每層岩甲護甲 | 15／層；基礎 5 層 = 75；萬象後 8 層 =120 | 20／層 | `src/ESSBElem2.psc:RockArmorPerLayer`（行 31） |
| 55 | 放電削魔及跳躍削魔 | 傷害未乘 G 的 amount ×0.5（電蝕 1.0）；跳躍再 ×0.4（電弧 0.8）；B_max=25 | amount ×原削魔比；跳躍仍 ×原跳躍比（保留） | `src/ESSBElem.psc:Discharge`（行 541） |
| 56 | 重擊消耗裂痕額外削耐 | 50 | 80 | `src/ESSBElem2.psc:OnEarthHit`（行 191） |
| 57 | 磐石每層岩甲護甲 | 25／層；厚土＋萬象最多 13 層 =325 | 25／層（保留） | `src/ESSBElem2.psc:RockArmorPerLayer`（行 29） |
| 58 | 滿電荷自動放電回魔 | B_max 25 ×charge；基礎 cap6=150；主線＋萬象 cap14=350 | B_max ×charge（保留） | `src/ESSBElem.psc:OnTick`（行 774） |
| 59 | 高點數星痕引爆延遲 | rank≥10 時 1 秒；否則2秒 | rank≥10 為 1 秒，否則 2 秒（保留） | `src/ESSBElem3.psc:AstralDelay`（行 813） |
| 60 | 神佑救命瞬間回血 | 1 HP，另給2秒守護 | 1 HP（保留） | `src/ESSBGuard.psc:OnHitEx`（行 66） |

### G(L) 單次縮放證據

`ESSBTrees.TreeG`＝`1.0 + 0.05 * TreeLevel(aiTree)`。下表列實際可 grep 的來源行與完整 utility 下游；`BaseMax`、`ReactDamage`、`HealAllies`、`Leech`、`ApplyUtil` 均不新增 G。回復／削減分類倍率只處理金額，不回寫 caller 的局部變數。傷害側 `ApplyDamage` 仍只在原 choke point 乘 G；放電的 utility 分支另乘，未提前污染共用 damage amount。

| ID | 所屬樹 | 唯一 G 來源（原碼） | 下游路徑 | G 次數 |
|---|---|---|---|---|
| 01 | 5 | `akCtl.Leech(50.0 * akCtl.GLevel(5) * akCtl.GetBloodLeechRatio() * mult)`；`src/ESSBReactions.psc:Open`（行 103） | Leech → ApplyUtil(4/19, abBalanced=True) | 1 |
| 02 | 5 | `Float amount = ESSBReactions.BaseMax(akCtl, 6) * 5.0 * akCtl.GLevel(5) * akCtl.GetBloodLeechRatio() * akCtl.GetDamageMult(6)`；`src/ESSBElem2.psc:OnBloodHit`（行 223） | Leech → ApplyUtil(4/19, abBalanced=True) | 1 |
| 03 | 豁免 | `SetGuardSwitch(2)`；`src/ESSBController.psc:SwitchForm`（行 789） | SetGuardSwitch → DurationInt；G 豁免 | 0 |
| 04 | 豁免 | `akCtl.ApplySilenceSpell(akTarget, 1)`；`src/ESSBNoForm.psc:OnManaBreak`（行 183） | ApplySilenceSpell → DurationInt；G 豁免 | 0 |
| 05 | 豁免 | `Float seconds = 1.0 + 0.2 * rank`；`src/ESSBNoForm.psc:ApplySilence`（行 194） | ApplySilenceSpell；G 豁免 | 0 |
| 06 | 6 | `akCtl.ApplyUtil(4, 1.0 * heal * akCtl.GLevel(6), 0, player)`；`src/ESSBElem2.psc:OnDivineHit`（行 235） | ApplyUtil(4) | 1 |
| 07 | 4 | `akCtl.ApplyUtil(6, 15.0 * akCtl.GLevel(4), 0, player)`；`src/ESSBElem2.psc:WindBladeOne`（行 589） | ApplyUtil(6) | 1 |
| 08 | 11 | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, aiElement) * 2.0 * aiMarks * akCtl.GLevel(11), 0, player)`；`src/ESSBNoForm.psc:OnBurst`（行 316） | ApplyUtil(5) | 1 |
| 09 | 7 | `heal = heal + 6.0 * akCtl.GLevel(7)`；`src/ESSBElem3.psc:PoisonFormTick`（行 1068） | heal 累加 → ApplyUtil(4) | 1 |
| 10 | 7 | `akCtl.ApplyUtil(4, 25.0 * akCtl.GLevel(7), 0, player)`；`src/ESSBElem3.psc:OpenPoison`（行 343） | ApplyUtil(4) | 1 |
| 11 | 3 | `Float drain = 3.0 * ESSBNodes.Rank(akCtl, 3, 0, 2) * akCtl.GLevel(3)`；`src/ESSBElem2.psc:OnEarthHit`（行 178） | drain ×0.5 → ApplyUtil(6)；同來源削耐走 ApplyUtil(3) | 1 |
| 12 | 5 | `Float heal = 25.0 * akCtl.GLevel(5)`；`src/ESSBElem2.psc:OpenBlood`（行 352） | 低血位 ×2 → ApplyUtil(4) | 1 |
| 13 | 6 | `akCtl.ApplyUtil(4, 25.0 * akCtl.GetDamageMult(7) * akCtl.GLevel(6) * mult, 0, player)`；`src/ESSBReactions.psc:Open`（行 108） | ApplyUtil(4) | 1 |
| 14 | 0 | `ApplyUtil(6, 20.0 * GLevel(0), 0, player)`；`src/ESSBController.psc:TickTimers`（行 2892） | ApplyUtil(6) | 1 |
| 15 | 1 | `akCtl.ApplyUtil(3, 25.0 * akCtl.GLevel(1), 0, akTarget)`；`src/ESSBElem.psc:OnFrozenTick`（行 515） | ApplyUtil(3) | 1 |
| 16 | 4 | `akCtl.ApplyUtil(6, 25.0 * akCtl.GLevel(4), 0, player)`；`src/ESSBElem2.psc:OnWindHit`（行 208） | ApplyUtil(6) | 1 |
| 17 | 8 | `ApplyUtil(4, 15.0 * GLevel(8), 0, player)`；`src/ESSBController.psc:TickDomain`（行 3527） | 生命／耐力各自同一 G → ApplyUtil(4/6) | 1 |
| 18 | 6 | `akCtl.ApplyUtil(4, 2.0 * rank * akCtl.GLevel(6), 0, player)`；`src/ESSBElem2.psc:OpenDivine`（行 382） | ApplyUtil(4) | 1 |
| 19 | current | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, aiElement) * 3.0 * akCtl.GLevel(TreeOf(akCtl.CurrentElement.GetValueInt())), 0, player)`；`src/ESSBNodes.psc:OnEndReward`（行 241） | ApplyUtil(5)；TreeOf(CurrentElement)，非 common 的 12 | 1 |
| 20 | 3 | `Float drain = 3.0 * ESSBNodes.Rank(akCtl, 3, 0, 2) * akCtl.GLevel(3)`；`src/ESSBElem2.psc:OnEarthHit`（行 178） | ApplyUtil(3)；回耐另取同一未分類 drain ×0.5 | 1 |
| 21 | 10 | `akCtl.ApplyUtil(19, 25.0 * aiLayers * akCtl.GLevel(10), 10, player)`；`src/ESSBElem3.psc:OnAstralDetonate`（行 838） | ApplyUtil(19) | 1 |
| 22 | 10 | `akCtl.ApplyUtil(4, 20.0 * aiLayers * akCtl.GLevel(10), 0, player)`；`src/ESSBElem3.psc:OnAstralDetonate`（行 842） | ApplyUtil(4) | 1 |
| 23 | 5 | `ApplyUtil(4, 20.0 * GLevel(5), 0, player)`；`src/ESSBController.psc:TickDomain`（行 3520） | ApplyUtil(4) | 1 |
| 24 | 9 | `Float gain = 30.0 * akCtl.GetDamageMult(10) * akCtl.GLevel(9)`；`src/ESSBElem3.psc:AfterDeathCurse`（行 647） | ApplyUtil(4/5) 各一次，不回寫 gain | 1 |
| 25 | 6 | `akCtl.ApplyUtil(4, afHealBase * akCtl.GetDamageMult(7) * akCtl.GLevel(6), 0, player)`；`src/ESSBElem2.psc:Judge`（行 709） | JudgeArea 分配 100 基準預算 → Judge → ApplyUtil(4) | 1 |
| 26 | 6 | `HealAllies(akCtl, 25.0 * akCtl.GLevel(6))`；`src/ESSBElem2.psc:OnDivineHit`（行 252） | HealAllies → ApplyUtil(4)；helper 不乘 G | 1 |
| 27 | 6 | `HealAllies(akCtl, 40.0 * akCtl.GLevel(6))`；`src/ESSBElem2.psc:OpenDivine`（行 398） | HealAllies → ApplyUtil(4) | 1 |
| 28 | 6 | `akCtl.ApplyUtil(4, 40.0 * akCtl.GLevel(6), 0, player)`；`src/ESSBElem2.psc:OpenDivine`（行 402） | ApplyUtil(4) | 1 |
| 29 | 6 | `ApplyUtil(4, 25.0 * GLevel(6), 0, player)`；`src/ESSBController.psc:TickDomain`（行 3523） | 生命 25G／魔力 20G → ApplyUtil(4/5) | 1 |
| 30 | 6 | `ApplyUtil(5, 20.0 * GLevel(6), 0, player)`；`src/ESSBController.psc:TickDomain`（行 3524） | 同 29 的 TickDomain；不重複領域回復 | 1 |
| 31 | 6 | `akCtl.ApplyUtil(4, 25.0 * akCtl.GetDamageMult(7) * akCtl.GLevel(6), 0, player)`；`src/ESSBElem2.psc:EndDivineNodes`（行 867） | ApplyUtil(4) | 1 |
| 32 | 11 | `akCtl.ApplyUtil(6, 60.0 * akCtl.GLevel(11), 0, player)`；`src/ESSBNoForm.psc:OnCombo`（行 97） | ApplyUtil(6) | 1 |
| 33 | 10 | `akCtl.ApplyUtil(5, 25.0 * akCtl.GLevel(10), 0, player)`；`src/ESSBElem3.psc:OnAstralHit`（行 301） | ApplyUtil(5) | 1 |
| 34 | 10 | `akCtl.ApplyUtil(5, 40.0 * akCtl.GLevel(10), 0, player)`；`src/ESSBElem3.psc:OpenAstral`（行 426） | ApplyUtil(5) | 1 |
| 35 | 9 | `Float drain = 40.0 * akCtl.GetDamageMult(10) * akCtl.GLevel(9) * mult`；`src/ESSBReactions.psc:Open`（行 121） | ApplyUtil(2/5) 各一次 | 1 |
| 36 | 9 | `Float drain = 30.0 * akCtl.GetDamageMult(10) * akCtl.GLevel(9)`；`src/ESSBElem3.psc:OnDarkHit`（行 259） | ApplyUtil(2/5) 各一次 | 1 |
| 37 | 9 | `akCtl.ApplyUtil(5, 15.0 * curse * akCtl.GLevel(9), 0, player)`；`src/ESSBElem3.psc:OnKill`（行 1157） | ApplyUtil(5) | 1 |
| 38 | 9 | `Float drain = 40.0 * akCtl.GetDamageMult(10) * akCtl.GLevel(9)`；`src/ESSBElem3.psc:OnAnyEnd`（行 1029） | ApplyUtil(2/5) 各一次 | 1 |
| 39 | 3 | `akCtl.ApplyUtil(3, 40.0 * akCtl.GLevel(3) * mult, 0, akTarget)`；`src/ESSBReactions.psc:Open`（行 92） | ApplyUtil(3) | 1 |
| 40 | 3 | `akCtl.ApplyUtil(6, 40.0 * akCtl.GLevel(3), 0, player)`；`src/ESSBReactions.psc:Open`（行 93） | ApplyUtil(6) | 1 |
| 41 | 4 | `akCtl.ApplyUtil(6, 40.0 * akCtl.GLevel(4), 0, player)`；`src/ESSBElem2.psc:OpenWind`（行 327） | ApplyUtil(6) | 1 |
| 42 | current | `Float amount = ESSBReactions.BaseMax(akCtl, akCtl.CurrentElement.GetValueInt()) * 4.0 * akCtl.GLevel(TreeOf(akCtl.CurrentElement.GetValueInt()))`；`src/ESSBNodes.psc:OnSyncStage`（行 117） | amount → ApplyUtil(4/5)；用當前元素樹 | 1 |
| 43 | 8 | `Float amount = ESSBReactions.ReactDamage(akCtl, 9, 2.0) * afMult * akCtl.GLevel(8)`；`src/ESSBElem3.psc:EndWaterNodes`（行 971） | ReactDamage 不乘 G；ApplyUtil(4/6)，不走 ApplyDamage | 1 |
| 44 | 0 | `akCtl.ApplyUtil(6, 50.0 * akCtl.GLevel(0), 0, player)`；`src/ESSBElem.psc:OpenFire`（行 248） | ApplyUtil(6) | 1 |
| 45 | 6 | `akCtl.ApplyUtil(4, 60.0 * akCtl.GLevel(6), 0, player)`；`src/ESSBElem2.psc:OnFormOpened`（行 464） | 玩家 ApplyUtil(4)；同伴 HealAllies → ApplyUtil(4) | 1 |
| 46 | 0 | `akCtl.ApplyUtil(4, 60.0 * akCtl.GLevel(0), 0, player)`；`src/ESSBElem.psc:OnIgnite`（行 402） | ApplyUtil(4) | 1 |
| 47 | 9 | `akCtl.ApplyUtil(5, 50.0 * akCtl.GLevel(9), 0, player)`；`src/ESSBElem3.psc:OnShadowBody`（行 1199） | ApplyUtil(5) | 1 |
| 48 | 6 | `akCtl.ApplyUtil(5, 60.0 * akCtl.GLevel(6), 0, player)`；`src/ESSBElem2.psc:OnAsh`（行 1014） | ApplyUtil(5) | 1 |
| 49 | 2 | `akCtl.ApplyUtil(2, 60.0 * akCtl.GetDamageMult(3) * akCtl.GLevel(2) * mult, 0, akTarget)`；`src/ESSBReactions.psc:Open`（行 87） | ApplyUtil(2) | 1 |
| 50 | 2 | `Ctl.ApplyUtil(2, 50.0 * Ctl.GetDamageMult(3) * Ctl.GLevel(2), 0, attacker)`；`src/ESSBGuard.psc:OnHitEx`（行 150） | ApplyUtil(2) | 1 |
| 51 | 2 | `akCtl.ApplyUtil(5, 60.0 * akCtl.GLevel(2), 0, player)`；`src/ESSBElem.psc:OpenShock`（行 305） | ApplyUtil(5) | 1 |
| 52 | 3 | `Return 4.0 * (1.0 + 0.03 * ESSBNodes.Rank(akCtl, 3, 2, 2)) * akCtl.GLevel(3)`；`src/ESSBElem2.psc:QuakeStamina`（行 490） | QuakeStamina → Quake.stamina → QuakeOne → ApplyUtil(3)；傷害 amount 另一支才進 ApplyDamage | 1 |
| 53 | 3 | `Return (60.0 + 4.0 * ESSBNodes.Rank(akCtl, 3, 0, 0)) * akCtl.GLevel(3)`；`src/ESSBElem2.psc:FissureArmor`（行 412） | FissureArmor → Open/OpenEarth → ApplyUtil(1)；擴散不乘開印 M | 1 |
| 54 | 3 | `Return 20.0 * akCtl.GLevel(3)`；`src/ESSBElem2.psc:RockArmorPerLayer`（行 31） | RockArmorPerLayer → SyncRockArmor → ApplyUtil(18) | 1 |
| 55 | 2 | `akCtl.ApplyUtil(2, amount * drainRatio * akCtl.GLevel(2), 0, akTarget)`；`src/ESSBElem.psc:Discharge`（行 541） | 直擊／跳躍 ApplyUtil(2) 各乘一次；amount 本身未乘 G，生命傷害另走 ApplyDamage | 1 |
| 56 | 3 | `akCtl.ApplyUtil(3, 80.0 * akCtl.GLevel(3), 0, akTarget)`；`src/ESSBElem2.psc:OnEarthHit`（行 191） | ApplyUtil(3) | 1 |
| 57 | 3 | `Return 25.0 * akCtl.GLevel(3)`；`src/ESSBElem2.psc:RockArmorPerLayer`（行 29） | RockArmorPerLayer → SyncRockArmor → ApplyUtil(18) | 1 |
| 58 | 2 | `akCtl.ApplyUtil(5, ESSBReactions.BaseMax(akCtl, 3) * charge * akCtl.GLevel(2), 0, player)`；`src/ESSBElem.psc:OnTick`（行 774） | ApplyUtil(5)；Discharge 不修改呼叫端 charge | 1 |
| 59 | 豁免 | `Return 1`；`src/ESSBElem3.psc:AstralDelay`（行 813） | AstralDelay；固定桶引爆延遲，G／Duration 豁免 | 0 |
| 60 | 豁免 | `Ctl.ApplyUtil(4, 1.0, 0, player, True)`；`src/ESSBGuard.psc:OnHitEx`（行 66） | 神佑保命 1 HP，G／Recovery 豁免 | 0 |

既有已乘 G 的破魔、反噬及生命傷害不再加第二次；破魔在 `OnManaBreak` 先算 G、再 `DrainAmount`、再限制可吸取魔力，`ApplyUtil(2, ..., True)` 跳過重乘，真傷沿用 `abLevelScaled=True`。百分比治療長流、血盾 20% 上限、抗性百分點不新增 G。

### 分類倍率與施放位置

| 分類／GLOB（FormID） | 唯一倍率位置 | 覆蓋呼叫端／例外 |
|---|---|---|
| 傷害／ESSB_BaseDamageMult（00515A；沿用） | ApplyProc、ApplyDamage、ApplyTrueDamage、ApplyUtil(7) | 各為互斥施放終點；ApplyDamageRaw 不增乘。既有元素直傷、真傷與放血；自損／施放代價／Execute 不作可調傷害。 |
| 持續傷害／ESSB_MultDot（005168） | ApplyDotDamage；ApplyUtil(7)；EndBlood 的 remaining 項 | Status.Tick 毒／催毒／流血；放血生命比例；TickDomain 死域；血潮只乘剩餘流血、不乘即時生命百分比。 |
| 冷卻／ESSB_MultCooldown（005169） | CooldownSeconds | OpenMark、OpenSecond、TakeEndSlot、TakeInterrupt、TakeIceHeart、TakeRetaliate、TakeSanctuary、TakeCleanse、TakePush、Guard.TakeAttacker、雷雨充能、AvatarCooldown、毒擴散、Trees.RespecReady。洗點以 86400 秒換回遊戲日。 |
| 回復／ESSB_MultRecovery（00516A） | ApplyUtil(4/5/6/10/11/18/19/25)；Leech→RecoveryAmount（下游 True）；RefreshRecovery；ApplyInherit | 全部玩家／同伴生命魔耐、長流、聖域、血池、潮池；岩甲／血盾／星盾；冰盾／電盾／水鏡／聖盾 PERK 強度；溫血／感應／星輝回復率；血承生命／護甲盾。神佑 1 HP 明確跳過。血盾在乘回復後套原 20% cap。 |
| 削減／ESSB_MultDrain（00516B） | ApplyUtil(1/2/3)；OnManaBreak→DrainAmount（下游 True） | 裂痕護甲、感電、放電、深寒、地震、震擊、破魔、蝕魔／暗開印／蝕魔終焉。QuakeOne 判定讀 DrainAmount 以與實際削耐一致；不另乘到 damage 或汲力。抗性百分點與沉默鎖零不變。 |
| 持續時間／ESSB_MultDuration（00516C） | DurationSeconds／DurationInt | ApplyMark 同時設定法術與 registry；ApplyUtil 的狀態 duration；ApplyManaBreakMark／ApplySilenceSpell／Fear／Frenzy／Reanimate／Inherit；StartDomain；保留副印記；自身狀態 setter；Status 的 Frozen、Catalyze、DeathCurse、Airborne、StarLock setter 與熱度／凍結／裂痕／失衡／聖印／浸濕／水壓／詛咒到期比較。pending 存原秒數、ImportState 不再縮放。 |

平衡頁所有 slider 均為 0.25–3.0、step 0.05；冷卻 max=2.0。節點倍率預設沿用 3.0，傷害預設沿用 1.0，五個新增倍率預設 1.0。EDID 不新增 ESSB_MultDamage，避免重複傷害控制與 FormID 改名。

驗證證據：`build/fix7-build.log`、`build/fix7-check.json`、`build/fix7-g-proof.json`、`build/fix5-mcm-check.json`、`build/v03-compile-results.json`。建置內繼續執行原 DOT、LAYOUT、FIX3、FIX4、MCM、FIX6、READBACK、PLAN、CSF、DELIVERY、PLAN COVERAGE、NODE INDEX、FX 與 ENGINE COVERAGE 檢查；新增 FIX7 檢查 5 GLOB 的型別／預設／VMAD、3907 舊 FormID、不同倍率的實際 utility 公式、裁決 1–6 目標上限、G 來源、固定匯出和編碼。

檔案編碼／換行：修改的來源沿用 UTF-8（無 BOM）及各檔原 LF／CRLF；`實作紀錄.md` 只在原始位元組尾部追加，原文未重寫。產物位於 `package/Elements Spellblade`，沒有安裝到 MO2 或遊戲。

## 2026-09-18 07:44 Asia/Taipei — fix round 7 驗收與部署（指揮官）

- 通過。指揮官重建 exit 0；3912 筆 == manifest；與 `.codex/pre-fix7-snapshot` 比對舊 3907 筆 0 變動，新增 5 個 GLOB（`ESSB_MultDot／MultCooldown／MultRecovery／MultDrain／MultDuration` 0x005168–0x00516C）；`MCM ok` 40 個引用；平衡頁七條桿子；五個倍率各只出現在 2–4 個咽喉點。
- 60 列固定數值：53 列上調並改乘所屬樹 G(L)（55 條路徑各乘一次），7 列依裁定保留；毒／血層壽命不受持續時間桿影響（環狀桶固定，核准豁免）。
- 部署：遊戲與 MO2 未執行時鏡像複製到 MO2（逐檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260918-074415`。
- 使用者方向（2026-09-18）：「末期這技能要能成為主要戰力」——節點倍率預設維持 3，所有數值以往上調為原則。


## fix round 8（維持費、無形態基礎、開印節點）

本輪為使用者明確核准的設計偏離：規劃 1.1 原先只有暗／血付維持費，現在所有形態都付較溫和的魔力維持費；新增無形態基礎真傷，並結清第 6 輪最後 14 列節點分類。`元素魔戰士規劃-v0.3.md`、review 文件及 `.strategic-advance/**` 不改。

| 狀態 | 項目 | 實作與證據 |
|---|---|---|
| FIXED | 所有 11 形態的魔力維持費 | `ESSBFormRules.MagickaUpkeep`：最大魔力 × 基礎百分比 / 100 × (1 − upkeep_level_relief × 該元素樹等級 / 100) × ESSB_MultUpkeep。等級讀該元素樹，限 1–100；一般基礎 1.0%，暗 2.0%，relief 0.7。每次扣魔最多到剩餘魔力量，避免透支。 |
| FIXED | 血形態維持 | 同時付上述魔力費，血位曲線／血氣折扣／血臨／血約／不死與留 1 HP 保護保持；血維持扣血額外乘一次 ESSB_MultUpkeep，讓 0＝免費成立。血重擊費不變。 |
| FIXED | MCM 平衡頁「維持費」 | 新增 float GLOB `ESSB_MultUpkeep`，`0x00516D`，預設 1.0；slider 0.0–3.0、step 0.1。原傷害／節點／持續傷害／冷卻／回復／削減／持續時間七項保持。 |
| FIXED | 魔力歸零自動關閉 | 既有每秒 `ESSBFormRules.OnUpdate` 首次觀察到 0 時記錄時間，持續為 0 至至少 2 秒後呼叫 `Ctl.CloseForm()`，只通知一次並立即 return。回魔取消寬限；再次耗盡重新計時；空魔切換元素不重設寬限；手動關閉清除計時。 |
| FIXED | 無形態基礎真傷 | `OnWeaponHit → OnNoFormHit → ApplyNoFormBaseline → ApplyTrueDamage`。預設 5 × G(L_無元素) × ESSB_BaseDamageMult，重擊 ×1.5；不要求購買節點，不吃 `TrueMult`，不額外給 XP；純武藝／破魔／餘燼的既有呼叫完整保留。 |
| FIXED | 覆蓋表新增列 | `plan_coverage.py` 的 `fix8 addition` 明記「文件化新增，非規劃節點」；生成列的 kind=mechanism、edid=-。規劃節點仍為 495，unmapped=0。 |
| FIXED | 11 個開印主線 | `ESSBElem.OpenMult` 改用 `ESSBNodes.Pct`；預設 NodeScale=3 時全部為「開印效果 +9%／點」。使用者核准整體效果一起縮放，包含層數／控制／回復。 |
| FIXED | 純武藝傳奇「終結」 | `ESSBNoForm.OnMartialHit` 的既有真傷係數改用 `ESSBNodes.Pct`；預設顯示 +9%／點。戰意滿層、重擊門檻及原真傷實作保持。 |
| FIXED | 血持續新手複合列 | `BleedTickMult` 的 +2%／點套 NodeScale，預設 +6%／點；`BleedDrainPercent` 的放血 +0.01%／點與 0.3% → 0.45% 完全不縮放，首領同比例規則保持。 |
| FIXED | 土關閉專精分類 | `QuakeStamina` 維持 +3%／點，不套 NodeScale；`QuakeOne → ApplyUtil(3)` 由 `ESSB_MultDrain` 套一次實際削耐。由 DECISION 改為 UNCHANGED，理由明記使用者核准。 |
| FIXED | 產生器與文字 | `build_v03.apply_fix8_decisions` 接在現有 plan 產生器後，原規劃與 `plan_trees.py` 不改。統一產出 ESP／CSF 參照的節點資料、`build/plan-tree-nodes.json` 及 `build/fix6-classification.md`；14 列全結清，195 主線中 118 列納入倍率，沒有 DECISION。 |
| FIXED | 既有身分與編譯 | 相對 `.codex/pre-fix8-snapshot/v03-formids.json` 的 3912 筆記錄身分全部相同，只追加一個 GLOB；`ESSB_DebugLevel=0x000811`。19 支腳本全部 0 errors；masters 只有 Skyrim.esm。 |
| NOT FIXED | 遊戲內實測 | 本輪只完成離線公式／事件狀態模擬、ESP 回讀及真實 Papyrus 編譯；未操作執行中的 Skyrim 或 MO2，因此通知顯示、排程延遲與存檔升級仍需遊戲內驗證。 |
| NOT FIXED | 原有穩步 DEFERRED | `ESSB_P_noform_0_0_B1` 格擋耐力消耗 -20% 的既有引擎進入點驗證仍未結清，非本輪範圍；PLAN COVERAGE 如實保留 1 個 DEFERRED，沒有 unmapped。 |

### 300 最大魔力：每秒維持費

以下為預設 `ESSB_MultUpkeep=1.0` 的毛扣費，未扣除原版／節點／裝備回魔。其他元素含血，血另付原血位規則的生命維持費。滑桿 0、0.5、3 分別使表內扣費成為 0、半額、三倍；等級 100 為一般 0.3%／暗 0.6%。

| 該元素樹等級 | 其他元素最大魔力％／秒 | 其他元素魔力／秒 | 暗最大魔力％／秒 | 暗魔力／秒 |
|---:|---:|---:|---:|---:|
| 1 | 0.993% | 2.979 | 1.986% | 5.958 |
| 25 | 0.825% | 2.475 | 1.650% | 4.950 |
| 50 | 0.650% | 1.950 | 1.300% | 3.900 |
| 75 | 0.475% | 1.425 | 0.950% | 2.850 |
| 100 | 0.300% | 0.900 | 0.600% | 1.800 |

`settings.json` 新增 `upkeep_base_pct=1.0`、`upkeep_dark_pct=2.0`、`upkeep_level_relief=0.7`、`noform_base_true=5.0`，以及 GLOB 預設來源 `mult_upkeep=1.0`。前三個 upkeep 設定以 float VMAD 綁到 FormRules；noform_base_true 綁到 Controller.NoformBaseTrue。基礎設定由建置讀入，遊戲中的維持費 slider 直接讀寫 ESSB_MultUpkeep。0 免扣維持費；若被其他來源耗盡魔力，自動關閉條件仍成立。

### 自動關閉與倍率只套一次的證據

正常關閉鏈：`ESSBFormRules.OnUpdate → ESSBController.CloseForm → OnFormClosed → OnBurstTarget / EndBothMarks`，完整保留融斷、釋放特效、同調保留與後續節點。沒有自行只清 GLOB 或移除形態能力來繞過融斷。觀察到空魔的 tick 記為 t=0，t≥2 秒才關閉；每秒排程與 Papyrus 實際負載可能使觀察／執行較晚。只用原有 `RegisterForSingleUpdate(1.0)` 三處入口，沒有新增迴圈、Utility.Wait、RegisterForUpdate 或逐幀 polling。

`ToggleForm` 的原開啟門檻判斷與節點豁免、切換不檢查魔力的分支逐字保持；沒有每擊扣魔。`OnWeaponHit` 全函式與原版相同，因此資格篩選（啟用開關、格擋／盾擊／爆炸排除、存活目標、武器類型、投射物條件）與無形態的一次命中 XP 都沿用。破魔、雷、暗既有回魔路徑不改。

| 路徑 | 上游 | 唯一倍率套用處 | 驗證 |
|---|---|---|---|
| 無形態基礎真傷 | ApplyNoFormBaseline 只取 base，重擊只乘 1.5 | `ApplyTrueDamage(amount, target, 11, False, False, True)`：GLevel(11) 一次、BaseDamageMult 一次；最後 True 只略過 TrueMult | 5 等級 × 4 傷害倍率 × 普通／重擊＝40 組；另測零節點與 base=0。動態計數 G 呼叫為 [11]；額外 XP=0。舊真傷呼叫預設仍套 TrueMult。 |
| 魔力維持費 | 最大魔力、元素基礎％、自身樹等級折扣 | MagickaUpkeep 的 MultUpkeep.GetValue 一次 | 11 元素 × 5 等級 × 5 倍率（0／0.5／1／1.7／3），並檢查元素樹 index 0–10；不經 Drain、Recovery、Duration 或 BaseDamageMult。 |
| 血生命維持費 | 原 BloodDrainPerSecond（含血位與節點折扣） | 呼叫 PayBloodCost 前乘 MultUpkeep 一次 | 5 倍率驗證；下游 PayBloodCost 不再乘，原留 1 HP 保護不動；BloodPowerCost 不改。 |
| 開印／終結／流血主線 | 原每點係數與 rank | 每一個核准項各呼叫一次 ESSBNodes.Pct | 4 NodeScale 值 × rank 0／1／15；11 開印 132 組。放血係數與地震削耐係數在各 NodeScale 下保持不變。其他獨立節點加成仍可正常疊加。 |
| 原有五個分類 slider | 第 7 輪既有 producer／delivery | 各原唯一入口保持 | FIX7 的 utility、G、裁決上限與固定狀態版面檢查全數通過；原五個 GLOB 身分／預設／VMAD 保持。 |

### 建置交付與驗證界線

已親自執行 `python build_v03.py`，exit 0。檢查包含：DOT ok、LAYOUT 13/13 ok、19 支 Papyrus 全部 0 errors、FIX3／FIX4／FIX6／FIX7／FIX8 ok、MCM 41 refs（38 GLOB + 3 QUST）全部解析成功、READBACK masters=['Skyrim.esm']／records=manifest=3913、CSF 13/13、DELIVERY ok、PLAN COVERAGE 542 rows／495 nodes／0 unmapped、NODE INDEX 421 calls／0 out of range、FX 與 ENGINE COVERAGE ok。

證據：`build/fix8-build.log`、`build/fix8-check.json`、`build/v03-compile-results.json`、`build/fix5-mcm-check.json`、`build/fix6-classification.md`、`build/plan-coverage.json`。本輪檢查納入 build 主流程，可重跑；舊 FIX6 檢查擴充新核准節點，舊 FIX7 身分檢查精確允許新增的 ESSB_MultUpkeep，其餘斷言保留。

產物：`package/Elements Spellblade`。`.codex/impl-fix-round8.html` 為逐項進度；`build/fix8-before` 保存修改前內容，`build/fix8-scope-before.json` 驗證範圍外檔案不變。來源沿用各檔原 BOM／LF／CRLF，本文只在原位元組尾端追加，不正規化先前混合換行。未存取網路，未寫入 MO2 或 SkyrimSE，未安裝或宣稱完成遊戲內驗證。

## 2026-09-18 08:02 Asia/Taipei — fix round 8 驗收與部署（指揮官）

- 通過。指揮官重建 exit 0；3913 筆 == manifest；與 `.codex/pre-fix8-snapshot` 比對舊 3912 筆 0 變動，新增 `ESSB_MultUpkeep` 0x00516D；`FIX8 ok`。
- 指揮官補丁（使用者 2026-09-18 追加）：血形態不扣魔力維持費，也不受魔力歸零自動關閉（`ESSBFormRules.OnUpdate`，element 6 跳過 `MagickaUpkeep` 與歸零判定）；`build/fix8_verify.py` 對應案例改為「血形態魔力不變」。
- 部署：遊戲與 MO2 未執行時鏡像複製到 MO2（逐檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260918-080246`。


## FIX9 — 舊存檔 schema 換代與 Papyrus flood HOTFIX

### 結果與交付

- 交付：`package/Elements Spellblade/`。親自執行 `python build_v03.py`，exit 0；完整輸出：`build/fix9-build.log`。未部署，未寫入 MO2 或 SkyrimSE，未存取網路；未進遊戲實測。
- `READBACK ok: masters=['Skyrim.esm'] records=3915 manifest=3915`；`CSF ok` 13/13；`DELIVERY ok`、`LAYOUT ok`、`DOT ok`、`MCM ok`、`FIX7 ok`、`FIX8 ok`、新增 `SCHEMA ok`／`FIX9 ok`；`PLAN COVERAGE` 542 rows、0 unmapped。既有「穩步」進入點的 1 項 DEFERRED 保留。
- 19 支打包腳本全部 exit_code 0（`build/v03-compile-results.json`）。所有既有來源維持 UTF-8／BOM 與原有 LF 或 CRLF；本段以 bytes append 加入，既有紀錄（包含原本混合換行）完整保留。

### A：陣列判斷與初始化

- `src/*.psc` 全掃描：原有 63 處陣列 `== None` 全改成 `!arrayVar`；沒有留下陣列 `!= None`。`InitFixState`／`InitRegistry` 各有單一成功旗標 `FixInitialised`／`RegistryInitialised`，成功後直接返回；`InitTables`、Guard 陣列亦有旗標，不再於每個 tick 重測／重建所有陣列。
- 配置後立即檢查，失敗呼叫 `BreakState()`；`StateBroken` 永久鎖住該 instance，先關閉 Ready、取消更新／武器事件，再顯示一次 MessageBox。沒有重試／清除 latch 的路徑。OnInit、OnPlayerLoadGame、OnUpdate、排程、hit、kill、mark/status callbacks、form powers、MCM／技能樹入口均受此狀態限制；所有 init 呼叫後也立即檢查，不會繼續索引失敗陣列。
- 訊息指引玩家退出遊戲、安裝「已提升 state_schema_version 並重新建置」的完整更新包後重載原存檔；無需清存檔。這是維護者的建置版本，不是讓玩家修改 JSON 就能修復。

### B：可延續的 save schema

`settings.json` 新增 `state_schema_version: 2`。未版本化的 rounds 1–8 視為 schema 1。只有兩個 QUST 使用 schema 配號，其他 record 身分固定。

| EDID | schema 1 / pre-fix9 ID | schema 2 ID／用途 |
|---|---|---|
| ESSB_MainQuest | 000800 | 006000，新 Player alias 0，掛 ESSBController／ESSBTrees／ESSBGuard |
| ESSB_MCMQuest | 00515B | 006001，新 ESSBMCM 與 SKI_PlayerLoadGameAlias |
| ESSB_MainQuest_Schema1_Stub | 無此 EDID | 000800，保留舊 form，無 VMAD／aliases／start-game-enabled |
| ESSB_MCMQuest_Schema1_Stub | 無此 EDID | 00515B，同上 |

- `build/state_schema.py` 的 `quest_ids(v)`：v=1 使用原 ID；v>=2 從 006000 起每代配置 2 個 ID；所有較舊版本逐代生成 stub，從不回收。schema 3 的 active IDs 為 006002／006003，schema 1、2 均留下 stub。專用範圍 006000–006FFF，超界直接失敗；HEDR `next_object_id` 隨 active IDs 推進，目前 006002。
- MCM 也換代：它本身保存 Quest instance、繼承 MCM/SkyUI 狀態，且 `Controller` 可能指向舊主 Quest；只換主 Quest 不足。回讀盤點只有這兩個 QUST 承載腳本。
- `own(ID_QUEST)`、`own(ID_MCM_QUEST)` 統一使用動態配號。驗證器真正解析每個 VMAD 的 scalar／array object refs 及 alias fragment refs，確認沒有指向任何 retired quest；SEQ 只有兩個新 active IDs。MCM JSON 的 41 個參照可解析，3 個動作皆指向新 MCM Quest。13 個 CSF 使用既有 GLOB／PERK 身分，不帶舊 Quest ID。
- 與 `.codex/pre-fix9-snapshot/v03-formids.json` 比對，3913 筆原身分中只有上述 2 個 active QUST 改 ID，新增 EDID 只有上述 2 個 stub；其餘 3911 筆完全相同。全部 150 GLOB、3226 PERK、120 SPEL、134 MGEF ID 不變，`ESSB_DebugLevel = 0x000811`。
- 離線成立的依據：新 QUST FormID 會取得全新、型別完整的 script／alias instance；舊 FormID 留下無 attachment 的 stub，依本輪提供的 Papyrus 存檔行為，舊 instance 在載入時被丟棄，可能出現一次 orphan warning。ESP 結構、參照及程式安全邊界已回讀／執行驗證；實際引擎丟棄行為沒有冒稱為已遊戲實測。

### schema 維護規則（必要）

1. **任何 persisted script 新增、移除、改名或改型別 member variable／有儲存狀態的 Property，都必須把 `state_schema_version` 加 1。** 陣列和 scalar 都算；改繼承型別、增刪 instance script 也算。僅函式內 local variable、函式邏輯、文字或預設數值變動，無 layout 變動時不必 bump。改變既有狀態語意或需要拋棄舊戰鬥狀態時，即使 signature 相同也應 bump。
2. `state-schema.lock.json` 是需隨源碼一起保存／提交的 schema 資產，保存版本、完整 signature、SHA-256、配號及歷史；不要刪除或手動改 hash 來繞過版本檢查。建置最開始執行 preflight，同版本 signature 不同會立即失敗，尚未產生 ESP／PEX。降版、跳號、lock 內容不一致也失敗。
3. 版本恰好 +1 時，建置將前一版摘要移入 history，更新 lock 並生成新 active quests 與所有舊 stub。每次已發布版本都必須保留歷史；不可把版本改回去重用 Quest IDs。若建置中途失敗而再修改 layout，需繼續 bump，或在**尚未發布**的同一輪開發中先還原上一個已發布 lock，再重新產生本輪 lock；不可改已發布版本的 signature。
4. signature 涵蓋本包 11 支有 instance 的腳本，另包含本機 3 個 MCM/SkyUI 編譯祖先介面；Hidden 全域函式腳本無 instance，locals 不納入。外部 MCMHelper/SkyUI 的真實 PEX 未被此編譯介面 hash 取代。
5. MGEF 的 ID 保持固定。短期 `ESSBStatus`／`ESSBMark`／`ESSBCounter`／`ESSBSilence` 以保存的 `Controller` Quest 與生成的 `ESSBState.ControllerQuest()` 比較世代；舊效果直接停止處理，等待自然到期。`ESSBSilence` 的舊 instance 沒有 Controller，因此安全返回。今後新增其他持久效果也必須保留這個世代入口；永續效果必須在新 Setup 重掛。`ESSBFormRules` 已如此處理。

### C：其他錯誤及判定界線

- 摘錄 histogram 的總計是 `Cannot create an array into a non-array variable` 181993 次；使用者另指出其中 InitFixState 約 105k。`Cannot access an element of a None array` 33348 次，以及 InDomain／DomainFlag／TickTimers 的無效讀取，是配置失敗後繼續執行的下游後果；schema 換代及 init 後返回共同封堵。
- `Cannot cast from None to ...[]` 是 A 的獨立根因，不能只靠換代修掉。
- `value()`／GetValue／GetValueInt／SetValue 的 None 接收物件符合舊 instance 缺少新 property 的問題；新 VMAD 重新綁定，Setup 在使用前驗證所有必要 GlobalVariable、Spell、Trees 與陣列／object 陣列元素。缺少必要 binding 也直接熔斷。摘錄只提供首 400 筆 ESSB 命中行與 aggregate histogram，未提供每個錯誤旁的完整 stack，因此不能宣稱 101 次 `value()` 都已逐筆定位到相同 property。
- 額外補上 `ESSBElem.Shatter` 的空控制器／空 target 提早返回，及 IsDead 前 target 檢查；Status frozen FX 與 update 路徑保留／補強 Holder 檢查。`ESSBCounter.OnEffectFinish` 不再於 AME native binding 已拆除後呼叫 UnregisterForAnimationEvent，FormRules／Silence 的 finish 同樣交由引擎解除註冊。
- histogram 也含 `newsparks`、`FloraHarvestScript`、invalid sound、對話等錯誤；在現有摘錄無法配對 ESSB stack 的項目不臆測為本模組，也未修改其他模組。本包没有 Sound.StopInstance／IsInDialogueWithPlayer 呼叫。

### D：保留進度與載入一致性

- 新 Quest 的第一次成功 Setup 才執行 `ReconcileLoadedForm()`：FormActive／CurrentElement／Sync 及戰鬥狀態鏡射歸零、移除所有 11 個 FormAbilities，移除旧 FormRulesAbility 再加回以建立新 AME，移除舊岩甲 buff；RefreshAbilities 依保留的 perks／新非形態狀態重算條件能力。
- SettingsPower／11 個 FormPowers 使用 HasSpell 判斷，只補缺少項；BaseRulesPerk 也僅在缺少時加入。舊 SPEL／MGEF IDs 全保留；form power cast 使用生成的 current Quest resolver。技能樹等級、Ratio、perk points、洗點冷卻、已購 perks 與設定 GLOB 不重設。
- `FirstSetupDone` 隨新 instance 保存；同 schema 後續讀檔不會再次強制關閉合法的現行形態，重複 Setup 也不反覆重掛隱藏能力。

### 離線驗證

- `build/fix9-runtime-check.json`：直接執行實際 Papyrus 函式內容（native 邊界 mock），逐一讓 67 個 Controller、6 個 Trees、2 個 Guard、4 個 Status 陣列的賦值失敗，共 **79** 例；每例最多一次配置失敗、一次訊息，後續 init/load/update/hit/kill/form/回呼不重試。另測 5 個必要 VMAD binding 缺失。
- 初始化成功後以「讀取 truthiness 就拋錯」的陣列替身驗證快路徑；40 次重複 Setup／init 不重建陣列、不重複增加 powers、不重掛 FormRules，保留 progression/settings。舊 AME 以任何 orphan 物件存取都拋錯的替身驗證先行隔離。
- schema 新增／移除／retype、降版、跳號反例失敗；單純增加 local 變數不影響 signature；下一版配號及 stub 累積經檢查。ESP 有 3915 個唯一 records，無新 masters。
- 原 FIX3–FIX8 驗證只針對兩個 Quest schema 例外、入口 safety guard 與新增的 resolver 調整，仍檢查非 Quest 身分、既有序列化 layout／公式／entry points／upkeep／CSF。原 FIX8 scope snapshot 早於使用者既有的部署備份、pre-fix9 snapshot 和 campaign ledger 變動；這些既存檔案改用本輪 protected hash 固定比對，未寫入 `.strategic-advance/**`。
- `build/fix9-protected-hashes.json` 驗證禁止修改的檔案未在本輪後續工作中變動；`實作紀錄.md` 僅附加。本包未加入 Utility.Wait 或 RegisterForUpdate。

### 剩餘 None 比較（全部是非陣列）

執行 `rg -n '[A-Za-z0-9_]+ (==|!=) None' src -g '*.psc'`（等價於指定 grep；PowerShell 不展開 src/*.psc 給 rg）。只剩 6 行、8 個 scalar object 比較：

| 檔案／行 | 左側變數 | 宣告型別／用途 |
|---|---|---|
| ESSBController.psc:1301、1304 | akProjectile | Projectile 事件參數 |
| ESSBGuard.psc:75、98 | akProjectile | Projectile 事件參數 |
| ESSBGuard.psc:75 | sourceSpell | Spell，來源施法判斷 |
| ESSBPlayerAlias.psc:34 | akProjectile | Projectile；歷史來源，v0.3 不編譯／不附掛 |
| ESSBTrees.psc:449 | player、branch | Actor、Perk，HasPerk 前物件判斷 |

### 遊戲內驗證界線

退出遊戲後安裝完整 `package/Elements Spellblade/`（包含 ESP、19 PEX、SEQ、MCM/CSF），直接載入原有存檔，不需 clean-save。預期首次新 schema 關閉舊形態且保留進度，powers 能再次開啟。若需要 [ESSB] gameplay 訊息，可暫將 MCM 除錯等級設為 1 後重載；預設仍為 0。須以新一輪 Papyrus log 確認沒有持續刷錯，才能宣稱實際遊戲問題已解除。

## 2026-09-18 21:15 Asia/Taipei — fix round 9（緊急修正）驗收與部署（指揮官）

- 起因：使用者第二次實機（round 1–8 版，載入仍保有 round 1 腳本實體的存檔）模組完全不運作，Papyrus.0.log 179 MB／190 萬行：`Cannot create an array into a non-array variable` 10.5 萬次（存檔內既有實體無法建立後加的陣列成員）、`Cannot cast from None to X[]` 1.7 萬次（round 4 引入的 `陣列 == None` 無效寫法，全新存檔也會刷）。指揮官 round 4 驗收未察覺此寫法，列為驗收疏失；此後驗收加掃 `(==|!=) None` 左側是否為陣列。
- 通過。指揮官重建 exit 0；3915 筆 == manifest；`SCHEMA ok version=2`、`FIX9 ok`。獨立讀回：`ESSB_MainQuest` 0x000800→0x006000、`ESSB_MCMQuest` 0x00515B→0x006001，舊編號各留一個無 VMAD、flags 0、無 alias 的空殼；其餘記錄編號 0 變動（`ESSB_DebugLevel` 0x000811）；SEQ 指向兩個新任務；MCM config 只引用 0x006001；剩餘 None 比較皆非陣列。
- 維護規則：persisted script 的成員變數有增刪或改型別時，必須把 `settings.json` 的 `state_schema_version` 加一；`state-schema.lock.json` 會讓建置在簽章變了但版本沒升時失敗。
- 部署：遊戲未執行（MO2 開著，只放檔案、不碰 profile）時鏡像複製到 MO2（逐檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260918-211525`。


## Fix round 10：致死元素、重複擊殺、形態循環音、無形態門檻

已完成 A–D 與完整 build（exit 0）。詳見 `build/fix10-report.md`、`build/fix10-check.json`、`build/fix10-build.log`。

- A：移除全域 LastDamageElement，8 格 RegLastDamage 與 128 格 Actor 後援記錄這名受害者的傷害。ApplyProc／ApplyDamageRaw／Execute／ApplyTrueDamage／ApplyUtil(7) 統一透過 ApplyTrackedDamage；非致死立即歸零、已死不覆寫；OnKillEvent 將 killingElement 快照交給化灰／亡者歸來，淨土例外仍需持有分支與當前神聖三段。
- B：log 存在 FC000800 orphan controller 與 FC006000 現行 controller；主事件區 91 名死者各 2 次 kill。原 8 格 ring 各屬自己的實例，並非每次被清空。加入 canonical alias 身分檢查與舊 Guard／Trees／MCM 入口隔離，保留 claim-before-dispatch。
- C：採規劃 2.12 的單次事件音方案，在 build_v03.py 把 FormActive／DrawSheathe 的 SNDR.LNAM[1] 設為 0，實際改 20 筆，其餘聲音欄位不變。Release／Charge／OnHit 本來就是單次；55 筆全數回讀通過。沒有 Sound.StopInstance，也沒有重複停止無效 handle。FrozenShader.Play 是 shader，已有 Stop；光環／領域 ambient 音由引擎效果生命週期持有。
- D：5.1 純武藝／破魔僅無形態生效；ESSBNodes.Rank／Br gate tree 11 route 0/1，有形態時回 0/False。route 2 融斷的明訂過渡效果保留；不改數值。
- schema 2 → 3（新增／移除成員）：ESSB_MainQuest 006000 → 006002、ESSB_MCMQuest 006001 → 006003；舊 ID 成為 Schema2 stub，先前 stub 保留。其他既有 FormID 不變，ESSB_DebugLevel 000811。state-schema.lock.json 正式記錄簽章與 history；VMAD／SEQ／MCM 已更新。
- A/B/C/D 每項皆確認 pre-fix 失敗、修正後通過。19 scripts 編譯 0 errors，3917 records=manifest，masters 僅 Skyrim.esm，CSF 13/13，READBACK／DELIVERY／LAYOUT／DOT／MCM／SCHEMA／FIX7／FIX8／FIX9／FIX10 全 ok，PLAN COVERAGE 0 unmapped。
- 證據檔後半段有重複 context：主事件區 1479 ESSB、182 kill、73 ash；全檔有 85 ash。原摘要「無 ESSB stack 錯誤」被舊 FC000800 stack 證據推翻。
- 未連網、未部署、未寫入 MO2／SkyrimSE，未修改規劃或 campaign ledger；尚未遊戲內驗證。不回復舊版已化灰的屍體。舊版已遺失的音效 handle 不能追溯停止；完全退出遊戲後再驗證新包。

Round 10 最終補記：破護的引擎 PERK 加上 FormActive=0 CTDA，避免 2 秒視窗跨入有形態狀態；數值不變。已重新完整 build exit 0，ESP 875536 bytes，3917 records。最終壓縮包為 build/Elements-Spellblade-fix10.zip。

## 2026-09-18 22:56 Asia/Taipei — fix round 10 驗收（指揮官）

- 起因（使用者第三次實機，round 9 版）：模組運作正常（1423 筆 [ESSB] 事件、無我方錯誤），但四個缺陷：(A) 神聖形態之後，黑暗／星界擊殺全部誤觸化灰 85 次，且化灰屍體被標記不可復生，導致 32 次黑暗擊殺只成功復生 1 次；(B) 每個擊殺與每次技能升級都跑兩次（升級變成每級 2 點）；(C) 形態音效無限循環，關形態也停不掉；(D) 無形態樹的「反噬」在有形態時仍觸發。
- Astra 根因：(A) `LastDamageElement` 是無主的全域，改為逐目標致死元素，`ShouldAsh` 改讀該victim的 element；(B) 舊 schema 的 controller 實例與新實例並存，兩邊都派發事件，加入實例身分檢查；(C) 改為修正 SNDR descriptor 為 one-shot（20 筆），不改 Papyrus 播放路徑；(D) 純武藝／破魔限無形態生效，保留融斷過渡。
- 通過。指揮官重建 exit 0；3917 筆 == manifest；19 腳本 0 errors；`FIX10 ok`（A–D 修正前失敗、修正後通過）。schema 2→3：`ESSB_MainQuest` 0x006000→0x006002、`ESSB_MCMQuest` 0x006001→0x006003，舊編號再留兩個空殼（共 4 個），其餘記錄 0 變動，`ESSB_DebugLevel` 0x000811。
- 指揮官獨立讀回：59 筆 SNDR 的 LNAM Looping 旗標只剩 1 筆為 Loop（`ESSBFX__VENOM_PoisonCloakSFX`），且經全記錄交叉參照確認無任何記錄引用它（孤兒備份複本），不會播放。
- 尚未部署：驗收時遊戲執行中。待使用者關閉遊戲後鏡像複製。


## Round 11：弓／弩命中入口修正

- 本輪只修正 `ESSBController.OnWeaponHit` 的來源解析與拒絕紀錄；未增加事件入口、成員變數、master、等待迴圈或更新訂閱。`ESSBGuard.psc` 維持逐 byte 不變。
- 根因界線：舊程式在 `akSource as Weapon` 失敗後把型別當成 0，有投射物即退回。離線用相同事件分別傳 `Ammo`、`None` 可重現無 proc、無印記、無反應且無紀錄。這證明丟棄路徑，尚未證明遊戲中 PO3 真正傳入哪種來源。
- `vendor/imports/PO3_Events_Alias.psc` 與 MO2 已安裝 `powerofthree's Papyrus Extender/Source/scripts/PO3_Events_Alias.psc` 完全相同；都只宣告 `Form akSource`。本機發行包沒有 C++ 事件派發原始碼；沒有使用網路。
- 另外讀取同一發行包的 `SKSE/Plugins/po3_PapyrusExtender.pdb`，從 CodeView `LF_ENUMERATE` 讀回 `RE::HitData::Flag`：`kBlocked=1`、`kBlockWithWeapon=2`、`kBash=16384`、`kTimedBash=32768`、`kExplosion=1048576`，合計 `1097731`；`kSneakAttack=2048`、`kPowerAttack=65536` 不在遮罩中。因此保留所有既有拒絕位元。旗標值可證實，實際箭擊是否帶有該遮罩位元仍須遊戲紀錄確認。
- 新增無狀態 `ResolveHitWeaponType`：`Weapon` 採原武器類型；`Ammo`、`None`、與 `akProjectile` 相同的 `Projectile` 僅在有投射物且攻擊者裝備弓／弩時回退。支援右手及左手查詢；其他來源一律 -1。`None` 且無投射物只在雙手裝備類型皆為空手時接受為 0，避免把法術或未知 Form 當成拳腳。
- 弓／弩仍須帶投射物，近戰仍須無投射物；8 法杖、非武器來源、爆炸、盾擊與格擋均拒絕。遠程 `power = sneak`，普通箭擊不因 65536 取得重擊倍率，潛行箭擊走既有 ×1.5 路徑。來源回退後的武器也傳給既有無形態命中路徑。
- 每個入口拒絕分支呼叫 `LogRejectedHit`，包含 not-operational、disabled、blocked-bash-explosion、invalid-actor、unsupported-source-or-weapon、ranged-without-projectile、melee-with-projectile、invalid-element。輸出 `[ESSB][hit-reject][L3] target=... reason=... sourceType=... sourceFormID=... weapon=... projectile=... flags=... rejectedMask=...`；FormID 及 FormType 為 Papyrus 原生整數表示，None 為 0。level <3 先退出，不探查 Form、不組字串；使用既有 `LogThrottled` 的 0.5 秒重複抑制、20 行／秒及 dropped 計數。
- `ESSBGuard.OnHitEx` 沒有同樣的全域武器來源轉型 gate。8 個 Weapon／Ammo／None／Projectile × 弓／弩事件，均執行岩甲、冰盾、水鏡消耗各一次，並不觸發近戰限定反震；保留原設計。
- 驗證直接執行選定 Papyrus 函式內容，原生 API 使用明確 mock：86 個攻擊案例含所有 0–7、9 武器、空手、staff、spell、explosion、bash、blocked、來源回退及潛行倍率；同一近戰主事件加投射物副事件、法術副事件只產生一次 proc／XP／同調。`ApplyProc`、`OnValidHit`、`InstallMark`、`OpenMark` 實際執行到 mock 原生施法及 reaction 呼叫邊界，不冒稱完整 Skyrim 排程／傷害引擎模擬。
- `build/fix8_verify.py` 原本要求整個命中事件逐字不變；本輪改為 gate 後的既有機制仍逐字相同（只排除新增 invalid-element 紀錄），gate 由 `build/fix11_verify.py` 行為案例驗證。`build/papyrus_harness.py` 加入 Weapon／Ammo／Projectile 語法與可選的紀錄執行；FIX11 使用嚴格原生轉型 mock，並真正執行所有拒絕及節流測試。首次完整建置在 FIX9 舊測試替身缺少裝備 API 處停止；修正舊測試的紀錄跳過規則後完整重建成功，未改 FIX9/FIX10 規則。
- `python build_v03.py` exit 0：19 scripts 全部 0 errors / 0 warnings；READBACK masters `['Skyrim.esm']`、records=manifest=3917；CSF 13/13；DELIVERY、LAYOUT、DOT、MCM、SCHEMA、FIX9、FIX10 均 ok；PLAN COVERAGE 542 rows、0 unmapped；新增 `FIX11 RANGED ok: 86 outgoing cases; 8 incoming arrows; proc/mark/reaction; melee duplicate gate; rejected-hit throttle; FormIDs unchanged; schema 3`。
- 與 `.codex/pre-fix11-snapshot/v03-formids.json` 比對 3917 筆完全相同；`ESSB_DebugLevel=0x000811`；`settings.json` 與 `state-schema.lock.json` 未變，`state_schema_version=3`，本輪沒有增加成員變數，因此不升版。FIX10 檢查輸出的「兩個 quest ID changed」是相對其第 10 輪前基線，本輪沒有任何新增變更。
- 原有編碼、CRLF／LF 保留，實作紀錄只追加；規劃檔、review-*.md、fx_extract.py hash 未變。未讀写 `.strategic-advance/**`；沒有寫入 MO2 或 SkyrimSE，沒有部署到執行中的遊戲。
- 交付：`build/Elements-Spellblade-fix11.zip` 與 `package/Elements Spellblade/`；完整建置紀錄 `build/fix11-build.log`，案例與 PDB 證據 `build/fix11-check.json`，修改差異 `build/fix11.patch`，進度 `.codex/impl-fix-round11.html`。
- 驗證界線：未進行遊戲內測試。來源缺失時只能採命中當下的裝備；箭在空中便卸下／換掉弓弩可能被拒絕，會有 level 3 診斷。`None`+projectile 的識別依賴 PO3 的 weapon-hit 事件來源及裝備弓弩，沒有增加 magic/projectile-hit 訂閱。下一次在測試存檔開 level 3，確認 `[ESSB][hit][L2]` 出現 `weapon=7/9` 並有 proc／印記／反應；如仍無反應，交叉檢查 `hit-reject` 的來源類型、FormID 與旗標。level 3 的預期負載不代表正式遊戲手感。

## 2026-09-18 23:12 Asia/Taipei — fix round 11 驗收與 round 10+11 部署（指揮官）

- 起因：使用者回報弓箭無法觸發效果。證據：`.codex/smoke3-essb-events.log` 整場 244 次命中全為 weapon=0／1／5，weapon=7（弓）與 9（弩）各 0 次，而規劃 2.1 明列支援。
- Astra 修法：遠程入口支援 `akSource` 為 `Weapon`／`Ammo`／`None`，必要時回退玩家當下裝備的弓弩；潛行射擊取得重擊倍率；近戰重複 gate 與法杖／法術／爆炸／盾擊／格擋排除保留；各拒絕分支新增 `[ESSB][hit-reject][L3]` 診斷（原因、來源型別與 FormID、武器型別、投射物、旗標）。未放寬旗標遮罩（以 PDB 確認 1097731 正確）。
- 通過。指揮官重建 exit 0；3917 筆 == manifest；與 `.codex/pre-fix11-snapshot` 比對 FormID 0 變動、0 新增，schema 維持 3；`FIX11 RANGED ok`（86 個攻擊案例、8 個受箭案例）；19 腳本 0 errors。
- 部署：遊戲未執行時把 round 10＋11 一起鏡像複製到 MO2（54 檔逐檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260918-231221`。
- 指揮官實測量化（`.codex/smoke3-essb-events.log`，命中→傷害落地延遲）：同秒 25、延遲 1 秒 34、2 秒 6、4 秒 1、7 秒 1；單秒最多 8 筆傷害。已列入審查範圍 G（每次命中的呼叫成本與可快取項目，只出報告不實作）。
- 使用者新需求（待第十二輪）：MCM 平衡頁加「回復預設設定」按鈕。


## fix round 12（選單與 MCM 預設）

記錄時間：2026-09-19 00:08 +08:00（執行前已查詢本機時間）。Skyrim SE 1.5.97；本輪只更新工作區 source/build/package，未安裝或寫入 MO2／SkyrimSE，未使用網路。權威工作清單是完整讀取的 `review-2026-09-18.md` findings 1–5 與 G1–6；findings 6–13 不屬本輪，沒有順手修改。

| 工作 | 狀態 | 實作與驗證邊界 |
|---|---|---|
| 0a-1 持續命中餓死 tick | FIXED | `ScheduleTick`／`ScheduleTickInternal` 只採較早 deadline；完成 `Tick` 才重建下次 deadline。35 秒、每 0.5 秒命中的 source harness 共跑 35 次 update dispatch；另實跑 TickTimers／SwapHosts 呼叫與 lift 較早期限。 |
| 0a-2 一般載入停止 FormRules | FIXED | 同世代暫時 NotReady 使用既有單次 update 重試；stale quest／StateBroken 立即退出。AME 啟動先註冊 `ESSB_FormRulesReady`，每次 controller Setup 都送出 wake-up，重置空魔起點並 re-arm 現存效果；2 秒寬限與血形態豁免回歸通過。 |
| 0a-3 application uptime 跨程序凍結 | FIXED | load 完成重設短期 cooldown／XP 視窗／log／push／lift／宿主請求等時戳；現存 status 的短期狀態時間重新起算。7200→60 秒負差回歸通過。換宿備份在匯入後只對未來時間戳重新起算，正常同 uptime swap 不改時間。 |
| 0a-4 直接命中友軍排除 | FIXED | 在 no-form／傷害／印記分流前檢查 `IsPlayerTeammate()`、`IsCommandedActor()`；隊友、召喚與復生在有／無形態皆拒絕，普通中立目標仍可被主動命中。 |
| 0a-5 身分 guard 熱路徑重複 | FIXED | 事件入口與每次 native 傷害前保留身分確認；同一已檢查交易走明確 Internal lookup，沒有 persisted「已授權」旗標。代表情境 152→2；舊世代 callback、受害者傷害歸屬與 duplicate kill/XP 回歸維持通過。 |
| 0b G1–6 | FIXED（見下方範圍） | 全 13 樹 direct-index rank／branch／level 快取、runtime knobs、同調值、局部 resource mirror、slot／damage-index 復用、空 lift 跳掃描、呼叫端 log gate；代表情境總呼叫 920→163，低於 320。 |
| 1 技能樹選單 | FIXED（source／offline） | AME 一次處理旗標＋canonical quest 排除＋alias picker／queued／opening lock；Show 返回只交出選項，0.2 秒單次 update 才開 CSF。慢 MessageBox 留 queued 等 close event 再排單次更新，無輪詢。StatsMenu close 以捕獲的 PendingTree 結清，reconcile 期間也鎖住選單；stale PendingTree 下次開啟補救保留。 |
| 1 遊戲內不再閃退、確實收到原生 close | NOT FIXED／未能宣告已驗證 | 這是原生執行期驗收，未啟動 Skyrim／安裝套件／取得本版新日誌。下面的 harness 是 source＋明示 UI mock，不能冒充引擎實測。 |
| 2 MCM 確認後恢復預設按鈕 | NOT FIXED／DEFERRED | 依本輪明確指示延期，不新增 reset action 或第二份預設常數。 |
| 2 預設值可見與 build 比對 | FIXED | 10 個既有 slider 的文字由 `settings.json` 產生工廠值，config defaultValue 與回讀 ESP GLOB 的 float32 逐筆完全一致；`MCM DEFAULTS ok`。 |
| 2b L0 日誌成本 | FIXED | `CachedDebugLevel` 在 init/load、設定力量改級與 MCM 的 Journal close 刷新；105 個呼叫端先比 Int 再組字串。L1 `LogEvent` 使用 cache，不查 Form；L2/L3 沿用原 `LogThrottled` 的 0.5 秒去重、20/s 與 dropped。高等級代表情境的輸出文字逐條相同。 |
| 3 交付紀錄與建置 | FIXED | 本節 append-only；進度表 `.codex/impl-fix-round12.html`；完整 build exit 0，驗證及量測 JSON 見下方。 |

### 選單：確立的事實、根因範圍與未證實部分

1. 題目提供的 Papyrus 摘錄是本輪 runtime 證據：23:18:23 `open tree common`，23:18:25 才 `[ESSB][Init] ready`；23:18:43 同秒有 common/current；後續 open 都補做 `refresh tree=11`／`reconcile tree=11`。它證明重疊開啟與 PendingTree 正常 close 路徑未完成的現象，秒級時間戳不能辨認 cast identity 或原生 teardown 的微秒順序。
2. 實讀 MO2 `Elements Spellblade/Source/Scripts/ESSBSettingsEffect.psc`、`ESSBTrees.psc`、`ESSBController.psc`，移除建置 BOM 後與 `.codex/pre-fix12-snapshot/src` 完全一致。已安裝 CSF 的 `Source/Scripts/CustomSkills.psc` 宣告 `OpenCustomSkillMenu` 為 native，沒有「Show 返回後引擎已完成拆除」契約；`StatsMenu` 名稱沿用已核對的正確值。
3. 能直接確立的 source 根因是：舊 Settings 沒有每效果去重或 alias 共用排他；阻塞 `Message.Show()` 返回後同一 continuation 直接 `OpenTree`；舊 `OpenTree` 不區分仍開著／仍排隊與真正 stale 的 PendingTree，任何第二次呼叫都先 Reconcile 再重新開。這些路徑在 pre-fix source harness 可重現雙 picker 與同 continuation native open。
4. 指揮者提出的「MessageBox 拆除把新選單關掉」與上述路徑相容，**尚未以本機引擎事件或 DLL 執行追蹤證實／排除**；本輪不宣稱它是唯一已證實根因。修法隔離此時序風險，並堵住已確定的重入。旧 AME 的 GetController 曾重新導向 canonical alias；現在先比較原 Controller，避免 round 10 同類 stale-instance 重新派送，但現有 log 無 instance/cast ID，不能斷言 23:18:43 必然來自舊世代。
5. 現在每次 cast 的 `Handled` 在任何跨腳本呼叫之前設定；alias `BeginSettings` 先確認 controller 與 tree Ready，否則顯示「元素魔戰士：尚未就緒」。選項只排隊一次；CSF 開啟前 snapshot＋註冊 StatsMenu，正常 close 捕獲同一 tree 並鎖住補扣流程。首次 deferred event 若 MessageBox 仍在，保留 queued 等事件，不忙等、不 Utility.Wait。

### G1–6 的實際範圍與快取生命週期

- G1/G5：13×15 格 rank／branch 分成 120+75 的兩組陣列，各 tree 有 valid 與 level cache，避免三槽 cross-tree miss；沒有跨 engine event 沿用 guard 授權。`RefreshActive` 對未初始化樹及 active/noform/common 冷刷新，Reconcile／RespecTree／OnCustomSkillIncrease 刷新受影響樹；load、form change、MCM close 重讀 runtime 倍率／duration／同步門檻／Sync。未初始化有原始查 perk fallback。G(L) 與固定倍率由已快取 level/rank/NodeScale 計算；依目標、血量、同調段、60 秒持有門檻的部分仍即時計算，避免改變公式或過期時機。
- G2：此代表情境的 GLOB GetValue/GetValueInt 34→5；Sync setter 每次更新鏡射 cache，包括 AddSync 後的新值。快取只服務讀取，權威 globals 保留。設定 cache 在 MCM 關閉後刷新；本輪沒有承諾繞過 MCM 直接用 console 改 GLOB 可立即更新 cache，需重新開關 MCM 或讀檔。
- G3：AddSelf／SetSelf／ClearSelf 只更新對應資源，數值未變不重寫；冰盾、電荷消耗與單項 tick 亦局部更新。`PushSelf` 保留於整批清空／狀態對帳等冷路徑。普通雷擊不再讀其他六個 resource mirror。
- G4：命中傳遞已解出的 slot/generation；native 傷害前後 damage index 以 actor 檢查復用，保留每受害者 attribution。代表情境 registry 搜尋 5→1，damage cache 搜尋 2→1。未取得或 owner/generation 已變的索引回退至安全查找。
- G5：沒有 queued lift 時 ArmUpdate 不掃 8 格；有 lift 仍取最早 due，使用原單次更新佇列。直接 cache 索引免 CacheSlotOf 搜尋。
- G6/2b：熱路徑全數先 gate；L1 hot logs 改用 cache-based LogEvent 保留非節流語義。未改的 legacy cold logs 是 `ESSBSettingsEffect.ToggleEnabled/CycleDebugLevel` 與 `ESSBFormPowerEffect` 起動／未就緒紀錄；手動 DumpRegistry/DumpTree 的 L0 Trace 仍是明確要求的診斷輸出。

### 每擊量測（與審查完全相同情境）

schema-ready、weapon=5、flags=0、普通非潛行近戰、雷形態、slot 0 已有雷印／活宿主、DamageActor[0]、全節點 rank=0、雷等級 1、Sync=0、XP 可用、DebugLevel=0。使用 `build/papyrus_harness.py` 的实际 source bodies，`build/fix12_cost.py` 補型別/cast、Papyrus 字串加法及明示 native mocks；原始 snapshot 確認完全重現審查所有關鍵數值。

| 每擊項目 | pre-fix12 | round12 |
|---|---:|---:|
| Scripted calls（含入口/global helpers） | 532 | 115 |
| Native calls | 388 | 48 |
| 合計（不混 Auto getter） | 920 | 163 |
| IsCurrentController | 152 | 2 |
| Game.GetFormFromFile / Quest.GetAlias | 160 / 152 | 2 / 2 |
| GLOB reads | 34 | 5 |
| 跨 instance methods（不含 Auto getter） | 159 | 27 |
| 外部 Auto property reads（另外計） | 206 | 83 |
| 字串串接（L0） | 8 | 0 |
| 記錄函式呼叫（L0） | 1 | 0 |

L0 的舊 1 次 logging call 是同 controller 的 `LogThrottled`，不是 159 次 cross-instance method 的全部；兩者不可混為一談。新 L0 無 logging method/global helper 呼叫。把 Auto getter 另外加總時為 1126→246。L3 為 533+395→117+55，兩条 proc/hit 訊息內容相同，串接同為 34。這些是明示情境的來源路徑計數，**不是** FPS、毫秒延遲、全元素平均或原生 VM profiler。

### MCM defaults（按鈕仍延期）

唯一數值來源為 build-time `settings.json`。六個 `ESSB_Mult*` 已包含 `ESSB_MultUpkeep`，所以總共是 10 個 tunable slider，沒有把 upkeep 重複算第 11 個。config 保存完整數值，文字為 6 位有效數字；ESP 比對使用一致的 float32 編碼。

| GLOB／slider ID | settings.json 預設 |
|---|---:|
| `ESSB_PoisonDotK` | `0.21164021164021163` |
| `ESSB_BleedDotK` | `0.11428571428571428` |
| `ESSB_BaseDamageMult` | `1` |
| `ESSB_NodeScale` | `3` |
| `ESSB_MultDot` | `1` |
| `ESSB_MultCooldown` | `1` |
| `ESSB_MultRecovery` | `1` |
| `ESSB_MultDrain` | `1` |
| `ESSB_MultDuration` | `1` |
| `ESSB_MultUpkeep` | `1` |

### Schema、短期狀態取捨與驗證

`state_schema_version` **3→4**：新增快取陣列、cache validity、便宜 debug level 與選單排程/去重成員，依 round 9 必須遷移，沒有手改同版 signature hash。`state_schema.preflight()` 自動保留歷史並產生 schema 4 lock。

- `ESSB_MainQuest`：`006002`→`006004`；`ESSB_MCMQuest`：`006003`→`006005`。舊兩筆保留無 VMAD／alias／start flag 的 Schema3 stubs。
- 與 `.codex/pre-fix12-snapshot/v03-formids.json` 比對，只有上述兩個 quest identity 變化；`ESSB_DebugLevel` 仍 `000811`，其他既有 FormID 不變。VMAD／SEQ／MCM refs 都指新世代，master 仍只 `Skyrim.esm`。
- 跨程序載入採「重置短期戰鬥狀態」策略：短期 cooldown 重新可用，combo／資源衰減起點重新起算；既有印記 deadline 設為 load 完成後 1 秒，交原 end/reconcile 路徑處理，lift 清空；status 的有限狀態期限重新起算，既有 DOT buckets／秒計時器保留；洗點的 game-time 冷卻不變。這是修復 uptime 凍結的明示取捨，不是精準保存每項剩餘秒數。沒有改樹等級、技能 XP ratio、點數或已購 perks。
- 完整 `python -B build_v03.py` 本機執行 **exit 0**，`build/fix12-build-final.log` 保存 stdout/stderr。19 scripts 全 OK；DOT／LAYOUT／MCM／MCM DEFAULTS／SCHEMA／FIX6–FIX12 全 OK；READBACK masters `['Skyrim.esm']`，records=manifest=3919；CSF=13/13；DELIVERY OK；PLAN COVERAGE unmapped=0。
- `build/fix12-check.json` 保存 5 個 pre-fix 負向重現、正向 source 回歸、完整逐函式/逐 native/Auto getter 計數、身份變動與預設表。`build/fix12_verify.py` 額外涵蓋慢 MessageBox、重複 AME/close、同樹分支補扣、stale 補救、早按通知、舊世代退出、callback/load 順序、負 uptime、友軍、lift、cache fallback/更新及 L3 文字等價。原 FIX11 節流測試仍檢查 0.5 秒、20/s、dropped；FIX10 attribution 與雙實例保護保留。
- 舊 verifier 的 fixture 新增目前 cache API；過時的「固定 schema 3／本輪不得新增 single update」文字 scope assertion 改成現行 schema／非 quest identity／相應事件回歸。沒有跳過遊戲機制測試，也沒有對 `.strategic-advance/**` 做 hash assert。
- 保留原檔 BOM/換行規則（`build_v03.py` 全 CRLF），本檔 append-only；未修改設計、review、`fx_extract.py` 或 campaign ledger。

交付：`package/Elements Spellblade/`。未執行安裝、存檔載入與原生 UI 驗證；本版安裝後仍需確認一次開啟→停留→購點→關閉的 StatsMenu lifecycle，並以新日誌辨認正常 close reconcile 而非每次 stale fallback。


### Round 12 收尾補證：Ready 過早發布（追加，不改寫前述紀錄）

進一步檢查確立了「初始化完成前仍能開選單」的直接 source 原因：pre-fix `ESSBController.Setup()` 在 `RefreshTrees()`、`RefreshAbilities()`、`EnvCheck()` 前就設 `Ready=True`，而 `IsOperational()` 只看這個 Ready。因此這三段跨腳本初始化仍在執行時，Settings 可以合法通過 guard；`[Init] ready` 則在三段完成後才輸出。這個缺陷與題目 23:18:23／23:18:25 的先後相容，無須假定 Show 自己繞過 IsOperational。

已修正為 Setup 入口先清 Ready，所有初始化／load clock reset 完成並再確認 canonical identity／StateBroken 後，才發布 Ready、註冊 weapon callback、發送 FormRules wake-up；原生 AME 若在這段期間收到 update，FormRules 與 Status 都區分暫時 NotReady 與 stale/broken，只前者重排單次 update。沒有把 Ready=False 的同世代永久餓死。

`fix12_verify.py` 新增實跑 Setup 的第六個 pre-fix 負向重現：舊 body 在 RefreshTrees 時錯誤回報 operational；新版在三個初始化階段各插入一次設定力量，均只顯示「尚未就緒」，Setup 返回才 operational。回歸數從上表原先 5 組增為 **6 組**；每擊計數仍 **115 scripted + 48 native = 163**。MessageBox teardown 是否導致本機引擎關閉新樹，仍保留未實測的界線，未以這個 Ready 缺陷冒充全部閃退原因。

## 2026-09-19 00:17 Asia/Taipei — 最終審查、指揮官弓弩補丁與 fix round 12 驗收部署

- 最終審查（Astra 唯讀，28 分鐘）產出 `review-2026-09-18.md`：13 項發現（5 個 P1、8 個 P2），逐項附 file:line、重現、設計依據、最小修法與信心度，另含 F 真實日誌核對與 G 每擊成本量測。指揮官確認它只寫了該報告檔。
- 指揮官弓弩補丁（在 round 11 之後）：round 11 保留了「遠程必須帶 Projectile」的條件，而本機 log 實測 PO3 的 `akProjectile` 為 None，且 Phenderix 的同名事件完全不看 `akProjectile`、只認武器型別 7／9。移除該條件並同步更新 `build/fix11_verify.py` 三個案例；重建通過後於 23:33 部署。
- fix round 12 驗收通過：指揮官重建 exit 0；3919 筆 == manifest；19 腳本 0 errors；`FIX12 ok`、`MCM DEFAULTS ok`。schema 3→4（`ESSB_MainQuest` 0x006002→0x006004、`ESSB_MCMQuest` 0x006003→0x006005，舊編號累積 6 個空殼），其餘記錄 0 變動。
- 內容：審查 P1 第 1–5 項（tick 餓死、讀檔停掉維持費、重啟後時間戳凍住、直接命中未排除同伴與僕從、身分確認重複）、技能樹選單（去重、排他鎖、改為監聽 MessageBoxMenu 關閉後 0.2 秒再開）、`Setup()` 過早發布 Ready、日誌先 gate 再組字串。
- 每擊成本（審查的代表情境，harness 實測）：532 scripted + 388 native = 920 → 115 + 48 = 163；身分確認 152 → 2；L0 字串串接 8 → 0；跨 instance 呼叫 159 → 27。
- 剩餘 `(==|!=) None` 6 處，指揮官逐一確認皆為 Projectile／Spell／Actor／Perk，非陣列。
- 待辦：審查 P2 第 6–13 項、MCM 回復預設按鈕（本輪依指示延期）。


## fix round 13（審查 P2 與 MCM 預設）

本輪依 `review-2026-09-18.md` 第 6–13 項、round 12 已接受來源與 `.codex/pre-fix13-snapshot/` 執行。進度頁先建立於 `.codex/impl-fix-round13.html`，每項離線反例與回歸通過後更新。原審查、規劃文件與歷史紀錄均未改寫。

| 項目 | FIXED / NOT FIXED | 審查診斷是否成立 | 修正與驗證 |
|---|---|---|---|
| 6 雷附傷 50% 削魔 | FIXED | 成立。普通／重擊附傷只有 Health 效果，`ApplyProc` 沒有削魔。 | 普通及重擊雷 SPEL 的第二個效果共用既有自有 `ESSB_UtilEffect_DrainMagicka`；每次 `ApplyProc` 設為該次 magnitude × 0.5，再套既有 `DrainAmount` 一次。普通、重擊與「極致」額外 proc 都走同一法術、同一次套用。不回玩家魔力、不轉真實生命傷害。 |
| 7 第九目標擠掉副印 | FIXED | 成立。舊 `EndMark` 提升副印後，`ClearSlot` 直接丟棄。 | `EndBothMarks` 新增 local parameter `aiReason=1`；逐出使用 reason 3。先副後主、共用一次 `TakeEndSlot`，清除兩顆實印 AME；殘印不冒充實印過期。兩次結算之間及最後清格前保留 actor／generation 檢查。 |
| 8 滿岩甲反震 | FIXED | 成立。先扣一層使一般滿層判斷永遠失敗。 | `ESSBGuard` 先取 `rockBefore`，交給 `OnEarthRetaliate` 判斷；成功先清層再傷害，失敗才一般扣層。滿層、未滿、冷卻、未購節點、遠程、山岳免扣層均回歸。 |
| 9 岩甲護甲自行到期 | FIXED | 成立。30 秒 buff 與「數值相同就返回」合併後會留下有層無護甲。 | 同 FormID 的 `ESSB_Util_ArmorBuff` 改為 Constant Effect／Self Ability，MGEF No Duration。`SyncRockArmor` 以層數與既有 Recovery 倍率設定，加／移能力；相同值且能力與效果仍存在才早退。零層、反震清層、關形態會移除；同 schema 載入時原 `RefreshRecovery` 會清掉舊 timed cast 並重掛。沒有新增計時器。 |
| 10 六元素開印倍率 | FIXED | 成立。火／雷／土／血／聖／暗未乘已核准整體倍率；土回耐亦漏乘。 | 六處 `OpenStacks × mult` 使用既有 `RoundStochastic`，土玩家回耐補 `mult`；不改 `AddStack`／`AddSelf` 上限路徑。rank 10、NodeScale 3、mult 1.9 用兩組隨機值驗證向上／向下取整，並覆核冰／風／毒原有縮放。 |
| 11 斷咒與冰心空耗冷卻 | FIXED | 成立。兩個 `Take*` 都在條件判斷之前寫時間戳。 | 斷咒先判施法，冰心先判玩家生命 <30%，才預約冷卻。未成立時時間戳不變，下一秒成立立即觸發；真觸發後仍受 5／30 秒冷卻。 |
| 12 反咒 AME native 生命期 | FIXED | 成立。舊 AME 在跨腳本查詢後自行 RegisterForAnimationEvent，晚到事件也未檢查 Listening。原審查日誌的 native 失綁符合此路徑；離線測試不推論遊戲內發生頻率。 | 註冊／取消與動畫處理移至既有穩定 `ESSBController` ReferenceAlias；每個 actor 的自有破魔印 native active effect 決定到期資格，含持續時間倍率。AME 只通知開始／結束，保留成員 layout；舊 AME 動畫事件為 inert。刷新中的舊 finish 不取消新印；失效／死亡／未知事件／舊 alias 均拒絕。註冊後、讀 cost 後及反咒查樹後再檢查，測試 finish 插入註冊與查詢的情境。 |
| 13 斷咒讀不存在 graph 變數 | FIXED | 成立。舊 `IsCasting` 讀 IsCastingRight／Left／Dual，缺變數會錯誤返回 False。 | 依本地 Actor API 讀裝備 Spell 槽 0 左、1 右、2 Other、3 Instant，以及裝備 Shout，僅對非空 magic item 呼叫 `PO3_SKSEFunctions.IsCasting`。不再讀三個 graph 變數；雙手、雙持施法、其他槽、語音、閒置、空裝備及 None 均回歸。原日誌不證明每次錯誤當刻都真的在施法。 |
| MCM 平衡頁「恢復預設設定」 | FIXED | 使用者新增需求，非審查錯誤；round 12 的延期已解除。 | 先 `ShowMessage` 確認，確認返回後再檢查 controller／operational，成功後重設頁面並通知。重設 10 個滑桿，含一般頁兩個 DoT 係數；取消與失效確認不寫值。 |

本輪要求內 **NOT FIXED：無**。Skyrim 實機、存檔 VM 排程與 UI 互動不在離線通過宣告內，尚未執行；不把 mock 原生呼叫視為實機驗證。

### MCM 預設的單一資料源

`settings.json → write_mcm` 的既有 slider rows 同時產生 `config.json` 的文字／defaultValue，以及 `ESSBState.RestoreTunableDefaults()` 的直接 GLOB 寫入。既有 `MCM DEFAULTS` 對 ESP float32 GLOB 的比對保留；本輪再執行產生出的函式，比對全部 10 個目標與數值，沒有手寫第二份 reset 常數表，也沒有新增 stored property／array。

10 個項目為 `ESSB_BaseDamageMult`、`ESSB_NodeScale`、`ESSB_MultDot`、`ESSB_MultCooldown`、`ESSB_MultRecovery`、`ESSB_MultDrain`、`ESSB_MultDuration`、`ESSB_MultUpkeep`、`ESSB_PoisonDotK`、`ESSB_BleedDotK`。六個 Mult* 已包含 Upkeep，不重複計數。Enabled、DebugLevel、技能等級、點數與其他設定不屬重設目標。確認後刷新 runtime cache、樹、能力與 Recovery，讓新值立即供後續交易使用。

### 離線回歸與建置

- `build/fix13_verify.py` 執行實際 Papyrus function/event bodies，native 明示 mock；舊來源取自 `.codex/pre-fix13-snapshot/src`。共 **10 組修正前失敗、修正後通過**：6、7、8、9、10、11-斷咒、11-冰心、12、13、MCM。詳細反例、正例、records 與成本記於 `build/fix13-check.json`。
- ESP 回讀：雷普通／重擊 SPEL 均有 2 個效果，第二顆為 Magicka AV 25；岩甲 SPEL 為 type 4、casting 0、delivery 0，MGEF No Duration、DamageResist AV 39，duration 0。
- 親自執行完整 `python -B build_v03.py`，exit 0；`build/fix13-build-final.log` 保存輸出。19 支腳本全部編譯成功、0 errors。READBACK masters `['Skyrim.esm']`，records=manifest=3919；CSF 13/13；DELIVERY、LAYOUT、DOT、MCM、MCM DEFAULTS、SCHEMA、FIX10、FIX11、FIX12、FIX13 均 ok；PLAN COVERAGE 542 rows、0 unmapped。既有「穩步」entry-point DEFERRED 保留，不屬此次 P2 或 MCM 需求。
- 舊 verifier 僅將「不得有 RestoreDefaults」的延期斷言改為必須存在，以及 MCM confirmation/action 數量更新；既有機制與數值驗證未跳過。
- 同一代表普通雷命中成本為 **116 scripted + 50 native = 166**；round 12 為 163，差額是新削魔的 `DrainAmount`、GLOB 讀取與第二效果 magnitude 寫入。兩次 identity checks、L0 零字串拼接保持；沒有復原每擊查表成本。MessageBoxMenu 關閉後 0.2 秒開樹機制未改，FIX12 原回歸仍通過。

### 相容性、交付與權限界線

`state_schema_version` 維持 **4**。沒有新增／刪除／改名／改型別任何持久成員或 instance script，也不需拋棄既有戰鬥狀態，因此依 round-9 規則不 bump。`settings.json` 與 `state-schema.lock.json` byte-for-byte 未變；目前 signature 通過同版本 lock。

相對 `.codex/pre-fix13-snapshot/v03-formids.json`，**3919 筆完整 record identity 全相同、0 新增、0 變更**；`ESSB_DebugLevel=0x000811`，主 Quest 0x006004、MCM Quest 0x006005。沒有新增 masters。岩甲同 ID 的記錄內容按上表修正，並不表示 ESP byte-for-byte 不變。

交付為 `package/Elements Spellblade/`（完整 ESP、19 PEX、來源與 MCM／CSF／SEQ）。只產生工作區 package，**未安裝到 MO2，未寫 SkyrimSE，未連網，未碰 `.strategic-advance/**`**。原始編碼／BOM／換行保留，`build_v03.py` 仍為 CRLF；規劃與 review 檔 hash 未变。進度頁與 `build/fix13-check.json` 可供復核。規劃偏離只在本節記錄：岩甲由舊 30 秒實作改為隨層數生命期能力；開印整體縮放沿用已核准決策。

## 2026-09-19 00:37 Asia/Taipei — fix round 13 驗收與部署（指揮官）

- 通過。指揮官重建 exit 0；3919 筆 == manifest 且與 `.codex/pre-fix13-snapshot` 比對 FormID 0 變動、0 新增；schema 維持 4；19 腳本 0 errors；`FIX13 ok`（10 組修正前失敗、修正後通過）、`MCM DEFAULTS ok`。
- 審查 P2 第 6–13 項全部成立並修正：雷普攻補上 50% 削魔、逐出時雙印共同結算、反震改用受擊前岩甲快照、岩甲護甲改由層數管理、六元素開印層數與土開印回耐補上倍率、斷咒與冰心先判條件再取冷卻、反咒監聽移到穩定 alias 並攔截晚到事件、施法判斷改用 `PO3_SKSEFunctions.IsCasting`。
- MCM 平衡頁加入「確認後回復預設」，涵蓋 10 個滑桿，共用產生出的預設資料。
- 代表命中 166 calls，round 12 的快取與選單延後保留。
- 部署：遊戲未執行時鏡像複製到 MO2（54 檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260919-003731`。
- 至此審查 13 項全數處理完畢；尚無待派工作。下一步是使用者實機回饋。


## Round 14：擊殺元素歸因（2026-09-19）

### 使用者核准的設計偏離與根因

使用者提供 smoke4 實戰證據：黑暗形態 15 次擊殺、已持有亡者歸來、復生 0 次；25% 獨立機率下 0.75^15 約 1.336%。本輪另實際讀取 `.codex/smoke4-Papyrus.0.log`，確認 227 行 `reason=invalid-actor`。程式根因是物理武器先致死，而原 `ApplyTrackedDamage` 又於每次非致死附傷後清除元素，因此 `LastDamageFor` 沒有可供擊殺掛勾使用的證據；機率本身不是本輪修正標的。

**使用者核准偏離**規劃 5.12「因黑暗傷害死亡」、5.9 與 8「神聖致死」的嚴格字面限制：死亡時，優先使用該受害者在 `kill_attribution_seconds` 秒內所受本模組最新元素傷害；無近期元素傷害才使用仍有效的實體印記；兩者皆無則為 0。`settings.json` 新增 `kill_attribution_seconds: 3.0`，由建置產生 `ESSBState.KillAttributionSeconds()`，不是即時 MCM 設定；不乘 `MultDuration`。最近傷害與印記元素相異時，最近傷害優先。兩枚實體印記同時存在且沒有近期傷害時，採 `RegSeq`／`RegSeq2` 較新的印記。虛擬疊印／汪洋殘留不作實體印記；尚未回報 OnEffectStart 的印記須有實際 MGEF 才可採用。

目前形態不參與一般歸因。切到黑暗並有新的黑暗傷害時，較早神聖傷害／神聖印記不會造成化灰；僅切換形態、沒有新的傷害或印記證據，不會抹掉仍在 3 秒內的神聖傷害。設計原有「淨土」（已持有、神聖形態、同調三段）明定所有擊殺化灰，是獨立形態天賦例外，保留原判定，不偽裝成元素歸因。一般無證據的擊殺為 0，不觸發化灰／亡者歸來／風連殺。

### 實作與儲存相容

- `ApplyTrackedDamage` 在立即投遞前先登記候選證據，保護致死原生呼叫期間的回呼順序；非致死但確有 Health 減少時保留。完全抵抗／未扣血則還原上一筆元素與時間，且已有較新紀錄時不回滾覆蓋它。已死亡目標仍拒絕新附傷，不用死亡後的命中推測形態。
- `LastDamageFor` 統一回傳有時效的每目標最近元素；`KillElementFor` 負責近期傷害 → 實體印記 → 0。`CaptureDeath` 在既有死亡清理及死亡導致的 `OnMarkFinish` 清除印記之前保存結果，重複清理不覆蓋第一份死亡證據；`OnKillEvent` 取出同一份歸因，先凍結一次 `ShouldAsh`，再傳給兩個元素模組。化灰永遠先決；化灰被拒絕也不回退復生。`OnAsh` 回饋只在 `ApplyAsh` 接受施放時執行。
- 沿用已退役的 `Float[] SwapFloats` 存放 128 格 `DamageActor` 對應時間戳；換宿早已改用 `BackupFloatsA/B`，沒有重疊用途。冷啟動配置，既有已初始化存檔在 `ResetLoadClock` 補配 None／舊 17 格陣列並清除前一執行期的傷害與死亡快照；不在命中熱路徑探測配置。配置失敗沿用 round-9 latch；新增該陣列失敗注入覆蓋。
- 沒有新增／移除／改型任何持久成員，因此 `state_schema_version` 保持 4，`state-schema.lock.json` 位元組不變。相對 `.codex/pre-fix14-snapshot/v03-formids.json`，3919 筆記錄身分全部相同，`ESSB_DebugLevel` 仍為 `0x000811`，不加 masters。

### 全量搜尋的消費者

| 消費者 | 用途／本輪處理 |
|---|---|
| `ESSBController.OnKillEvent` | 原直接取 `LastDamageFor(akVictim)`；改用統一 `KillElementFor` 或死亡快照，凍結元素與化灰決策後派送。 |
| `ESSBElem2.OnKill` → `ShouldAsh` → `ApplyAsh` → `OnAsh` | 化灰（7）與聖灰／淨灰的間接觸發入口；一次化灰決策，施放拒絕不發回饋。淨土明文例外保留。 |
| `ESSBElem3.OnKill` → `Reanimate` | 亡者歸來（10）；接收同一歸因與化灰決策，25%／死靈主 100% 與等級、召喚上限維持。 |
| `ESSBElem2.OnKill` 的風「連殺」 | 另一個按元素篩選的擊殺效果。原 `aiElement == 5` 可來自目前形態；改由 `TryKillStreak` 消費同一 `aiKillingElement`，保留持有分支與潛行條件。 |
| `ESSBController.OccupySlot` | 呼叫 `LastDamageFor` 寫入舊 `RegLastDamage` 欄位；保留布局。擊殺解析不再依赖該冗餘快取。 |
| `ESSBController.LastDamageWasElement` | 包裝 `LastDamageFor`；全量 src 搜尋無執行期呼叫者，現在同樣受時間窗約束。 |
| `ESSBController.KillElementFor` | 本輪新增的 `LastDamageFor` 消費者；供 `CaptureDeath`／`OnKillEvent` 共用。 |
| `ESSBElem3.PoisonTickHook(..., Int aiKillingElement = 0)` | 既有未使用的形參；函式內不讀該值，沒有擊殺元素門檻，本輪保留。 |
| `ESSBElem.OnKill`、`ESSBElem3.OnKill` 的 `aiElement` | 形參本身無條件讀取；連鎖冰封依凍結層，蔓延依毒層，收割依詛咒層，不是 killing-element 消費者，未擴大修改。 |
| `ESSBElem2.OnKill` 的飲血／血承／不死、`ESSBNoForm.OnKill` | 各依流血／天賦／施法者條件，不使用 `LastDamageFor`／`LastDamageWasElement`／killing element；本輪未更改條件。 |

### 診斷與離線驗證

新增 `[ash-reject]`、`[reanimate-reject]`、`[kill-streak-reject]` 的 L2 限流紀錄。含 no-element、element-mismatch、branch-not-owned、roll-failed（roll/chance）、can-reanimate-invalid-target／vip／dragon／no-reanimate-keyword、tier-refused、servant-cap-full、ash-priority、not-sneak 與 apply-busy／stale-or-broken／missing-player-spell-target／pending-slots-full／dragon／vip。下層已記錄具體拒絕原因時，上層不再加一條泛稱失敗；所有訊息建構都位於 `CachedDebugLevel >= 2` 之內。化灰是元素基礎能力，本身無需購買分支，也沒有機率抽籤；不新增不存在的門檻。`ApplyReanimate` 的 stale/broken 失敗同時釋放本次交易鎖。

`build/fix14_verify.py` 執行實際 Papyrus 函式內容（原生呼叫為明示 mock）：29 類歸因案例、28 類拒絕診斷，黑暗／神聖的印記物理一擊、無印記近期傷害、相異元素優先、3.000／3.001 秒邊界、切換、无證據、失敗傷害回滾、虛擬印記、未回報真印記、死亡清理前後、重複事件、目標隔離／129 次環覆寫、淨土互斥、風連殺、載入時鐘與舊陣列遷移、實際 ApplyReanimate 交易與死靈主成功皆有覆蓋。修正前快照實際執行黑暗／神聖印記物理擊殺均沒有對應效果，是本輪反例。L0 測試確認拒絕路徑沒有字串組裝或 Trace；同一拒絕限流生效。

既有測試調整限於核准規則與測試支援：FIX6 公式 mock 明確委派傷害投遞，公式斷言不變；FIX9 加入 SwapFloats 的配置失敗測試且保留熱路徑零探測；FIX10 將「非致死神聖後物理擊殺不得化灰」更新為核准的 3 秒規則，保留跨目標／真傷／流血／舊實例等斷言；FIX13 不再寫舊 `.codex/impl-fix-round13.html`，settings 以「只新增 kill_attribution_seconds，其餘 JSON 完全相同」取代過期整檔 hash。不得斷言 `.strategic-advance` hash 的既有豁免保留。

代表命中維持 round-12/13 同一計數器與原生 mock：115 scripted + 50 native = **165 calls**（round 13 為 166），identity 檢查 2 次，L0 字串組裝 0；不宣稱此計數等於遊戲內耗時。完整建置結果於下段追加。沒有遊戲內重跑或安裝；不寫 MO2／SkyrimSE、不改設計書／review 檔／pre-fix14 快照／fx_extract.py／commander ledger。原 UTF-8、BOM 與各檔 LF／CRLF 保留，`build_v03.py` 維持 CRLF，本檔僅追加。


### Round 14 最終建置完成

親自執行 `python build_v03.py`，exit 0。`build/fix14-build-final.log` 保存完整輸出：LAYOUT／DOT／MCM／MCM DEFAULTS／SCHEMA／FIX10／FIX11／FIX12／FIX13／FIX14 ATTRIBUTION 全部 ok；READBACK `masters=['Skyrim.esm'] records=3919 manifest=3919`；CSF `files=13 valid=13`；DELIVERY ok；PLAN COVERAGE `unmapped=0`；19 支腳本全數 `0 error(s), 0 warning(s)`。新歸因測試 29 類、拒絕診斷 28 類；代表命中 165 calls。既有 PLAN COVERAGE 的穩步 CK 進入點實測 deferred 1 項保留，與本輪擊殺歸因無關。

交付：`package/Elements Spellblade/` 與 `build/Elements-Spellblade-fix14.zip`；檢查結果 `build/fix14-check.json`，交付雜湊 `build/fix14-delivery.json`。所有檔案只在核准範圍內產生；沒有安裝或實戰驗證。

## 2026-09-19 08:07 Asia/Taipei — fix round 14 驗收與部署（指揮官）

- 起因：`.codex/smoke4-Papyrus.0.log` 黑暗形態擊殺 15 次、已購亡者歸來、復生 0 次（25% 機率下全落空機率約 1.3%）；同場 227 次 `hit-reject reason=invalid-actor`（武器物理先致死，附傷後到被拒）。
- Astra 找到的實際根因：`ApplyTrackedDamage` 在非致死附傷之後會清掉該目標的元素紀錄，所以之後物理致死時沒有任何證據。
- 使用者核准的規則偏離（相對於規劃 5.12「因黑暗傷害死亡」與 5.9／8「神聖致死」的字面）：擊殺歸屬＝3 秒內本模組最新一次元素傷害 → 否則實體印記元素 → 否則 0；不以當前形態猜測；化灰先決並與復生互斥。`settings.json` 新增 `kill_attribution_seconds`（3.0）。同一規則也套到風的「連殺」元素門檻。
- 通過。指揮官重建 exit 0；3919 筆 FormID 0 變動、schema 維持 4（沿用已退出換宿流程的 `SwapFloats` 存放 128 名目標的傷害時間戳，不新增成員）；`FIX14 ATTRIBUTION ok`（29 類歸因回歸、28 類拒絕診斷）；代表命中 165 calls；剩餘 `(==|!=) None` 6 處皆非陣列。
- 部署：遊戲未執行時鏡像複製到 MO2（54 檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260919-080746`。


## fix round 15（review-fable：實戰靜默不觸發機制）

權威清單 `review-fable-2026-09-19.md` 已完整讀取；其來源為 pre-fix14。以下先核對已安裝 round 14，再列本輪修正。原規劃、review、pre-fix 快照及 `.strategic-advance/**` 未改寫。進度頁 `.codex/impl-fix-round15.html` 先建立，再逐項更新。

| 項目 | round 14 涵蓋 | 本輪結果與接線 |
|---|---|---|
| 1 死咒／饕餮／處刑／亡魂 | 未涵蓋。只有死亡歸屬快照，沒有保留死咒結算。 | FIXED：`ESSBStatus.ResolveDeathCurse` 先清除一次性狀態，再處理結果。活目標依真實期限结算；死亡的宿主／控制器死亡捕獲／清格前結算固定項與玩家收益。pending 死咒也在清格前處理。屍體不補傷害、不處刑；亡魂只由實際擊殺事件的死咒來源觸發。 |
| 2 致命潛行、連殺、奇襲、風勢 | 部分涵蓋。round 14 統一連殺的元素歸屬，但 `LastHitSneak` 仍在死者拒絕後。 | FIXED：有效武器、隊友等過濾後，先保存每目標的 sneak／power／weapon，再拒絕死者傷害流程。擊殺讀目標自己的事實；`SettledElement` 保留已凍結歸屬，支援 kill callback 先於 weapon callback。致命潛行補滿風勢、消耗上一輪連殺與處理附近活敵奇襲；不向屍體施加 proc、開印或吹飛。活目標風附傷 ×3／暗風 ×5 及連殺 ×2 原路保留。 |
| 3 反擊 | 未涵蓋。實作紀錄舊稱 ApplyProc ×1.3，來源沒有對應實作。 | FIXED：純武藝所在的 `OnNoFormHit` 消耗 3 秒期限，將該次 baseline 與 `OnMartialHit` 的真傷乘 1.3；不再假設無形態分支能走元素 ApplyProc。第二擊或超時均無加成。 |
| 4 神佑 | 未涵蓋。傷害後 Health <=1 判斷救不到致命傷。 | FIXED：`RefreshDivineProtection` 在神聖分支、同調三段、Enabled、形態與未使用條件成立時，事前 `StartDeferredKill()`。致命傷後先鎖本場已用、開短暫神佑、清負面、以 `RestoreActorValue` 恢復到實際 1 HP，最後 `EndDeferredKill()`。不合資格／停用／失敗關閉會解除自己持有的保護；脫戰沿原 tick 規則重置。 |
| 5 切換雷終焉 0 電荷 | 未涵蓋。 | FIXED：離開雷形態前保存 `SwitchCharge`，自身資源仍依 2.3 歸零；reason 0 終焉一次取走此快照。放電、過載終焉、雷殛與餘電使用同份電荷值。`PendingDischarge` 現在保存實際電荷，後續開印不再讀已歸零的 SelfCharge。 |
| 6 tick 秒數與真實時間混用 | 未涵蓋。round 12 修正排程與跨程序時鐘，仍保留 tick 計數。 | FIXED：所有 `TickTimers`、OpenBoost／EndBoost、浮空、死咒、催毒、星鎖、領域、化身、雷雨充電改成真實時間期限；電荷衰減按已流逝秒數補算。血毒與星痕仍使用固定小型桶，但 `RingClock` 按真實經過秒數推进；一個晚 tick 有界掃過到期桶，先累計應結算的 layer-seconds／星痕再移除。催毒倍率按每段是否在期限內積分。熔身及領域依期限內經過時間結算，不再每個 callback 當一秒。讀取／加入狀態前可結算已到期工作，既有排程仍為一秒 single update；沒有新增輪詢或 Wait。 |
| 7 擊殺層數與宿主消失 | 部分涵蓋。round 14 凍結元素並保護 mark-finish 的歸屬，但未保存 curse。 | FIXED：凍結 freeze／bleed／poison／curse／heat／holy，`ESSBElem3.OnKill` 使用傳入 curse，不再死後現讀。`OnStatusFinish` 在解除註冊前捕獲死亡；StatusHost 與 11 個 MarkEffect 加 No Death Dispel，死亡移除不再冒充自然終焉。其他 MGEF 不擴大套用此旗標。 |
| 8 熔斷 | 未涵蓋。 | FIXED：`EndFire` 接收 aiReason，只有 reason 1 且 `KeepHeatOnBurst` 才保留熱度；切換與過期保持原消耗規則，猛爆的例外也保留。 |
| 9 火葬／亡魂依即時 HP 判殺 | 部分涵蓋。round 14 改善一般傷害歸屬，但這兩個效果仍在施法返回後立刻讀 HP。 | FIXED：爆燃／死咒傷害前記錄目標、原因、量與期限；`OnKillEvent` 兌現一次火葬／亡魂。沒有以 ApplyDamage 返回後 HP <=0 判殺。來源過期不兌現，同目標新的來源取代舊來源，死亡快照捕獲有效來源後保留到延遲的 kill callback。一般 `ApplyTrackedDamage` 的 round-14「最近有效元素傷害 → 實印 → 0」仍保留，未改為目前形態歸屬。 |
| 10 斷咒判斷過晚 | 未涵蓋。round 13 修正 IsCasting API，但仍在破魔傷害處理後查詢。 | FIXED：`OnNoFormHit` 入口、任何傷害／開印之前保存施法狀態，再以參數傳入 `OnManaBreak`；同時使用穩定 alias 的左右手 SpellFire 動畫事件保存最近 1.5 秒施法事實。效果處理不再重新讀易失的 IsCasting。未施法不消耗冷卻的 round-13 規則保留。 |

### 明示的實作取捨與原生界線

1. 神佑使用本地 `vendor/imports/Actor.psc` 已宣告的原生 `StartDeferredKill`／`EndDeferredKill`（原生註解要求成對結束，否則角色不死）。這是先阻止死亡轉換，再讓既有 OnHit／tick 兌現的一次救命；不依赖「致命傷扣完後還能搶在死亡前補血」或 15% 血量閾值。實際 1.5.97 玩家死亡畫面、其他死亡替代模組是否共用 deferred-kill 狀態，未做實機測試；離線只證明資格、預先武裝、負 HP、一次／戰鬥及解除呼叫順序，沒有聲稱 native mock 等於引擎驗證。
2. 2.3／2.6 的衝突採使用者指定：雷的自身電荷立即清空，舊雷終焉取得「切換瞬間的電荷」一次性快照。其後不借用新形態累積的電荷。原規劃未修改。
3. 致命物理攻擊在 callback 抵達時已經死去，不能再以元素 proc 改寫那一刀傷害。本輪只補回仍有意義的玩家／附近活敵效果；連殺仍必須通過 round-14 元素歸屬。完全沒有近期元素傷害、也沒有實印的純物理一刀，歸屬仍為 0，不用目前風形態偽造擊殺元素。
4. 時限統一不會使堵塞的 Papyrus VM 準時醒來：工作仍可能晚執行，但一次醒來會結算到期內容，不把 34 秒當一秒。DoT 過期前應有的傷害以單次合計交付；領域使用結算時的幾何位置，沒有試圖重建卡頓期間的移動軌跡。固定血毒桶壽命與星痕的秒級桶精度保留，不新增逐層計時器。
5. 沿用 round-12 讀檔政策：重置控制器短期戰鬥視窗，清除相關 PERK mirror／連殺偵測保護；保留的宿主桶以新 uptime 起算，未結算的宿主／pending 有限動作改在載入後一秒到期。pending 死咒、催毒、浮空、冰封、星鎖保留请求時期限；同程序宿主搬移以 24 個 floats 保存精確 deadline，兩個 backup bank 各 96 floats，未超過 Papyrus 的 128 上限。
6. 火葬／亡魂原因視窗沿用 `kill_attribution_seconds=3.0`，不另藏一個硬編碼一秒門檻。它是近時來源證據，不是原生最後傷害封包證明；若回呼與最早死亡觀察本身都晚於視窗，仍可能無法還原引擎確切致死時間。斷咒的 snapshot 是腳本收到命中處理時的最早可用狀態，SpellFire 是補充證據；PO3 OnWeaponHit 沒有提供原生命中時的施法旗標／時間戳，故不聲稱能還原 VM 尚未收到事件前的瞬間。

### Schema、身份、驗證與交付

- `state_schema_version` **4 → 5**：新增目標命中事實／死亡來源陣列、神佑 latch、切換電荷；將剩餘整秒成員改成 Float deadline，並擴充宿主序列化 floats。這些是持久 member layout 變動，依 round-9 必須新世代。`state_schema.preflight()` 生成 lock，歷史 schema 4 的 signature 與快照來源交叉核對，沒有繞過同版 hash gate。
- `ESSB_MainQuest` **0x006004 → 0x006006**；`ESSB_MCMQuest` **0x006005 → 0x006007**。舊 schema 4 ID 保留為兩筆 inert stubs；全部舊非 Quest identity 不變，`ESSB_DebugLevel=0x000811`，masters 僅 `Skyrim.esm`。
- 親自執行完整 `python -B build_v03.py`，**exit 0**；`build/fix15-build-final.log` 保存輸出。19 支腳本全部成功、0 errors；READBACK **3921 records == 3921 manifest**；CSF **13/13**；DELIVERY、LAYOUT、DOT、MCM、MCM DEFAULTS、SCHEMA、FIX10–FIX14 全 ok；PLAN COVERAGE **542 rows，0 unmapped**。原「穩步」optional deferred 維持既有狀態。
- 新增 `FIX15 ok`：`build/fix15_verify.py` 實際執行 source bodies，**10 個案例在 `.codex/pre-fix15-snapshot/src` 失敗，在新來源全部通過**。包含死亡／清格／pending、致命 hit 與逆序 kill→hit、反擊消耗／超時、神佑一次性、切換快照／餘電、34 秒晚 tick 與 DoT／星痕／落地／死咒單次结算、swap／load deadline、死亡 curse 快照、熔斷、非同步傷害後擊殺、施法狀態在中途改變。ESP 回讀另外驗證 12 個 No Death Dispel 旗標及 Engaged 未擴大變動。細節在 `build/fix15-check.json`。
- 相同代表命中仍為 **116 scripted + 50 native = 166 calls**；2 次 identity、L0 零 logging／字串拼接。未撤銷 round 12 成本改善。沒有新增 Utility.Wait、RegisterForUpdate 或 per-frame polling。
- 舊 verifier 僅更新失效的固定 schema／FormID 範圍、序列化 layout、deadline clock fixture，以及神佑改接線的來源位置；原效果、失敗注入、數值、歸屬、MCM、native 邊界檢查仍執行。新增陣列也納入 FIX9 的配置失敗鎖定測試（105 項）。
- 原檔 BOM／換行保留，`build_v03.py` 保持 CRLF；本檔 append-only。沒有寫入 MO2、SkyrimSE，沒有網路操作或遊戲執行。

交付：`package/Elements Spellblade/`；封裝 `build/Elements-Spellblade-fix15.zip`。**未安裝，未進遊戲驗證。**

## 2026-09-19 08:48 Asia/Taipei — Fable 審查與 fix round 15 驗收部署（指揮官）

- Fable 唯讀審查（對 round 14 前的快照）產出 `review-fable-2026-09-19.md`：10 項「實機中幾乎不會觸發」的機制。指揮官抽查第 1、3、4、8 項與程式和 `.codex/smoke4-Papyrus.0.log` 對得上（5 次黑暗終焉、0 次死咒結算；`RiposteLeft` 無任何傷害路徑讀取；神佑的受擊事件晚於致命傷害；`KeepHeatOnBurst` 無呼叫者）。另量得 tick 間隔 11–34 秒、融斷在按 Z 後 13–23 秒才結算。
- fix round 15（Astra，29 分鐘）：10 項全數修正（死咒死亡前結算、致命潛行的命中事實先記錄、反擊接入 ×1.3、神佑改用 `StartDeferredKill` 事前保護、切換瞬間保存雷電荷、所有遊戲時窗改真實時間期限並在晚到的 tick 補結算、擊殺層數死亡快照、熔斷接線、火葬與亡魂改由擊殺事件確認、斷咒保存命中時的施法狀態）。schema 4→5（`ESSB_MainQuest` 0x006004→0x006006、`ESSB_MCMQuest` 0x006005→0x006007，累計 8 個空殼）。
- 指揮官補丁：`StartDeferredKill` 是存在角色上的狀態，若舊世代實體在 schema 升版時被丟棄而沒跑 `BreakState`，玩家會永遠卡在延遲死亡。於 `ESSBController.Setup` 每次無條件 `EndDeferredKill()` 並重設 `DivineArmed`，再交由 `RefreshDivineProtection` 依資格重新上鎖；同步為 `build/fix9_verify.py`、`build/fix12_verify.py` 的 Player mock 補上兩個 native。
- 通過。指揮官重建 exit 0；3921 筆 == manifest；非任務 FormID 0 變動、`ESSB_DebugLevel` 0x000811；19 腳本 0 errors；`FIX15 ok`（10/10 修正前失敗、修正後通過）；代表命中 166 calls。
- 部署：遊戲未執行時鏡像複製到 MO2（54 檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260919-084811`。


## 2026-09-19 09:37 Asia/Taipei — fix round 16：修正 round 15 獨立審查

工作清單：完整讀取 `review-fable-2026-09-19-r15.md`，依使用者指定 A1–A5、B1.2、B1.6、B1.9、B1.10 次序完成。沒有修改規劃、審查報告、任何 pre/post snapshot 或 `.strategic-advance/`；不對指揮官 ledger 的 hash 作斷言。沒有連網，沒有写入 MO2 或 SkyrimSE。

| 項目 | 實作與離線證據 |
|---|---|
| A1 屍體融斷 | `OnFormClosed` 遇死者先 `CaptureDeath`、`ClearSlot`，不計入 burst。`FinishMark` 在解除印記前捕獲死亡，死者只 detach／dispel；`ESSBReactions.End` 入口亦拒絕死者。測試確認沒有終焉 XP、End、FX 或回流計數，死亡快照仍存在。|
| A2 神佑殘留 | `RefreshDivineProtection`、`RefreshAbilities` 加 `IsCurrentController()` 守衛；三個 ESSBTrees 呼叫點也要求 `Controller.IsOperational()`。保留每次 `Setup` 的無條件 `EndDeferredKill()`。MCM 平衡頁新增「解除神佑保護」：先確認，解析目前世代，停用模組、清保護、disarm、`EndDeferredKill`、通知；不要求 Ready／IsOperational，因此 StateBroken 亦可解除。停用是防止下一次 tick／戰鬥／Setup 重新武裝；一般頁手動重新啟用可恢復功能。取消完全不動狀態。|
| A3 pending 冰封 | `SetFrozen(0.0, deadline)` 儲存綁定時刻及剩餘秒數，已過期時保留原到期點；不再把未來 deadline 當起點，不再重複套用 MultDuration。測試綁定 101、期限 103，以及 110 才綁定；換宿不會把死咒／浮空改成 now+1。|
| A4 領域 | 三格領域各保留最多六個居民（沿用最近五敵＋玩家），保存首次觀測與已結算時間。每目標從自己進入紀錄結算，首見者不追領舊帳；觀測到離開／掉出最近五名後清除，重入重新起算。34 秒晚 tick 測試：原居民 34 秒，新居民 0 秒，下一秒各 1；毒層與玩家回血／回耐同樣隔離。領域重開與讀檔會清居民紀錄。|
| A5 命中成本 | 無形態命中只有持有斷咒才呼叫 `IsCasting`／`RecentCast`；三段同調只有持有神佑才呼叫 `RefreshDivineProtection`。GetStack／AddStack／SetStack／AddAstral 先判斷未滿一秒再呼叫 Holder.IsDead；已到結算秒仍跑 Tick。OpenBoost／EndBoost 為零時先回傳，省下無效 SecondsLeft 呼叫。|
| B1.2 連殺 | 新增 `HitForm`，在有效武器命中入口與 sneak／power／weapon 一起保存；連殺使用這份形態，而非 killingElement 或擊殺結算當下的形態。纯物理無印一刀、風形態潛行致死也成立；切成黑暗後才收到擊殺仍成立，反方向不成立。非潛行舊命中不先消耗 `HitKillDone`，因此 kill→fatal hit 逆序仍可兌現。|
| B1.6 防護視窗 | 九種短期防護改由 PERK 的 HasMagicEffect 條件讀取獨立原生限時 MGEF（順轉、安全閥、冰晶、殘影、神佑、破護、影身、預知、星光）。過期由引擎處理，不等待 AME／控制器腳本 tick；不新增更新事件或 polling。鏡射保留供其他腳本／診斷使用，GetGuardWindLeft／GetGuardDarkLeft 讀取時也更新鏡射。ESP 回讀核對九個 PERK 條件、MGEF 無 VMAD、有限 duration、Self delivery。|
| B1.9 火葬／亡魂 | 不再為所有爆燃／死咒開三秒窗口。`ApplyDamage` 算出實際交付的 level／base／target-scaled magnitude 後，在 native 傷害前以 HP 預測致死；只有預期致死才保存 -1 claim。獎勵保留原基準 amount，不重複放大。未致死 20/100 不會兌現兩秒後的無關武器擊殺；預期致死 20/20 即使 7／34 秒後 CaptureDeath→ClearSlot→OnKillEvent 仍只兌現一次。一般元素歸屬的 `kill_attribution_seconds` 保持原用途與值。|
| B1.10 斷咒 | 持有節點時，在命中回呼入口先保存施法狀態，再處理傷害；後續不重新讀動畫。未持有節點不支付施法查詢成本；事件晚七秒且施法已結束時，結果明確為 false。|

### 指定偏離、時間語意與實機界線

1. 連殺明示偏離 round 14 的「never guess from the current form」：連殺是風形態分支，使用已記錄的命中處理時形態 `HitForm`，不是在擊殺结算時猜形態，也不改寫其他擊殺效果的元素歸屬。PO3 未提供原生碰撞時的形態／施法時間戳，仍接受命中事件處理本身 0–7 秒的延遲；沒有宣稱重建 VM 尚未收到的事實。
2. 領域採保守的首次觀測時間。無額外 polling／原生進出事件，不能還原兩次觀測間完整移動軌跡；首見與已觀測到離開後重入的目標不會吃歷史整批傷害，未觀測到的離開再返回仍無法辨識。最近五名名額更換也重新起算，可能少給，但不把新選入者當成全程在場。
3. 防護效果的開始仍可能因 VM 排程晚發生；一旦 native 成功套用，有限 active-effect duration 由引擎到期，PERK 不讀過期的腳本 mirror。遊戲暫停／選單等引擎 active-effect 時鐘行為與效能、HasMagicEffect 實際到期行為尚未進遊戲確認，離線 native mock 只證明接線與期限模型。
4. 火葬／亡魂採使用者指定的「傷害前 HP 預測致死」，並納入本模組實際交付倍率。這不等於可觀測原生最後傷害封包；外部抗性、傷害替代／必要角色、其他模組干預可能讓預測與引擎結果不同。離線證明已消除普遍的非致死三秒窗口，與晚捕獲造成的窗口漏兌現；不宣稱外部模組下的引擎歸因已驗證。
5. 神佑的 ≤0 HP 等待回呼、StartDeferredKill 是否旗標／計數、是否持久化到 actor save change、和其他死亡替代模組互動，仍須 Skyrim SE 1.5.97 實機確認。卸載前請以 MCM「解除神佑保護」解除並存檔；不能在移除腳本後保證補救舊的 actor 狀態。

### Schema、建置與交付

- `state_schema_version` **5 → 6**：需要保存三領域各目標的 `DomainResident: Actor[18]`、`DomainResidentAt: Float[18]`，以及 `HitForm: Int[8]`。這三個持久 member 無法僅用區域變數跨 tick／事件保存；沒有借用仍在使用中的其他狀態陣列，依 round-9 規則新世代，沒有手改同版本 signature。新陣列纳入配置失敗檢查。
- 唯一既有 FormID 變動：`ESSB_MainQuest` **0x006006 → 0x006008**、`ESSB_MCMQuest` **0x006007 → 0x006009**；舊 ID 保留為 schema 5 inert stubs。其餘既有記錄身份全部與 `.codex/pre-fix16-snapshot/v03-formids.json` 相同，`ESSB_DebugLevel` **0x000811**。另新增九對防護 MGEF／SPEL，使用未占用的 **0x005170–0x005181**；masters 仍僅 `Skyrim.esm`。
- 親自執行 **`python build_v03.py`，exit 0**，完整輸出 `build/fix16-build-final.log`。19 支腳本全部 0 errors；READBACK **3941 == manifest 3941**；CSF **13/13**；DELIVERY、LAYOUT、DOT、MCM、MCM DEFAULTS、SCHEMA、FIX10–FIX15 全 ok；PLAN COVERAGE **542 rows、0 unmapped**。既有 optional deferred「穩步」不變。
- 新 **FIX16 ok**：九項實際原始碼離線回歸，每項皆在唯讀 pre-fix16 失敗、修正後通過。報告 `build/fix16-check.json`；source diff `build/fix16.patch`。既有 verifier 只為新增記錄、schema、MCM action、命中形態事實及被本輪明確替換的錯誤預期調整；沒有略過既有完整檢查，沒有寫舊進度帳。
- 相同代表命中：**116 scripted + 50 native = 166 → 115 scripted + 50 native = 165**，2 次 identity，L0 零 logging／字串拼接。另在實際 IsCasting body 的無節點分支，施法 native **10 → 0**；十次同秒 GetStack：**50 scripted + 40 native → 10 scripted + 10 native**，Holder.IsDead **10 → 0**。這些是明示 native mocks 的呼叫數，不是遊戲耗時。
- 原始編碼、BOM、換行保留；`build_v03.py` CRLF，`state-schema.lock.json` 升版時亦保留 CRLF；本紀錄只追加。未新增 Utility.Wait loops、非 single RegisterForUpdate 或 per-frame polling。

交付：`package/Elements Spellblade/`；`build/Elements-Spellblade-fix16.zip`。**未部署至 MO2，未進遊戲驗證。**

## 2026-09-19 09:47 Asia/Taipei — Fable 二次審查、fix round 16 驗收部署與使用者環境診斷（指揮官）

- Fable 對 round 15 的二次審查（`review-fable-2026-09-19-r15.md`）：原 10 項中 6 項確認修好、4 項部分修好；另找到 round 15 新引入的 5 個問題（融斷結算屍體印記、神佑殘留路徑、某路徑冰封 6 秒、領域補結算打到新進者、多餘的每擊成本）。
- fix round 16（Astra，19 分鐘）：上述 9 項全部修正（FIX16 9/9 修正前失敗、修正後通過）；格擋類 2 秒窗口改用真正的魔法效果計時（新增 9 組 MGEF＋SPEL 共 18 筆），不再依賴 tick；連殺改看命中當下的形態；MCM 新增「解除神佑保護」。schema 5→6（`ESSB_MainQuest` 0x006006→0x006008、`ESSB_MCMQuest` 0x006007→0x006009，累計 10 個空殼）。記錄 3921→3941，其餘既有 ID 不變；代表命中 165 calls。
- 指揮官補丁：`CachedDebugLevel` 原本只在載入、換形態、關 MCM 時刷新；使用者在遊戲中用主控台開除錯時看不到任何紀錄（這場開了除錯只記到 4 行）。改為每個 controller tick 讀一次 GLOB（非每擊）。
- 通過並部署：指揮官重建 exit 0；3941 筆 == manifest；19 腳本 0 errors；遊戲未執行時鏡像複製到 MO2（54 檔 sha256 相符）；前一版備份於 `mo2-installed-backup-20260919-094733`。
- 使用者環境診斷（非本模組）：
  - 2026-09-19 09:27 的閃退位於 `SchlongsOfSkyrim.dll+000F1F9`（讀取位址 0x14 的空指標），堆疊內容是 SOS 處理 `SOS_ConcealingArmors／RevealingArmors` 與 `For Honor in Skyrim.esp` 的護甲；本模組只出現在插件清單中，不在堆疊內。
  - 載入慢與 tick 延遲的來源更正：大量 `FloraHarvestScript cannot be bound` 的附掛記錄來自 CACO（`complete alchemy & cooking overhaul.esp` 及其補丁、`PG_1.esp`），Freak's Floral Fields 只是放置了大量使用這些基底物件的花草。CACO 的 BSA 內含該腳本，但實際執行時綁定失敗，原因待使用者排查（可能是 CHS 版 ESP 與原版 BSA 版本不一致或封存未載入）。


## Round 18 — 第 1 期前置核實：依目標狀態層停止條款停止（未完成）

- 先建立 `.codex/pre-fix18-snapshot/`，保存 `src/`、`build_v03.py`、`settings.json`、`build/v03-formids.json`（快照內名為 `v03-formids.json`）；產品修改前建立 `.codex/impl-fix-round18.html`。
- 停止原因是分期接縫，而非工具不可用：第 1 期 item 2 要把 `ApplyProc` 縮為「連殺 ×2、極致、雙生的補刀」，但現有一般附傷仍負責消費目標狀態。`ESSBController.ApplyProc:1797` → `GetHitMult:2971` → `ESSBElem.FireHitMult:63` → `ESSBController.GetStack:2593` → `ESSBStatus.GetStack:389`，會讀取各目標的 `Heat`（以及其他元素的凍結、聖印、水壓、星痕等）。第 2 期 item 1 才生成目標階梯與 0x1D/0x23 表示；第 1 期的固定共用法術尚無這些目標狀態的輸入。
- 可重跑反例：`python -B build/fix18_scope_verify.py` 執行現有 `ApplyProc/GetHitMult/GetStack/FireHitMult/ESSBStatus.GetStack` 的實際 source bodies，不另抄公式。相同玩家、零節點、樹等級 1、普通非開印命中；native actor 值相同，目標 A 的 Heat=0，目標 B 的 Heat=4。附傷寫入值依序為 **11.55、15.246（比值 1.32）**。這是保存既有狀態行為的反例，不是新 alter 數值的替代規格。RandomFloat 在既有 fixture 中明確 mock 為中點；並非引擎實測。
- 若第 1 期直接刪除這條消費路徑，熱度對一般附傷的效果便消失；若要保留，必須額外保留目標倍率的脚本補刀，或提前實作目標狀態的引擎表示。前者超出第 1 期 item 2 列定的三類補刀，後者跨越「不動狀態層」。依使用者「若 phase-1 迫使 status-layer change，stop and report」停止，不擅自選擇。
- 六項進度：item 1/2 卡在上述接縫；item 3/4/5 尚未實作；item 6 未升版（沒有持久成員變更，不產生半成品 schema）。没有宣稱完成新的機制、HITPROC、magnitude 寫入點對照或 section 11 引擎探針。section 11 本輪沒有實作觸及的引擎行為，未把任何猜測當已驗證。
- 已親自執行 `python -B build_v03.py`：首次基線建置 exit 1，原因是 `build/fix8_verify.py` 的舊新增檔名白名單尚未列入使用者本次提供的兩份規格文件。只為 `design-latency-2026-09-20.md`、`元素魔戰士規劃-v0.3_alter.md` 加入精確允許名單與現有內容 SHA-256 保護，其餘判斷不變；不對 `.strategic-advance/**` 的 hash 作斷言。重跑 exit **0**，日誌 `build/fix18-baseline-build-retry.log`。首次失敗保存在 `build/fix18-baseline-build.log`。
- 基線結果：READBACK masters `['Skyrim.esm']`，records=manifest=**3941**，CSF **13/13**，DELIVERY、LAYOUT、DOT、MCM、MCM DEFAULTS、SCHEMA、既有 FIX10–FIX16 全部 ok，PLAN COVERAGE **0 unmapped**，**19** 支腳本 0 errors。檔案現況的 build 主程式沒有 FIX17 驗證入口；也尚未新增 HITPROC。因此此次 exit 0 僅代表既有實作基線，**不滿足 round 18 acceptance**。
- 代表 outgoing hit：既有 `fix12_cost.scenario` / `papyrus_harness` 實測 **165 = 115 scripted + 50 native**，沒有降低。報告 `build/fix18-scope-check.json`；沒有把未實作的引擎路徑算成 0 calls。
- `build/fix18-preservation-check.json` 確認所有 `src/*.psc`、`build_v03.py`、`settings.json` 與本輪快照逐位元組相同；3941 筆既有 record 身分全部相同，`ESSB_DebugLevel=000811`，schema 保持 **6**。包括 build 重新生成的 `ESSBState.psc` 亦相同。既有 guard 與 status 行為未改。
- 寫入僅為本輪快照、進度紀錄、新核實 helper、上述 FIX8 驗證維護、build/package 生成物及本段追加。未連網，未寫入 MO2 或 SkyrimSE，未改三份設計／規劃文件或任何 review／歷史快照，未進遊戲、未部署。
- 繼續所需的範圍裁定：允許第 1 期暫時保留「既有目標狀態倍率」的額外腳本路徑，或重排該倍率表示的分期。此處僅列解法，沒有採用或實作其中任何一種。


## Round 18 resumption：差額補丁裁定與零差額驗收邊界

- 已完整重讀 design-latency-2026-09-20.md section 6 的新裁定；接受 engine base + script difference，先前「不能只留三種補刀」的停止理由已解決。本次沒有重開該裁定。
- 新增驗收要求「when the difference is zero the number of GetStack calls is 0」需要區分：可在讀取目標前證明不可能有倍率的快速路徑，以及讀取目標後才知道差額為零的路徑。已提出釐清，未擅自把前者當成後者的完整驗收。
- 核實工具：`python -B build/fix18_zero_difference_verify.py`，exit 0。這是 preflight，**不是新 ApplyProc／HITPROC acceptance**。沿用 `fix12_cost` 的 native mocks，執行 `.codex/pre-fix18-snapshot/src` 的真實 ApplyProc、GetHitMult、FireHitMult、GetStack、Tick、ExportInts 函式內容；無遊戲內執行。

| 同樣零節點、樹等級 1、普攻、非開印、既有宿主 | 原 ApplyProc | 參考基礎 | 算術差額 | Controller.GetStack / Status.GetStack | Status.Tick |
|---|---:|---:|---:|---:|---:|
| Heat=0 | 11.55 | 11.55 | 0 | 1 / 1 | 0 |
| Heat=4，尚未到期 | 15.246 | 11.55 | 3.696 | 1 / 1 | 0 |
| Heat=4，已逾 6 秒，宿主尚未 tick | 11.55 | 11.55 | 0 | 1 / 1 | 1 |

- `ESSBElem.psc:65-70`：基礎熱度每層 +8% 不需要 Rank/Br，因此相同的節點資格不能區分前兩案。`ESSBStatus.psc:389-400`：GetStack 可能觸發 Tick；`ESSBStatus.psc:889-890`：到期清除熱度。第三案 `ExportInts()[0]` 先回 4，GetStack 結算後回 0，故直接改用 ExportInts 不等價。這裡只主張節點資格不能預知數值差額、直接 export 不保留到期行為；沒有把 preflight 的舊函式呼叫數假裝成新實作的失敗測試。
- 問題已送出：零 GetStack 是否限定在「讀取前已排除所有目標倍率」的快速路徑；其餘容許必要的原 GetStack，差額 <=0 不寫 magnitude、不施法。若要求全部數值零差額情況零呼叫，則需另外裁定如何獲得準確的目標狀態資訊，本次不擴大狀態層範圍。
- 產物：`build/fix18-zero-difference-preflight.json`、`build/fix18-resumption-preservation.json`；`.codex/impl-fix-round18.html` 已追加本次進度。
- 保留：src、build_v03.py、settings.json 與 pre-fix18 快照逐位元組相同；3941 筆 FormID 身分相同；schema=6、ESSB_DebugLevel=000811；未寫 snapshot，未修改 FIX8，未部署到 MO2／SkyrimSE，未存取網路。既有綠色 baseline build 沒有重跑；本次尚無新 hit calls 數字、無 HITPROC／FIX17 通過宣告，round 18 未完成。

## Round 18 第 1 期完成實作（第三次接續；離線驗收）

### 範圍、裁定與不可變資料

承接使用者的 difference-patch 及 zero-GetStack 裁定，不重建 `pre-fix18-snapshot`，不覆寫兩次 preflight。採用 standing authority 的事項只涉及驗收表示法：

1. **零讀取**限定 Controller-local Rank/Br 可排除「任何目標倍率可能性」。火熱度 +8%/層與聖印基礎 +20% 都不需購買節點，故火/聖即使零節點仍須原 `GetStack`；其他九元素在沒有相關節點時零跨腳本、零 GetStack。持有資格但倍率最後為零的情況照舊 lazy Tick，差額 <=0 不寫入、不施法。未使用 ExportInts 替代讀目標。
2. **雷真值表**的「恰好一段」指 chain 的第一個成功選擇；五段原始隨機條件在某些 RNG 值本來就會重疊，不能同時要求任意 RNG 下原始條件互斥。固定 RNG=99 驗完整 1536 個攻擊/血位狀態的唯一路由；另依 0..99 整數量化反算五個 first-success 機率，絕對誤差 <0.5%。設定 `lightning_roll_mode` 保留 `chain`/`additive` 兩種生成方式；是否第一段即停列實機探針，沒有用其他模組的存在冒充已證明執行語意。
3. **等值參考**：非雷使用規劃明定平均 B，雷差額使用 N=1 的期望 13；血附傷使用 alter 核准四區間及反轉，保留血怒中血位 +15%。這些本期明定的表示近似不與舊隨機單次抽樣/連續血曲線作逐次等值宣稱。其他倍率和狀態沿用舊函式執行；血的吸血曲線不改。
4. **探針包**：文件要求的「已開新存檔」會越過本輪不得寫遊戲/不執行遊戲的權限，交付可選離線 probe ESP 與主控台卡，不捏造存檔或實測。兩個提前探針使用可分別加卸的 perk，移除依賴正式已交戰標記的前置條件，避免正式 74 段的 first-success 影響被測樣本。正式 ESP 不帶額外 probe 傷害。此安排只隔離驗收機制，不碰正式玩法。
5. **致死刀與光效的驗收邊界**：`NoteDamageElement` 在有效且存活的元素命中不依賴差額必定執行，並記 real-time 傷害時間；已死目標仍走 rounds 14–16 的先前凍結事實與 once-only 結算，不為追認引擎傷害而覆寫屍體歸屬（§10 探針 1-9 本來允許明確 element=0）。§4.4 已明定能力/武器光仍可能因 Controller 晚到；下一刀元素讀 GLOB 的驗收與武器光亮起時間分開記錄。

`ESSBStatus.psc`、`ESSBMark.psc`、`ESSBElem.psc`、`ESSBElem2.psc`、`ESSBElem3.psc`、`ESSBReactions.psc` 對 pre-fix18 逐位元組不變，沒有引入第 2 期階梯、餘燼標記或火披風。FIX8 whitelist/content-hash 機制保留；驗證新增 EDID 用精確集合，不用整段 FormID 豁免。既有測試與新表示不相容之處只改其邊界：例如原雷兩 effect 增為原傷害/削魔兩 effect 加已交戰第三 effect，原 `AddSelf` 逐字比對扣除新增快取刷新三行後仍需完全相同。

### 第 1–6 項產物

- 原生 `ESSB_P_HitProc` 共 74 段：EP 51、Select Spell、目標分頁友軍/受命/死亡/格擋過濾；弓弩原生條件使用 7/12，PO3 腳本武器型別依 round 11 保留 7/9。
- 42 主法術變體（原 22 加風潛行、血四檔、雷五檔與 additive 增量）、11 quiet bonus MGEF/SPEL。主法術嵌入原已交戰效果 30 秒；差額沿用相同學派、抗性、元素關鍵字、Novice casting perk，NoHitEvent、零衝擊/聲音/學派 XP，避免重播主特效。
- `RefreshProcMagnitudes` 只在數值不同時寫共享法術；讀檔使快取失效而重寫全部元素。新 Float[64] cache 在事件註冊前配置，失敗走原 BreakState latch；無 `arr == None`。
- `ESSBNodes.Rank/Br` 讀 Controller mirror；Trees 匯出 rank/branch/level。SyncStage 快取；Guard 用本地 GLOB/NodeBits 早退，GDivineArmed 與 StartDeferredKill 仍有成對解除，九原生防護視窗不改。
- `ESSBInput` 提前發布形態 GLOB，Controller 用最新 GLOB 合併過時切換請求；力量也走 Input。Numpad 1–9、0、decimal 為 11 形態預設鍵；MCM 熱鍵頁可改鍵、停用、關閉通知/聲音。開形態魔力 10% 門檻、免費許可、失效實體/StateBroken/Ready、死亡、選單、文字輸入均有測試。
- 形態全身 aura 不再掛載；33 自有武器 shader/effect（11×dim/mid/bright），無 fade transition。舊光效記錄 ID 保留。武器能力的 AddSpell/RemoveSpell 仍走 Controller，符合 §4.4 的本期限制。
- schema 6→7：MainQuest 006008→00600A，MCMQuest 006009→00600B；006008/006009 變為 Schema6 inert stub，無 VMAD、flags=0、無 aliases。舊版全部 stub 仍保留；lock 由 round-9 preflight 從原 schema6 lock 生成，非手改 signature。SEQ/VMAD 全指新任務。DebugLevel 000811。

### 玩家倍率：變數 → 寫入/變更點 → 已接刷新

| 變數/依賴 | 寫入或事件 | 刷新 |
|---|---|---|
| 全部元素/初始資料 | Setup、讀檔 invalidation | InitProcCache、RefreshSyncStage、RefreshProcMagnitudes；Ready 前完成 |
| RankCacheA/B、BranchCacheA/B、LevelMirror | Trees.RefreshTree、RefreshActive | 匯出後刷新 Sync/Proc；另更新 Guard.NodeBits、Input 免費節點資格 |
| 活躍樹、形態 | RefreshTrees、SwitchForm、OnFormSwitched | RefreshSyncStage/Proc；先寫全元素強度，切換後更新窗口 |
| 同調點/階段/持續滿 60 秒 | AddSync、PushSyncStage、OnSyncStage、TickTimers | 點數變更重算快取；階段事件刷新 Proc；計時跨階後更新 GLOB/Divine/Proc |
| 基礎倍率、節點倍率、學派/削魔滑桿 | RefreshRuntimeValues、RefreshAbilities、RefreshTrees | 全 42 變體刷新；雷削魔 magnitude 獨立 cache |
| MoltenLeft | SetMolten、TickTimers 到期 | 刷新 Proc；原熔身自身資源邏輯保留 |
| OpenBoost[] | SetOpenBoost、TickTimers 到期 | 刷新 Proc |
| EndBoostLeft/Amount | SetEndBoost、TickTimers 到期 | 刷新 Proc |
| BloodthirstLeft | SetBloodthirst、TickTimers 到期 | 刷新 Proc |
| SelfOverheat | AddSelf/ClearSelf kind 4、ClearSelfAll | 刷新 Proc；原封頂/爆發/清除流程原樣 |
| EnvNight | EnvCheck 值變更 | 刷新 Proc；白天聖、夜晚暗加成保留 |
| 玩家血量/潛行/重擊/右手武器 | 原生命中條件每刀評估 | 選已烤好的 blood/wind/power 變體；不新增輪詢 |
| 目標熱度/印記/凍結/詛咒等 | 原 GetStack/host lazy Tick、原 TargetDamageMult | 不進玩家快取；差額讀舊路徑，<=0 不施法 |

### 離線引擎記錄證據與限制

唯讀掃描 `Skyrim.esm`、本機 Ordinator、Phenderix，機讀證據 `build/fix18-engine-evidence.json`。

- EP51 native Select Spell：原版 `TrickShot` 105F1A、`HackAndSlash30` 03FFFA、`Limbsplitter30` 0C5C05；Phenderix `ZZPerk_PoisonParalyze` 29FB10。DATA = [51,10,3]，EPFT=5，EPFD 為 SPEL reference。本機統計原版 57、Ordinator 132、Phenderix 1 段（文件的 129 是其他快照，不拿來硬套本機檔案）。
- 條件分頁/priority：Ordinator `ORD_Arc70_PinningShot_Perk_70` 058F62、劍類 NPC 05F56F 有多個 EP51 段，PRKE priority 非相同且條件互斥。它證明記錄格式，不證明同時命中多條件時 all/first；本輪使用明確遞減 priority。
- `GetEquippedItemType` 函式 597，弩值 12：Ordinator `ORD_Arc20_Wingstrike_Perk_20` 0064D3、`ORD_ArcMAX_PerfectAim_Perk_MAX_OrdASISExclude` 007526。避免把 Papyrus Weapon.GetWeaponType 的 9 當成 CTDA 列舉。
- `HasMagicEffectKeyword` 函式 699：Ordinator `ORD_Hea60_BreakUponMe_Perk_60` 00803A、`ORD_Res70_BastionWard_Perk_70` 014E7C、`ORD_Ill70_ShadowRefuge_Perk_70` 01F70E。浴火/水膜/影甲三處 693→699。
- 需實機：多 EP51 全部套用或首段即停、0x1D 是否放大 EP51 法術、拳腳/致死刀時序、OnSpellCast/瓦希安、GLOB 切換與視覺實際延遲。`build/fix18-probes.md` 列操作、通過條件及 fallback。獨立 opt-in ESP 8 記錄、唯一 master Skyrim.esm；未加進正式插件或自動啟用。第 2/3 期披風/階梯問題本期未碰，不聲稱已解決。

### 驗證資料

- 代表性 outgoing hit：pre-fix18 **165 = 115 scripted + 50 native** → **101 = 70 scripted + 31 native**，少 64 次（約 38.8%）。由相同 `fix12_cost.scenario`／`papyrus_harness.py` 執行真實源碼計數；不是遊戲幀延遲量測。
- 熱度 4：engine 11.55 + bonus 3.696 = old 15.246；熱度 0、未結算但已逾 6 秒、不同 rank 都驗 equality tolerance 1e-4；新舊 GetStack/host Tick 次數與結算結果相同。
- 6 玩家狀態序列共 120 比較；重複 RefreshProc 寫入 0。另 88 目標倍率組合（含開印、連殺、跨元素倍率）、88 血位/血怒/反轉邊界、200 SyncStage 樣本、15 Input gates。Guard idle 對 Controller 零呼叫。
- FormID 差異：3941→4067，新增 126（124 本期記錄＋2 schema6 stub）、刪除 0；僅兩個任務搬移。詳 `build/fix18-formid-diff.json`。
- 完整 build 及最終狀態以 `build/fix18-build-final.log`、`build/fix18-check.json`、`build/fix18-final-preservation.json` 為準；未部署、未執行遊戲。既有 PLAN COVERAGE 的穩步可選 deferred 項保留，unmapped 仍為 0。


### Round 18 最終驗收落款

- `python -B -u build_v03.py` 已親自執行，**exit 0**；20 scripts 全部 OK（0 errors）。READBACK/CSF/DELIVERY/LAYOUT/DOT/MCM/MCM DEFAULTS/SCHEMA、FIX10–FIX18、HITPROC 全通過，PLAN COVERAGE unmapped=0。
- `python -B build/fix18_final_preservation.py` exit 0：快照 24 檔及原 zero-difference preflight SHA-256 全不變；六個狀態層原始檔逐位元組不變；既有 BOM/CRLF 保留；正式 ESP+20 PEX 已記 hash。
- 上文證據名稱校正（append-only）：058F62 的實際 EDID 是 `ORD_Arc50_PinningShot_Perk_50_WasPowerShot`；05F56F 為 `ORD_OneNPC_NPCSwordPerks_Perk_30_WasBladesman_OrdASISExclude`，兩者是 Ordinator 對 Skyrim.esm 身分的 override。FormID 與原證據一致，僅校正文字名稱。
- 交付 `package/Elements Spellblade/`；正式包與 probe 包分離。最終呼叫 101，熱度例 15.246 等值。未部署、未啟動遊戲、未存取網路；所有需引擎實測之處仍明列為未驗證。


## Round 18b：提前探針自動量測與 VERDICT（選配包限定）

先保存 `.codex/pre-fix18b-snapshot/`：正式包 56 檔及 SHA-256、原探針、建置器、操作卡、實作紀錄；既有驗證 helper 在修改前亦各自保存。正式 `package/Elements Spellblade/**` 沒有寫入；`build_v03.py` 最終與快照逐位元相同。沒有部署到 MO2／SkyrimSE，沒有存檔寫入或網路存取。

### 為何依生命差判定，而不相信 GetMagnitude

本機 `vendor/imports/ActiveMagicEffect.psc` 對 `GetMagnitude()` 只有 native 宣告與回傳 effect magnitude 的註解，沒有交代 0x1D 調整在讀值之前或之後套用；ESP 只能證明 entry／keyword／倍率配置，不能證明引擎執行次序。因此沒有把這個 API 當成已證實的倍率判據。兩支新腳本同時記錄 `reported_magnitude` 與目標實際 `Health` 差；VERDICT 明列 `basis=health_delta`。

量測能力先於命中保存 Health。玩家用未附魔、未塗毒的鐵匕首，AttackDamageMult=0，只作普通單擊。量測器先以 Gate 關閉 native entry 51 附傷，要求第一刀實測生命差嚴格等於 0；再自行用 `DoCombatSpellApply` 施放不帶 ESSB_Probe 關鍵字的原生 10 點火傷參考法術，要求生命差為 10±0.15。這兩關通過，才開放真正的 entry-point 探針。參考 spell/MGEF 都不帶倍率 perk 的關鍵字；沒有拿 GetMagnitude 的值去呼叫 DamageActorValue 造出傷害，所以沒有循環論證。

測試須用只有原版主檔、SKSE 與探針的設定檔及可丟棄新檔。目標沒有其他攻擊、持續傷害或治療來源。FireResist、MagicResist、AbsorbChance、HealRate、HealRateMult 必須全為 0；起始及輪詢都檢查，ENV 保留實值。非零直接拒絕判定，不用未證實的抗性公式正規化；因此火抗不能把 20 誤判成 10。原生 10 點參考也能攔下難度或整體倍率造成的偏差。這是受控單目標序列實驗，不是通用戰鬥傷害歸因器。

每刀等最後一個命中／段落事件後 3 秒，再以間隔 1 秒的 Health 讀值確認穩定。所有行使用 `[ESSB-PROBE]`，包含 HIT／SEGMENT／MEASURE／VERDICT、目標、session、trial、Health 前後值、生命差、A/B/C 次數及回報 magnitude。連砍、非普通攻擊、換武器、改 perk、非零抗性、死亡／低血量、生命值不穩、事件數不符或控制失敗，均不得推出模式。Papyrus 忙碌會延長等待，以 READY 為準；沒有完整日誌也不是任何模式的證據。

### 判定與 opt-in 隔離

- A/B 各一次，生命差 12±0.15：`lightning_roll_mode = additive`。
- 只有 A 一次，生命差 5±0.15：`lightning_roll_mode = chain`。
- 只有 C 一次，生命差 20±0.15：`tier_multiplier_mode = entry`。
- 只有 C 一次，生命差 10±0.15：`tier_multiplier_mode = spell_variant`。
- 其他：MEASURE 保留觀察數值，`unexpected, report`，不改設定。

`xx000820`、`xx000821`、`xx000822` 的身分、entry point 51/29、5/7/10、乘 2、優先序 200/199 保留。A/B/C 使用獨立原生傷害 MGEF 綁定 segment，避免混淆回報；新增 10 點參考法術、Gate/Session，以及手動裝到目標的 `xx000831` 量測能力。entry 51 加 Gate 與 HasMagicEffect 條件，控制刀與未裝量測器的目標不觸發探針。能力條件要求玩家持有探針 perk；無 QUST，無自動 AddPerk／AddSpell。ESP 共 16 records，唯一 master 為 Skyrim.esm；2 支腳本使用 SKSE 的 GetMagnitude/ModEvent，沒有依賴正式 ESP／腳本。

操作增加「先 AddPerk，再對指定目標 AddSpell 量測器」，以便在第一刀之前取得生命快照；玩家不必讀血條或手算基準。控制刀與自動參考法術的 VERDICT 是量測資格，不是設定結果。完整步驟已更新在 `build/fix18-probes.md`。原版測試物件已從本機 Skyrim.esm 回讀：`IronDagger 0001397E`、`EncBandit01Melee1HKhajiitM 000C3CA0`。

### 建置、驗收文字裁決與證據

`python -B build/fix18b_rebuild.py` 啟動真正的 `python build_v03.py` 子程序；快照中的啟動 hook 將建置副產物及編譯輸出導向 `.codex/pre-fix18b-snapshot/rebuild-4/`。原建置配方及所有既有檢查完整執行，成功後只把選配探針複製回允許的資料夾。子程序退出 0，各 check 行 ok；正式 20 支及探針 2 支腳本編譯成功。既有 PLAN COVERAGE 單項 deferred 原樣保留。

依 standing authority，明示處理兩項不適用的舊驗收條件，沒有改變玩法：

1. 本次開始前實作紀錄已是完整 CRLF，round 8/10/11 的舊文件及 round 13–16 的雜湊／換行基準卻仍為更早的混合換行，未改動現況也會失敗。只把相應 helper 的實作紀錄基準改成 pre-fix18b 的精確原文，要求逐位元前綴不變、只能追加；有完整舊文件時，也核對正規化換行後的歷史文字前綴。round 8 新來源白名單精確加入兩支 ESSBProbe 檔名。未略過任何玩法、身分、原生記錄、成本或狀態回歸。
2. PapyrusCompiler 輸出不是決定性的：36 個非 PEX 正式檔案（含 ESP、來源、設定）重建後直接 byte-identical；20 個 PEX 的宣告／字串表順序及時間資訊會不同，不能聲稱原始重編譯 SHA 相同。完整解析每個 PEX 及全部 string ID，核對版本、字串多重集、物件、變數型別／初值、屬性、user flags、函式參數、依原次序的每條指令／運算元及 debug 行表；只排除編譯／debug 時間，並按名稱對齊無語意順序的宣告。20/20 相同後，組包保留原正式 PEX，56 檔 SHA-256 全等於快照。原始重編譯 PEX 與 SHA 另存，供覆核。實際正式目錄從未寫入，56 檔亦直接比對相同。

離線 harness 執行實際 `ESSBProbeMeter.psc` 的函式與事件：12 個判定案例包括 5／12／10／20／unexpected／重複／污染，完整建置另跑 5 個事件流程及 20 個抗性／吸收／回血拒絕案例。完建後補跑實際事件流程，加入非零物理控制及參考 5／20 的拒絕，共 8 個事件流程全部通過。事件測試涵蓋 A/B 逆序、回報 magnitude 與生命差故意相反；VERDICT 仍只依真實生命差與事件數。回讀檢查 master、無 QUST、VMAD、能力條件、entry／priority／Gate、原生 Health 傷害、EFIT 與 2 個 PEX。native 呼叫在離線環境是明確 mock，不能證明實際引擎模式。

證據：`build/fix18-probe-package/manifest.json`、`offline-check.json`、`preservation.json`；完整日誌 `.codex\pre-fix18b-snapshot\rebuild-4\build.log`；`rebuild-4/raw-pex/`；台帳 `.codex/impl-fix-round18b.html`。尚未進遊戲執行、未部署，實際 additive/chain 與 entry/spell_variant 仍須由玩家日誌決定。


## Round 18c：probe START／拒絕原因與 Finish 防護

- 僅修改 `src/ESSBProbeMeter.psc`、`src/ESSBProbeSegment.psc` 及允許的 build helper／操作卡；`build_v03.py` 完全未改。輸出仍在選配 `build/fix18-probe-package/`。
- 兩支 OnEffectStart 第一個指令為 `[ESSB-PROBE] START`。錯誤 perk 組合列出三個 perk 的實際載入序十六進位 FormID、持有狀態、探針 1／2 的必要組合及 removeperk／addperk 操作；誤選玩家時提示先移除玩家量測能力、點選強盜。
- 舊 session、不同目標、segment 建立／傳送失敗都有原因與重試指示；等待生命值穩定的提前 return 加上 WAIT。OnEffectFinish 不再呼叫失效 Self 的 UnregisterForAllModEvents，保留原有 session 判斷與 Gate 清理。
- 修正離線 harness 對字串內分號的解析，加入可選的 Papyrus 字串串接評估；新增 `build/fix18c_verify.py`，由 probe 建置 helper 自動執行。48 組啟動（8 種 perk 組合 × 玩家／強盜 × 05／81／FD 載入序）、setup、舊 session、segment 失敗、Finish 清理皆通過。回讀編譯 PEX 確認 START 是首指令、Finish 無 Self native 呼叫；原 18b verdict／controls 回歸亦通過。
- 保存：正式包 56 檔 SHA-256 與本輪修改前及 round18b 清單完全一致；probe ESP 位元組一致；既有編碼與換行保留。未部署、未連網、未實機驗證。
- 完整驗收尚未通過：`python -B build/fix18c_rebuild.py` 以 18b 隔離機制啟動 `python build_v03.py`，exit 1；在讀取權限邊界阻擋 MO2 的 Phenderix Elements.esp，檔案未開啟。原始完整建置另需其他 FX plugin 及 PO3 PSC/PDB 的唯讀授權；目前等待範圍確認，沒有略過檢查或將舊結果宣稱為本輪完整通過。
- 證據：`build/fix18-probe-package/refusal-check.json`、`offline-check.json`、`preservation.json`、`round18c-validation/rebuild-1/build.log`。所有完整建置暫存寫入皆隔離於本輪允許的 probe 子目錄。


## Round 18d：以原生參考校準探針倍率，修正失效原因（2026-09-21）

### 實機證據與修改

- 讀取 `.codex/smoke7-probe-Papyrus.0.log` 的 `[ESSB-PROBE]`：物理控制 delta=0；Master 的標稱 10 點參考實扣 4.75，Adept 實扣 9.50；參考 `reported_magnitude=0`。這是本次修正依據，不代表新版已進遊戲完成 verdict。
- `src/ESSBProbeMeter.psc` 改為 `k = reference_delta / 10`，穩定且 `0.2 <= k <= 2.0` 才輸出 `CALIBRATE k=...` 並開 gate；用 `health_delta / k` 送入原有 Verdict。5／12／10／20 的 ±0.15 容差與段落數判斷完全保留，reported magnitude 只供診斷。
- 合理帶選 0.2～2.0（含端點）：相當於參考實扣 2～20 HP，容納此次 0.475／0.95 與五分之一至兩倍的共同縮放，同時拒絕零／負傷害、接近零而放大量測誤差的分母及極端放大。這是量測器的保守有效範圍，不是宣稱所有模組倍率都應落在其中。
- 保留物理控制必須精確零、控制／probe 各一次 OnHit、參考零次 OnHit、reference 段恰一次、控制與參考不能有 A/B/C、最後活動後等待 3 秒並隔 1 秒確認血量穩定、perk 組合／武器／環境檢查、session ownership 與 STOP/gate 行為。
- `INVALID` 與失敗 `VERDICT` 現在記錄真實原因：額外／缺少 hit 的 count、expected、逐筆 source；非零物理控制 delta；sample/after 不同的 unstable health；perk 變更、武器變更／附魔、非零環境值、段落數錯誤與不合法命中旗標。delta=0 的 FE38982D 額外事件與雙揮只報事件問題，不再稱 CONTROL 非零。

### 校準等價性及限制

本次依玩家實機環境採共同乘數模型：難度倍率與其他模組造成的額外 x0.95，都同樣作用於參考和探針，因為它們是同一玩家對同一目標施加的同類原生火焰傷害法術；因此以參考取得的 k 可消去這兩項共同倍率。參考與 A/B/C 的原生 MGEF 傷害、學派、抗性 AV、施放／投遞設定一致，離線 ESP readback 有逐欄驗證。參考刻意不帶探針 keyword，故不受本探針自己的 0x1D keyword x2；這個差異正是探針 2 要量測的效果，不能一併校準掉。

不等價的情況包括：其他模組只修改特定 FormID／keyword／entry-point 施法途徑（參考使用 DoCombatSpellApply）；固定傷害扣減、門檻、傷害上限或其他非線性縮放；同一 session 中途更改難度、perk 或動態 buff；額外附傷／吸血／回血或目標狀態改變。既有 guards 可拒絕看得見的污染，無法保證偵測所有隱藏且恰好看似合法的差異。因此不能宣稱 k 能修復任意模組干預，也沒有追查 x0.95 來源。

### 驗收結果與重現

- `python -B build/fix18d_rebuild.py` 實際啟動 `python build_v03.py` 完整建置，exit 0；沒有略過 release、FX、powerofthree、歷輪回歸、probe 編譯及 readback。20 支 release 腳本及 2 支 probe 腳本編譯成功，所有檢查行 ok。完整日誌：`build\round18d\rebuild-1\build.log`；啟動日誌：`build/round18d/full-build-launch.log`。
- 使用既有隔離建置方法的 round18d helper，將 full build 原本會寫入未授權 `src/ESSBState.psc` 等路徑的產物導到 `build/round18d/rebuild-*/`；Python 網路呼叫被拒絕，編譯器 output/cwd 也導到 build。`build_v03.py` 本身逐位元組未改，CRLF 保留。沒有写入 MO2／SkyrimSE、設計／規劃文件或 `.strategic-advance/`。
- `fix18d_verify.py` 執行實際 PSC 函式與事件（原生邊界為 mock）：36 組完整校準流程，k=0.475／0.95／1.0 與邊界 0.2／2.0 各跑四種 verdict 且連續兩刀共 40 個有效 trial；0.1／2.1／0／-0.1 對四種 verdict 都拒絕。64 組診斷流程、16 個容差邊界通過。既有 12 個純 verdict、20 個抗性 guard、48 種開始組合及舊 session／事件失敗／Finish 回歸也通過。
- 修改前本機 release 的 20 個 PEX hash 已不同於部署版，36 個非 PEX 相同；先確認原本 20 個 PEX 的完整解碼內容等價，重編後再確認 20 個 PEX 亦等價（包含指令、屬性、locals、debug 行對照、flags 及 metadata，只排除編譯／debug 時間戳並正規化宣告順序）。保留 raw rebuild PEX 證據，組裝時重用經驗證等價的部署版 PEX，未跳過編譯。
- 最終 `package/Elements Spellblade/` 共 56 個檔案，路徑集合和每檔 SHA-256 全部等於目前 `MO2/mods/Elements Spellblade/`；部署目錄前後 SHA-256 不變。這是以部署版作為 byte-identical 基準，沒有將原本本機 PEX hash 差異隱藏為「從未不同」。詳見 `build/fix18-probe-package/preservation.json` 和 `build/round18d/baseline.json`。
- `build/fix18-probes.md` 已更新：難度不需精確指定但仍建議 Adept；session 內不能改倍率；看 CALIBRATE 行；用鐵匕首避開徒手額外事件；採校準後數值解讀 verdict。PSC 維持 UTF-8/LF，原本 CRLF 的 Python helper／操作卡維持 CRLF；實作紀錄僅附加。
- 交付：`build/fix18-probe-package/`。本輪未自動部署、未改正式玩法、未宣稱更新後已取得遊戲 verdict。需手動更新探針 ESP＋Scripts，再依操作卡重測。


## Round 18e：三個免主控台探針能力（2026-09-21）

- 交付 `build/fix18e-probes-install.zip` 與 `build/fix18-probe-package/`；ESP 共 23 筆記錄、4 支編譯 PEX、啟動 SEQ，唯一 master 仍是 Skyrim.esm。新增 0x840 玩家別名任務、0x841～0x843 次級能力與 0x844～0x846 效果；無需載入序查詢或主控台選取。
- `ESSBProbeSetup` 在玩家別名 OnInit／OnPlayerLoadGame 自動補發「探針：準備＋第一題」、「探針：第二題」、「探針：清理」。`ESSBProbePower` 僅接受玩家自施放，再呼叫共用設定流程。任務採 Start Game Enabled／Run Once、Player forced alias；附 SEQ 支援啟用時啟動。
- 第一題卸下玩家裝備、缺少時補一把原版 IronDagger 0001397E 並裝備，保存第一次準備前的 AttackDamageMult，再 force 0。強盜使用本機 Skyrim.esm 確認的 EncBandit01Melee1HKhajiitM 000C3CA0，延續舊卡測試對象；先以 disabled/persistent 生成，停 AI、停止戰鬥與警報、Aggression=0、卸下並清空裝備、Health force/restore 10000、五項抗性／吸收／回血 AV=0，再放到玩家前方 128 遊戲單位並啟用。只給 AB perk，量測器直接 AddSpell 到受 guard 保護的 Bandit 參照。
- 第二題先失效化 session、移除舊量測器與全部 probe perks，再沿用活強盜（死／disabled／deleted／無參照則換新），重做環境準備、給 Hit＋Multiply、掛新 meter。第一題重施放則刪除前一隻再建新。Busy 防止 latent 設定流程重入；spawn 或 meter 掛載失敗有明確提示。
- 清理關 gate、移除玩家誤掛與自有強盜量測器、移除三個 perk、Disable/Delete 強盜、還原保存 AV；重複施放／讀檔不覆蓋原值。三個操作能力與鐵匕首保留，卸下裝備不自動重穿。所有 Bandit 操作與 meter 加入都排除玩家；保留原 meter 自身的玩家目標拒絕防線。
- 每次能力一行 `[ESSB-PROBE] SETUP ...`，列強盜參照、三個 perk 狀態與 AttackDamageMult，並用繁中 Debug.Notification 指示下一步。含中文的新增 Setup PSC 使用 UTF-8 BOM，避免本機 PapyrusCompiler 的預設碼頁誤讀；新離線檢查回讀 PEX 字串確認中文不亂碼。原有 PSC／Python／操作卡換行與 BOM 慣例保持。
- 原有 16 筆量測記錄逐 subrecord／flags／本地 FormID 不變，Meter 與 Segment PSC 逐位元組不變；控制刀、自動參考校準 k、容差、事件計數與 verdict 全部不改。不加入 qasmoke 傳送，選平坦空曠處即可；Papyrus 不設定難度，session 中保持倍率固定。每題仍需 READY CONTROL 的控制刀及 READY PROBE 後的正式測試刀。
- 舊版整 ESP 固定 hash 檢查改為驗證原始 snapshot hash，並逐筆比較全部 16 筆原量測記錄；其餘原有邏輯檢查保留。fix8 範圍白名單只新增兩支獲准的 ESSBProbeSetup／ESSBProbePower，沒有關閉範圍檢查。build_v03.py 完全未改、CRLF 保留。
- `python -B build/fix18e_rebuild.py` 實際以子程序執行 `python build_v03.py` 完整建置，exit 0，每項檢查 ok，20 支正式腳本與 4 支探針腳本均編譯成功。完整日誌：`build\round18e\rebuild-2\build.log`；啟動輸出：`build/round18e/full-build-launch-2.log`。第一次 build 被舊白名單拒絕，已補精確路徑後完整重跑，失敗證據另保留於 rebuild-1。
- 新增 26 組實際 PSC 執行流程（原生呼叫為 mock），涵蓋 init/load 授予、目標狀態、各題 perk 集、玩家誤參照、原 AV 還原、重施放、四方向定位、死／失效／無目標、spawn／meter 失敗；另測 busy reentry 與非玩家 power 事件拒絕。ESP/QUST/VMAD/SEQ/次級能力的裝備欄位、4 支 PEX、中文與 vanilla bases 回讀通過。既有 36 校準流程、40 有效 trial、64 診斷流程、16 容差、48 開始組合等保持全綠。
- 正式包 56 檔的路徑集合與 SHA-256 同時等於本輪開始前 package 與只讀 MO2 部署版。完整建置所有生成寫入先導到 build/round18e，保留未授權 src 生成檔與報告的隔離副本；正式 PEX 完整重編、解碼與部署 PEX 等價後重用部署 bytes（排除非決定性時間戳／宣告排序，原始重編 PEX 留證）。沒有寫入 MO2／SkyrimSE、設計／規劃或 .strategic-advance，也未連網。
- 操作卡已改成收藏三能力、第一題、第二題、清理；控制／正式各一刀明確分開，舊 console 路線保留於附錄。ZIP 12 個檔案逐檔回讀一致；驗收摘要 `build/round18e/acceptance.json`，封裝 SHA-256 `bb1e4c03539ccfeb906047c8d0948ded118e59c3c3b2b911a8afa708b1c10cee`。
- 本輪僅完成編譯、完整建置及離線驗證，未自動部署，尚未在遊戲中驗證能力啟動與新 verdict；不將原生呼叫 mock 視為實機證據。

## Round 18f — 第三題跨 perk 探針與準備強化

- smoke8 已確認第一題 B only／normalized 7（k=0.95）與第二題 normalized 20；新增 `lightning_roll_mode = exclusive` verdict，保留 additive／chain，不改正式設定。
- 新增第三題能力（四能力總計）與兩組 separate PERK，各一筆 entry 51；A/B=5/7，同 rank，A 低 FormID 先加入；變體互換 priority 200/199。各判 12 additive／5 A winner／7 B winner／unexpected，日誌列屬性並保留因果不確定性。清理涵蓋七 perk、meter、目標與新增第三題能力。
- 3D 載入後保留 AI，以 restrained/dont-move 固定、non-ghost；等初始化與 AV settle，ModAV 只補實際殘差，最大／目前 Health=10000，連續六個 0.5 秒穩定及掛 meter 前 final check。控制刀可忽略有證據的非武器／法術／投射物零傷額外事件；REFERENCE／PROBE 與 ENV/Clean 維持嚴格。
- `python -B build/fix18f_rebuild.py` 啟動完整 `python build_v03.py`，exit 0；所有 check 行 ok。全量編譯正式 20 PEX、探針 4 PEX；29 筆探針 records，master 僅 Skyrim.esm。128 perk 子集合、15 新 verdict 流程、3 合格忽略控制、17 污染拒絕、5 AV/失敗流程、29 setup 流程及既有回歸均通過。
- 正式 56 檔修改前／重建成品／MO2 部署 SHA-256 全同；原始重編 PEX 留存，解碼指令／資料／除錯內容相等後重用原 PEX bytes。build_v03.py（CRLF）未修改，非探針 src 未動，無網路或 MO2／SkyrimSE 寫入。
- 產物：`build/fix18f-probes-install.zip`、`build/fix18-probes.md`、`build/round18f/acceptance.json`、`.codex/impl-fix-round18f.html`。新版 hittability 與跨 perk 行為待遊戲實測；離線 native mocks 不作 runtime 證據。


## Round 19：phase 2N 第一切片 — native 命中附傷（2026-09-22）

- 先建立 `.codex/pre-fix19-snapshot/`，包含 src、build、package、生成器、設定、schema、實作紀錄；產品修改前建立 `.codex/impl-fix-round19.html`。沒有寫入 MO2／SkyrimSE，沒有部署；設計文件及其他並行產物未由本輪修改。
- 先完成 `build/native-verification.md` 的 4.11 全部八列核實，再撰寫 DLL。選 CommonLibSSE-NG commit `b93280e832f263dbef44e44cbe2936622a02f91a`，SE-only、AE/VR/Xbyak OFF；依賴 commits 記於 `native/dependencies.lock.json`。MSVC 19.44.35229.0／VS2022 17.14，CMake 3.31.8 portable 的 SHA-256 及 SDK 版本記於 `native/toolchain.lock.json`；所有 cache 留 native/deps，不進套件。
- 自有 TESHitEvent sink，玩家武器／徒手來源、開關、形態、元素、存活／隊友／受控／格擋目標過濾；排除 spell/enchantment、未知來源與重入。依原 CTDA 597／673／286／640／77 讀條件，再由純函式選擇原 SPEL。命中不 lookup EditorID、不配置 JSON；data-loaded 一次解析 manifest。
- 唯讀追蹤本機 1.5.97 DoCombatSpellApply 與 CommonLib CastSpellImmediate 的兩條路，確認到同一目標施法核心。使用玩家 instant caster、magnitude override 0.0；仍交引擎處理抗性及 0x1D。證據見 `build/fix19-binary-evidence.txt`；未寫入或 hook 引擎程式碼。外部倍率／Vancian 等仍須實機驗證。
- Runtime 只接受 1.5.97 + SKSE 2.0.20；Address Library 在 REL 初始化前檢查存在及已核實 SHA-256，缺少／未知／損毀拒絕啟動。事件外層 SEH、內層 C++ catch；fault latch 停止後續處理並將 ESSB_NativeHit 清0。原生函式 `ESSBNative.SetNativeHit(Bool)`、`IsNativeHitActive()`、`NativeVersion()` 已編譯註冊。
- MCM 一般頁原「附傷開關」改「元素魔戰士總開關」，新增 DLL 命中附傷偏好、DLL 狀態與版本按鈕。ESSB_NativeHit 是 Constant GLOB、預設0，避免存檔殘留狀態；偏好另存 ESSB_NativeWanted。DLL 自有 ElementsSpellblade.log，L0 只有 load/version/fault，L2 每次套用一行 `[ESSB][hit][L2]`。
- Release ESSB_P_HitProc 保留原 ID／名稱，移除全部74段 entry51；整份 release ESP 自有 entry51 = 0。沒有 fallback。DLL 缺失／關閉／拒絕時，Papyrus 差額入口也不送額外附傷；狀態層保留。
- 除命中 perk 外，4,066 筆既有 ESP records 的 flags/subrecords 完全相同；全部既有 FormID 不變、無刪除。新增僅 GLOB `ESSB_NativeHit=0052D1`、`ESSB_NativeWanted=0052D2`。共4,069 records，master 僅 Skyrim.esm。`build/fix19-audit.json` 列完整差異與保留性證據。
- ESSBController 只插入四行原生可用性 guard；移除該 guard 後與 snapshot 逐位元組相同。原 base × (M_target−1) 差額及狀態腳本保留。HITPROC 仍檢查 DLL base + script bonus == old ApplyProc（誤差≤1e−4），OFF 的 mock 流程也確認0次差額施放。這是離線證據，不是假稱遊戲原生呼叫已測。
- 原生 CTest 通過：39,888 組獨立舊 ESP CTDA oracle，涵蓋 round18 真值表、血量邊界、target veto、所有雷電判斷組合；整數／連續亂數模型各1,000,000次分佈測試。`build/fix19-native-test.log` 保留結果；實際遊戲亂數仍调用原 GetRandomPercent。
- 已知可感知差異：舊 release 雷電 R5→R1 priority 遞減，按 smoke 的最低 priority 規則會被 R1 蓋住；舊 HITPROC 只測 random=99，未抓到此矛盾。本輪依 acceptance 明列的五檔 N=1 實作 R5→R1 首成功門檻。原真值表全通過，**但不能宣稱舊 release 全部亂數结果等價**；此點亦載於核實文件與探針卡。
- `python -B native/build.py` 完成 DLL、原生測試及 source-hash receipt。`python build_v03.py` 的完整流程通過（執行時另加 -B/-u，避免 pycache 並即時留 log）；21支正式 PEX、既有探針及各輪回歸均成功。日誌 `build/fix19-build.log`；既有「穩步」延後項目維持原狀。
- 生成器先驗 DLL／receipt／sources／settings／toolchain lock／依賴 HEAD 與 tracked diff，再寫 package。兩次負向測試確實以 exit1 拒絕缺 DLL 及變更來源後的 stale DLL，60個套件檔案 SHA-256 皆未變，最後還原並確認 fresh：`build/fix19-negative.json`。套件只帶 DLL/manifest，不帶 dependency cache／PDB。
- 舊 entry51 檢查改為「release 必須空，74段 snapshot 僅作 oracle」；其他既有 identity 檢查只允許新增上述兩個 GLOB。fix12 mock 補 native availability=true 以繼續驗原公式。fix8 歷史 scope 對設計器並行更新的兩份設計文件取消舊 digest 假設，辨識現存並行 art-book／Git／設計筆記路徑，沒有修改那些文件；其他範圍檢查保留。
- 原有 BOM／換行保留，build_v03.py 仍全 CRLF；settings.json／state-schema.lock.json 未變。實作紀錄僅追加。交付 `package/Elements Spellblade/`、`build/native-verification.md`、`build/fix19-probes.md`、`native/README.md`。尚未遊戲實測 DLL 載入、實際倍率／事件時序、MCM 互動、Vancian 或外部 entry51 共存；五分鐘卡提供步驟與 PASS/FAIL。

## Round 19b：native 地基修正（F1–F8，2026-09-23）

- 快照沿用 `.codex/pre-fix19b-snapshot/`（前次中止的嘗試所建，未覆寫）；進度帳 `.codex/impl-fix-round19b.html`。
- **F1 通知**：DLL 用 `RE::DebugNotification`（CommonLib-NG Misc.h，ID 52050）經 SKSE task interface 在主執行緒顯示：故障（附原因）、讀檔時 MCM 已關或 debug≥1「運作中」、MCM 開關切換、debug≥2 每次附傷一行（元素＋法術名，1 秒最多一行）。資料載入期的通知延到讀檔後。版本／Address Library 拒絕發生在 DLL 能呼叫遊戲函式之前，改由 `ESSBController.Setup` 在讀檔後顯示通用「DLL 命中附傷未運作」通知。
- **F2**：`native-verification.md` 撤回「CSI 等同 DoCombat」。同法術關卡 0x140552E80 只對 SpellType {1,4,10} 生效（我們 53 筆附傷都是 0）；兩路仍不同（DoCombat 多 CheckCast）。反組譯證據補齊 13 個函式，`build/fix19_binary_evidence.py` 可重現。**另發現並修正**：round 19 傳 effectiveness 0.0，引擎 0x140540360 會把附傷 magnitude 壓成 1 點；改為 1.0（DoCombat 對 SpellType 0 傳的就是 1.0）。
- **F3**：命中路徑改讀 `ESSB_NativeHit` GLOB（ESSBController 新增 `NativeHit`、`NativeWanted` 兩個 VMAD property），不再每擊呼叫 DLL；DLL 不在時不會有 unbound-native 錯誤。MCM 仍用三個 native 函式。新增 property 是持久成員變更 → **schema 7→8**：`ESSB_MainQuest` 0x00600A→0x00600C、`ESSB_MCMQuest` 0x00600B→0x00600D，舊 ID 成 Schema7 stub（累計 14 個空殼）。
- **F4**：Selection.h 刪除正式路徑不會用到的 veto／float 血量欄位。新測試 `hit_pipeline_test`：A 228 組正式輸入 vs 舊 74 段 ESP oracle（38 個法術全可達）；B 1,376 組 mock 引擎接線；C 4,368 組命中事實 vs Papyrus 命中閘門轉寫；D 2,000,000 次雷電抽樣。5 個注入錯誤全部被抓到。
- **F5**：fix8 的 `continue` 改為「規格檔工作樹＝HEAD」；fix18 的 HITPROC 標籤改成實際檢查內容；receipt 從建置樹讀回 cl／SDK／cmake／CTest 並與 lock 比對；entry-51 檢查只留一處且先看 PRKE 類型。
- **F6**：刪 `build/fix19_edit.py`、cmake 分段檔與多餘 zip、兩個 evidence clone、舊 `native/out`；fetch_cmake 改單檔下載＋lock SHA-256。freshness 只 hash DLL 的生成來源，另重算 ManifestData.h 比對；Address Library 路徑改 `settings.json` 的 `address_library_bin`。
- **F7**：DLL 過濾器改為 Papyrus `OnWeaponHit`／`ResolveHitWeaponType` 的移植，base 與 bonus 接受同一批命中。可感知變化：(1) 從背後／側面打正在舉盾（但這刀沒被擋）的敵人，現在有 base（舊 CTDA「目標 IsBlocking」拿掉，改用精確的 kHitBlocked）；(2) 無來源但有投射物、手上有弓弩的命中現在有 base；無來源空手命中若手上有法術／盾／火把則無 base（Papyrus 本來就不給 bonus）。保留：盾擊、被格擋、法杖、法術／附魔來源都不觸發。已知仍不一致：武器來源的爆炸濺射（Papyrus 拒絕，TESHitEvent 無爆炸位元）。
- **F8**：Plugin.cpp 分五段、狀態集中、每行一敘述、BCrypt RAII、log 改單次 WriteFile（無 mutex）。/MD 需 VC++ 執行階段 ≥ 14.44（本機 14.51）。
- 建置：`python -B native/build.py` exit 0（cl 19.44.35229.0、SDK 10.0.26100.0、cmake 3.31.8，ctest 1/1）；`python build_v03.py` exit 0，全部檢查行 ok（`build/fix19b-build.log`；既有 optional deferred「穩步」維持）。`fix19_audit.py`：4,037 筆記錄不變、30 筆變更全部是預期（清空 perk、任務搬 ID、主任務 +2 property）；`fix19_negative.py`：缺 DLL／來源過期皆在寫 package 前拒絕。release ESP 3,227 筆 PERK 依 PRKE 類型掃描，entry-51 為 0。
- 未做：遊戲內實測（DLL 載入、通知顯示、實際傷害、Vancian／For Honor 共存）——見 `build/fix19-probes.md`。GlobalVariable.GetValue 本身是否需等主執行緒未核實。

## Round 20：N2 — 命中時由 DLL 算附傷強度（2026-09-25）

- **做了什麼**：附傷強度改在命中當下由 `ElementsSpellblade.dll`（0.20.0）計算，用 `CastSpellImmediate` 的強度覆寫值施放（覆寫會套到每個效果，所以每個數值一顆單效果法術）。公式照 v0.4：`B×R×G×BDM×(1+Σ節點%)×血位×血怒×環境×亡靈×驅魔×風潛行×暴擊`。純計算在 `native/include/HitMath.h`，引擎讀取對應在 `native/include/EngineFacts.h`，`native/src/Plugin.cpp` 只讀引擎與施放。
- **無元素**：基準真傷、吸魔、小滅法、滅法、滅法印、沉默（1～8 秒 8 顆法術挑一顆）與同格節點全在 DLL；Papyrus 只剩純武藝、斷咒（`ESSBNoForm.OnInterruptCast`）、反咒。
- **護血池**：血溢的溢出灌進玩家身上 `ESSB_BloodGuard` 的強度（上限最大生命 20%）；扣減是 N4。沒有計時執行緒。
- **刪掉的 Papyrus**：`RefreshProcMagnitudes` 與所有觸發點、Proc* 快取、烘焙變體（`build/fix18_records.py` 只剩 22 顆元素×普通／重擊）、命中吸血、血盾暫時生命、土／風／水命中平值。差額補丁 `ESSBController.ApplyProc` 只補 DLL 讀不到的目標側與腳本狀態項（到 N3）。state schema 8→9。
- **測試**：native A0 6、A 694（對 `build/fix20_reference.py`）、B 295、C 8442、D 1,140,000 次擲骰（5σ）；`build/fix20_verify.py` 鏡像 2792、目標側 110、差額 672、未動腳本 20 支位元組相同、無預寫、切換標記 4。注入錯誤 native 17/18、Papyrus 5/5 被抓到。
- **修正**：`build/fix19_native.py` 的 `node_table` 原本用 `plan_trees.build()`，會把未套 fix8 決策的樹覆寫回 `build/plan-tree-nodes.json`；改用 `apply_balance_text(parse())`。
- **清理**：round 19 的 09-22 舊證據 7 檔搬到 `build/retired/2026-09-22-round19/`；`build/native-verification.md:85` 改指 `build/fix19b-build.log`。
- 建置：`python -B native/build.py` exit 0；`python build_v03.py` exit 0，全部檢查行 ok（`build/fix20-build-final.log`）；release ESP 3,227 筆 PERK，entry-51 為 0。
- 未做：遊戲內實測（見 `build/fix20-probes.md`）；N2 未做列與原因見 `.codex/impl-fix-round20.html`。

### Round 20 審查修正（2026-09-25）

- 建置可重現：`build/fix8_verify.py` 的雜湊比對跳過 git 忽略的 `__pycache__/`；根目錄的舊快取移出工作樹。建置本身不寫快取（`sys.dont_write_bytecode`／`-B`）。
- 測試補洞：小滅法真傷倍率（沉默＋靜寂、目標魔力低於 25%＋低魔主線）8 個情境；護血池剛好滿與超過上限 8 個情境；`build/fix20_verify.py` 新增 `DifferencePossible` 各元素條件 16 例（Papyrus 突變 8/8 被抓到），並補上預期值漏掉的風潛行連殺 ×2。
- `Plugin.cpp`：餘響標記的驅散改成先收集、走完清單再驅散，task 包 C++ 與 SEH 兩層防護。
- 護血池：讀取時先壓到最大生命 20%（最大生命下降後會壓回），滿池不重施。同一幀兩刀時第二刀的溢出會遺失，記為 N4 已知項（修正需要 DLL 存狀態）。
- 玩家感受補充：
  - 無形態每次重擊付掉最大魔力 15%（滅法是基本能力）。
  - 吸血量約剩以前的五分之一到六分之一：舊 `B_max × 5 × G × 吸血比例 × 舊血位倍率`（50% 生命、樹等級 1 ≈ 10 × 5 × 1.05 × 0.25 × 1.1 ≈ 14.4），新 `附傷強度 × 吸血比例`（≈ 9 × 1.05 × 0.95 × 0.25 ≈ 2.2）。
  - 驅魔從節點合計內 +0.5 改成最後 ×1.5。
  - 雷暴擊不放大 Papyrus 補的目標側加成。
  - 雙生左手打出目前元素的那幾刀沒有目標側加成。
  - 無元素樹（ESP 仍是 v0.3）：破魔新手主線 (11,1,0) 與熟練主線 (11,1,1) 的點數完全沒有效果（`OnManaBreak` 已刪、DLL 不讀；分支蝕魔、斷咒、反噬、反咒照常）；(11,0,0)／(11,0,3)／(11,2,3) 與 (11,0,1)／(11,0,2)／(11,0,4) 仍是 v0.3 效果。等技能樹重建切片處理。
- 探針卡 `build/fix20-probes.md`：燒魔「約 5.2～5.3」；吸血改看通知強度；新增潛行旗標、無形態重擊耗魔 15%、自耗法術無受擊反應三步；沉默改看 NPC 魔力一秒內維持 0。

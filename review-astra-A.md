# gpt-6-astra 獨立審查 ｜ 批次 A（元素節點效果）｜ 2026-09-17

來源：Codex CLI，model gpt-6-astra，effort high，read-only，原始碼內嵌。已排除 review-2026-09-17.md 的 13 項。

本批找到 **15 項新增缺陷**，依影響排序。僅審查內嵌快照，未使用工具、存取網路、修改檔案或執行建置。以下 `high` 表示可由提供的程式路徑與節點文字直接確認，不代表已完成遊戲實測。

**新增發現**

1. **P1｜冰終焉提前清空凍結，使「冰河」失效，並取消「冰棺」**
   
   **位置：** `src/ESSBReactions.psc:271-275,235-236`；`src/ESSBElem.psc:473-475,638-644,656-662`。
   
   **觸發：** 對已有狀態宿主的冰封目標觸發終焉；取得「冰河」但未取得「冰崩」，或取得「冰棺」。
   
   **原因：** `Shatter` 剛替冰棺設定凍結，`EndFrost` 隨即 `ClearStack(2)`；接著才執行 `EndFrostNodes`，冰河讀到的凍結也已是 0。範圍碎冰同樣在 `Shatter` 後清空。違反 frost r2 t3 B0「冰封時引發範圍碎冰」及 r2 t4 B0「未處決者再冰封 2 秒」。
   
   **最小修法：** 結算前保存冰封條件供冰河使用；清除原凍結後才施加冰棺，並明確設定 2 秒，不能直接沿用一般冰封時長。
   
   **信心：** high。

2. **P1｜「雷殛」必須另點「蓄餘」才會工作**
   
   **位置：** `src/ESSBReactions.psc:280-285,235-236`；`src/ESSBElem.psc:558-559,588-589,673-685`。
   
   **觸發：** 有電荷、取得「雷殛」，但未取得「蓄餘」，觸發雷終焉。
   
   **原因：** 基礎放電先清空電荷，後執行的 `EndShockNodes` 再查 `GetSelf(1)`，得到 0，因此跳過雷殛。lightning r2 t3 B0 未要求取得蓄餘。
   
   **最小修法：** 保存終焉開始時的電荷，將同一份快照傳給基礎放電及雷殛；結算後只消耗一次。
   
   **信心：** high。

3. **P1｜「蝕魔終焉」在原本的暗終焉立即兌現**
   
   **位置：** `src/ESSBReactions.psc:235-238`；`src/ESSBElem3.psc:975-978,1015-1024`。
   
   **觸發：** 取得 darkness r2 t1 B1，將暗印記切換成其他元素。
   
   **原因：** `OnEnd` 先設定 `SetPendingDrain()`，緊接著同一次 `End` 呼叫 `OnAnyEnd`，立即 `TakePendingDrain()`。接管元素的下一次終焉反而沒有吸魔，與節點文字相反。
   
   **最小修法：** 先兌現進入此次終焉前已有的 pending，再執行會設定新 pending 的元素掛勾。
   
   **信心：** high。

4. **P1｜冰、風、毒、星的「開印效果 +3%／點」沒有作用**
   
   **位置：** `src/ESSBReactions.psc:108-115,121-128,140-144,154-157,169-175`；`src/ESSBElem2.psc:308-339`；`src/ESSBStatus.psc:648-650`。
   
   **觸發：** 對上述任一元素投入 r1 t4 主線，再開印。
   
   **原因：** `mult` 雖計算完成，這四個元素的開印路徑都沒有使用它；額外掛勾也未收到它，星痕桶只保存層數。滿點的 +45% 因此完全落空。由 `TakeNextOpenMult()` 取得的接管開印加成也在相同路徑被消耗而未使用。
   
   **最小修法：** 將倍率傳入實際開印效果；整數層數需明確換算，延遲星傷需保存加權傷害，不能只計算後丟棄。
   
   **信心：** high。

5. **P1｜毒、水終焉丟棄已計算的終焉倍率**
   
   **位置：** `src/ESSBReactions.psc:189-205,373-385`；`src/ESSBStatus.psc:354-355,604-614`。
   
   **觸發：** 毒／水終焉受到關閉新手主線、通用終焉主線、三重奏或協奏等倍率加成。
   
   **原因：** `EndPoison` 只保存催毒秒數；後續毒傷沒有保存或讀取 `afMult`。`EndWater` 則直接設定 `GuideMult()`，同樣忽略 `afMult`。這違反各樹「終焉 +2%／點」及共通終焉倍率的文字；三重奏 ×3 等效果也可能被白白消耗。
   
   **最小修法：** 催毒狀態保存終焉倍率，僅在催毒期間套用並納入 Export/Import；水導引設定值應納入傳入的終焉倍率。
   
   **信心：** high。

6. **P1｜「星斷＋流星雨」只有中心目標受到 ×0.6 真實傷害**
   
   **位置：** `src/ESSBElem3.psc:855-869`。
   
   **觸發：** 同時取得 astral r2 t1 B0、B1，對附近有其他敵人的星印記融斷。
   
   **原因：** 中心目標走 `ApplyTrueDamage(amount * 0.6, ..., 10)`，範圍目標卻固定走 `ApplyDamage(11, amount, ...)`，既未轉真傷，也未乘 0.6。兩者都是同一次星落的命中。
   
   **最小修法：** 抽出單目標星落結算，中心與範圍目標共用相同的 `aiReason`／星斷判定。
   
   **信心：** high。

7. **P1｜死咒的「已損失生命」項漏乘終焉加成**
   
   **位置：** `src/ESSBReactions.psc:390-395`；`src/ESSBStatus.psc:666-673`。
   
   **觸發：** 目標已損失生命，且暗終焉有 `afMult` 或 darkness r2 t4 加成。
   
   **原因：** 基礎項先乘 `afMult * SignatureMult`，延遲結算時新增的失血項只乘 `GetDamageMult(10)`。節點要求放大死咒整體，不只固定傷害。其他倍率取 1、失血 100、終焉倍率 ×2 時，程式得到 `14×2+15=43`，應為 `(14+15)×2=58`。
   
   **最小修法：** 保存終焉倍率，結算時套用到完整的固定項與失血項；新增保存值需同步搬家。
   
   **信心：** high。

8. **P1｜「血海」會沿目標接力擴散，越過原本範圍**
   
   **位置：** `src/ESSBReactions.psc:218-233,345-355`。
   
   **觸發：** 血海啟用，流血目標 A、B、C 排成鏈；B 在 A 範圍內，C 在 B 範圍內但在 A 範圍外。
   
   **原因：** A 呼叫 `End(...B..., True)` 後，B 仍會進入 `EndBlood` 的血海掃描，再對 C 呼叫 `End`。`abChain` 的阻止位置在元素本體執行完之後，攔不住這段遞迴。blood r0 t4 描述的是一次指定半徑的範圍效果。
   
   **最小修法：** 將連鎖旗標傳入 `EndBlood`，子結算禁止再次啟動血海，只由原始中心掃描一次。
   
   **信心：** high。

9. **P1｜狀態桶新增層數可能少結算一次傷害**
   
   **位置：** `src/ESSBStatus.psc:195-207,595-614,658-660`。
   
   **觸發：** Tick 已取得血／毒層數快照，執行跨腳本呼叫期間，另一個命中新增層數；之後原 Tick 繼續老化桶。
   
   **原因：** 依題目提供的 Papyrus 規則，呼叫其他腳本物件會釋放呼叫者鎖。新層數沒有進入本次傷害快照，卻立即被 `RingAge` 從 `[0]` 移到 `[1]`。新增毒層因此只剩 5 次未來結算，流血只剩 4 次，違反六格／五格容器的計傷承諾。
   
   **最小修法：** 在不呼叫外部腳本的同一段中完成兩個桶的快照與老化，再以快照執行傷害及擴散；其間新增的層數留在新桶。
   
   **信心：** high。

10. **P2｜「血斷」額外治療套用到所有血終焉**
    
    **位置：** `src/ESSBReactions.psc:218-219,320,341-343`。
    
    **觸發：** 取得 blood r2 t1 B1，透過切換、自然過期或連鎖觸發血潮。
    
    **原因：** `EndBlood` 沒有收到 `aiReason`，只要有分支就額外治療 `amount * 0.5`。文字限定「血印記融斷」。
    
    **最小修法：** 傳入 `aiReason`，額外治療限定 `aiReason == 1`。
    
    **信心：** high。

11. **P2｜血潮結算的剩餘流血傷害漏掉節點倍率**
    
    **位置：** `src/ESSBReactions.psc:320-324`；`src/ESSBStatus.psc:331-336,595-598`；`src/ESSBElem2.psc:650-657`。
    
    **觸發：** 有剩餘流血，並投入 blood r0 t0、啟用「止血」或取得「萬象」，再觸發血潮。
    
    **原因：** 正常流血使用 `OmniMult * BleedTickMult`，傳給 `BleedRemaining` 的每層傷害卻沒有這兩項。提前結算的因此不是文字所稱的「剩餘流血總傷」；滿點新手主線的 ×1.3 也會消失。
    
    **最小修法：** 正常 Tick 與 `BleedRemaining` 共用同一個每層固定流血傷害公式。
    
    **信心：** high。

12. **P2｜自燃基礎倍率錯用爆燃公式**
    
    **位置：** `src/ESSBElem.psc:379-396`。
    
    **觸發：** 熱度恰好 10、沒有其他倍率加成時自燃。
    
    **原因：** fire r0 t4 明列自燃「×2 → ×3.5」，程式卻由 `1 + 10×0.2 = 3` 起算，主線滿點成為 ×4.5；還額外吃到關閉專精的爆燃每層係數。
    
    **最小修法：** 自燃使用獨立的基礎 ×2 與業火加成，不直接沿用 `DetonatePerLayer`。
    
    **信心：** high。

13. **P2｜落地傷害主線滿點只有 ×0.875**
    
    **位置：** `src/ESSBElem2.psc:620-627`。
    
    **觸發：** wind r2 t4 投入 15 點，未取得「上天」。
    
    **原因：** 文字指定 `×0.5 → ×1.25`，程式計算 `0.5 * (1 + 0.05*15) = 0.875`，每點實際只增加 0.025。
    
    **最小修法：** 基礎主線公式改為 `0.5 + 0.05 * rank`；再處理「上天」的基礎值替換。
    
    **信心：** high。

14. **P2｜風的拉近距離主線意外增加附傷**
    
    **位置：** `src/ESSBElem2.psc:83-87,418-420`。
    
    **觸發：** wind r1 t1 投點，開印後 5 秒內命中。
    
    **原因：** 同一個 Rank 在 `PullDistance` 增加拉近距離，又在 `WindHitMult` 每點增加 1% 附傷。節點只描述拉近距離，滿點卻額外送出 15% 附傷。
    
    **最小修法：** 刪除 `WindHitMult` 中對 `(4, 1, 1)` 的附傷加成，保留距離公式。
    
    **信心：** high。

15. **P2｜「星耀」立即引爆漏掉星痕加成與引爆回饋**
    
    **位置：** `src/ESSBElem3.psc:437-444,825-849`；`src/ESSBStatus.psc:648-651`。
    
    **觸發：** 同調三段取得星耀，同時投入星痕延遲傷害主線、萬象、星盾或星體。
    
    **原因：** 星耀清空桶後直接造成 `ReactDamage(..., 2.0) * layers`，沒有正常星痕引爆的 `AstralTickMult`、`OmniMult`，也未呼叫 `OnAstralDetonate`。因此「延遲星傷改為立即並 ×2」會丟失原有加成、護盾與治療。
    
    **最小修法：** 延遲與立即引爆共用結算函式；星耀只改變時機並追加 ×2，保留既有倍率及回饋。
    
    **信心：** high。

**已檢查、未發現問題的部分**

- 元素分派的 `1..11` 與樹索引 `0..10` 對應，以及 `TreeOf` 的換算。
- 電荷、岩甲、血痕、聖印、水壓、詛咒、星痕的上限計算函式；冰凍結增量以隨機補足小數的計算。
- `FlowPercent` 的長流百分比；星臨、星落／星域及群星各自的範圍成長公式。
- 共通樹的同調門檻縮放、三分之一承接、開印同調、附傷及融斷倍率的局部算式。
- 破魔固定削魔量與最大魔力 2% 取高、按命中前魔力限制削減量、真傷比例上限 0.95，以及枯竭的局部判定。
- 無形態融斷的範圍、餘燼比例及 `OnBurst` 傳給控制器的時窗數值。
- 狀態搬家的 **27 個 Int 與 12 個 Float 欄位對稱**；本批宣告的陣列均未超過 128。這不包含換宿期間的跨腳本原子性保證。

**覆蓋範圍與未檢查部分**

| 要求 | 本批到達範圍 |
|---|---|
| 1. 三個元素腳本 | 已審完整內嵌內容，對照提供的節點文字；未讀原始 JSON 或完整設計章節。 |
| 2. 共通／無形態樹 | 已審 `ESSBNodes.psc`、`ESSBNoForm.psc`，另審 `ESSBCounter.psc`、`ESSBSilence.psc`；控制器內的時窗消耗、同調承接與真傷封裝未檢查。 |
| 3. 狀態／反應 | 已審 `ESSBStatus.psc`、`ESSBReactions.psc`；完整設計 2.6／2.7 未提供，只能使用節點表及內嵌註解交叉核對。 |
| 4. 控制器／守衛／技能樹／印記 | 四份原始碼未內嵌，未檢查八目標登記、逐出、形態關閉清理、載入後重新註冊、主 Tick 延續及 CSF 點數結清。 |
| 5. ESP 產生器 | `build_v03.py`、`vendor/wbDefinitionsTES5.pas`、`build/v03-formids.json` 未提供，未檢查記錄格式、flags、CTDA、alias 與 property bindings。 |

另未檢查 `實作紀錄.md`、`build/plan-coverage.json`、實際 ESP／PEX、引擎執行結果或即時修改後的檔案。原始碼明示的既有偏離，例如整數秒近似、治療抑制的 AV 替代及幻影的攻擊削弱替代，未列為新增缺陷。

**既有 13 項**

未重報。就本批可見材料，沒有足夠證據需要撤回或更正既有項目；既有 advisory 也未重新計入。
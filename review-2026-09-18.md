# 元素魔戰士 v0.3 獨立唯讀審查 — 2026-09-18

審查對象：D:\Game\Other\SKSE\.codex\elemental-spellblade，Skyrim SE 1.5.97。以現有 src、build_v03.py、已產出的 ESP／manifest，以及兩份真實日誌交叉核對；設計以「元素魔戰士規劃-v0.3.md」為準，後來決策以「實作紀錄.md」覆蓋。以下不是對十一輪修復的再次通過宣告。

**結論：13 項具體問題。** P1 優先處理持續命中的計時、一般讀檔後維持費、跨程式啟動的時間戳、友軍排除與每擊 VM 成本。沒有把已列案的 array/None、schema 舊存檔、雙重 kill／skill、化灰、循環音效、no-form gate 或弓弩零命中重新列為新發現。

只寫本報告。未連網，未執行 build_v03.py、compiler、installer，未寫 MO2／SkyrimSE。計數使用既有 harness 的記憶體內副本與明示 native mocks，不是 Skyrim 執行結果；所有 Python 都使用 -B。未修改原始碼、測試或 harness。

已產出 ESP：package/Elements Spellblade/Elements Spellblade.esp  
SHA-256：32218582957c68a04f53d7dc6c10d565846c9e42b8f4ff33c452cca087eb2126  
實讀 3917 records，manifest 3917，master 僅 Skyrim.esm，schema 3。

## 發現（按影響排序）

1. **[P1] 連續命中會餓死控制器 tick。**  
   **位置：** `src/ESSBController.psc:783-812, 1601-1635, 3258-3380`。  
   **重現：** 開形態後持續每 0.5 秒有效命中；每擊把 `NextTickAt` 改成「現在 +1 秒」，`OnUpdate` 到達時又因尚未到期而只重新排程。  
   **錯誤依據：** `元素魔戰士規劃-v0.3.md:125, 146, 184` 的資源／限時效果，以及 `SwapHosts` 的 25 秒換宿要求（`src/ESSBController.psc:2294-2310`）。`TickTimers`、領域、元素每秒掛勾與換宿都因此延後；持續超過宿主的 30 秒生命期，更可能直接遺失未匯出的狀態。維持費另有 AME 計時器，不屬於此條。  
   **最小修正：** 命中只可提前既有 deadline，不可延後；僅在完成 tick 後建立下一次 deadline，保留 lift 的較早排程。  
   **信心：high。**

2. **[P1] 一般讀檔也可能永久停掉維持費與魔力歸零自動關閉。**  
   **位置：** `src/ESSBFormRules.psc:74-83, 112-125`；`src/ESSBController.psc:432-439, 1036-1054`。  
   **重現：** 同 schema、開著形態存檔；讀檔後控制器設 `Ready=False`，延後 2 秒 Setup。若原本的 FormRules 單次更新先到，`IsOperational()` 為假便直接返回；Setup 發現能力仍在身上，不重加，也不發出重新啟動更新的事件。直到再次切換形態才恢復。  
   **錯誤依據：** `ESSBFormRules.psc:2-4` 的每秒付費／兩秒空魔規則；`實作紀錄.md:3062` 只豁免血形態的魔力費，沒有豁免一般讀檔。這不是已列案的 schema 升級失敗。  
   **最小修正：** 控制器完成每次 Setup 後明確重新喚醒現存 FormRules；將暫時未 Ready 與失效／舊世代分開處理，舊世代仍直接退出。  
   **信心：high（觸發取決於載入後事件到達次序）。**

3. **[P1] 持久化的 application-time 時間戳會在重啟遊戲後凍住冷卻／經驗。**  
   **位置：** `src/ESSBTrees.psc:76-77, 88-95, 656-669`；`src/ESSBController.psc:1894-1900, 1976-1982, 3821-3837`。  
   **重現：** 遊戲已開 7200 秒、`XPCount` 達每秒上限時存檔；退出程式，重新啟動並於 uptime 60 秒讀取。差值為負，`XPBudget` 不重置，命中經驗持續被拒，直到新 uptime 追上舊時間。開印／終焉／斷咒／冰心也有同型判斷。  
   **錯誤依據：** `vendor/imports/Utility.psc:11-14` 明定 `GetCurrentRealTime` 是「程式啟動後秒數」；這些成員隨存檔保存，但一般載入未重新定基準。  
   **最小修正：** 在載入完成時重置短期戰鬥冷卻、XP 視窗及暫存 deadline；要保留剩餘時間者改保存剩餘量並重建 deadline。不要重置技能等級與點數。  
   **信心：high。**

4. **[P1] 直接命中沒有排除同伴與受命角色。**  
   **位置：** `src/ESSBController.psc:1371-1374, 1414-1434, 1610-1612`；對照 `3136-3156`。  
   **重現：** 誤擊隊友或自己的召喚／復生僕從；目前會繼續附傷、破魔或掛印／已交戰標記。範圍掃描有排除，直接命中沒有。  
   **錯誤依據：** `元素魔戰士規劃-v0.3.md:286-295` 明確要求直接命中與範圍效果都排除同伴、召喚物與復生僕從。  
   **最小修正：** 在任何附傷及 no-form 分流之前，加入 `IsPlayerTeammate()`／`IsCommandedActor()` 排除；保留中立者第一次被主動命中的資格，不可直接套用只接受已交戰者的完整範圍 gate。  
   **信心：high。**

5. **[P1] 普通命中反覆跨腳本確認身分，造成可避免的 VM 排隊成本。**  
   **位置：** `src/ESSBController.psc:5639-5650`；`src/ESSBState.psc:4-5`；`src/ESSBTrees.psc:470-480, 537-611`；`src/ESSBNodes.psc:60-80`。  
   **重現：** 零節點、目標已帶雷印的一次普通命中，原始碼路徑計數已達 **152 次 `IsCurrentController`、304 次身分解析 native、159 次跨 instance 腳本呼叫**；詳見 G。多段命中與其他模組共享 VM 時會累積等待。  
   **錯誤依據：** 題目指定的 Papyrus 跨 instance 呼叫會釋放 caller lock／可能排隊；目前把事件入口保護重複放進熱路徑的查表／索引函式。`ESSBNodes.psc:59` 宣稱的命中快取，仍落回大量查詢。這不是否定 round 10 的入口身分保護。  
   **最小修正：** 保留所有外部事件入口 guard；同一次已驗證交易改用內部查表路徑，傷害套用前再保留一次 guard。只做這項，代表情境可由 **532 →232 次 scripted calls、388 →88 次 native calls**，不用改傷害公式。  
   **信心：high（呼叫數）；medium（占實際秒數的比例，未有 VM profiler）。**

6. **[P2] 雷電的普通附傷漏掉設計要求的 50% 削魔。**  
   **位置：** `src/ESSBController.psc:1483-1516, 1718-1732`；`build_v03.py:1441-1477`。  
   **重現：** 對有魔力的活目標，雷形態在已存在印記後普通命中；只有生命附傷與玩家電荷增加。開印／放電的削魔不能代替「每次附傷」。  
   **錯誤依據：** `元素魔戰士規劃-v0.3.md:69`；ESP 實讀 `ESSB_HitEffect_Lightning` 為 archetype 0、AV 24（Health）、secondary AV -1，命中 SPEL 只有一個效果，亦無正常命中的削魔 hook。  
   **最小修正：** 為自有雷附傷法術加入同次 magnitude 50% 的 Magicka 傷害效果，或在同一附傷路徑精確套用一次自有削魔法術；同樣涵蓋重擊與額外 proc，不回魔、不轉真傷。  
   **信心：high。**

7. **[P2] 第九個目標擠出雙印目標時，副印記被遺棄。**  
   **位置：** `src/ESSBController.psc:1842-1847, 1880-1889, 2021-2034, 2107-2118`。  
   **重現：** 八個目標已占滿，最舊目標帶兩個實印記；再命中第九個。`EndMark` 只結算主印記並將副印記升為主印記，緊接的 `ClearSlot` 又把它丟掉，未結算／未消除該 AME；其到期回呼因找不到 slot 而退出。  
   **錯誤依據：** `元素魔戰士規劃-v0.3.md:107` 規定擠出要觸發過期終焉，「不是白白消失」，雙印也不另占名額。  
   **最小修正：** 擠出時以一次冷卻預約結清兩個實印記並消除各自 AME，再清格；沿用 `EndBothMarks` 的共同預約方式，但 reason 使用 3，保留 actor／generation 檢查。  
   **信心：high。**

8. **[P2] 岩甲先扣層，讓正常的「反震」永遠過不了滿層 gate。**  
   **位置：** `src/ESSBGuard.psc:108-114`；`src/ESSBElem2.psc:532-543`。  
   **重現：** 已有反震、岩甲正好滿層，沒有啟用「山岳＋同調三段」免扣層，受到近戰命中。Guard 先扣一層，反震才比較 `GetSelf(2) < RockCap`，必然返回。  
   **錯誤依據：** `元素魔戰士規劃-v0.3.md:584-585`；反震本身沒有山岳前置要求。  
   **最小修正：** 以受擊前岩甲快照判定反震；成功則按反震清空，失敗才執行一般扣一層。  
   **信心：high。**

9. **[P2] 岩甲層數仍在，護甲增益卻會自行到期且不再補回。**  
   **位置：** `src/ESSBController.psc:3069-3088, 3275-3276`。  
   **重現：** 土形態保持固定岩甲層數，等待超過 30 秒（使用時長倍率時等到實際法術到期），不受擊；護甲法術消失。滿層後再命中，`wanted == RockArmorShown` 仍直接返回。  
   **錯誤依據：** `元素魔戰士規劃-v0.3.md:125, 577, 828` 的不衰減岩甲；`實作紀錄.md:992-998` 雖記錄 30 秒與只在變更時施放，但兩者合起來和同一岩甲資源的持續護甲承諾內部不一致。  
   **最小修正：** 改為由層數生命週期管理的持續自有 buff；或在到期前刷新，並使「法術已不在」能越過 magnitude 相同的早退。  
   **信心：high。**

10. **[P2] 六種開印層數沒有套用已核准的整體倍率。**  
    **位置：** `src/ESSBReactions.psc:68-120`，尤其 `74, 86, 94, 102, 107, 120`。  
    **重現：** 開啟傳奇主線 rank 10、`NodeScale=3`，其他加層節點未買且未碰上限。火／雷／土／血／聖／暗的基礎開印層數仍直接用 `OpenStacks`；例如火依然 +2，沒有依 `mult=1.9` 取 3／4 層。  
    **錯誤依據：** `實作紀錄.md:3014` 是後來的明確決策：「整體效果一起縮放，包含層數／控制／回復」。同函式的冰、風、毒已乘 `mult`，不是共同的刻意豁免。  
    **最小修正：** 六處對數值層數統一先乘 `mult`、使用既有 `RoundStochastic`，再由現有上限限制；土開印的玩家耐力回復（93）也補上這個倍率。  
    **信心：high。**

11. **[P2] 尚未滿足觸發條件就消耗冷卻，漏掉斷咒與救命冰心。**  
    **位置：** `src/ESSBNoForm.psc:177-185`；`src/ESSBElem.psc:750-765`；`src/ESSBController.psc:3821-3837`。  
    **重現：** 先打未施法者，再於 5 秒內打施法者，斷咒不觸發；或冰形態滿血經過一次 tick，接著掉到 30% 以下，冰心必須等先前空耗的 30 秒冷卻。  
    **錯誤依據：** `元素魔戰士規劃-v0.3.md:481, 543` 把冷卻限定在實際觸發的斷咒／冰心；`TakeInterrupt`／`TakeIceHeart` 都會立即寫時間戳，不是唯讀測試。  
    **最小修正：** 先判斷正在施法／玩家低血，再取用冷卻；未成立不得寫入冷卻。  
    **信心：high。**

12. **[P2] 破魔印 AME 已結束，仍可能註冊／處理反咒事件。**  
    **位置：** `src/ESSBCounter.psc:18-39, 42-67`。  
    **重現：** 已購反咒，快速刷新破魔印或殺死目標；`OnEffectStart` 經過跨腳本查詢後才註冊動畫事件，這時 native AME 可能已消失。另一方面，`OnEffectFinish` 把 `Listening=False`，但 `OnAnimationEvent` 完全不檢查它，排隊中的晚到事件仍可反咒。  
    **證據：** `.codex/smoke3-essb-events.log:2857-2863`，`[09/18/2026 - 10:17:14PM]` 的 `Unable to call RegisterForAnimationEvent - no native object bound...`，stack 指向 `ESSBCounter.OnEffectStart()` line 35；接著出現 `Assigning None to a non-object variable named "::temp1"`。新 quest 身分檢查無法保證同世代短命 AME 的生命期。  
    **最小修正：** 晚到事件先檢查 finished／listening；將需跨腳本準備的動畫監聽移到有穩定 native 生命期的 alias，按目標維護破魔印到期資格。只加一個 `Ctl.IsOperational()` 不能修復已失綁的 AME。  
    **信心：high（native 錯誤與缺少事件生命期 gate）；medium（晚到反咒的實際發生頻率）。**

13. **[P2] 斷咒依賴不存在的 animation graph 變數，會誤判並刷 native error。**  
    **位置：** `src/ESSBNoForm.psc:229-236`。  
    **重現：** 購買斷咒，命中使用不含 `IsCastingRight/Left/Dual` 的 graph 的角色。`GetAnimationVariableBool` 回傳 False 並報錯，三個判斷都不能辨認其施法。  
    **證據：** `.codex/smoke3-essb-events.log:2864-2870, 2877-2888`：`[09/18/2026 - 10:17:14PM] Error:  (0003B9F8): cannot fetch variable named IsCastingRight of type bool, returning false.`，Left／Dual 同秒亦報錯；stack 為 `ESSBNoForm.IsCasting` → `OnManaBreak`。不是除錯等級 3 才有的 Trace。`元素魔戰士規劃-v0.3.md:481` 要求判斷施法，`實作紀錄.md:776` 只是留下可靠度待驗證，沒有核准漏判。  
    **最小修正：** 對有效裝備法術使用已具備的 `PO3_SKSEFunctions.IsCasting(akRef, akMagicItem)`（`vendor/imports/PO3_SKSEFunctions.psc:651`），涵蓋雙手及適用的其他施法槽；不以未保證存在的 graph 變數作唯一依據。  
    **信心：high（錯誤）；medium（日誌未證明該角色當刻確實在施法）。**

## F. 真實日誌核對

### 採樣與去重

- .codex/smoke3-essb-events.log 共 4653 行；1–1479 是按時間排列的 ESSB 事件串。1480 起是錯誤上下文，包含再次收錄的 ESSB 行，不能把全檔當成單一未重複 session 計數。
- 主事件串：76 Hit、80 proc、5 damage、134 TRUE、56 Open（含冷卻拒絕文字）、28 End、22 status、182 Kill、208 TREES。76 Hit 只有 element=7（8 次）及 element=10（68 次），weapon 均為 5。182 Kill 對應 91 個各出現兩次的 victim；屬已列案，未重報。
- smoke1-Papyrus.0.log 共 29088 行，檔內時間為 09/17/2026，不能將檔名或任務的相對日期當成時間證據。逐個 error／warning 解析至下一時間戳或「--」，只保留 stack 觸及 ESSB 的區塊，避免把附近其他模組錯誤誤算成本模組。
- smoke3 有 218 個符合條件的錯誤／警告區塊收錄，按「完整時間戳＋完整區塊」去重為 164 組。這也不等於 164 個不同引擎事件：同秒相同 stack 可能被合併；檔案是擷取結果，不是完整原始 profiler。

### 新問題的直接證據

N12 的原文（2857–2863）：

> [09/18/2026 - 10:17:14PM] Error: Unable to call RegisterForAnimationEvent - no native object bound to the script object, or object is of incorrect type  
> [None].ESSBCounter.RegisterForAnimationEvent() - "<native>" Line ?  
> [None].ESSBCounter.OnEffectStart() - "ESSBCounter.psc" Line 35  
> [09/18/2026 - 10:17:14PM] warning: Assigning None to a non-object variable named "::temp1"  
> [None].ESSBCounter.OnEffectStart() - "ESSBCounter.psc" Line 35

去重後有 48 組 RegisterForAnimationEvent error、50 組相同文字的 ::temp1 warning；後者不能直接視為 50 次獨立根因。證明 AME 註冊路徑確實已失去 native 綁定，並有連帶回傳值型別警告。這些是引擎錯誤，關掉 ESSB DebugLevel 不能阻止它們產生。

N13 的原文（2864–2870）：

> [09/18/2026 - 10:17:14PM] Error:  (0003B9F8): cannot fetch variable named IsCastingRight of type bool, returning false.  
> [ (0003B9F8)].Actor.GetAnimationVariableBool() - "<native>" Line ?  
> <unknown self>.essbnoform.IsCasting() - "ESSBNoForm.psc" Line 230  
> <unknown self>.essbnoform.OnManaBreak() - "ESSBNoForm.psc" Line 179

另有同目標同秒的 Left／Dual，以及 10:17:58PM 的 FE08BB05、10:18:12PM 的 0003A1E1、10:18:32PM 的 FE08BB07；合計 11 組 graph 變數錯誤。證明讀取策略不普遍成立；不證明這四個角色在報錯當刻都正施法，因此不把 11 當成漏斷咒次數。

### 已記錄問題與不能據此新增的指控

smoke3:2646–2650 另有：

> [09/18/2026 - 10:12:39PM] Error: Unable to call Dispel - no native object bound to the script object, or object is of incorrect type  
> [None].essbstatus.Dispel() - "<native>" Line ?  
> [None].essbstatus.DispelIfActive() - "ESSBStatus.psc" Line 180  
> [alias Player on quest ESSB_MainQuest (FC006000)].essbcontroller.OnStatusStart() - "ESSBController.psc" Line 2062

這是當時 schema 2 的控制器，不能一律歸因於 000800 舊 alias。實作紀錄.md:2078 已列過「DispelIfActive 解決宿主消除」；因此此處列為**既有修復的殘留反證**，沒有湊成第 14 項新問題。現有 ESSBStatus.psc:176–180 的 Finished 是腳本旗標，不能單憑它證明 native 尚存活。smoke3 的其他 SendModEvent／RegisterForSingleUpdate orphan errors，及 smoke1 的 array cast／AME teardown 類別，均按既有案件處理。

主事件串沒有 dropped=；但缺少 dropped 不代表沒有排隊、沒有 native error，也不能排除最後一個節流視窗的丟棄尚未被報出。L3 的 env 週期校準、status 快照確實產生額外 I/O；L1 的 env 只在值改變時輸出，未找到可獨立列案的正常 L1 環境刷頻。

對「應該出現卻沒出現」的限制：本次事件串未建立火／冰／雷／土／風／血／毒／水／星的有效元素命中樣本，不能從沒有該元素 status／reaction 推論它們壞掉。神聖 status holy=0、同目標短間隔 End/Open、延後清除的 evict，均受本次已知雙控制器重疊污染；沒有足夠資料從中分離新的根因，未重報。死咒只見一筆 fire，也不能假定每次預約都該 fire：死亡、重設、換宿與舊控制器都會影響它。

例如 1349、1356–1391：

> [09/18/2026 - 10:29:40PM] [ESSB][status][L3] 1057010891 heat=0 freeze=0 bleed=0 poison=0 astral=0 curse=2 holy=0 wet=0 fissure=0 catalyze=0 deathcurse=5 pressure=0 starlock=0 wetlock=0  
> [09/18/2026 - 10:29:48PM] [ESSB][damage][L3] 1057010891 element=10 amount=145.587128  
> [09/18/2026 - 10:29:48PM] [ESSB][deathcurse][L1] 1057010891 fire base=24.000000 lost=141.634369

中間 10:29:42PM 又有 deathcurse=5，其後 4、3、2、1、0。這能證明離散狀態 tick／延後傷害存在，不能把 40→48 秒直接定為單次普通命中延遲，也不能把 base=24 當成樹等級錯算：缺少當擊完整 rank、倍率、snapshot。沒有以「數值看起來大」填充 findings。

## G. 延遲與每擊成本

### 先修正日誌的配對方法

src/ESSBController.psc:1433–1434 先 ApplyProc；proc 在 1513–1516 設 magnitude、套用法術後輸出；Hit 卻在 1474–1478，之後才跑印記、累積與經驗。**Hit 不是引擎命中抵達時間，也不是普通附傷的起點。**

可直接核對 smoke3:436–444：

> [09/18/2026 - 10:12:30PM] [ESSB][proc][L3] -32982278 element=7 mag=17.645754 power=False open=TRUE  
> [09/18/2026 - 10:12:30PM] [ESSB][Hit][L2] -32982278 element=7 weapon=5 power=False left=False flags=0 sync=0  
> [09/18/2026 - 10:12:30PM] [ESSB][Open][L1] -32982278 element=7 mult=1.000000  
> [09/18/2026 - 10:12:30PM] [ESSB][proc][L3] -32982278 element=7 mag=15.147801 power=False open=TRUE  
> [09/18/2026 - 10:12:30PM] [ESSB][Hit][L2] -32982278 element=7 weapon=5 power=False left=False flags=0 sync=3  
> [09/18/2026 - 10:12:31PM] [ESSB][proc][L3] -32982278 element=7 mag=15.583646 power=False open=False  
> [09/18/2026 - 10:12:31PM] [ESSB][Hit][L2] -32982278 element=7 weapon=5 power=False left=False flags=0 sync=3

因此題目提供的 25／34／6／1／1 秒差分布，不能作為「同一擊施加傷害耗時」；下一個 proc 常是下一擊，而 damage 可能是開印、終焉或死咒。

只使用 1–1479 原始事件區，我將每個 Hit 配到同目標下一個 proc 或 damage，得到 59 個配對：0 秒 20、1 秒 30、2 秒 5、4 秒 1、7 秒 1，另有 **915 秒及 984 秒各 1**。這兩個荒謬長值尤其證明它不是同擊關聯。反向取前一個同目標 proc，76 Hit 中 70 在同秒、6 在前一秒；這仍只有秒級文字關係，不是 profiler。全檔重複上下文亦會放大每秒事件數，不能把「最多 8 條傷害文字」直接當成八個獨立 native damage dispatch。

真實的突降體感可能包含 VM 排隊及設計上的延遲傷害。已能定位的可避免成本是下列熱路徑；N01 另外會拖延控制器 tick。DoCombatSpellApply 返回／proc Trace 並不提供引擎實際扣血的完成時間。本次沒有毫秒級 engine callback／health trace，不能聲稱某一秒數已分配給特定函式。

### 明確代表情境與計數方法

同一次 OnWeaponHit：
- 已就緒的 schema 3 controller，普通近戰 weapon=5、flags=0，非潛行／非重擊。
- 雷形態、已有雷印與活宿主，目標在 registry slot 0、DamageActor[0]；無新開印、死亡、領域、旁支觸發。
- 全部節點 rank=0，樹快取 [2,11,12]，雷樹等級 1，同調 0，XP 預算足夠，DebugLevel=0。
- 在記憶體讀取 build/papyrus_harness.py；只補解析器所需型別／cast 支援，執行實際 source function bodies，native 全為顯式 mock。沒有執行 build／verify 主程式。
- 攔截 Script.call、外部 Auto property 讀取及 native mock 呼叫；每種記數分開。原 harness 跳過記錄敘述，因此另外依 src/ESSBController.psc:1515、5273–5275 補回即使 L0 也會求值的 GetFormID、LogThrottled、DebugLevel.GetValueInt。

**這是該明確情境的路徑計數，不是全元素平均，也不是遊戲執行時間。** 未購節點仍要查門檻；雷的附傷倍率也呼叫其他樹的 target multiplier，造成三槽快取以外的查詢。

| 每擊項目 | 現行 | 只做 N05 內部 guard 去重後（推算） |
|---|---:|---:|
| Scripted calls，含入口與 Global helper | 532 | 232 |
| 其中：Global helper calls | 181 | 31 |
| 其中：跨 instance method calls，不含 Auto getter | 159 | 27 |
| Native calls | 388 | 88 |
| 其中：GlobalVariable.GetValue／GetValueInt | 34 | 34 |
| 原始碼 property references，含內部與 AutoReadOnly 常數 | 907 | 775 |
| 其中：外部 Auto property reads，另計，不混入 scripted calls | 206 | 206 |
| IsCurrentController | 152 | 2 |
| Game.GetFormFromFile | 160 | 10 |
| Quest.GetAlias | 152 | 2 |
| Scripted + native calls，不含 Auto getter | 920 | 320 |

「後」只是保留事件入口及 ApplyTrackedDamage 前各一次身分確認，將已驗證交易內的重複身分查詢改走內部路徑；不刪任何外部入口 guard，不改 StateBroken 的現有檢查，不改公式／印記／XP。移除 150 次 IsCurrentController 及其 150 次 ControllerQuest，共少 300 scripted 與 300 native；其中 Trees→Controller 的 132 次重複呼叫消失，所以 159→27。**未實作，未宣稱效能提升的毫秒數或 FPS。**

907 是來源層級的 property 名稱讀取次數，包含編譯器可能直接存取／內嵌的內部 Auto 與 AutoReadOnly，不能全部當成跨物件 VM 呼叫。後值少掉 Trees 中 132 次以 Controller property 取得身分查詢 receiver；保留其非空與 StateBroken 檢查。現行外部 property 206 次中：StateBroken 156、Trees 44、NodeScale 4、ElementDamageMin/Max 各 1。這些 getter 也可能涉及腳本物件同步，不能在看完 920 就忽略。原樣保留 getter 的保守計數下，三類合計 1126→526。

GLOB 的 34 次明細：
- Sync 4；SyncT1/T2/T3 合計 9。
- NodeScale 4；FormActive 2；DebugLevel 2。
- 七種 self-resource 鏡射 GLOB 各 1。
- Enabled、CurrentElement、BaseDamageMult、Level2、MultDuration、XPPerHit 各 1。

native 也包括 3 次 DoCombatSpellApply（附傷、engaged、mark）、8 次 HasPerk、2 次 CustomSkills.AdvanceSkill、目標生命／死亡檢查、隨機值與法術 magnitude／duration 設定，沒有把它們當成免費操作。

陣列／快取成本：
- FindSlot 4 次＋AcquireSlot 1 次，共 **5 次 8-slot 搜尋**；此情境命中 slot 0，共讀 5 個 RegActor，最壞各查到第 8 格。
- NoteDamageElement 2 次 **128-slot 搜尋**；此情境各讀首格，不是宣稱每擊必讀 256 格。尾端命中時則各最多 128。
- CacheSlotOf **22 次**，三槽合計 **54 次比較**。其中 4 個 branch miss、1 個 mainline miss，落回 8 次 HasPerk 與 8 次 perk GetFormFromFile。
- ArmUpdate 每擊還遍歷 8 個 LiftActor。
- InitRegistry 7 次、InitTables 24 次，旗標避免重建陣列，但它們前面的身分確認仍有成本。

### 進一步不改行為的削減方案

1. **在切換形態／購點或洗點／等級增加／MCM 倍率改變／載入完成時刷新衍生值。** 緩存 rank、branch、G(L)、固定 multiplier、同步門檻、duration 等。三個樹槽不足以應付 target multiplier 的其他樹查詢；可固定保存所有樹或必要衍生 flags，依 Papyrus 陣列長度上限分塊。未初始化快取必須有正確 fallback。
2. **同擊只讀一次不變 GLOB。** 在本情境，NodeScale 4→1（少 3）、三門檻 9→3（少 6）、Sync 4→1（少 3；AddSync 後更新 local，不能沿用舊值）、FormActive 2→1、DebugLevel 2→1，合計 **34→20 次 GetValue**。其他條件不變時，上一階段的 native **88→74**，scripted+native **320→306**；這是算術保守值，未把可能少掉的 helper calls 重複算成收益。
3. **資源局部更新。** 增加電荷只更新 GRockArmor 等以外真正改變的鏡射值；現行 PushSelf 讀七個 GLOB。直接由同一 setter 維護 mirror，可省這七讀，代表情境再由 **20→13 GLOB reads、74→67 natives**。若保留一次 self mirror 對帳則是 14／68；需確保沒有外部 setter 繞過鏡射。
4. **把已取得的 slot 傳入內部函式。** 5 次 registry search→1；保存或回傳 damage-cache index，並核對 actor／generation 後復用，使第二次 128-slot 搜尋消失；active-slot 命中可直接使用具 owner 的 damage index。不要藉此刪掉 round 10 的逐目標 damage attribution。
5. **快取查表直接索引、避免跨樹 miss；lift 不存在時跳過掃描。** 節點購買後仍要立即可見；有飛空任務時維持原有 deadline。這些額外重構尚未有完整 after-call trace，沒有虛構單一「全部優化後」總數。
6. **日誌先 gate 再組字串。** ApplyProc:1515／ApplyDamage:2349 等呼叫先計算字串與 GetFormID，LogThrottled 才檢查等級，違反設計 6.1:783、788。將 guard 放在呼叫端並復用同擊 debug level；保留原事件資訊與節流語義。

如增加 persisted cache 成員，仍必須照 state_schema_version 規則升版；不能為了減少呼叫而繞過 schema 或 stale-instance 保護。快取失效與外部回呼都以各自入口處理，不能跨 Wait／新 engine event 沿用交易授權。

### Double dispatch 與 L3 成本分開說

**Round 10：** 日誌確實含重複 kill／升級及兩代控制器證據，故當時重複路徑會增加 VM 工作。對「相同 callback 完整跑兩次」的部分，修成一次就是少掉一份工作；不能推成全 session 延遲剛好減半。這份 smoke3 早於目前所有修復的共同驗證，而且沒有 instance id／hit sequence，無法把 1、2、4、7 秒拆成各有多少由雙發造成。現行 152 次身分確認的成本則是目前 source 的問題，不能直接套回較早日誌作精準時間解釋。

**Level 3：** 上述普通命中，若沒有被節流，增加 proc L3 與 Hit L2 兩次 Trace。相對 L0，額外 **1 scripted call、7 native calls**，其中 GLOB 多 2（Sync 與 DebugLevel）、GetCurrentRealTime 多 2、GetFormID 多 1、Debug.Trace 多 2；所以本代表路徑變成 **533 scripted、395 native、36 GLOB reads**。另有格式化、VM 排程、log I/O；開印、DOT、反應、status 又各自增加輸出。節流通常只抑制最終 Trace，不能取消呼叫端已完成的字串與多數 native 讀取。沒有 profiler，不能把這些呼叫換算成幾毫秒；L3 會放大負載，但無法解釋為「關掉 L3 就已修復」。

若後續允許實作量測，應以單調 hit sequence 串接「engine event 入口、proc 前後、OnValidHit 完成」，附 controller quest ID 與高解析度時間；另量測傷害生效時點。這只是建議，本審查沒有加入任何 logging 或程式碼。

## 已檢查且在該範圍未發現新問題

| 範圍 | 達到的覆蓋與通過邊界 |
|---|---|
| A — ESSBController | 檢查命中資格、八目標 registry、mark/status 回呼、generation、換宿、推力／冷卻、領域、逐目標傷害歸屬及 schema guard。除 findings 外，未找到另一個可由舊 quest instance 通過的新遊戲事件入口。不是說同世代 AME 生命期安全（N12）。 |
| B — Status/Reactions/Mark | 檢查 bleed 10、poison 12 個 bucket 的新增／aging、ExportInts/ImportState 的 38 格配置與 poison offset 18／tail offset 30、Float 匯出對稱、主副印記收尾、開印／終焉 cooldown。桶的欄位排列未見新的不對稱；自然過期回呼與 round 10 damage attribution 按 owner 對應。換宿排程與擠出見 N01／N07。 |
| C — Elem/Elem2/Elem3/Nodes/NoForm | 對照 build/plan-tree-nodes.json 及設計／決策，追查命中、開印、終焉、狀態、G(L) 中央傷害入口、六個 Mult* 類別、NodeScale、no-form baseline/gate。未在抽查路徑發現新的 G(L) 重乘；層數倍率漏用見 N10。沒有逐一證明所有節點組合。 |
| D — Trees/Guard/MCM/State/FormRules | 檢查購點 snapshot/reconcile、branch 補扣不足退款、respec 主線與分支退款、menu／MCM callback、血形態維持費豁免、魔力歸零兩秒流程。點數與 MCM 引用未見新的獨立問題；讀檔 timer、真實時間基準與受擊先後次序另列。 |
| E — builder／records | 直接讀現有 ESP，核對 MGEF DATA／SPEL SPIT、PERK entry 與 CTDA 格式、QUST alias／VMAD、MCM refs、FX 與聲音。不執行 build。參考 vendor/wbDefinitionsTES5.pas 的 MGEF、PERK、EXPL、HAZD、LIGH、SNDR 與 SPEL 定義。除雷附傷語义缺漏，所查欄位沒有新的格式錯置。 |
| F — logs | 兩檔皆實際解析 ESSB error stacks；對 smoke3 原事件區逐類統計，檢查值／順序／缺席證據的限制，詳見上節。 |
| G — cost | 實際 source 的代表性普通命中呼叫計數，加上逐步、明示假設的 after 推算；未量測真實 VM 毫秒延遲。 |

Schema／入口的具體核對：
- settings.json 與 state-schema.lock.json 都是 3；用唯讀 signature 計算得到 8a940ebccca3824f1368751f3530155990b9f27c27c80717e9a730fdb14e915b，與 lock 一致。
- 涵蓋 11 個編譯且有 instance 的本地腳本：ESSBStatus、ESSBMark、ESSBTrees、ESSBCounter、ESSBSilence、ESSBController、ESSBGuard、ESSBFormPowerEffect、ESSBFormRules、ESSBSettingsEffect、ESSBMCM；並含 MCM_ConfigBase、SKI_ConfigBase、SKI_QuestBase 的 inherited layout。ESSBPlayerAlias.psc 是未編譯／未掛載的舊來源，不冒充漏鎖的活腳本。
- ESP VMAD 實讀的本地 instance 腳本集合與之相符；另有外部 SKI_PlayerLoadGameAlias，掛在會重配的 MCM quest alias。沒有在 FX／其他 record 找到藏著的本地 script instance。
- 主 quest 006002、MCM 006003；舊 000800、00515B、006000、006001 為無 VMAD／alias／start flag 的 stub。所有已讀 VMAD object refs 都沒有指向舊 ID。MCM config 的 action form 指向 Elements Spellblade.esp|006003。
- 已查 OnInit／OnPlayerLoadGame／OnUpdate、武器／受擊／kill、custom skill／menu、MCM 方法、FormRules mod event、Mark/Status/Counter/Silence AME 入口。舊世代的 finish handler 有些只清理自身旗標／FX，沒有發現能再次派送目前遊戲效果的缺口。Settings/FormPower 解析 canonical controller 屬重新導向，不是舊控制器繼續執行。原生 SkyUI／MCM Helper 內部另見未檢查範圍。
- src 中剩餘 None 比較的 operand 是物件引用；未找到殘留 array == None／!= None 或同型的 array-to-scalar cast。N12 的 ::temp1 是 native 失綁造成的回傳值問題，不能當成還有 63 處舊 array 比較。

FX 的具體核對：
- 已讀 EFSH 79、ARTO 22、IPDS 16、EXPL 11、HAZD 1、LIGH 1，以及連結的 IPCT／SNDR／SOUN；這些 FX 上沒有 VMAD。
- 11 個 EXPL 所查 damage 為 0，無新增傷害 enchantment／spawn object／projectile。唯一 ESSBFX_ZZHazard_DarknessUpgraded 的 lifetime 為 7 秒、Spell 為 0；沒有找到借用 hazard 持續施放他人傷害法術的路徑。
- LIGH 的 time=-1 並不單獨證明永久殘留：引用處是爆炸所擁有的 light，未見腳本直接 PlaceAtMe 常駐 LIGH，未把它誤報。
- 55 個 event SOUN 所指 SNDR 的 LNAM loop/envelope 位元檢查均為 one-shot。另兩個仍有 loop/envelope 的 ambient descriptor 是效果生命期綁定的素材，不在 Sound.Play event bank；符合實作紀錄的刻意保留，未再報已修的循環形態音。
- 以上是 records／引用與腳本生命期審查；NIF 內部 controller、粒子音源與實際退出 cell 的視聽殘留沒有執行測試。

## 未檢查／不能宣稱已驗證

- 沒有啟動 Skyrim、載入存檔、實測這 13 項修法或新版本手感；沒有編譯、build、install，也沒改任何程式。
- 沒有完整窮舉所有技能節點、195 個主線 cell 的 15 rank × 所有分支交互，以及所有玩家／NPC／怪物／騎乘／巨型／unique 角色組合。C 是跨路徑審查，不是全節點形式證明。
- 沒有反編譯所有 PEX、核對執行中遊戲實際載入的每一個 PEX 或 save 中全部 instances；目前 source 的結論不能直接冒充 smoke3 使用了完全相同的新 source。
- 未深入 SkyUI／MCM Helper／CSF／PO3 的 DLL 或完整 inherited UI 事件實作；讀本地 import 宣告與綁定。VIP 以 IsUnique 過度涵蓋、少數 PERK 延後項、範圍中心加五人、共享 spell magnitude 競態等已記錄決策／限制不重列新缺陷。
- 沒有對所有第三方模組逐條跑 conflict resolution，沒有讀取授權範圍以外的 profile／系統／記憶體檔，未使用網路。
- 沒有 NIF／HKX 動畫資產內部稽核、全部聲音的播放／停止實測、真正的 Papyrus lock／stack profiler 或毫秒級傷害完成時間。報告中的 call count 是明示情境的 source path count，after 是建議的算術推算。

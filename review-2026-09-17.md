# 指揮官親審紀錄 ｜ 2026-09-17

範圍：`src/ESSBController.psc`（命中路徑、形態層、傷害封裝、推力、擊殺、掃描）、`src/ESSBTrees.psc`、`src/ESSBMark.psc`、`src/ESSBGuard.psc`，以及產生器寫出的 SPEL／MGEF／GLOB 與 CSF JSON 的實際位元值。未逐行審 `ESSBElem*.psc`（三千行），那部分靠建置時的節點索引交叉檢查與覆蓋報告。

## P0：編譯會過、遊戲裡不會動

1. **`ApplyHavokImpulse` 對站著的活人無效。** `BlowBack`、`PullTo`／`PullIn`、`LiftUp`（控制器約 3066–3120 行）直接對目標呼叫 `ApplyHavokImpulse`。活著且未 ragdoll 的角色由角色控制器驅動，不受 Havok 衝量影響，所以風的拉近、吹飛、吹上天、風渦、氣旋全部靜默失效，連帶「失衡」「浮空」「空中追擊」沒有觸發來源。
   - 修法：吹飛用 `player.PushActorAway(target, +force)`；拉近／風渦用 `centre.PushActorAway(target, -force)`（負力道＝拉向來源，這是原版與眾多模組的拉人做法）；吹上天先 `PushActorAway(target, 小力道)` 讓目標進 ragdoll，再由一次性延遲更新（0.1～0.2 秒）補 `ApplyHavokImpulse(0,0,1,mag)`。延遲用狀態宿主或控制器的單次更新佇列，不用 `Utility.Wait` 迴圈。力道需進遊戲調。

2. **所有以 `DoCombatSpellApply` 套用的敵對法術目前是 delivery = Target Actor(3)。** 我讀回的實際值：`ESSB_Hit_*` 與其 MGEF 都是 cast 1／delivery 3。原版與各大模組用 `DoCombatSpellApply`／「Apply Combat Hit Spell」時的標準配置是 **Contact(1)**；Target Actor 是否可行我沒有把握，而它一旦不行，附傷、印記、反應、真傷、破魔印、沉默全部沒有效果，冒煙測試第一步就失敗。
   - 修法：凡是由腳本 `DoCombatSpellApply` 到敵人身上的 SPEL 與其 MGEF，delivery 一律改 Contact(1)。對玩家自己套用的（血承、自我治療、自我增益）改用 delivery Self(0) 並確認套用方式（AddSpell 能力或 DoCombatSpellApply 到自己）在實作紀錄寫清楚，列入進遊戲驗證。

## P1：行為錯誤

3. **處決會被元素抗性擋掉。** `Execute` 走 `ApplyDamageRaw(元素)`，冰抗 100% 的目標永遠處決不了。處決應走真實傷害法術路徑（不乘 G(L)、不乘破魔加成）。

4. **擊殺掛勾可能重複觸發。** Tick 的後援路徑（目標已死就跑 `OnKill` 並清格）與 `OnActorKilled → OnKillEvent` 沒有互斥：Tick 先跑、事件後到時，`OnKillEvent` 找不到登記格仍會以當前元素再跑一次 `OnKill`（飲血回血、血承、化灰、亡者歸來都會兩次）。加一個最近 8 名已結算死者的去重環。

5. **印記時長寫反了。** `ApplyMark` 是「環境潮濕或目標浸濕時 10 秒」，規劃 2.2 是「水元素的印記（浸濕）10 秒，其餘 8 秒」。應為 `aiElement == 9`。

6. **無形態命中的經驗重複發放。** `OnWeaponHit` 已給無元素樹經驗，`ApplyTrueDamage` 又給一次；規劃 4 明寫延伸效果不重複發放。真實傷害只在「不是由命中直接觸發」的來源（反咒、星斷、沉默相關）才另外給。

## P2：要進遊戲確認，先記著

- CSF 是否評估 perk 的 CTDA（階層門檻與分支前置）；選單關閉事件是否真的是 `StatsMenu`（目前有「下次開樹先補結清」的保底，不會重複扣或漏扣）。
- `SetNthEffectMagnitude` 後立刻套用：雙持同幀兩擊可能互相覆蓋數值，影響是傷害數字偶爾對調，可接受。
- `IsVIPTarget` 把所有 Unique NPC 當首領（不可跌倒、不可化灰、沉默減半、處決改傷害）。有名字的強盜頭目都算，範圍偏大但方向安全。
- 化灰對已死亡目標套用、血承對自己 `DoCombatSpellApply` 是否生效。
- CSF 經驗曲線：`improveMult 0.05、improveOffset 2`，單棵樹滿級約一萬三千次有效命中，每秒上限 10 次。手感進遊戲再調。

## 我認為做得對的

- 形態切換在同一腳本幀內完成，沒有等待；狀態全部鏡射到全域變數，方便條件式特效。
- 命中旗標沿用原型驗證過的 RE::HitData 位元；弓弩與潛行射擊的判定清楚。
- 沒有 `Utility.Wait`、沒有持續性 `RegisterForUpdate`、沒有直接扣敵人生命；自損（血位、維持費）才用 `DamageActorValue`。
- 目標資格照規劃 2.9：敵意、交戰中、已交戰標記；排除同伴與受命角色。
- 技能樹的 FormID 算術與三槽快取讓命中路徑不做 HasPerk。

## 處置

P0 與 P1 共六項，在特效前線回報後、安裝前，開一輪修正（同一批檔案，必須循序）。P2 併入冒煙測試清單。

---

## 附：第三輪代理自派的程式碼審查（同日回報，已逐項附原始碼行號）

與我的六項不重疊，全部併入同一輪修正。

| # | 檔案與位置 | 缺陷 | 最小修法 |
|---|---|---|---|
| R1 | ESSBElem3.psc:109-110 | 水的兩條「長流回復」主線被當成附傷加成，同一批點數同時給回復又給附傷；水的附傷成長只應走水壓 (8,0,2) | 刪掉這兩行 |
| R2 | ESSBStatus.psc:383-387 與 ClearStack(8) 無呼叫者 | 汪洋之始的 WetLock 永不解除，目標終生浸濕；「汪洋」與基礎行為無法區分 | 水印記被切掉／融斷／過期時呼叫 ClearStack(8) |
| R3 | ESSBGuard.psc:125-130 | 影身的抵銷視窗不會被近戰命中關掉，反而被箭矢關掉，與設計相反 | OnShadowBody 進入時先 SetGuardDark(0) |
| R4 | ESSBStatus.psc:624-628、ESSBController.SpreadPoison | 毒擴散點滿是同一人吃兩層，不是每秒兩個目標 | SpreadPoison 加排除參數或一次取兩人 |
| R5 | ESSBController.TickDomain 2884-2953 | 領域每秒效果少跑一次（先倒數再結算） | 倒數移到結尾 |
| R6 | SetStarLock／SetWetLock | 目標第一次被開印時狀態宿主尚未就緒，星鎖與汪洋之始被靜默丟掉 | 比照 RegPendAmt 加緩衝 |
| R7 | ESSBElem3.psc:760 | 同一幀多個擊殺會突破召喚上限，含「永久上限 1 名」 | 本 tick 已復生的 pending 計數 |

建議一併處理的 advisory：預知在星痕延遲主線 ≥10 點時不觸發；處刑對首領實際 ×4（應補 ×2 比照碎冰慣例）；ESSBGuard 註解提到不存在的 GuardKill 旗標。

該審查確認乾淨的項目：倍率沒有重複相乘、125 筆 trees 7–10 的節點索引全部有效、狀態搬家 27 格對稱、復生階級與時長與規劃一致、ESSBElem3 沒有孤兒函式。

## 修正輪總表

P0-1 推力、P0-2 法術投遞方式、P1-3 處決走真傷、P1-4 擊殺去重、P1-5 印記時長、P1-6 經驗重複、R1–R7，共 13 項。

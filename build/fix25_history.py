"""Round 25 (N6, the per-second work, the hotkeys and the domains): the declared changes the historical verifiers
accept. GENERATED by build/fix25_history_gen.py from this template -- edit the reasons there, never the digests here.

Round 25 deleted ESSBFormRules.psc (the upkeep), ESSBInput.psc (the hotkeys) and ESSBSilence.psc (the silence's drain)
and the Papyrus domains, the environment check, the storm's charge clock and 長流 of the other scripts: the DLL runs them
(native/include/Timer.h, the timer task and the input sink of native/src/Plugin.cpp). The verifiers of rounds 6-24
exercise the scripts as they were; the round-24 seal (build/fix24_history.py) now runs on the pre-fix25 snapshot
(.codex/pre-fix25-snapshot/src, taken before any round-25 edit), and this module proves that today's scripts differ from
that snapshot ONLY by the changes declared below, each bound to the code by digest (comment-stripped body, sha256[:16],
build/fix21_history.digest):

  CHANGED     (script, function) -> (why, before, after)
  REMOVED     (script, function) -> (why, before)
  ADDED       (script, function) -> (why, after)
  PROPERTIES  script -> (why, code lines added outside functions, code lines removed)
  FILES       whole scripts removed or added -> (why, sha256 of the file, 'removed' | 'added')

Anything else fails the build: an undeclared edit, a declared function edited after sealing, a removed script that
comes back. legacy_source() hands the older seals and verifiers the snapshot only after that proof; the chain is
fix21 -> ... -> fix24 (23 -> 24, "now" = pre-fix25) -> this one (24 -> 25, "now" = src/).
The round-25 behaviour itself is checked by build/fix25_verify.py and the native timer test.
"""
from __future__ import annotations

import hashlib
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
sys.path[:0] = [p for p in (str(ROOT / 'build'),) if p not in sys.path]
SNAPSHOT = ROOT / '.codex/pre-fix25-snapshot'

import fix21_history as _h21

CHANGED = {
    ('ESSBController.psc', 'ClearSelfAll'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（雷雨計時變數拿掉）', '8846069875e4b6dd', '05bdc9192a37746c'),
    ('ESSBController.psc', 'InitTables'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除（領域的陣列不再配置）', 'df51cff894696b36', '6a60b9360f35a382'),
    ('ESSBController.psc', 'KeepSneak'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', 'd484ee6d92986905', 'dc0e9d0de0647cb7'),
    ('ESSBController.psc', 'OnESSBDomain'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；這裡只放開場特效', '3f3041c8a580c71b', 'e313eaaf5503ef1e'),
    ('ESSBController.psc', 'OnFormSwitched'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime（同調保留的期限）', '281cf7999960faf0', 'dc5201e5076f4c13'),
    ('ESSBController.psc', 'OnWeaponHit'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime（雙生的 30 秒）', '0baebd42ebc924aa', '268f520f01f2e5de'),
    ('ESSBController.psc', 'ReconcileLoadedForm'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除（領域鏡射屬性拿掉；聖域鏡射由 DLL 每秒寫）；形態維持能力不再移除（沒有再加）', 'd42643a8312c1799', '0abe409f9191101d'),
    ('ESSBController.psc', 'ResetLoadClock'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；雷雨計時、環境計時、領域鏡射的重設拿掉；審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime（雙生）；審查修正 4：沒有讀者的「節奏」「疾攻」連段（ComboHits、ComboTime、GCombo）刪除', 'a6c8c5a09a179bb6', 'b26bd3bc61f399a4'),
    ('ESSBController.psc', 'SecondsLeft'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '2c4c6d879ee3367b', 'd9f5e1fd10f0ac32'),
    ('ESSBController.psc', 'SetEmber'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', 'c5c5d3473b9f072b', '6a54110a10caf725'),
    ('ESSBController.psc', 'SetGuardAstral'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', 'c2c348361c951f24', '7f3cb79665b23a19'),
    ('ESSBController.psc', 'SetGuardDark'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '33a14a5233f3f1f5', '930b69db94d93f10'),
    ('ESSBController.psc', 'SetGuardDivine'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '066ed6519a9331e5', '9579e68dcfed6c04'),
    ('ESSBController.psc', 'SetGuardIce'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', 'd2f3e39c5f1dedd7', '7a26a3f75aaae7c4'),
    ('ESSBController.psc', 'SetGuardStar'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', 'faacb24a03a3ea66', '7b527481f62c0053'),
    ('ESSBController.psc', 'SetGuardSwitch'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '610271941783ab10', 'a36c6d1977a87ea5'),
    ('ESSBController.psc', 'SetNoBloodCost'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '97cab62391bc599b', '9b64ca1952d3602d'),
    ('ESSBController.psc', 'SetQuench'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', 'b71f943e48781a13', '5f04f531325d60b5'),
    ('ESSBController.psc', 'SetShockRecent'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '977d7b4fc7b90cc8', 'ab42ba387ec39d65'),
    ('ESSBController.psc', 'SetSyncKeep'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '3227e8c5f6a7bf48', 'cdcd6bdc99bc14c6'),
    ('ESSBController.psc', 'Setup'): ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除；ESSB_Switch／ESSB_Close 登記；形態維持能力不再加（ESSBFormRules 刪除）；環境偵測在 DLL', '3cbfa6e1c34f7ebc', '9657c1070ea85763'),
    ('ESSBController.psc', 'SwitchForm'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime（雙生的 30 秒從遊戲時鐘起算）', 'a4b47ee37dbb0535', '61cef548a93bb65e'),
    ('ESSBController.psc', 'Tick'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（雷雨電荷與環境偵測拿掉；留下的每秒工作：神佑、秒計時器、毒形態的以毒攻毒與百毒不侵、風的潛行）；審查修正 4：沒有讀者的「節奏」「疾攻」連段（ComboHits、ComboTime、GCombo）刪除', '87a37894319e52fc', '1149b7554884daec'),
    ('ESSBController.psc', 'TickTimers'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime', '170281dc9d062ddb', '602d47eb7535fa78'),
    ('ESSBController.psc', 'TimersActive'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime；審查修正 4：沒有讀者的「節奏」「疾攻」連段（ComboHits、ComboTime、GCombo）刪除', '6e8c6272f0721032', '12ff9f7acd6e3520'),
    ('ESSBController.psc', 'ValidateBindings'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除；GameHour、形態維持能力、反應法術、過熱自傷法術這些屬性拿掉；審查修正 4：沒有讀者的「節奏」「疾攻」連段（ComboHits、ComboTime、GCombo）刪除', 'e4ce614c89ec6045', '3b2a5e7f40367984'),
    ('ESSBElem3.psc', 'OnTick'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（水形態的每秒掛勾拿掉，毒形態的留著）', 'f6372c5c36d0dbb8', '7afd84b1465ff12b'),
    ('ESSBFormPowerEffect.psc', 'OnEffectStart'): ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除', '6a5c5c243c9fe0b7', '0954724068a224f6'),
    ('ESSBState.psc', 'ControllerQuest'): ('R1：state_schema_version 13 → 14 的新控制器任務 ID', 'b7b3473bfd1497ea', '9d9b532252b3e255'),
    ('ESSBTrees.psc', 'RefreshActive'): ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除（ESSBInput.RefreshPermission 拿掉：順轉由 DLL 在切換當下讀）', '214e9ab0062474e0', 'af51edfc2a21629f'),
    ('ESSBTrees.psc', 'RefreshTree'): ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除（同上）', '448e3fc55105d61a', 'a9b501b91f20acbb'),
}

REMOVED = {
    ('ESSBController.psc', 'AddSelf'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（雷雨的 +1 電荷）', '706992c20854134a'),
    ('ESSBController.psc', 'AddStackTo'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（毒霧的 +1 劑，現在是 DLL 的擴散一劑，R5）', '8d01d8d79c6214ca'),
    ('ESSBController.psc', 'ApplyDamage'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', '0cf742d3b1944703'),
    ('ESSBController.psc', 'ApplyDamageRaw'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', '275108809c91dc09'),
    ('ESSBController.psc', 'ApplyDotDamage'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', '70827d8c33442e01'),
    ('ESSBController.psc', 'ApplyStrip'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（潮池每秒沖刷一個增益，現在是 DLL 的沖刷）', '74665b40d2ea30eb'),
    ('ESSBController.psc', 'BloodBand'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', 'e8e7a3da699475fb'),
    ('ESSBController.psc', 'BloodBandMult'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', '27afadafd0b9a89c'),
    ('ESSBController.psc', 'BloodDrainPerSecond'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（血位損血曲線，DLL Timer.h BloodUpkeepFraction）', 'd4de1e4b638e1adf'),
    ('ESSBController.psc', 'BloodPercent'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', '3adaba31ffe00f82'),
    ('ESSBController.psc', 'ClearDomainResidents'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '7fc871665dc5a25b'),
    ('ESSBController.psc', 'DomainActive'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '896521ae9a4dd3db'),
    ('ESSBController.psc', 'DomainFlag'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', 'db20d4bd0b2f6484'),
    ('ESSBController.psc', 'DomainTargetTicks'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '01f03fb0f58289af'),
    ('ESSBController.psc', 'EnvCheck'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（環境偵測：天氣、室內、水中、時間 → ESSB_EnvWet／EnvStormy／EnvNight，DLL 寫）', '5588e78f9366796d'),
    ('ESSBController.psc', 'GetBloodHitMult'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', 'd7c08eb6270c8e34'),
    ('ESSBController.psc', 'GetDamageMult'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', 'e5e81806bfede233'),
    ('ESSBController.psc', 'InDomain'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '9118a8ab244cf568'),
    ('ESSBController.psc', 'InsideDomainSlot'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', 'f7bb445ab595e283'),
    ('ESSBController.psc', 'ObserveDomainResidents'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', 'a114c625d0d77620'),
    ('ESSBController.psc', 'PayBloodCost'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（血形態維持扣血，DLL 走代價路徑、留 1 點）', 'd856f2aee2aedbf0'),
    ('ESSBController.psc', 'PlayerInDomain'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '8ea9f6a90ab2256d'),
    ('ESSBController.psc', 'ReactDamage'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（死域的每秒傷害是這些函式唯一的呼叫者，現在是 DLL 的 D_react）', '0c211f4676fa72e3'),
    ('ESSBController.psc', 'RememberDomainResident'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', 'b4adc41690010e06'),
    ('ESSBController.psc', 'ScanDomainTargets'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '2929c4a3aedaccaa'),
    ('ESSBController.psc', 'StartDomain'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '8d7217e97e3997e8'),
    ('ESSBController.psc', 'TickDomain'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除', '3a6078a0d54a915c'),
    ('ESSBController.psc', 'ToggleForm'): ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除；這個函式本來就沒有呼叫者', '005f0ede68652e76'),
    ('ESSBElem2.psc', 'TargetDamageMult'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（御風／空中追擊的 Papyrus 反應倍率只給領域的傷害；DLL 的反應讀同樣兩項）', '5019b618bd68c906'),
    ('ESSBElem2.psc', 'WindSlowImmune'): ('御風的免疫減速：這個守衛從來沒有呼叫者（假 DONE）；round 25 在 DLL 計時器每 tick 驅散你身上的減速（Timer.h SlowImmune）', '99085e10b467eb75'),
    ('ESSBElem3.psc', 'FlowPercent'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（長流的百分比，DLL Timer.h FlowFraction）', 'ac8a43211990d0f1'),
    ('ESSBElem3.psc', 'TargetDamageMult'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；沒有呼叫者了（呼叫它的 Papyrus 領域／每秒工作搬進 DLL）（星鎖／星域的 Papyrus 反應倍率只給領域的傷害；DLL 的反應讀同樣兩項）', '4ce6080470546a99'),
    ('ESSBElem3.psc', 'WaterFormTick'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）（長流、長河；v0.4 1.1 的「另回維持費 80% 的魔力」Papyrus 本來就沒做，DLL 補上）', '5fec30e89e0b52de'),
    ('ESSBNative.psc', 'ExtendFuse'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；唯一的呼叫者 TickDomain 刪除（火域的白熱引信 +5 秒在 DLL）', '0af385a4ca713521'),
    ('ESSBNative.psc', 'WashBuffs'): ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；唯一的呼叫者 ApplyStrip 刪除（潮池的沖刷在 DLL）', 'b22cc7356536a0a9'),
    ('ESSBNodes.psc', 'SelfSlowImmune'): ('定神／御風／冰原的免疫減速：這個守衛從來沒有呼叫者（假 DONE）；round 25 在 DLL 計時器（Timer.h SlowImmune）', 'b29c4f550ee2bf29'),
}

ADDED = {
    ('ESSBController.psc', 'Now'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime（新函式）', '65571e1b147a16be'),
    ('ESSBController.psc', 'OnESSBClose'): ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）：魔力歸零 2 秒由 DLL 判定，這裡關閉形態並提示', '6be6269af932ddc2'),
    ('ESSBController.psc', 'OnESSBSwitch'): ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除：DLL 判定魔力門檻並寫好全域變數後，這裡換形態（規劃 1.1 形態開關 Papyrus）', 'b5e99cfc80b66893'),
    ('ESSBNative.psc', 'RequestSwitch'): ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除（新原生函式，排進主執行緒 task）', '9eeb6b834de56ed6'),
    ('ESSBNative.psc', 'RunningSeconds'): ('審查修正 4：秒計時器改用 DLL 的遊戲時鐘（ESSBController.Now → ESSBNative.RunningSeconds，只算遊戲在跑的秒數，暫停與讀檔不走），不再用 Utility.GetCurrentRealTime（新原生函式：讀一個 atomic，可在 VM 的 tasklet 執行）', '9a204a9e2444c998'),
}

PROPERTIES = {
    'ESSBController.psc': ('R4：領域是引擎的 hazard（DLL 在融斷目標腳下放 Spawn Hazard 法術，引擎管壽命與數量；對你與對內部敵人的每秒效果在 DLL 計時器 Timer.h）；Papyrus 的三格領域刪除；成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）；R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除：三格領域的成員、雷雨與環境的計時、GameHour、形態維持能力、InputLayer、反應法術、過熱自傷法術、9 個領域鏡射屬性刪除（R1：新 schema 14，不遷移）；審查修正 4：沒有讀者的「節奏」「疾攻」連段（ComboHits、ComboTime、GCombo）刪除', (), ('GlobalVariable Property GameHour Auto', 'Spell Property FormRulesAbility Auto', 'Spell[] Property ReactSpells Auto', 'Spell Property OverheatSelfSpell Auto', 'ESSBInput Property InputLayer Auto', 'GlobalVariable Property GDomainPoison Auto', 'GlobalVariable Property GDomainWater Auto', 'GlobalVariable Property GDomainDark Auto', 'GlobalVariable Property GDomainAstral Auto', 'GlobalVariable Property GDomainEarth Auto', 'GlobalVariable Property GDomainBlood Auto', 'GlobalVariable Property GDomainDivine Auto', 'GlobalVariable Property GDomainFire Auto', 'GlobalVariable Property GDomainFrost Auto', 'GlobalVariable Property GCombo Auto', 'Bool PlayerInFireDomain', 'Float StormCharge', 'Float LastEnvCheck', 'Int ComboHits', 'Float ComboTime', 'Actor[] DomainResident', 'Float[] DomainResidentAt', 'Int[] DomainElem', 'Float[] DomainTickAt', 'Float[] DomainLeft', 'Float[] DomainX', 'Float[] DomainY', 'Float[] DomainZ', 'Float[] DomainR')),
}

FILES = {
    'ESSBFormRules.psc': ('成果 1：每秒工作搬進 DLL 計時器（Timer.h PlanFormSecond：維持費、魔力歸零 2 秒、血形態扣血、長流與長河、雷雨電荷；環境每 5 秒）：ESSBFormRules（形態維持能力上的每秒維持費、魔力歸零、血形態扣血）整檔刪除，MGEF 的 VMAD 拿掉', 'a4d6a5205e5626da674844183ec94509fbb129d1a39ad6ba58fb9da2ffe3289e', 'removed'),
    'ESSBInput.psc': ('R6：熱鍵由 DLL 的輸入事件 sink 處理；Z 路線與熱鍵呼叫同一個 DLL 切換函式（ESSBNative.RequestSwitch），ESSBInput 刪除：ESSBInput（玩家別名上的按鍵輪詢）整檔刪除，QUST VMAD 的別名腳本 3 → 2', '91f4d99eb55ed735ba34beac70a69920d9aaa92666c667a27f9d2f381cded57b', 'removed'),
    'ESSBSilence.psc': ('成果 1：沉默的每秒扣魔搬進 DLL（施放當下 StatusEngine.h RunOp、之後每秒 Plugin.cpp SilenceSecond）；ESSBSilence 整檔刪除，MGEF 的 VMAD 拿掉', '50063babe6a5c6e8896388091f5fc7cd9cb9ba46f59ef435372a3ec5ce319554', 'removed'),
}

digest = _h21.digest
code = _h21.code
functions = _h21.functions
body_of = _h21.body_of
outside_functions = _h21.outside_functions


def snapshot_text(script: str) -> str:
    return (SNAPSHOT / 'src' / script).read_text(encoding='utf-8-sig')


def current_dir() -> Path:
    """Today's scripts (a later round moves this to its own pre-round snapshot, as round 25 did for build/fix24_history.py)."""
    return ROOT / 'src'


def current_text(script: str) -> str:
    return (current_dir() / script).read_text(encoding='utf-8-sig')


def round25_diff(script: str, text: str | None = None) -> dict:
    """pre-fix25 snapshot -> now (or `text`) for one script: every function the same, comments only, or declared
    with matching digests; code outside functions the same apart from comments, or exactly the declared lines."""
    old_text = snapshot_text(script)
    new_text = current_text(script) if text is None else text
    old_fns, new_fns = functions(old_text), functions(new_text)
    out = {}
    for fn in sorted(set(old_fns) | set(new_fns)):
        before, now = old_fns.get(fn), new_fns.get(fn)
        if before is None:
            decl = ADDED.get((script, fn))
            assert decl is not None, (script, fn, 'added in round 25 but not declared in ADDED')
            assert digest(now) == decl[1], (script, fn, 'added body differs from the declared one')
            out[fn] = 'added'
        elif now is None:
            decl = REMOVED.get((script, fn))
            assert decl is not None, (script, fn, 'deleted in round 25 but not declared in REMOVED')
            assert digest(before) == decl[1], (script, fn, 'pre-fix25 body is not the one the removal was declared against')
            out[fn] = 'removed'
        elif before == now:
            continue
        elif code(before) == code(now):
            out[fn] = 'comments'
        else:
            decl = CHANGED.get((script, fn))
            assert decl is not None, (script, fn, 'changed in round 25 but not declared in CHANGED')
            assert digest(before) == decl[1], (script, fn, 'pre-fix25 body is not the one the declaration was made against')
            assert digest(now) == decl[2], (script, fn, 'body differs from the declared round-25 change (edited after sealing)')
            out[fn] = 'declared'
    old_lines, new_lines = outside_functions(old_text).splitlines(), outside_functions(new_text).splitlines()
    if old_lines != new_lines:
        decl = PROPERTIES.get(script)
        assert decl is not None, (script, 'code outside functions changed in round 25 but not declared')
        added = tuple(x for x in new_lines if x not in old_lines)
        removed = tuple(x for x in old_lines if x not in new_lines)
        assert (added, removed) == (decl[1], decl[2]), (script, 'lines outside functions differ from the declared ones',
                                                        added, removed)
        out['<properties>'] = 'declared'
    return out


_VERIFIED = None


def verify() -> dict:
    """Every script: whole-file removals / additions by sha256, everything else through round25_diff."""
    global _VERIFIED
    if _VERIFIED is not None:
        return _VERIFIED
    old = {p.name for p in (SNAPSHOT / 'src').glob('*.psc')}
    new = {p.name for p in current_dir().glob('*.psc')}
    report = {}
    for name in sorted(old | new):
        if name not in old or name not in new:
            decl = FILES.get(name)
            assert decl is not None, (name, 'script added or removed in round 25 but not declared in FILES')
            path = SNAPSHOT / 'src' / name if name in old else current_dir() / name
            assert decl[2] == ('removed' if name in old else 'added'), (name, 'declared the other way round')
            assert hashlib.sha256(path.read_bytes()).hexdigest() == decl[1], (name, 'file differs from the declared one')
            report[name] = decl[2]
            continue
        changes = round25_diff(name)
        if changes:
            report[name] = changes
    for (script, fn) in list(CHANGED) + list(REMOVED) + list(ADDED):
        assert script in report and fn in report[script], (script, fn, 'declared but not a round-25 change')
    _VERIFIED = report
    return report


def legacy_source() -> Path:
    """The scripts round 24 shipped: the pre-fix25 snapshot, once verify() tied it to today (build/fix24_history.py reads it)."""
    verify()
    return SNAPSHOT / 'src'


# Silent edits (a number or a call changed inside a declared, an added or an untouched function): each must fail.
SILENT_EDITS = [
    ('ESSBController.psc', 'OnESSBSwitch', 'SwitchForm((afElement + 0.5) as Int)', 'SwitchForm((afElement + 1.5) as Int)'),
    ('ESSBController.psc', 'OnESSBClose', 'CloseForm()', 'SwitchForm(0)'),
    ('ESSBController.psc', 'OnESSBDomain', 'PlaceFx(element, target)', 'PlaceFx(element + 1, target)'),
    ('ESSBController.psc', 'Setup', 'RegisterForModEvent("ESSB_Close", "OnESSBClose")', 'RegisterForModEvent("ESSB_Close", "OnESSBDomain")'),
    ('ESSBFormPowerEffect.psc', 'OnEffectStart', 'ESSBNative.RequestSwitch(ElementIndex)', 'ESSBNative.RequestSwitch(ElementIndex + 1)'),
    ('ESSBElem3.psc', 'OnTick', 'PoisonFormTick(akCtl)', 'PoisonFormTick(None)'),
]


def self_check() -> dict:
    """Every declaration is true now, and the silent edits are caught."""
    verify()
    faults = []
    for script, fn, old, new in SILENT_EDITS:
        text = current_text(script)
        body = body_of(text, fn)
        assert body is not None and old in body, (script, fn, old)
        mutated = text.replace(body, body.replace(old, new, 1), 1)
        try:
            round25_diff(script, mutated)
        except AssertionError:
            faults.append(f'{script}:{fn}')
            continue
        raise AssertionError(('silent edit not caught', script, fn, old, new))
    return dict(changed=len(CHANGED), removed=len(REMOVED), added=len(ADDED), properties=len(PROPERTIES),
                files=len(FILES), silent_edits_caught=faults)

# 妥協盤點：alter 相對 v0.3 的每一項改動，哪些是被逼的、DLL 能不能還原

- 撰寫：2026-09-22。使用者原話：「我覺得都檢查一下」。
- 對象：`元素魔戰士規劃-v0.3_alter.md`（現行設計）相對 `元素魔戰士規劃-v0.3.md`（原設計）的全部改動，以我自己重掃兩份文件為準，不只指揮官舉的例子。
- 分類：**(A) 被 Papyrus／entry 51／引擎限制逼出來的妥協**——逐項給「原設計／現在／當初為什麼／DLL 能不能還原／怎麼還原／成本與崩潰面／建議」；**(B) 使用者為了玩法主動選的改動**——預設保留，只註記 DLL 讓它更好做的地方。
- 還原的前提是第 4 節的兩個技術：**層數放在效果強度**（DLL 讀→加→以 magnitude 覆寫重套；引擎持有）與**階梯的階數可以等於 v0.3 的上限**（DLL 決定升階，不再受進入點段數限制，一階一顆 MGEF 由生成器吐）。DLL 自己不記任何時間、不存任何東西；需要「每秒」的只有放血一項，另列。
- 實作分期見 `design-latency-2026-09-20.md` 第 6 節的 DLL 切片；本檔最後一節把勾選項對應到切片。

---

## 1. (A) 被限制逼出來的妥協

### A1 血痕：疊層與放血
- **v0.3**：開印 2 層、命中 +1（重擊也 +1）、上限 8（深創 12、萬象 15）、每層 5 秒（實作取 10 秒）、命中刷新全部；每層每秒 `B_max × k_dot` 的 DoT，另每層每秒扣目標**當前生命** 0.3%（首領 0.1%）；血潮結算剩餘。
- **現在（alter 2.3／5.8）**：三階 DoT（滲血／流血／大出血，等價 2／5／8 層；深創第四階 12），成熟 2 秒才升；放血在套用當下以目標當時生命換算成固定每秒值，命中刷新時重算；〔引擎無法〕「每秒扣當前生命百分比」。
- **當初為什麼**：疊層要腳本容器（180126 那類 bug）；每秒讀目標生命要每目標一支腳本 tick；進入點段數不能承受 15 階。
- **DLL 能不能還原**：**能**（層數）；**能，但需要每秒**（放血）。
- **怎麼還原**：層數＝血痕效果的 magnitude 的倍數（DLL 命中時讀 magnitude → +1 層（開印 +2）→ 以「層數 × 每層每秒」覆寫重套，時長 10 秒重設＝「命中刷新全部」）。上限由 DLL 讀節點（深創、萬象）決定。放血：DLL 每秒掃描帶血痕效果的角色，`層數 × 0.3% × 當下生命`（首領 0.1%）直接扣——DLL 不存狀態（層數在效果裡、生命在角色上），但需要一個**每秒的執行點**：C++ 沒有 hook 就沒有原生計時，可用的做法是每幀任務自我重排＋比對遊戲時間（一個 1 µs 的比較），或掛引擎的更新 hook（待查證，崩潰面較大）。退而求其次：放血在每次命中時以當時生命重算（現況）。
- **成本與崩潰面**：層數：零額外面（命中 sink 內多讀一個 magnitude）。放血每秒：新的每幀任務或 hook，是 DLL 第二個執行點，掃描帶效果的角色（走引擎的 process lists）。
- **建議**：**還原層數**（N3）；**放血依 N5 的每秒點決定**——有每秒點就還原「當下生命」，沒有就維持「命中時重算」。

### A2 凍結量表
- **v0.3**：量表 0～5，開印 +3、命中 +1、重擊 +2；5 = 冰封；6 秒未命中歸零；冰封 3 秒後量表歸零（永凍保留一半）；暴風雪累積 ×2。
- **現在**：三階（寒意／凍結／冰封），開印直接凍結，凍結成熟 2 秒後再一擊冰封；暴風雪改成熟減半。
- **當初為什麼**：進入點段數與「每階一段」的組合；成熟計時是用來近似「還要再累積」。
- **DLL 能不能還原**：**能**。
- **怎麼還原**：量表 0～5 做成五階效果（`凍結 1～5`，第 5 階＝冰封，各帶著色），DLL 依開印／命中／重擊／暴風雪加幾格套對應階；6 秒未命中歸零＝各階 duration 6 秒（過期即無）；冰封 3 秒＝第 5 階 duration 3 秒；永凍＝第 5 階到期時 DLL 套第 3 階（效果移除事件，待查證；否則 `ESSBTierDecay`）。
- **成本與崩潰面**：MGEF +2、SPEL +2；無新執行點。
- **建議**：**還原**（N3）。碎冰 20% 真傷（B4）不受影響，掛在第 5 階。

### A3 詛咒層
- **v0.3**：開印 2 層、命中 +1、上限 5（深淵 +1／每 3 點到 10、萬象 13）、8 秒命中刷新；每層抗性侵蝕 −2%（主線加成）；滿層目標受所有傷害 +%；深淵滿層可侵蝕到負值；亡者強化每層 +10%。
- **現在**：三階（深淵四階），−2／6／10／16%。
- **當初為什麼**：同 A2。
- **DLL 能不能還原**：**能**。
- **怎麼還原**：階數＝上限：詛咒 1～13 各一顆 MGEF（每顆帶自己的五種抗性削減值），DLL 依節點算上限；著色每 3 階加深一次。或改「層數在強度」：一顆效果、五個抗性效果的 magnitude 都 = 層數 × 2——但 magnitude 覆寫參數是否能分別給多個效果**待查證**，所以用階數。
- **成本與崩潰面**：MGEF +9、SPEL +9；無新執行點。
- **建議**：**還原**（N3）。

### A4 水壓、星痕、聖印（目標側）的層數
- **v0.3**：水壓 5（萬象 8）、每層水附傷 +10%；星痕 3（專精 +1／每 5 點到 6，萬象 9），每層 2 秒後各自引爆；聖印 5（萬象 8），每層目標受聖傷 +1%／點。
- **現在**：水壓三階、星痕三階（引爆全部）、聖印退成單一印記（神聖改自我強化，見 B3）。
- **當初為什麼**：同 A2；星痕的「每層各自 2 秒引爆」需要每層一個計時。
- **DLL 能不能還原**：水壓**能**；星痕**部分**；聖印**由 B3 決定**。
- **怎麼還原**：水壓 1～8 階（同 A3 做法）。星痕：v0.3 是每層獨立引信——引擎一顆效果一個 duration，做不到每層獨立；折衷＝階梯 1～9 各自 2 秒、命中升階並重計時（現況邏輯，只是階數從 3 放大到 9）；引爆量 = 階數 × B_max。聖印：若 B3 的聖佑保留，目標側就不需要層數。
- **成本與崩潰面**：MGEF +11、SPEL +11；無新執行點。
- **建議**：水壓**還原**、星痕**折衷**（階數放大到 9，仍一起引爆）、聖印**維持**（N3）。

### A5 火的目標熱度
- **v0.3**：熱度在目標身上 0～10（添薪 15、萬象 18），開印 +2、命中 +1、重擊 +2，滿即自燃，每層火附傷 +8%，6 秒歸零。
- **現在**：熱度搬到玩家身上（B1 的收割者設計）。
- **當初為什麼**：一半是限制（目標疊層），一半是使用者覺得「疊滿爆」無聊而重設計。
- **DLL 能不能還原**：技術上能（同 A2），但這是 B1 的一部分。
- **建議**：**維持**（依 B1）。

### A6 自身資源的計數：電荷、岩甲、風勢、戰意
- **v0.3**：電荷 6（蓄電 10、萬象 13）、10 秒未命中後每秒 −1；岩甲 5（厚土 10、萬象 13），每層護甲 +15（磐石 +25）、物理減傷 +1%／層，被打 −1；風勢門檻 4（亂舞 3），5 秒歸零；戰意 5，5 秒歸零。放電 = 電荷 × 30% B_max，滿格 ×2。
- **現在**：三～四階梯，成熟 2 秒，數值按階。
- **當初為什麼**：計數在腳本；階梯讓 PERK 條件讀得到。
- **DLL 能不能還原**：**能**。
- **怎麼還原**：層數放在自身效果的 magnitude（DLL 命中時讀→+1→重套）；PERK 條件要讀的（電盾「電荷 ≥5」、不動「岩甲 ≥5」、戰意每層武器傷害）由 DLL 在變動時寫既有的 GLOB 鏡射（`ESSB_Charge`、`ESSB_RockArmor`、`ESSB_Wind`、`ESSB_Resolve`，round 1 的樣式 C 已經存在），PERK 段照 round 1 的寫法（`gv_ge` 逐層一段）。岩甲的護甲值＝效果 magnitude（護甲增益 = 層 × 15），被打 −1＝DLL 在受擊 sink 重套少一層。電荷「10 秒後每秒 −1」＝效果 10 秒到期後 DLL 每秒減一——又是每秒；折衷＝到期一次歸零（現況），或依 A1 的每秒點。三檔視覺＝DLL 在 1／3／5 層門檻套三顆視覺標記。
- **成本與崩潰面**：命中 sink 與受擊 sink 內各多一次讀寫；無新執行點（電荷衰減除外）。
- **建議**：**還原**（N4）；電荷衰減用「到期歸零」。

### A7 同調：計數改時間
- **v0.3**：每次有效命中 +1，門檻 5／15／30。
- **現在**：三階梯，成熟 4／8／12 秒後的下一擊升階（總表第 1 條，使用者依「取消疊層」定案）。
- **當初為什麼**：計數不能放腳本；「取消疊層」的規則本身是為了 180126 那類 bug。
- **DLL 能不能還原**：**能**——計數放在同調效果的 magnitude，DLL 命中時 +1、到門檻換段效果（段效果供 PERK 條件與武器光），DLL 不存任何東西。
- **怎麼還原**：`ESSB_Sync` 效果 magnitude = 計數；一／二／三段各一顆標記效果（現有）；門檻由 DLL 讀節點（同調門檻 −2%／點、專一）；切換／關閉時 DLL 清除。
- **成本與崩潰面**：零額外面。
- **建議**：**還原**（N4）——v0.3 的「攻速快的武器更快到三段」是原設計的一部分；時間版是為限制找的近似。這一項最需要使用者親自勾，因為它改變手感方向。

### A8 以計數為本的節點被取代（總表第 14 條）
| 節點 | v0.3 | 現在 | DLL 還原 |
|---|---|---|---|
| 極致 | 同調三段時每 10 次命中額外一次全額附傷 | 10% 機率 | **能**：計數放自身效果 magnitude，DLL 第 10 擊觸發並歸零 |
| 追擊 | 每第 3 次命中額外 35% 星傷 | 33% 機率 | **能**，同上 |
| 疾攻 | 連續命中每次重擊耐力消耗 −10%，最多 −50% | 連段視窗 −30% | **能**：連段數放效果 magnitude，DLL 寫 `ESSB_Combo` GLOB，PERK 段照 round 1（五段） |
| 節奏 | 4 秒內連續命中 3 次 → 回耐＋戰意 +1 | 2 秒視窗 | **能**：同上，效果 4 秒時長＋計數 |
| 五棵樹「開印 +1／每 5 點」 | 開印層數隨點數 | 免等待／起始階 | **能**：DLL 開印時多套幾層（A2～A6 還原後自然回來） |
| 通用「上限 +1 層／每 5 點」 | 每種狀態上限 +1（最多 +3） | 頂階效果 +1%／點 | **能**：DLL 算上限時加進去（階數＝上限的做法要多吐 3 階 MGEF） |
| 萬象「層數效果 +25%」 | 每層效果 ×1.25 | 階梯強度 +25% | **能**：DLL 套用時乘 |
| 星痕上限、電荷上限、深創、厚土、蓄電、深淵 | 上限 +N | 多一階／取代 | **能**：回到上限語意 |
| 吹飛距離主線 | +0.2 公尺／點 | 改為風多段 N | 由 B5 決定（維持） |
- **建議**：**全部還原**（節點用途、點數位置本來就沒動，只是文字改回 v0.3），除吹飛距離（B5）。

### A9 附傷固定為平均值、雷五檔近似、血位四段階梯
- **v0.3**：每擊隨機 B；雷 1～25；血位三條曲線線性內插。
- **現在**：N2 起已還原（DLL 命中時擲骰、精確 best-of-N、線性內插）——alter 第 5、15 條已改回。
- **建議**：已還原（N2），列此只為完整。

### A10 重擊與潛行的判定
- **v0.3／實作**：PO3 HitData 旗標（65536 重擊、2048 潛行攻擊＝未被發現）。
- **現在（總表第 18 條）**：`IsPowerAttacking`／`IsSneaking` 引擎當下狀態（被發現的潛行也算）。
- **當初為什麼**：entry 51 的條件式只能讀狀態，讀不到這一擊的旗標。
- **DLL 能不能還原**：**能**——命中事件帶旗標（待查證欄位名）。
- **建議**：**還原**（N1／N2）：潛行射擊視為重擊、風的 ×3 都回到「真的偷襲」。

### A11 印記的 8 個目標上限
- **v0.3**：同時帶印記的目標上限 8，第 9 個開印時最舊的提前過期終焉。
- **現在**：無上限。
- **當初為什麼**：上限是為腳本容器設的；容器不存在了。
- **DLL 能不能還原**：能，但要 DLL 掃描帶印記的角色找最舊者（讀效果 elapsed）。
- **建議**：**維持無上限**——v0.3 自己說「8 涵蓋絕大多數」，上限的目的是負載，不是玩法；融斷仍受 15 公尺限制。

### A12 雙印「只結算較舊的那個」
- **現在**：唯一留在腳本裡的順序判斷（總表第 17 條）。
- **DLL**：讀兩個印記效果的已過時間，零成本。**還原到 DLL**（N3）。

### A13 印記終焉分類：餘燼標記（計畫 4.9）
- **v0.3**：印記過期／被切掉／融斷各自終焉。
- **現在**：為了不依賴事件順序而加的 1 秒「餘燼」效果與 `BurstDone` 標記。
- **DLL**：被切掉在命中內自己處理；過期與死亡若效果移除事件可用（待查證）就進 DLL；餘燼整個不需要。**還原為直接判定**（N3，視查證）。

### A14 毒層每層獨立壽命、擴散間隔
- **v0.3**：無上限、每層獨立 12 秒、命中不刷新舊層；≥5 層每 2 秒傳 1 層。
- **現在**：一顆成長效果（使用者定案，B6）；擴散改瘴氣披風每秒 0.5 劑。
- **DLL**：每層獨立壽命仍做不到（一顆效果一個時長）——除非 DLL 記每層時間（違反不存狀態）；**維持 B6**。擴散間隔：披風每秒跳是引擎固定，「每 2 秒 1 劑」與「每秒 0.5 劑」總量相同；催毒「每秒一次」變「每秒 1 劑」。**維持**。

### A15 〔引擎無法〕清單裡的其他項
| 項 | 現在 | DLL |
|---|---|---|
| 疾風「衝刺耐力消耗 −50%」後半 | 找不到進入點用例，未落地 | DLL 沒有更好的入口（衝刺耗耐是引擎內部）；**維持未落地**，除非查證找到 API |
| 破護「第一跳仍會吃到」（決定 65） | 視窗在受擊後才開 | DLL 在受擊 sink 內同幀開視窗，但傷害已結算；**維持** |
| 殘影／影身「這一擊無效」改「下一擊無效」（決定 71.13） | 同上 | 同上，**維持** |
| 血咒／腐朽「受治療 −50%」改回復速率 | Skyrim 沒有「受治療」修正 | **維持** |
| 神佑「留 1 血」改延遲死亡（決定 71.12） | Papyrus API | **維持**（C++ 對等未查證） |
| 火葬／亡魂的致死判定 | 傷害前預測（round 16） | DLL 有死亡事件可直接看「死時帶灼熱／死咒」，**可改為事後判定**（N5） |
| 領域只有 3 格、後開取代先開（決定 70／77） | 控制器幾何＋每秒 tick | DLL 可改用引擎 Hazard（原型放置物）讓領域是真的地面效果、不限格數；**選配**（N6，崩潰面：放置物件） |

---

## 2. (B) 使用者主動選的改動（預設保留）

| # | 改動 | DLL 讓它更好做的地方 |
|---|---|---|
| B1 | 火：收割者（熱度在玩家身上、爆燃吃目標狀態、白熱火源披風、0.5% 生命代價、<10% 自動洩壓、過熱走代價路徑） | 熱度層數可放 magnitude（A6 的做法）而不必三階；爆燃的「消耗」在 DLL 內讀目標效果一次算完；火源不變 |
| B2 | 碎冰＝冰封中第一擊 20%（首領 10%）真傷 | DLL 在命中 sink 內判定並套真傷，同幀 |
| B3 | 神聖＝聖佑自身三階（打帶印記目標才升），目標側只剩印記 | 同 A6：階數可放 magnitude；逐階退回需效果移除事件（待查證） |
| B4 | 風終焉多段觸發（N 基礎 2 到 5，第 2 次起 ×0.5） | N 次迴圈在 DLL 內同幀完成，不再是 N 次 `DoCombatSpellApply` 的 Papyrus 排隊 |
| B5 | 吹飛距離主線改為觸發次數 | — |
| B6 | 毒＝一顆強度時長都會長的效果；死亡時 15 公尺內全場分發、每跳 50% | 讀→加→重套在 DLL；死亡分發走死亡事件（N5） |
| B7 | 死亡機制看死時印記（化灰／亡者歸來），擊殺歸屬整套刪除 | 死亡事件在 DLL 內直接讀屍體效果，沒有回呼順序問題 |
| B8 | 同調成熟秒數 4／8／12（若 A7 不還原） | — |
| B9 | 長流回魔（維持費 80%）、湧泉改名 | 留 Papyrus 每秒 |
| B10 | 視覺只在武器、白熱全身火焰例外、無轉場、熱鍵預設開 | 熱鍵可改 DLL 輸入 sink（選配） |
| B11 | 血位四段→已還原線性（A9），不再是 B |  |
| B12 | 只走 SKSE、無 entry 51 退路 | — |

---

## 3. 總表（使用者 2026-09-22 已裁決）

使用者裁決 2026-09-22。

| # | 項目 | 類別 | 建議 | 裁決 |
|---|---|---|---|---|
| A1a | 血痕層數（開印 2、+1、上限 8／12／15、刷新全部）放效果強度 | A | 還原（N3） | ✔ 還原 |
| A1b | 放血依目標**當下**生命每秒扣（需 DLL 每秒點） | A | 有每秒點就還原（N5），否則維持命中時重算 | ✔ 能每秒就還原，否則維持 |
| A2 | 凍結量表 0～5，五階效果 | A | 還原（N3） | ✔ 還原 |
| A3 | 詛咒 1～13 階 | A | 還原（N3） | ✔ 還原 |
| A4a | 水壓 1～8 階 | A | 還原（N3） | ✔ 還原 |
| A4b | 星痕階數放大到 9（仍一起引爆） | A | 折衷（N3） | ✔ 折衷 |
| A4c | 聖印目標側層數 | A | 維持單一印記（依 B3） | ✔ 維持 |
| A5 | 火的目標熱度 | A/B | 維持（依 B1） | ✔ 維持 |
| A6 | 電荷／岩甲／風勢／戰意回到計數（放效果強度＋GLOB 鏡射） | A | 還原（N4） | ✔ 還原 |
| A7 | 同調回到命中計數 5／15／30 | A | 還原（N4）——改變手感方向，請親自決定 | ✔ 還原（計數 5／15／30） |
| A8 | 極致、追擊、疾攻、節奏、開印 +N、上限 +N、萬象等節點回到 v0.3 文字 | A | 全部還原（N3／N4） | ✔ 全部還原 |
| A9 | 隨機 B、雷 best-of-N、血位線性 | A | 已還原（N2） | ✔ 已還原 |
| A10 | 重擊／潛行用命中旗標 | A | 還原（N1／N2） | ✔ 還原 |
| A11 | 印記 8 目標上限 | A | 維持無上限 | ✔ 維持 |
| A12 | 雙印順序判斷進 DLL | A | 還原（N3） | ✔ 還原 |
| A13 | 餘燼標記 | A | 視查證改直接判定（N3） | ✔ 視查證 |
| A14 | 毒每層獨立壽命／擴散間隔 | A | 維持（B6） | ✔ 維持 |
| A15 | 火葬／亡魂改事後判定；領域改 Hazard | A | 前者還原（N5）、後者選配（N6） | ✔ 火葬／亡魂還原、領域選配 |
| B1～B12 | 使用者主動的改動 | B | 保留 | ✔ 保留 |

**若 A1～A8 全部勾選還原**：alter 的 2.3 表回到 v0.3 的層數表（只有中毒與火例外）、5.x 的〔取代〕節點回到原文；設計文件會更接近 v0.3，差異只剩使用者主動的 B 項。生成器的變化是「階數＝上限」帶來的約 +60 筆 MGEF／SPEL 與對應的 GLOB 鏡射段。

---

## 4. 對應到 DLL 切片（`design-latency-2026-09-20.md` 第 6 節）

| 切片 | 含的還原項 |
|---|---|
| N1／N2 | A9（已）、A10 |
| N3 | A1a、A2、A3、A4a、A4b、A8 的開印／上限／萬象、A12、A13 |
| N4 | A6、A7、A8 的極致／追擊／疾攻／節奏 |
| N5 | A1b（若有每秒點）、A15 火葬／亡魂 |
| N6 | A15 領域 Hazard（選配） |

# gpt-6-astra 獨立審查 ｜ 批次 B（引擎與控制器）｜ 2026-09-17

來源：Codex CLI，model gpt-6-astra，effort high，read-only，原始碼內嵌。已排除 review-2026-09-17.md 的 13 項。

本批確認 **11 項新增缺陷**，依影響排序。僅依貼出的原始碼快照與節點文字判斷；未使用工具、連網、建置或修改檔案。行號均指貼文版本，路徑相對於 `D:\Game\Other\SKSE\.codex\elemental-spellblade`。

**新增 findings**

1. **P1 — 雙印的主／副格管理互相覆蓋，會結算錯誤元素、驅散錯誤印記。**  
   **位置：** `src/ESSBController.psc:834-862, 1137-1142, 1163-1167, 1195-1224`。  
   **觸發：** 點「雙印」，依序對同一目標以 A、B 元素命中，等印記啟動後再用 B 命中。第二次 B 命中仍因主格是 A 而進入切印；B 的 AME 又已被 `OnMarkStart` 寫入 `RegMark`，因此 `EndMark` 結算 A，卻驅散 B。換成第三種元素 C，也會把遞補上來的 B 覆寫掉。  
   **依據：** `common r1 t2 B0` 要求同時保留兩種印記、切換時只結算較舊者。程式沒有按元素分派 AME；整份控制器也沒有將印記實體寫入 `RegMark2`。自然過期回報同樣只辨識主格。  
   **最小修法：** 命中、啟動與結束回報都須區分兩格；命中既存副元素只刷新；新元素取代最舊者時，保留另一枚印記及其 AME。  
   **信心：high。**

2. **P1 — 副印記與連鎖終焉绕過每目標冷卻，連鎖還能反覆使用同一枚印記。**  
   **位置：** `src/ESSBController.psc:1055-1081, 1144-1149, 1172-1183`；`src/ESSBReactions.psc:252-258`。  
   **觸發：** A 開印後不到一秒便透過雙印開 B，`OpenSecond` 不檢查 `RegLastOpen`。另讓兩個鄰近目標先後終焉，兩次連鎖都能在一秒內打到同一個帶印目標，因為 `ChainEnd` 直接呼叫 `End`，沒有消耗登記印記或更新 `RegLastEnd`。  
   **依據：** 控制器 `10-12` 行引述規劃：每目標開印、終焉各有一秒冷卻，過期終焉也受限制。  
   **最小修法：** 統一由控制器入口管理開印／終焉冷卻與印記消耗；連鎖以旗標禁止再次擴散。同一次雙印融斷需明確作為一次結算交易處理。  
   **信心：high。**

3. **P1 — 換宿途中目標離開登記表，會永久卡住全體後續換宿。**  
   **位置：** `src/ESSBController.psc:1040-1051, 1240-1253, 1321-1340`。  
   **觸發：** `SwapHosts` 設定 `SwapSlot`、移除舊宿主後，新宿主尚未執行 `OnStatusStart`；此時目標死亡或被逐出而執行 `ClearSlot`。新宿主回報找不到目標便直接返回，沒有清掉 `SwapSlot`。  
   **結果／依據：** 後續每次 `SwapHosts` 都在 `1323-1325` 行返回，其他仍需持續工作的 30 秒宿主也無法搬家。若該槽先被另一個目標重用，其新宿主甚至可能匯入前一目標的狀態，因為搬家暫存只核對槽號。這違反狀態搬家必須保持目標身分的基本不變條件。  
   **最小修法：** 暫存同時保存 Actor 與槽位世代；清槽時取消對應搬家；匯入前核對身分，並加入失敗／逾時復原。  
   **信心：high。**

4. **P1 — 死咒只有固定傷害吃終焉倍率，「已損失生命」項漏乘。**  
   **位置：** `src/ESSBReactions.psc:390-395`；`src/ESSBStatus.psc:662-673`。  
   **觸發：** 對已有生命損失的目標，以高同調融斷暗印記，或取得其他終焉倍率後觸發死咒。  
   **結果／依據：** `DeathCurseAmount` 保存的是已乘 `afMult` 與 `SignatureMult` 的固定項；三秒後追加的生命損失項只乘 `GetDamageMult(10)`。依 `ESSBReactions.psc:9-14, 196-205` 的公式與關閉路線定義，這些倍率應放大整個死咒。忽略其他加成，固定項 14、生命損失項 15、倍率 ×3 時，目前算成 `42 + 15`，而非 `(14 + 15) × 3`。  
   **最小修法：** 分別保存未放大的固定項與本次結算倍率，在引爆時合併兩項後統一相乘；搬家資料同步增加該倍率。  
   **信心：high。**

5. **P1 —「不死」連重擊扣血也免除。**  
   **位置：** `src/ESSBController.psc:1682-1708, 1712-1727`。  
   **觸發：** 點 `blood r0 t4 B0`，血形態同調三段時重擊。  
   **結果／依據：** `BloodPowerCost()` 與維持費共用 `BloodCostScale()`；「不死」使後者直接返回零，重擊也變成零生命成本。節點只寫「維持扣血歸零」，沒有免除重擊費用。  
   **最小修法：** 將「不死」判定移至 `BloodDrainPerSecond()`，或讓成本函式區分維持與重擊。  
   **信心：high。**

6. **P2 — 電荷、風勢衰減與岩甲增減沒有同步 PERK 使用的全域值。**  
   **位置：** `src/ESSBController.psc:1879-1888, 1923-1938, 1985-2004, 2184-2189`。  
   **觸發：** 電荷由至少五格衰減至四格；風勢閒置歸零；或透過 `AddSelf(2, ...)` 增減岩甲。  
   **結果／依據：** 這些路徑修改內部值，卻沒有呼叫 `PushSelf()`。`SyncRockArmor()` 也不更新 `GRockArmor`。因此 `GCharge`、`GWind`、`GRockArmor` 可能一直保持舊值，違反 `141-142` 行「變動那一刻鏡射」的設計；例如「電盾」的五格門檻讀值會與實際電荷不同。實際 PERK CTDA 本批未驗證。  
   **最小修法：** 在上述變動路徑同步對應全域值，或統一透過會同步的資源 setter 修改。  
   **信心：high（全域值不同步可直接確認）。**

7. **P2 — 領域先截取玩家附近五人，導致領域內敵人完全漏算。**  
   **位置：** `src/ESSBController.psc:2103-2153, 2919-2949`。  
   **觸發：** 玩家附近有五個不在死域／毒霧中的敵人；第六個敵人在領域內，也在玩家 15 公尺內，但離玩家較遠。  
   **結果／依據：** `ScanTargets(player, ..., 5, ...)` 先排除第六人，再檢查領域位置。該敵人整段時間都不會受到領域每秒效果。這違反毒霧、死域等節點對「內部敵人」的描述；即使有五人上限，也應從領域內符合條件者挑選。  
   **最小修法：** 掃描候選池後先判斷領域，再對符合者套用人數上限；仍可共用一次候選池掃描。  
   **信心：high。**

8. **P2 —「血斷」在一般切印、自然過期與連鎖時也額外回血。**  
   **位置：** `src/ESSBReactions.psc:218-219, 320-343`。  
   **觸發：** 點 `blood r2 t1 B1`，用另一元素切掉血印記，或讓血印記自然過期。  
   **結果／依據：** `EndBlood` 沒有接收 `aiReason`，只要有分支就額外 `Leech(amount * 0.5)`。節點明定「血印記融斷治療你該傷害的 50%」。  
   **最小修法：** 傳入 `aiReason`，這段額外治療限定 `aiReason == 1`。  
   **信心：high。**

9. **P2 — 三重奏實作成「相鄰事件相隔不到十秒」，而非十秒內三種元素。**  
   **位置：** `src/ESSBController.psc:2678-2699`。  
   **觸發：** 依序在第 0、9、18 秒結算 A、B、C 三種終焉。  
   **結果／依據：** 每次都更新 `TrioTime`，既有種類卻保留，所以第三次回傳三種；實際沒有任何十秒窗口包含三種終焉。重複同元素也能一直延長保存時間。違反 `common r2 t2 B1`。  
   **最小修法：** 保存各元素最近一次終焉時間，計數前排除超過十秒者；若設計採固定窗口，至少應從第一筆開始計時，而非每筆重設。  
   **信心：high。**

10. **P2 —「永續」被加上未列於節點文字的六十秒期限。**  
    **位置：** `src/ESSBController.psc:575-581, 2392-2396, 2621-2625`。  
    **觸發：** 點「永續」，同調三段關形態後，超過六十次一秒 tick 才重新開形態。  
    **結果／依據：** `SetSyncKeep(t1, 60)` 到期清空 `SyncKeep`；`common r0 t4 B0` 寫的是保留一段「到下一次開形態」，沒有等待期限。  
    **最小修法：** 永續保留值使用獨立、直到開形態才消耗的狀態；五秒「連斷」仍保留自己的期限。  
    **信心：high。**

11. **P2 — 買點／洗點沒有刷新腳本掛上的能力，洗掉「抗咒」仍保留魔抗。**  
    **位置：** `src/ESSBTrees.psc:543-549, 611-657`；`src/ESSBController.psc:1542-1574`。  
    **觸發：** 無形態時已掛上 `AntiMagicAbility`，洗掉無元素樹後繼續維持無形態。  
    **結果／依據：** 洗點只刷新節點快取，沒有呼叫 `RefreshAbilities()`，原本透過 `AddSpell` 掛上的魔抗能力不會被移除；買入相關分支也不會立即掛能力。控制器 `1551-1552` 行明定能力取決於是否持有「抗咒」。  
    **最小修法：** 點數結算與洗點完成後，通知控制器刷新能力；批次洗點結束時刷新一次即可。  
    **信心：high。**

**既有項目的補充**

既有 **P1-4「擊殺掛勾重複觸發」修法不完整**：去重仍無法處理擊殺歸屬。

`src/ESSBController.psc:2219-2225` 對任何已登記死者都發出玩家擊殺掛勾；但 `src/ESSBGuard.psc:188-196` 明確只接受玩家擊殺。因此玩家先掛印、同伴補刀時，事件路徑拒絕，Tick 後援仍會發獎。應在同一修正項目加入擊殺歸屬判定，不能單靠 `IsDead()` 推定玩家擊殺。**信心：high。**

**已檢查、在下列限定範圍未發現問題**

- 明確配置的陣列均未超過 Papyrus 的 128 格限制；三棵樹快取各 15 格，合計 45 格。
- `ESSBStatus` 的 27 個整數與 12 個浮點搬家欄位逐項對稱；問題在搬家協調，不在欄位對應。
- 無新增層、無刷新、無搬家時，毒與流血分別結算六次、五次再移出桶；基礎星痕在第二次更新移出並引爆。這是 tick 次數核對，未保證實際牆鐘時間。
- `ESSBController` 的正常 `Setup → Tick` 路徑都會重新排程；未看到正常分支直接讓控制器更新鏈永久停止。
- 單主印記主動結束時，先解除 AME 登記再 `Dispel()`，能擋住該實體結束回報造成的重複終焉；雙印例外見第 1 項。
- CSF 單一分支結算的基本算術正確：補扣四點；不足時移除分支並退回已扣的一點。

**覆蓋與未檢查範圍**

| 要求 | 本批到達範圍 |
|---|---|
| 1. 元素節點 | 部分：核對本批控制器、反應與狀態容器中直接實作的節點。未提供 `src/ESSBElem.psc`、`src/ESSBElem2.psc`、`src/ESSBElem3.psc`，未完成逐節點覆蓋。 |
| 2. 通用／無形態 | 部分：同調保存、三重奏、雙印、融斷入口。未提供 `src/ESSBNodes.psc`、`src/ESSBNoForm.psc`，未完成破魔完整流程及門檻公式核對。 |
| 3. 狀態／反應 | 已逐段檢查兩份來源；基礎公式依貼出的註解核對。完整設計 2.6、2.7 未提供，不能宣稱已逐表驗證。 |
| 4. 控制器／Guard／Trees／Mark | 已檢查四份來源的登記、冷卻、更新、搬家、關形態與點數結算邏輯。 |
| 5. ESP 生成器 | 未檢查：`build_v03.py`、`vendor/wbDefinitionsTES5.pas`、`build/v03-formids.json` 均未貼出。 |

另已閱讀貼出的 `ESSBFormRules.psc`、`ESSBFormPowerEffect.psc`、`ESSBSettingsEffect.psc`、`ESSBLog.psc`。

未檢查完整 `元素魔戰士規劃-v0.3.md`、`實作紀錄.md`、兩份節點 JSON、生成記錄、VMAD 綁定、PERK CTDA，以及遊戲內存讀檔、事件註冊保存與實際排程行為。

前述進度中提到的**催毒倍率疑點不列為確定缺陷**：`EndPoison` 本身確實未使用 `afMult`，但後續 `ESSBElem.OnEnd(..., mult)` 的實作未提供，尚不能排除倍率在該掛勾處理。